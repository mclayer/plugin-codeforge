# docs/evidence-checks-registry.yaml
# Evidence-enforceable governance check registry — single SSOT
# Schema doc: docs/inter-plugin-contracts/evidence-check-registry-v1.md
# Carrier ADR: ADR-060 (Evidence-enforceable promotion framework)
# Lint chain: check-doc-frontmatter.sh + check-doc-section-schema.sh (kind:registry pattern)
# parallel-edit policy: append-only (docs/parallel-work/section-ownership.yaml)
#
# 18 entry backfill complete Phase 2 (CFP-390 §결정 13 SSOT, ADR-060 Amendment 1, 2026-05-11).
# Phase 1 PR (#415) = SSOT 만 (ADR Amendment 본문 표 + sibling_dependencies field 정정).
# Phase 2 PR = 본 §결정 13 표의 18 entry 실제 row append (CFP-390 그룹 A 인벤토리).
# Total entries 가변 — 본 PR base 시점 = 2 + 18 = 20. main rebase 후 CFP-393 / CFP-410
# 추가 entry (rate-limit-fallback-rate, story-section-9-typed) 병합 = 22 entry.
schema_version: "1.2"  # CFP-509 / ADR-060 Amendment 6 — recurrence field 정식 도입 (MINOR bump v1.1 → v1.2)
introduced_by: CFP-389
last_updated: 2026-05-14  # CFP-631 — marketplace-description-verbatim 42번째 entry append (ADR-063 Amendment 2 carrier — blocking-on-pr 직접 시작, §결정 19 auto_blocking manual gate + 사용자 directive)

entries:
  - name: adr-sunset-criteria
    description: |
      ADR-058 §결정 1-3 mechanical verification — is_transitional frontmatter +
      ## 해소 기준 섹션 + 측정성 3-tuple (metric / who / how) + 모달 어휘 1차 사전 검사.
      ADR-058 declaration 의 첫 evidence-enforceable check (CFP-B 잠정 carrier).
    detect_command: bash scripts/check-adr-sunset-criteria.sh
    workflow: templates/github-workflows/adr-sunset-criteria.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:adr-sunset
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:            # ADR-060 §결정 6 (c) — Amendment 2 정정 (CFP-455 append)
        - CFP-390  # 인벤토리 backfill — registry yaml row append (Amendment 1 carrier, merged 2026-05-11)
        - CFP-412  # 4-tier amendment 재예약 carrier — closed without delivery (Issue #412, 2026-05-11)
                   # CFP-391 (Issue #396) closed without delivery (2026-05-11) — Amendment 1 정정
        - CFP-455  # 4-tier 정식 amendment carrier (재재예약) — current_tier required 전환 deliver (Amendment 2)
      evidence_artifacts:              # 승격 carrier PR 의 manual 첨부 의무 (자동화 인프라 별도 carrier)
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary:
      version: "1.0"                   # ADR-060 §결정 9 — 4 표현 only (ADR-058 §결정 8 verbatim)
      dictionary:
        - "안정화되면"
        - "임시"
        - "한시적"
        - "until further notice"
    introduced_by: CFP-389
    owner_adr: ADR-058
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: carrier-bootstrap
    description: |
      ADR-062 §결정 1-2 mechanical verification — carrier Story frontmatter
      `carrier_story` + `bootstrap_exempt_protocols` typed key list 정합 (5 standard
      prefix: adr / contract / policy / workflow / script) + §3 본문 ADR/contract
      참조 정합. self-application 첫 사례 = CFP-407 자체 (§결정 8).
    detect_command: bash scripts/check-carrier-bootstrap.sh
    workflow: templates/github-workflows/carrier-bootstrap-check.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    # bypass_label: omit — 4 known carrier 중 0건 bypass 필요, 후속 carrier 가 도입 시 추가
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a)
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}  # 본 lint = schema 검증, modal phrase 검사 미포함
    introduced_by: CFP-407
    owner_adr: ADR-062
    carrier_adr: ADR-062               # self-carrier
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: rate-limit-fallback-rate
    description: |
      ADR-057 §결정 2 sunset gate measurement — 월간 Sonnet spawn 총 카운트 (분모) +
      §14 Lane Evidence `transcript` 필드의 `[rate-limit-fallback:sonnet→opus]` 태그
      발화 카운트 (분자) → 3 month rolling window fallback rate. runtime cron metric
      pattern (vs static lint, 첫 entry `adr-sunset-criteria` 와 patterns 대비) — advisory
      dashboard, no PR block. framework 첫 non-sunset application (CFP-388 / ADR-060
      §결정 12 CFP-C carry). sample size sufficient sentinel (월 < 50 spawn 환경 = N/A).
    detect_command: bash scripts/measure-rate-limit-fallback.sh
    workflow: templates/github-workflows/rate-limit-fallback-kpi.yml
    current_tier: warning  # advisory dashboard, no PR block — semantic = "warning mode 의 runtime metric extension"
    # bypass_label: omit — runtime metric pattern 에서 의미 부적용
    # bypass_audit_lint: omit — bypass_label 미정의
    promotion_criteria:
      # 본 entry 의 promotion 의미 = lint warning→blocking promotion 아닌,
      # sample size sufficient 도달 시 ADR-057 §결정 2 sunset gate measurement.
      # pr_cumulative_min=0 = promotion semantic 부적용 표기.
      pr_cumulative_min: 0
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - kpi_dashboard_3month_window_evidence  # sunset 판정 carrier PR 의 산출물 (별도 carrier)
    # modal_anti_pattern_dictionary: omit — lint 가 모달 어휘 검사 미포함
    introduced_by: CFP-393
    owner_adr: ADR-057           # 정책 ADR — fallback rate sunset gate owner
    carrier_adr: ADR-060         # framework carrier (CFP-388 / ADR-060 §결정 12 CFP-C)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: story-section-9-typed
    description: |
      Story §9.x sub-section yaml block schema validation — review-verdict-v4 packet
      inline embed 표준 패턴 검증. free-form markdown → typed yaml block 으로 정보 손실
      최소화 + 자동 검증 가능성 확보. yaml block 부재 = OK (backward compat),
      block 존재 시만 schema 검증. Warning tier (PR-block 없음, console warning 만).
      CFP-391 retro follow-up 5건 중 P3 최종 (CFP-410).
    detect_command: bash scripts/check-story-section-9-typed.sh
    workflow: templates/github-workflows/story-section-9-typed-check.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:story-section-9-typed
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []         # 독립 entry — registry append + carrier patterns 검증 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-410
    owner_adr: ADR-044           # review-verdict-v4 schema source
    carrier_adr: ADR-060         # framework carrier
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-390 Phase 2 backfill (18 entries, ADR-060 Amendment 1 §결정 13 SSOT, 2026-05-11) ───
  # 그룹 A — 4-criteria PASS (detect_command + workflow + owner_adr/contract + tier signal)
  # tier 최종 분포: blocking-on-merge 1 + blocking-on-pr 1 + warning 16 (manifest 부착 entry 만 blocking)

  - name: lane-evidence-trail
    description: |
      ADR-031 lane-spawn evidence mandate — Story §14 Lane Evidence row 부재 검출 +
      Phase 2 PR `## Lane evidence` 블록 cross-validate (CFP-126). bypass = env
      BYPASS_LANE_EVIDENCE=1 + reason.

      Actual recurrence (description-only documentation signal, machine-usable
      promotion signal 아님 — ADR-060 4-tier promotion path / failure_threshold
      / pr_cumulative_min 와 무관):
        - CFP-500 FIX-5 (1차): #456 merge 직전 첫 actual collision (table format +
          7-row format 두 heading 등장 → first-match capture boundary 로 인해
          첫 heading 만 capture, 7-row regex 0/7 match → fail)
        - CFP-451 본 세션 transient (2차): #486 step 3 시점 동일 패턴 재현 후
          step 4 awk-delete fix → step 5 final PASS

      Logic refinement (CFP-490 Phase 2): 5a guard tie-break case A/B/C 식별 +
      .mjs extraction (.github/scripts/check-lane-evidence-block.mjs) +
      analyzeDuplicateHeadings() 함수로 valid heading 명시. Option A strict
      (CFP-465 invariant 보존) — lenient fallback 채택 안 함. recurrence_count
      framework / promotion gate input / lint warning condition 모두 무관 (description-only narrative).
      CFP-509 schema v1.2 (ADR-060 Amendment 6) 에 `recurrence:` field 정식 도입 — 본 entry 는
      schema-level count: 2 보유 (CFP-500 FIX-5 + CFP-451 transient 2회 historical evidence).
    detect_command: bash scripts/check-lane-evidence.sh
    workflow: .github/workflows/lane-evidence-check.yml
    current_tier: warning            # manifest 미부착 — Phase 2 backfill 시점 정합 (§결정 13 표)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-031 §결정 3 + fix-event-v1
    carrier_adr: ADR-060
    recurrence:
      count: 2
      last_occurrence: "2026-05-12T00:00:00Z"    # CFP-451 본 세션 transient 2차 발생 (대략 timestamp)
      threshold: 3
      promotion_trigger: advisory              # threshold 도달 시 PR comment advisory
    status: Active

  - name: doc-locations-registry
    description: |
      ADR-041 doc taxonomy 위치 SSOT 검증 — docs/doc-locations.yaml 의 정의 path
      대비 실제 doc file 존재 + naming pattern 정합 검증 (--full mode).
      CLAUDE.md "branch protection 5번째" narrative 와 manifest 부재 drift 존재 (별도 Issue 권고).
    detect_command: bash scripts/check-doc-locations.sh --full
    workflow: .github/workflows/doc-locations-check.yml
    current_tier: warning            # manifest 미부착 (narrative drift, §결정 13 표 row 2)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-041
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: marketplace-parity
    description: |
      ADR-016 marketplace registration parity — wrapper + 6 lane plugin 의 mirrored
      field (name / version / description / author) cross-repo drift 검증.
      ADR-023 §결정 5 lane plugin lifecycle 동반.
    detect_command: bash scripts/check-marketplace-parity.sh
    workflow: .github/workflows/marketplace-parity.yml
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-016 / ADR-023 §결정 5
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: invariant-check
    description: |
      5 invariant aggregated check — Story §1 immutable + ADR-002 footer pattern +
      phase label invariant + script exec bit + workflow yaml syntax 등 multi-CFP
      (5 / 7 / 8 / 10) 위반 검출. branch-protection-manifest row 2 부착.
    detect_command: workflow inline — 5 invariant 직접
    workflow: .github/workflows/invariant-check.yml
    current_tier: blocking-on-pr     # branch-protection-manifest row 2 (§결정 13 표)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-002 (footer pattern) + 다중 CFP (5/7/8/10)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: phase-gate-mergeable
    description: |
      ADR-031 phase gate mergeable — cross-repo Story fetch + label-registry-v2 phase
      label 정합 검증 + dynamic checks.create 발화. branch-protection-manifest row 1
      부착 — merge 시점 차단 (blocking-on-merge).
    detect_command: workflow inline — cross-repo Story fetch
    workflow: .github/workflows/phase-gate-mergeable.yml
    current_tier: blocking-on-merge  # branch-protection-manifest row 1, dynamic checks.create (§결정 13 표)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-031 §결정 3 + label-registry-v2
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: inter-plugin-contracts
    description: |
      ADR-008 / ADR-010 inter-plugin contract MANIFEST.yaml 정합 검증 — 6 typed
      contract 의 producer plugin / canonical-sibling pair / status enum + active
      한 row 보장. wrapper sibling sync 의무 cross-validate.
    detect_command: bash scripts/check-inter-plugin-contracts.sh
    workflow: .github/workflows/contract-lint.yml  # job: inter-plugin-contracts
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-008 / ADR-010 / MANIFEST.yaml
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: inter-plugin-drift
    description: |
      ADR-011 inter-plugin contract drift 검증 — canonical (lane plugin) vs sibling
      (wrapper) verbatim mirror 정합. drift 검출 시 sibling sync PR 후속 의무.
    detect_command: bash scripts/check-inter-plugin-drift.sh
    workflow: .github/workflows/contract-lint.yml  # job: inter-plugin-drift
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-011
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: comment-prefix-registry
    description: |
      comment-prefix-registry-v1 (kind:registry) 11 phase prefix taxonomy 검증 —
      `[요구사항]` / `[설계]` / `[설계-리뷰]` / `[구현]` / `[구현-리뷰]` 등 lane
      plugin self-write comment prefix 정합.
    detect_command: bash scripts/check-comment-prefix.sh
    workflow: .github/workflows/contract-lint.yml  # job: comment-prefix-registry
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: comment-prefix-registry-v1 (kind:registry)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: label-registry-sync
    description: |
      label-registry-v2 (kind:registry) phase/gate/fix label taxonomy sync 검증 —
      GitHub repo label state vs registry SSOT drift.
    detect_command: bash scripts/check-label-registry.sh
    workflow: .github/workflows/contract-lint.yml  # job: label-registry-sync
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: label-registry-v2 (kind:registry)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: marketplace-sync
    description: |
      ADR-016 marketplace mirrored field sync 검증 — codeforge plugin family 7 plugin
      의 marketplace.json mirrored field (name / version / description / author) 정합.
      [CFP-457 retired 2026-05-12] — redundant with marketplace-parity (CFP-50 / ADR-023).
      Single SSOT = marketplace-parity entry above.
    detect_command: bash scripts/check-marketplace-sync.sh  # DELETED in CFP-457
    workflow: .github/workflows/contract-lint.yml  # job: marketplace-sync (removed in CFP-457)
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-016 (mirrored field)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Retired  # CFP-457 — replaced by marketplace-parity (CFP-50 / ADR-023)
    retired_by: CFP-457
    retired_date: 2026-05-12

  - name: dogfood-artifact-paths
    description: |
      ADR-013 / ADR-017 dogfood-out path 검증 — codeforge family plugin repo 의
      brainstorming spec + writing-plans plan 저장 위치가 internal-docs override
      (default `docs/superpowers/*` 아님) 정합.
    detect_command: bash scripts/check-dogfood-artifact-paths.sh
    workflow: .github/workflows/dogfood-artifact-paths.yml
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-013 / ADR-017
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: superpowers-integration
    description: |
      ADR-028 / CFP-113 superpowers plugin 8 skill × 17 lane agent integration
      매트릭스 정합 검증 — skill 호출 가능 surface drift.
    detect_command: bash scripts/check-superpowers-integration.sh
    workflow: .github/workflows/superpowers-integration.yml
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-028 / CFP-113
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: superpowers-schema-drift
    description: |
      ADR-028 / CFP-121 superpowers skill schema drift 검증 — quarterly scheduled
      (3 month rolling) + PR:paths trigger.
    detect_command: bash scripts/check-superpowers-schema-drift.sh
    workflow: .github/workflows/superpowers-schema-drift.yml
    current_tier: warning            # scheduled quarterly + PR:paths (§결정 13 표 row 13)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-028 / CFP-121
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: parallel-epic-conflict
    description: |
      ADR-050 parallel epic coordination — 복수 open Epic 의 scope_manifest 교집합
      검출 + merge-order label 자동 부여. continue-on-error prior art (warning mode
      reference).
    detect_command: workflow inline — PR file 교집합
    workflow: .github/workflows/parallel-epic-conflict-check.yml
    current_tier: warning            # continue-on-error prior art (§결정 13 표 row 14)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-050
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: branch-protection-drift
    description: |
      ADR-024 Amendment 2 branch protection drift 검출 — gh api response vs
      templates/branch-protection-manifest.yaml diff. weekly cron (Mon 09:00 UTC) +
      manual trigger.
    detect_command: workflow inline — gh api + manifest diff
    workflow: .github/workflows/branch-protection-drift-check.yml
    current_tier: warning            # weekly Mon 09:00 UTC cron (§결정 13 표 row 15)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-024 Amendment 2
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: required-workflow-drift
    description: |
      ADR-048 §결정 3 required workflow drift — enterprise admin 가용 가정 하
      required_status_checks contexts vs manifest contexts diff. weekly cron
      (Mon 10:00 UTC).
    detect_command: workflow inline + check-enterprise-admin.sh
    workflow: .github/workflows/required-workflow-drift-check.yml
    current_tier: warning            # weekly Mon 10:00 UTC cron (§결정 13 표 row 16)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-048 §결정 3
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: rulesets-drift
    description: |
      ADR-048 §결정 1 GitHub repository rulesets drift — daily cron (09:00 UTC)
      gh api rulesets response vs templates/rulesets-manifest.yaml diff.
    detect_command: workflow inline + check-enterprise-admin.sh
    workflow: .github/workflows/rulesets-drift-check.yml
    current_tier: warning            # daily 09:00 UTC cron (§결정 13 표 row 17)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-048 §결정 1
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: write-permission-redistribution
    description: |
      CFP-26 / ADR-009 write 권한 invariant — lane plugin 의 self-write boundary
      (docs/** + GitHub Issue/PR/comment) 정합. Phase 0a write 권한 재분배 후
      regression 방지.
    detect_command: bash scripts/check-write-permission-redistribution.sh
    workflow: .github/workflows/lint.yml  # job: write-permission-redistribution
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: CFP-26 / ADR-009 (write 권한 invariant)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-426 Phase 2 backfill (4 entries, ADR-040 Amendment 3 §결정 7.D verbatim, 2026-05-12) ───
  # Story 1 skeleton (warning tier) — actual wire 는 후속 Story:
  #   - CFP-427 (Story 2): worktree-first-session-start-wire + worktree-first-spawn-evidence-cwd actual wire
  #   - CFP-428 (Story 3): worktree-first-pre-checkout + worktree-first-pre-commit-main-block actual git hook
  #   - CFP-429 (Story 4): 4 entry warning → blocking-on-pr 승격 평가 (ADR-060 §결정 6 gate)
  # carrier ADR-040 mechanical_enforcement_actions[] 4 entry self-application 첫 사례 (Amendment 3 §결정 7.D)

  - name: worktree-first-session-start-wire
    description: |
      ADR-040 §결정 5 (Stale GC) + §결정 7 (Amendment 3) — wrapper repo `.claude/settings.json` 의
      `hooks.SessionStart[]` 안에 `check-worktree-stale.sh` 호출 entry 존재 검증. SessionStart hook
      wire 누락 시 stale worktree 자동 GC 미동작 (declaration only → mechanical enforcement 분리).
      warning tier — continue-on-error: true. CFP-427 Story 2 가 actual wire 첫 사례 —
      `scripts/check-session-start-hook-presence.sh` `exec` swap wrapper 패턴 (RefactorArch 결정).
      CFP-429 (Story 4) Amendment 4 declaration: 4/4 actual wire 완료 + ADR-060 §결정 6 promotion
      gate (b) bypass 외 failure > 0 FAIL → current_tier: warning 유지 + actual 승격 follow-up CFP
      carrier (from-cfp-425-followup label). Self-test integration test: tests/integration/stories/CFP-425/self-test-worktree-block.sh E4-1 (Phase 2 PR CFP-429).
    detect_command: bash scripts/check-worktree-first-session-start-wire.sh
    workflow: templates/github-workflows/worktree-first-session-start-wire.yml
    current_tier: blocking-on-pr  # CFP-531 / ADR-040 Amendment 5 — warning → blocking-on-pr 승격 (2026-05-13)
    bypass_label: hotfix-bypass:worktree-session-start-wire
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse (BYPASS_LABEL_PREFIX=hotfix-bypass:)
    promotion_criteria:
      pr_cumulative_min: 20         # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0          # ADR-060 §결정 6 (b)
      sibling_dependencies:         # ADR-060 §결정 6 (c) — Story 4 진입 시 평가
        - CFP-427
        - CFP-428
        - CFP-429
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-426
    owner_adr: ADR-040
    carrier_adr: ADR-040
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: worktree-first-pre-checkout
    description: |
      ADR-040 §결정 7 (Amendment 3) — main working tree 에서 `git checkout cfp-NNN` 등 lane branch
      checkout 차단 (worktree-first 정책 정합). pre-checkout git hook sample 존재 검증 +
      install 권고 출력 (warning tier — install 강제 X). CFP-428 (Story 3) 가 actual wire 첫
      사례 — `templates/.git-hooks/pre-checkout.sample` + `scripts/install-git-hooks.sh` opt-in
      installer. lint pattern = verification-only (sample 존재 + executable bit 검증,
      Story 2 exec swap 패턴 부적용 — canonical 이 git hook sample 자체).
      CFP-429 (Story 4) Amendment 4 declaration: 4/4 actual wire 완료 + ADR-060 §결정 6 promotion
      gate (b) bypass 외 failure > 0 FAIL → current_tier: warning 유지 + actual 승격 follow-up CFP
      carrier (from-cfp-425-followup label). Self-test integration test: tests/integration/stories/CFP-425/self-test-worktree-block.sh E4-3 (Phase 2 PR CFP-429).
    detect_command: bash scripts/check-worktree-first-pre-checkout.sh
    workflow: templates/github-workflows/worktree-first-pre-checkout.yml
    current_tier: blocking-on-pr  # CFP-531 / ADR-040 Amendment 5 — warning → blocking-on-pr 승격 (2026-05-13)
    bypass_label: hotfix-bypass:worktree-pre-checkout
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies:
        - CFP-427
        - CFP-428
        - CFP-429
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-426
    owner_adr: ADR-040
    carrier_adr: ADR-040
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: worktree-first-pre-commit-main-block
    description: |
      ADR-040 §결정 7 (Amendment 3) — main working tree 에서 src/docs commit 차단 (worktree-first
      정책 정합). pre-commit-main-block git hook sample 존재 검증 + install 권고 출력. CFP-428
      (Story 3) 가 actual wire 첫 사례 — `templates/.git-hooks/pre-commit-main-block.sample` +
      `scripts/install-git-hooks.sh` opt-in installer. path matching = `src/**` 또는 `docs/**`
      only (Plan SSOT verbatim — chore commit / typo fix 등 차단 0). lint pattern =
      verification-only.
      CFP-429 (Story 4) Amendment 4 declaration: 4/4 actual wire 완료 + ADR-060 §결정 6 promotion
      gate (b) bypass 외 failure > 0 FAIL → current_tier: warning 유지 + actual 승격 follow-up CFP
      carrier (from-cfp-425-followup label). Self-test integration test: tests/integration/stories/CFP-425/self-test-worktree-block.sh E4-4 (Phase 2 PR CFP-429).
    detect_command: bash scripts/check-worktree-first-pre-commit-main-block.sh
    workflow: templates/github-workflows/worktree-first-pre-commit-main-block.yml
    current_tier: blocking-on-pr  # CFP-531 / ADR-040 Amendment 5 — warning → blocking-on-pr 승격 (2026-05-13)
    bypass_label: hotfix-bypass:worktree-pre-commit-main-block
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies:
        - CFP-427
        - CFP-428
        - CFP-429
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-426
    owner_adr: ADR-040
    carrier_adr: ADR-040
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: worktree-first-spawn-evidence-cwd
    description: |
      ADR-040 §결정 5 + §결정 7 (Amendment 3) + ADR-031 §결정 4 — §14 Lane Evidence row 의
      `Working dir:` field 가 worktree path (`${HOME}/.claude/worktrees/<repo>/<branch-flat>` 정합)
      또는 `N/A — <30자 이상 사유>` (ADR-031 §결정 4 bypass mechanism) 인지 검증. wrapper repo
      `docs/stories/**.md` scan. CFP-427 Story 2 가 actual wire 첫 사례 — enforce-from filter
      도입 (본 Story merged at 이후 신규 Story 만, false-positive 회피) + PLACEHOLDER sentinel
      safe-fallback (Phase 2 PR merge 전 enforce 전체 skip).
      CFP-429 (Story 4) Amendment 4 declaration: 4/4 actual wire 완료 + ADR-060 §결정 6 promotion
      gate (b) bypass 외 failure > 0 FAIL → current_tier: warning 유지 + actual 승격 follow-up CFP
      carrier (from-cfp-425-followup label). Self-test integration test: tests/integration/stories/CFP-425/self-test-worktree-block.sh E4-2 (Phase 2 PR CFP-429).
    detect_command: bash scripts/check-worktree-first-spawn-evidence-cwd.sh
    workflow: templates/github-workflows/worktree-first-spawn-evidence-cwd.yml
    current_tier: blocking-on-pr  # CFP-531 / ADR-040 Amendment 5 — warning → blocking-on-pr 승격 (2026-05-13)
    bypass_label: hotfix-bypass:worktree-spawn-evidence-cwd
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies:
        - CFP-427
        - CFP-428
        - CFP-429
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-426
    owner_adr: ADR-040
    carrier_adr: ADR-040
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: evidence-registry-schema-validation
    description: |
      ADR-060 Amendment 2 §결정 14 verbatim — evidence-checks-registry.yaml schema
      mechanical verification (6 validation rule: schema_version field + entry
      required field + current_tier enum membership + bypass_label↔bypass_audit_lint
      pair 정합 + entry name uniqueness + owner_adr/carrier_adr ADR file cross-ref).
      메타 lint self-application 첫 사례 — self-entry 도 다른 entry 와 동일 검증
      (Q4 결정 verbatim — special-case 도입 X). 본 entry 와 §결정 14 의 메타 anomaly
      lint 는 분리 (Q6 결정 verbatim — 별도 carrier).
      Exit code 3-tier (ADR-060 Amendment 2 §결정 15): 0=PASS / 1=validation FAIL /
      2=META-ERROR (pyyaml 미설치 / file 부재 / unexpected exception).
    detect_command: bash scripts/check-evidence-registry.sh
    workflow: templates/github-workflows/evidence-registry-check.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    # bypass_label: omit — ADR-060 Amendment 2 §결정 16 정합 (warning tier 의 bypass 의미 부적용)
    # bypass_audit_lint: omit — bypass_label 미정의
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []         # 독립 entry — schema validation, 외부 의존 없음
      evidence_artifacts:              # 승격 carrier PR 의 manual 첨부 의무
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    # modal_anti_pattern_dictionary: omit — schema validation lint, modal 어휘 검사 미포함
    introduced_by: CFP-455
    owner_adr: ADR-060                 # framework SSOT — schema 정합 검증의 정책 owner
    carrier_adr: ADR-060               # Amendment 2 carrier (self-application 의 self-carrier 패턴)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-449 Phase 2 (decision principle forbid-list vocabulary lint, 2026-05-12) ───
  # ADR-064 §결정 2 forbid-list 8 어휘 dictionary 의 mechanical lint — 2nd warning-tier entry
  # (1st = adr-sunset-criteria). framework multi-entry 운영 검증 cross-validation 신호.

  - name: decision-principle-vocab
    description: |
      ADR-064 §결정 2 forbid-list 8 어휘 mechanical lint — 8 어휘 dictionary verbatim mirror
      detection in 5 scope 영역 (ADR / change-plans / CLAUDE.md / playbook / templates).
      Exempt 영역 = markdown blockquote + fenced code block + dictionary 본문 영역
      (ADR-064 §결정 2 verbatim) + registry yaml description + script self + bats fixture
      self. 사용자 발화 verbatim invariant (Story §1) 영역 exempt 보장 (Story file 은
      internal-docs repo 영역, wrapper repo lint scope 외 자연 처리). evidence-enforceable
      framework 2nd warning-tier entry (1st = adr-sunset-criteria).
    detect_command: bash scripts/check-decision-principle-vocabulary.sh
    workflow: templates/github-workflows/decision-principle-vocabulary.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:decision-principle-vocab
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — registry append + lint patterns 검증 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary:
      version: "1.0"                 # ADR-064 §결정 2 verbatim — 8 어휘 only
      dictionary:
        - "임시"
        - "단계적"
        - "일단"
        - "우선"
        - "잠정"
        - "가벼운"
        - "minimal viable"
        - "quick win"
    introduced_by: CFP-449
    owner_adr: ADR-064               # forbid-list dictionary SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-475 Phase 2 (duplicate-session-start-hook-check, 2026-05-12) ───
  # ADR-038 Amendment 3 §결정 12 one-channel rule mechanical lint
  # plugin-root hooks/hooks.json SSOT 이전 후 settings.json 중복 등록 detect

  - name: duplicate-session-start-hook-check
    description: |
      ADR-038 Amendment 3 §결정 12 (one-channel rule) — `.claude/settings.json` `hooks.SessionStart[]`
      와 plugin-root `hooks/hooks.json` 양 channel 안 prereq-check entry 동시 정의 detect.
      하나의 channel 만 SSOT (plugin-root first-class, settings.json fallback deprecated).
      warning tier — continue-on-error: true. CFP-475 Phase 2 PR 가 actual wire 첫 사례 —
      `scripts/check-no-duplicate-session-start-hook.sh` (bash + jq) + `hotfix-bypass:duplicate-session-start-hook` label.
    detect_command: bash scripts/check-no-duplicate-session-start-hook.sh
    workflow: templates/github-workflows/duplicate-session-start-hook-check.yml
    current_tier: warning          # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:duplicate-session-start-hook
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20         # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0          # ADR-060 §결정 6 (b)
      sibling_dependencies: []      # 본 entry 단독 — Story 4 진입 시 평가 sibling 부재
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-475
    owner_adr: ADR-038
    carrier_adr: ADR-038
    status: Active

  # ─── CFP-481 Phase 2 (PR open 시 phase label 자동 부착, 2026-05-12) ───
  # ADR-024 Amendment 4 §결정 6.A.1 branch → phase mapping 표 SSOT 의 mechanical enforcement —
  # 3rd warning-tier entry (1st = adr-sunset-criteria / 2nd = decision-principle-vocab).
  # CFP-455 + CFP-449 retro 식별 sentinel 2 (재현 2회) → mechanical enforcement 도입 timing 도달.

  - name: auto-phase-label
    description: |
      ADR-024 Amendment 4 §결정 6.A.1 branch → phase mapping 표 mechanical lint —
      PR open 시 4-tier inference fallback chain (branch parse / Related Issue inherit /
      terminal default / unclassified+warning) 으로 phase:* label 자동 부착.
      story-init.yml 가 만든 PR 은 가드 skip (책임 분리). race 회피 = concurrency.group
      `phase-label-auto-N` 직렬화 + phase-label-invariant.yml 와는 trigger event type 분리
      (`pull_request:opened` vs `pull_request:labeled`). idempotency = PR body marker
      `<!-- attached_by_auto: true -->` presence dedup. evidence-enforceable framework
      3rd warning-tier entry (1st = adr-sunset-criteria / 2nd = decision-principle-vocab).
    detect_command: github-actions-runtime  # workflow self-fire (PR open event), no static script
    workflow: templates/github-workflows/auto-phase-label.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:auto-phase-label
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — workflow self-app + ADR-024 Amendment 4 정합
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    # modal_anti_pattern_dictionary: omit — workflow lint, modal 어휘 검사 미포함
    introduced_by: CFP-481
    owner_adr: ADR-024 Amendment 4 §결정 6.A.1   # branch → phase mapping 표 SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: claude-md-line-cap
    description: |
      ADR-012 Amendment 1 §결정 6 mechanical lint — CLAUDE.md line count cap ≤320.
      ADR-060 Amendment 5 (4번째 warning-tier entry): scripts/check-claude-md-line-cap.sh
      exit 0 PASS / exit 1 cap 초과 / exit 2 meta-error (ADR-060 Amendment 2 §결정 15 3-tier).
      ADR-058 §결정 5 ratchet 정합 — cap 강화 방향, sunset_justification 면제.
      CFP-455 + CFP-449 retro sentinel 2 재현 2회 → mechanical enforcement 도입 timing 도달 (4th entry).
    detect_command: bash scripts/check-claude-md-line-cap.sh
    workflow: templates/github-workflows/claude-md-line-cap.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:claude-md-line-cap
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_story_merged: required # ADR-060 §결정 6 (c)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    sibling_dependencies: []         # 독립 entry
    introduced_by: CFP-506
    introduced_date: 2026-05-13
    owner_adr: ADR-012               # Amendment 1 cap ≤320 declaration SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry, Amendment 5)
    carrier_amendment: 5
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-521 Phase 2 (sibling-pr label anti-misuse lint, 2026-05-13) ───
  # ADR-010 Amendment 4 §결정 5 anti-misuse 안전망 mechanical enforcement —
  # 5th warning-tier entry (1st = adr-sunset-criteria / 2nd = decision-principle-vocab /
  # 3rd = auto-phase-label / 4th = claude-md-line-cap).
  # EPIC-RESULTS-CFP-462 §6 carrier #2 — sibling-pr label fast-pass mechanism (CFP-499) 후속.

  - name: sibling-pr-label-author-check
    description: |
      ADR-010 Amendment 4 §결정 5 anti-misuse 안전망 mechanical lint — sibling-pr label
      부착 PR 의 paired wrapper PR link 검증 (PR body grep mclayer/plugin-codeforge#NNN
      패턴 — short form + URL form 양쪽). 부재 시 audit comment 부착 + workflow failure
      (warning tier, advisory only — PR merge 미차단). Guard 3종: (a) sibling-pr label
      미부착 PR skip / (b) hotfix-bypass:sibling-pr-author-check label 부착 skip /
      (c) wrapper repo self-PR skip (self-referential 영역). audit comment dedup =
      `[sibling-pr-anti-misuse]` marker presence. CFP-499 sibling-pr fast-pass mechanism
      (ADR-010 Amendment 4) 의 anti-misuse 안전망 forcing function.
      EPIC-RESULTS-CFP-462 §6 carrier #2.
    detect_command: github-actions-runtime  # workflow self-fire (PR label event), no static script
    workflow: templates/github-workflows/sibling-pr-label-author-check.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:sibling-pr-author-check
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — workflow self-app + ADR-010 Amendment 4 정합
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    # modal_anti_pattern_dictionary: omit — workflow lint, modal 어휘 검사 미포함
    introduced_by: CFP-521
    introduced_date: 2026-05-13
    owner_adr: ADR-010 Amendment 4 §결정 5   # sibling-pr fast-pass anti-misuse 안전망 SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-491 Phase 2 (impl manifest AC mapping cross-ref lint, 2026-05-13) ───
  # ADR-060 Amendment 6 — 6번째 warning-tier entry (CFP-451 F-001 Option C systematization)
  # (1st = adr-sunset-criteria / 2nd = decision-principle-vocab / 3rd = auto-phase-label /
  # 4th = claude-md-line-cap / 5th = sibling-pr-label-author-check)
  # 1차 단순화: §8.5 → §5.1 only (2-way). 3-way (Change Plan §1.3/§3.5/§8.1) = follow-up CFP.

  - name: ac-mapping-cross-ref
    description: |
      ADR-060 Amendment 6 / CFP-451 F-001 Option C systematization — Story §8.5 Impl Manifest
      의 AC id 인용 ↔ §5.1 AC 정의 영역 의 cross-reference 검증. 1차 단순화 = §8.5 → §5.1
      only (2-way drift detection). 3-way (Change Plan §1.3/§3.5/§8.1 의 AC 정의) cross-check
      = follow-up CFP scope. Exit code 3-tier (ADR-060 Amendment 2 §결정 15): 0 PASS / 1
      violation (--strict mode 또는 향후 enforce) / 2 meta-error (file 부재). 기본 mode =
      LLM trust (exit 0 + stderr advisory) — CI workflow continue-on-error:true 와 defense in depth.
    detect_command: bash scripts/check-impl-manifest-ac-mapping.sh
    workflow: templates/github-workflows/ac-mapping-cross-ref-check.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:ac-mapping
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — Story §8.5 / §5.1 lint patterns 검증 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    # modal_anti_pattern_dictionary: omit — schema cross-reference lint, modal 어휘 검사 미포함
    introduced_by: CFP-491
    introduced_date: 2026-05-13
    owner_adr: CFP-451 F-001 Option C   # F-001 retro follow-up systematization 동인
    carrier_adr: ADR-060                # framework carrier (warning mode entry)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-527 Wave 2A (boundary completeness wording SSOT lint, 2026-05-13) ───
  # ADR-068 §결정 5 wording SSOT mechanical enforcement — 8번째 warning-tier entry
  # (1st = adr-sunset-criteria / 2nd = decision-principle-vocab / 3rd = auto-phase-label /
  # 4th = claude-md-line-cap / 5th = sibling-pr-label-author-check / 6th = ac-mapping-cross-ref /
  # 7th = evidence-registry-naming)
  # wording-ssot-grep-lint: Story §3/§7 enum identifier → impl source ripgrep 매칭 (warning advisory).

  - name: wording-ssot-grep-lint
    description: |
      ADR-068 §결정 5 wording SSOT mechanical lint — Story §3/§7 의 enum-like identifier
      (UPPER_SNAKE_CASE 4+ chars) 추출 후 impl source (src/ scripts/) ripgrep 매칭.
      불일치 시 warning 출력 (advisory only — PR merge 미차단). Story file 미제공 시
      skip exit 0. Warning tier (continue-on-error: true workflow 정합).
      Hotfix bypass: hotfix-bypass:boundary-wording (ADR-024 Amendment 3 per-entry namespace).
      4 invariants (I-1 API contract semantic / I-2 cross-module propagation /
      I-3 guard placement intent / I-4 wording SSOT) 중 I-4 wording SSOT 의 mechanical enforcement.
      CFP-529 Wave 3 Phase 2 = handoff wording linter expansion (scripts/check_handoff_wording.py +
      3-direction enum forward/backward/lateral + 5 mechanical pre-screen patterns + 3 AI escalate
      stub patterns). 본 entry 의 mechanical scope = ADR-068 wording SSOT 단방향 (§3 → src/scripts);
      CFP-529 Phase 2 가 bidirectional + cross-section coverage 확장 (severity-propagation-v1 SSOT).
    detect_command: bash scripts/check-wording-ssot.sh
    workflow: templates/github-workflows/wording-ssot-check.yml
    enforcement_script_phase_2: scripts/check_handoff_wording.py            # CFP-529 Phase 2 carrier (handoff wording linter, bidirectional + 3-direction enum)
    enforcement_workflow_phase_2: templates/github-workflows/handoff-wording-check.yml  # CFP-529 Phase 2 carrier
    carrier_story_phase_2: CFP-529   # mechanical impl carrier (severity-propagation-v1 SSOT + handoff wording linter)
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:boundary-wording
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: [CFP-529]  # CFP-529 Phase 2 handoff wording linter (bidirectional + 3-direction enum) 가 mechanical coverage 확장
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-527
    introduced_date: 2026-05-13
    owner_adr: ADR-068               # boundary completeness invariants SSOT — wording SSOT §결정 5
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    related_contracts:               # CFP-529 — severity-propagation-v1 (RC#8 bidirectional binding SSOT)
      - severity-propagation-v1
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-508 Phase 2 (entry name ↔ workflow file naming convention lint, 2026-05-13) ───
  # ADR-060 Amendment 7 — 7번째 warning-tier entry (CFP-490 FU-2 carrier)
  # (1st = adr-sunset-criteria / 2nd = decision-principle-vocab / 3rd = auto-phase-label /
  # 4th = claude-md-line-cap / 5th = sibling-pr-label-author-check / 6th = ac-mapping-cross-ref)
  # Conservative no-rename policy: multi-job workflow pattern + naming divergence 정식 인정.

  - name: evidence-registry-naming
    description: |
      ADR-060 Amendment 7 / §결정 20 — evidence-checks-registry entry name ↔ workflow file
      naming convention 검증. workflow file 존재 의무 + DRIFT (no match) allowlist hardcode.
      multi-job workflow pattern 인정 (contract-lint.yml + lint.yml = 5+ entry 공유 jobs).
      Conservative no-rename policy — workflow rename 회피 (CI history + branch protection
      `required_status_checks.contexts` 영향 회피). Exit code 3-tier (ADR-060 Amendment 2
      §결정 15): 0 PASS / 1 violation (workflow file 부재 OR allowlist 밖 DRIFT) / 2
      meta-error (yaml parse 실패).
    detect_command: bash scripts/check-evidence-registry-naming.sh
    workflow: templates/github-workflows/evidence-registry-naming-check.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:evidence-naming
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — naming convention lint 검증 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-508
    introduced_date: 2026-05-13
    owner_adr: ADR-060               # framework SSOT — entry name convention policy owner
    carrier_adr: ADR-060             # Amendment 7 carrier
    recurrence:
      count: 0
      promotion_trigger: none        # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active
  - name: workflow-permissions-block-presence
    description: |
      ADR-060 Amendment 8 §결정 21 mechanical verification —
      .github/workflows/*.yml + templates/github-workflows/*.yml top-level
      `permissions:` block 부재 detect. Glob scope = *.yml only.
    detect_command: bash scripts/check-workflow-permissions-presence.sh
    workflow: templates/github-workflows/workflow-permissions-check.yml
    current_tier: warning
    bypass_label: hotfix-bypass:workflow-permissions
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-530
    introduced_date: 2026-05-13
    owner_adr: ADR-060
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none
    status: Active

  - name: workflow-yaml-parse
    description: |
      CFP-583 / ADR-060 Amendment 9 §결정 22 — workflow yml YAML parse validation gate.
      .github/workflows/*.yml + templates/github-workflows/*.yml 전체 대상 PyYAML safe_load +
      actionlint dual validation. BODY heredoc anti-pattern (multi-line + backtick fence +
      variable interpolation) 재발 방지 recurrence prevention forcing function.
      broken workflow = absent 와 동치 → framework promotion gate measurement 무효.
    detect_command: bash scripts/check-workflow-yaml-parse.sh
    workflow: templates/github-workflows/workflow-yaml-parse.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:workflow-yaml-parse
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []         # 독립 entry
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-583
    introduced_date: 2026-05-13
    owner_adr: ADR-060
    carrier_adr: ADR-060
    recurrence:
      count: 6                         # CFP-449 carrier 이후 broken 상태 6 workflow 동시 영향 (§2.1 evidence)
      threshold: 3                     # ADR-060 §결정 6 promotion gate
      promotion_trigger: advisory      # recurrence count ≥ threshold → advisory comment (별도 carrier 승격 평가)
      last_occurrence: 2026-05-13      # CFP-583 fix 시점 — broken-baseline 회복 baseline 재시작
    status: Active

  - name: debate-convergence-quality-marker-presence
    description: |
      CFP-582 Phase 2 / ADR-059 Amendment 2 §결정 8 — convergence_quality_invariant
      3 marker pattern mechanical enforcement. Story §9 debate transcript 내
      [COUNTERARGUMENT] / [ALTERNATIVE_PROPOSED] / [DEBATE_PURPOSE_STATEMENT]
      section header 존재 여부 검사. debate-protocol-v1 v1.2 schema
      convergence_quality_invariant block 과 cross-validate.
      ADR-060 warning-tier entry (첫 debate 영역 entry).
    detect_command: python scripts/check_debate_convergence_quality.py
    workflow: templates/github-workflows/debate-convergence-quality.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:debate-convergence-quality
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-582
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-582
    introduced_date: 2026-05-13
    owner_adr: ADR-059
    carrier_adr: ADR-059
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: Active

  - name: parallel-dispatch-prompt-check
    description: |
      CFP-609 Phase 1 / ADR-064 Amendment 1 §결정 4 — parallel-dispatch-protocol-v1
      §8 mechanical enforcement. Orchestrator → PL spawn prompt 의 4 의무 항목 박제
      검사 (registry §4):
        1. plan DAG batches list verbatim (registry §4.1)
        2. pl_autonomous_parallel_authority required 박제 (disabled 차단, registry §4.2)
        3. sequential_mandate_reasons 6 enum close-set 위반 detect (registry §3)
        4. file_conflict_resolution_patterns 박제 (registry §4.4)
        5. worker_count <= worker_count_max default 7 (registry §6.2, OpRiskArch §7.4.4)
      parallel-dispatch-protocol-v1 kind:registry mechanical enforcement carrier.
      ADR-060 warning-tier entry — execution-time enforcement (proposing-time anchor 와 직교).
    detect_command: bash scripts/check-parallel-dispatch-prompt.sh
    workflow: templates/github-workflows/parallel-dispatch-prompt-check.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:parallel-dispatch-prompt
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-609
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-609
    introduced_date: 2026-05-13
    owner_adr: ADR-064  # Amendment 1 carrier
    carrier_adr: ADR-060
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: Active

  - name: wording-dictionary
    description: |-
      CFP-610 / ADR-064 Amendment 2 — wording-dictionary lint.
      카테고리 (a) forbid 어휘 발견 시 exit 1 warning (박제 / 못 박기 / pin / freezing).
      카테고리 (b) 어휘 평문 정의 누락 시 exit 0 advisory (normative / sibling sync / kind:contract / ratchet / mirrored field).
      SSOT: docs/wording-dictionary.md. 5 scope: docs/adr/** / docs/change-plans/** / CLAUDE.md / docs/orchestrator-playbook.md / templates/**
    detect_command: bash scripts/check-wording-dictionary.sh docs/adr docs/change-plans CLAUDE.md docs/orchestrator-playbook.md templates
    workflow: .github/workflows/wording-dictionary.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:wording-dictionary
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-610
    introduced_date: 2026-05-13
    owner_adr: ADR-064
    carrier_adr: ADR-064
    sunset_gate:
      metric: "Orchestrator user-facing 응답 안 forbid 어휘 발생률 < 1건/100 turn + 사용자 frustration 발화 0건/3 Story"
      who: "PMOAgent (cross-Story retro 패턴 분석)"
      how: "PMOAgent retro file §wording-discipline 표 (lint warning count + frustration timestamp delta)"
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: Active

  - name: retro-mandatory-deployed
    description: |-
      CFP-619 / ADR-045 mandate — retro-mandatory.yml workflow .github/workflows/
      배포 sentinel. byte-identical mirror invariant (ADR-005) + workflow runtime
      활성화 detection. drift 시 required-workflow-drift-check.yml 가 SSOT 와의
      차이를 별도로 lint — 본 entry 는 deployment fact 자체의 evidence-enforceable
      tracking (warning tier, ADR-060 framework 첫 deployment-class entry).
      CFP-612 Phase 2 PR #618 merge 시점 manual fallback 첫 observed sample.
    detect_command: bash -c 'test -f .github/workflows/retro-mandatory.yml && diff -q templates/github-workflows/retro-mandatory.yml .github/workflows/retro-mandatory.yml'
    workflow: null  # static deployment detection — runtime workflow 별도 (retro-mandatory.yml 자체)
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:retro-mandatory-deployed
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a)
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-619
    introduced_date: 2026-05-14
    owner_adr: ADR-045
    carrier_adr: ADR-060
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: Active

  - name: retro-alert-pickup-rate
    description: |-
      CFP-628 Story 2 / ADR-045 §D-5 — Orchestrator SessionStart retro alert scan
      mandate 의 KPI sentinel. open `phase:완료` issue 안 `[PMO] retro alert` prefix
      comment (35min filter 후) 발화 대비 retro file 생성 비율 측정.
      pickup_rate ≥ 90% (3 month window) = sunset_gate 충족.
      monthly cron `retro-alert-pickup-kpi.yml` 자동 측정.
      scripts/check-retro-alerts.sh SessionStart hook 으로 Orchestrator prompt-injection.
    script: scripts/check-retro-alerts.sh
    detect_command: bash scripts/check-retro-alerts.sh
    workflow: .github/workflows/retro-alert-pickup-kpi.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:retro-alert-pickup
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a)
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - kpi_rate_json_history
        - pr_cumulative_count_proof
    introduced_by: CFP-628
    introduced_date: 2026-05-14
    owner_adr: ADR-045
    carrier_adr: ADR-060
    sunset_gate:
      metric: "retro-alert-pickup-rate ≥ 90% (3 month rolling window)"
      who: "PMOAgent (monthly cron retro-alert-pickup-kpi.yml 자동 측정)"
      how: "docs/kpi/retro-alert-pickup-rate.json gate_status 3 consecutive months = ok → sunset 판정"
    recurrence:
      count: 0
      promotion_trigger: none
      last_occurrence: null
    status: Active

  - name: marketplace-description-verbatim
    description: |-
      CFP-631 / ADR-063 Amendment 2 §결정 11 — wrapper plugin.json description ↔
      marketplace.json plugins[codeforge].description byte-identical verify (PR-time enforce).
      mirrored field 4종 중 description 단독 PR-time enforce 추가 — name/author = check-marketplace-parity
      warning sufficient, version = version-bump-atomic-check blocking-on-pr 기존 cover.
      6 sample 누적 drift evidence (CFP-387/393/423/597/612/619, CFP-619 retro §5.2 SSOT).
      blocking-on-pr tier 직접 시작 근거 = ADR-060 §결정 5 default warning explicit exception
      + §결정 19 (Amendment 6, CFP-509) auto_blocking manual gate path (recurrence.count=6 +
      threshold=6 + promotion_trigger=auto_blocking) + 사용자 directive Story §1 verbatim —
      본 carrier Story 자체가 §결정 6 별도 promotion carrier 의무 합병.
      Amendment 1 (design-time self-check, CFP-597) 와 layered 2-layer proactive forcing function.
    detect_command: bash scripts/check-marketplace-description-verbatim.sh
    workflow: templates/github-workflows/marketplace-description-verbatim.yml
    current_tier: blocking-on-pr   # ADR-060 §결정 5 explicit exception + §결정 19 auto_blocking manual gate (Phase 2 PR 부터 active enforce)
    bypass_label: hotfix-bypass:marketplace-description-verbatim
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []         # 독립 entry — 본 carrier Story 자체가 carrier
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-631
    introduced_date: 2026-05-14
    owner_adr: ADR-063
    carrier_adr: ADR-060
    recurrence:
      count: 6                       # CFP-387/393/423/597/612/619 6 sample 누적 [empirical-source: CFP-619 retro §5.2]
      threshold: 6                   # ADR-060 §결정 19 (Amendment 6) — recurrence threshold (count >= threshold = signal 발화)
      promotion_trigger: auto_blocking  # ADR-060 §결정 19 enum — actual transition 본 carrier Story 자체가 별도 carrier 의무 합병 (사용자 explicit directive Story §1)
      last_occurrence: 2026-05-14   # CFP-619 byte-identical fix commit c199dee [empirical-source: marketplace git log]
    status: Active
