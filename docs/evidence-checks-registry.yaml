# docs/evidence-checks-registry.yaml
# Evidence-enforceable governance check registry — single SSOT
# Schema doc: docs/inter-plugin-contracts/evidence-check-registry-v1.md
# Carrier ADR: ADR-060 (Evidence-enforceable promotion framework)
# Lint chain: check-doc-frontmatter.sh + check-doc-section-schema.sh (kind:registry pattern)
# parallel-edit policy: append-only (docs/parallel-work/section-ownership.yaml)
#
# 18 entry backfill complete Phase 2 (CFP-390 §결정 13 SSOT, ADR-060 Amendment 1, 2026-05-11).
# Phase 1 PR (#415) = SSOT 만 (ADR Amendment 본문 표 + sibling_dependencies field 정정).
# Phase 2 PR = 본 §결정 13 표의 18 entry 실제 row append (CFP-390 그룹 A 인벤토리).
# Total entries 가변 — 본 PR base 시점 = 2 + 18 = 20. main rebase 후 CFP-393 / CFP-410
# 추가 entry (rate-limit-fallback-rate, story-section-9-typed) 병합 = 22 entry.
schema_version: "1.3"  # CFP-963 / ADR-060 Amendment 14 + ADR-081 Amendment 4 — lane_evidence[].network_scope_actual optional field 정식 도입 (MINOR bump v1.2 → v1.3, evidence-check-registry-v1 §3.1 codify, MANIFEST drift catch-up "1.1" → "1.3" 동반 — CFP-509 v1.1→v1.2 sibling MANIFEST sync miss 영역의 INV-1 parity ratchet) + CFP-509 / ADR-060 Amendment 6 — recurrence field 정식 도입 (MINOR bump v1.1 → v1.2)
introduced_by: CFP-389
last_updated: 2026-05-19  # CFP-967 Phase 2 — parallel-work-sentinel-pickup status deferred-followup → warning + detect_command + workflow populated (script + workflow 신설 mechanical wire 완료). | CFP-963 Phase 1 — codex-network-scope-presence entry append (ADR-060 Amendment 14 §결정 28 + ADR-081 Amendment 4 §결정 D1.D body 확장 carrier — declaration source ADR-081 / enforcement source ADR-060 dual-binding 첫 사례, owner_adr ADR-081-Amendment-4, warning tier, deferred-followup status — Phase 2 actual lint script + workflow + bats fixture pair (CX-963-3 P2 boundary mandate per Codex TP#4) wire 후 Active 전환, recurrence count 0 / threshold 3 / promotion_trigger none — sentinel-driven 아닌 ratchet 확장 carrier, schema_version 1.2→1.3 동반 — lane_evidence[].network_scope_actual optional field SSOT codify, sibling_dependencies CFP-963 Phase 2 actual wire, ADR-068 I-3 unconditional guard placement 정합 boolean grace `[legacy-boolean-detected]` unconditional advisory exit 0). | CFP-966 Phase 1 — parallel-work-sentinel-pickup entry append (ADR-073 Amendment 2 carrier — declarative anchor, owner_adr ADR-073-Amendment-2, warning tier, deferred-followup status (CFP-967 mechanical wire Phase 2 carrier 후 Active, L1417/L1452/L1574 precedent 답습), recurrence count 2 / threshold 3 / promotion_trigger auto_blocking, schema v1.2 정합, sibling_dependencies CFP-967 mechanical wire, sentinel evidence CFP-953+CFP-946 same-day 2-occurrence 2026-05-18 KST, entries[] 말미 relative append, post-CFP-899/923/898 collision-free rebase, Codex #2 FIX iter 2 owner_adr+status SSOT 통일) | CFP-841 Phase 1 — corpus-claim-verify + cross-plugin-ownership-verify 2 entry append (ADR-082 Amendment 1 §결정 6 behavioral→mechanical 전환 carrier — scope 2(a) corpus annotation lint + scope 2(d) cross-plugin ownership queryable lint, owner_adr ADR-082 / carrier_adr ADR-060, warning tier deferred-followup status — Phase 2 actual lint script + workflow wire 후 Active 전환, entries[] 말미 relative append, post-CFP-833/820 collision rebase) | CFP-833 Phase 1 — dialog-fidelity-effect entry append (Epic CFP-761 Story-3 closing-the-loop, ADR-071 Amendment 3 carrier, owner_adr ADR-071 / carrier_adr ADR-060, precedent rate-limit-fallback-rate 동형 runtime cron metric pattern, warning tier advisory, bypass_label omit OQ-3 사용자 확정, entries[] 말미 relative append) | CFP-745 Phase 2 — wrapper-template-managed-coverage 59번째 entry (CFP-722 57th + CFP-771 58th + CFP-745 59th, authoring-guard lint Phase 2 §8.2 declare, post-CFP-810/795/801/777/751 collision rebase) | CFP-722 Phase 2 — story-section-ownership 57번째 entry (ADR-060 Amendment 13 §결정 27 carrier — per-section ownership mechanical lint, warning tier, PR #441 incident prevention) | CFP-442 Phase 2 — evidence-registry-anomaly 51번째 entry (ADR-060 Amendment 11 §결정 25 carrier — framework self-application 5-piece chain 완성) | CFP-662 — bootstrap-labels-precondition 46번째 entry (ADR-060 Amendment 10 §결정 24 carrier — PR-time precondition check pattern 의 첫 baseline, RETRO-MCT-104 carrier, deferred-followup status — Phase 2 carrier 신설 후 Active 전환) | CFP-660 — workflow-version-drift 45번째 entry (ADR-032 Amendment 2 §결정 6 carrier — strict-eligible 5번째 drift consumer workflow version drift, check 10 NEW carrier) | CFP-658 — section-1-verbatim-postmerge 44번째 entry append (ADR-027 Amendment 2 §결정 6.C carrier — manual fallback path §1 verbatim invariant post-merge lint warning tier, deferred-followup status, Phase 2 carrier 신설 후 Active 전환) | CFP-627 FIX iter 3/3 — marketplace-drift-detection entry deferred to Phase 2 carrier (ADR-067 §결정 3 cross-lane RESET 차원 전환) | CFP-638 — stop-time-continuous-confirm-detect 43번째 entry (ADR-064 Amendment 3 Story D) | CFP-631 — marketplace-description-verbatim 42번째 entry (ADR-063 Amendment 2 §결정 11 blocking-on-pr)

entries:
  - name: adr-sunset-criteria
    description: |
      ADR-058 §결정 1-3 mechanical verification — is_transitional frontmatter +
      ## 해소 기준 섹션 + 측정성 3-tuple (metric / who / how) + 모달 어휘 1차 사전 검사.
      ADR-058 declaration 의 첫 evidence-enforceable check (CFP-B 잠정 carrier).
    detect_command: bash scripts/check-adr-sunset-criteria.sh
    workflow: templates/github-workflows/adr-sunset-criteria.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:adr-sunset
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:            # ADR-060 §결정 6 (c) — Amendment 2 정정 (CFP-455 append)
        - CFP-390  # 인벤토리 backfill — registry yaml row append (Amendment 1 carrier, merged 2026-05-11)
        - CFP-412  # 4-tier amendment 재예약 carrier — closed without delivery (Issue #412, 2026-05-11)
                   # CFP-391 (Issue #396) closed without delivery (2026-05-11) — Amendment 1 정정
        - CFP-455  # 4-tier 정식 amendment carrier (재재예약) — current_tier required 전환 deliver (Amendment 2)
      evidence_artifacts:              # 승격 carrier PR 의 manual 첨부 의무 (자동화 인프라 별도 carrier)
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary:
      version: "1.0"                   # ADR-060 §결정 9 — 4 표현 only (ADR-058 §결정 8 verbatim)
      dictionary:
        - "안정화되면"
        - "임시"
        - "한시적"
        - "until further notice"
    introduced_by: CFP-389
    owner_adr: ADR-058
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: carrier-bootstrap
    description: |
      ADR-062 §결정 1-2 mechanical verification — carrier Story frontmatter
      `carrier_story` + `bootstrap_exempt_protocols` typed key list 정합 (5 standard
      prefix: adr / contract / policy / workflow / script) + §3 본문 ADR/contract
      참조 정합. self-application 첫 사례 = CFP-407 자체 (§결정 8).
    detect_command: bash scripts/check-carrier-bootstrap.sh
    workflow: templates/github-workflows/carrier-bootstrap-check.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    # bypass_label: omit — 4 known carrier 중 0건 bypass 필요, 후속 carrier 가 도입 시 추가
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a)
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}  # 본 lint = schema 검증, modal phrase 검사 미포함
    introduced_by: CFP-407
    owner_adr: ADR-062
    carrier_adr: ADR-062               # self-carrier
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: rate-limit-fallback-rate
    description: |
      ADR-057 §결정 2 sunset gate measurement — 월간 Sonnet spawn 총 카운트 (분모) +
      §14 Lane Evidence `transcript` 필드의 `[rate-limit-fallback:sonnet→opus]` 태그
      발화 카운트 (분자) → 3 month rolling window fallback rate. runtime cron metric
      pattern (vs static lint, 첫 entry `adr-sunset-criteria` 와 patterns 대비) — advisory
      dashboard, no PR block. framework 첫 non-sunset application (CFP-388 / ADR-060
      §결정 12 CFP-C carry). sample size sufficient sentinel (월 < 50 spawn 환경 = N/A).
    detect_command: bash scripts/measure-rate-limit-fallback.sh
    workflow: templates/github-workflows/rate-limit-fallback-kpi.yml
    current_tier: warning  # advisory dashboard, no PR block — semantic = "warning mode 의 runtime metric extension"
    # bypass_label: omit — runtime metric pattern 에서 의미 부적용
    # bypass_audit_lint: omit — bypass_label 미정의
    promotion_criteria:
      # 본 entry 의 promotion 의미 = lint warning→blocking promotion 아닌,
      # sample size sufficient 도달 시 ADR-057 §결정 2 sunset gate measurement.
      # pr_cumulative_min=0 = promotion semantic 부적용 표기.
      pr_cumulative_min: 0
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - kpi_dashboard_3month_window_evidence  # sunset 판정 carrier PR 의 산출물 (별도 carrier)
    # modal_anti_pattern_dictionary: omit — lint 가 모달 어휘 검사 미포함
    introduced_by: CFP-393
    owner_adr: ADR-057           # 정책 ADR — fallback rate sunset gate owner
    carrier_adr: ADR-060         # framework carrier (CFP-388 / ADR-060 §결정 12 CFP-C)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: story-section-9-typed
    description: |
      Story §9.x sub-section yaml block schema validation — review-verdict-v4 packet
      inline embed 표준 패턴 검증. free-form markdown → typed yaml block 으로 정보 손실
      최소화 + 자동 검증 가능성 확보. yaml block 부재 = OK (backward compat),
      block 존재 시만 schema 검증. Warning tier (PR-block 없음, console warning 만).
      CFP-391 retro follow-up 5건 중 P3 최종 (CFP-410).
    detect_command: bash scripts/check-story-section-9-typed.sh
    workflow: templates/github-workflows/story-section-9-typed-check.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:story-section-9-typed
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []         # 독립 entry — registry append + carrier patterns 검증 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-410
    owner_adr: ADR-044           # review-verdict-v4 schema source
    carrier_adr: ADR-060         # framework carrier
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-390 Phase 2 backfill (18 entries, ADR-060 Amendment 1 §결정 13 SSOT, 2026-05-11) ───
  # 그룹 A — 4-criteria PASS (detect_command + workflow + owner_adr/contract + tier signal)
  # tier 최종 분포: blocking-on-merge 1 + blocking-on-pr 1 + warning 16 (manifest 부착 entry 만 blocking)

  - name: lane-evidence-trail
    description: |
      ADR-031 lane-spawn evidence mandate — Story §14 Lane Evidence row 부재 검출 +
      Phase 2 PR `## Lane evidence` 블록 cross-validate (CFP-126). bypass = env
      BYPASS_LANE_EVIDENCE=1 + reason.

      Actual recurrence (description-only documentation signal, machine-usable
      promotion signal 아님 — ADR-060 4-tier promotion path / failure_threshold
      / pr_cumulative_min 와 무관):
        - CFP-500 FIX-5 (1차): #456 merge 직전 첫 actual collision (table format +
          7-row format 두 heading 등장 → first-match capture boundary 로 인해
          첫 heading 만 capture, 7-row regex 0/7 match → fail)
        - CFP-451 본 세션 transient (2차): #486 step 3 시점 동일 패턴 재현 후
          step 4 awk-delete fix → step 5 final PASS

      Logic refinement (CFP-490 Phase 2): 5a guard tie-break case A/B/C 식별 +
      .mjs extraction (.github/scripts/check-lane-evidence-block.mjs) +
      analyzeDuplicateHeadings() 함수로 valid heading 명시. Option A strict
      (CFP-465 invariant 보존) — lenient fallback 채택 안 함. recurrence_count
      framework / promotion gate input / lint warning condition 모두 무관 (description-only narrative).
      CFP-509 schema v1.2 (ADR-060 Amendment 6) 에 `recurrence:` field 정식 도입 — 본 entry 는
      schema-level count: 2 보유 (CFP-500 FIX-5 + CFP-451 transient 2회 historical evidence).
    detect_command: bash scripts/check-lane-evidence.sh
    workflow: .github/workflows/lane-evidence-check.yml
    current_tier: warning            # manifest 미부착 — Phase 2 backfill 시점 정합 (§결정 13 표)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-031 §결정 3 + fix-event-v1
    carrier_adr: ADR-060
    recurrence:
      count: 2
      last_occurrence: "2026-05-12T00:00:00Z"    # CFP-451 본 세션 transient 2차 발생 (대략 timestamp)
      threshold: 3
      promotion_trigger: advisory              # threshold 도달 시 PR comment advisory
    status: Active

  - name: doc-locations-registry
    description: |
      ADR-041 doc taxonomy 위치 SSOT 검증 — docs/doc-locations.yaml 의 정의 path
      대비 실제 doc file 존재 + naming pattern 정합 검증 (--full mode).
      CLAUDE.md "branch protection 5번째" narrative 와 manifest 부재 drift 존재 (별도 Issue 권고).
    detect_command: bash scripts/check-doc-locations.sh --full
    workflow: .github/workflows/doc-locations-check.yml
    current_tier: warning            # manifest 미부착 (narrative drift, §결정 13 표 row 2)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-041
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: marketplace-parity
    description: |
      ADR-016 marketplace registration parity — wrapper + 6 lane plugin 의 mirrored
      field (name / version / description / author) cross-repo drift 검증.
      ADR-023 §결정 5 lane plugin lifecycle 동반.
    detect_command: bash scripts/check-marketplace-parity.sh
    workflow: .github/workflows/marketplace-parity.yml
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-016 / ADR-023 §결정 5
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: invariant-check
    description: |
      5 invariant aggregated check — Story §1 immutable + ADR-002 footer pattern +
      phase label invariant + script exec bit + workflow yaml syntax 등 multi-CFP
      (5 / 7 / 8 / 10) 위반 검출. branch-protection-manifest row 2 부착.
    detect_command: workflow inline — 5 invariant 직접
    workflow: .github/workflows/invariant-check.yml
    current_tier: blocking-on-pr     # branch-protection-manifest row 2 (§결정 13 표)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-002 (footer pattern) + 다중 CFP (5/7/8/10)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: phase-gate-mergeable
    description: |
      ADR-031 phase gate mergeable — cross-repo Story fetch + label-registry-v2 phase
      label 정합 검증 + dynamic checks.create 발화. branch-protection-manifest row 1
      부착 — merge 시점 차단 (blocking-on-merge).
    detect_command: workflow inline — cross-repo Story fetch
    workflow: .github/workflows/phase-gate-mergeable.yml
    current_tier: blocking-on-merge  # branch-protection-manifest row 1, dynamic checks.create (§결정 13 표)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-031 §결정 3 + label-registry-v2
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: inter-plugin-contracts
    description: |
      ADR-008 / ADR-010 inter-plugin contract MANIFEST.yaml 정합 검증 — 6 typed
      contract 의 producer plugin / canonical-sibling pair / status enum + active
      한 row 보장. wrapper sibling sync 의무 cross-validate.
    detect_command: bash scripts/check-inter-plugin-contracts.sh
    workflow: .github/workflows/contract-lint.yml  # job: inter-plugin-contracts
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-008 / ADR-010 / MANIFEST.yaml
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: inter-plugin-drift
    description: |
      ADR-011 inter-plugin contract drift 검증 — canonical (lane plugin) vs sibling
      (wrapper) verbatim mirror 정합. drift 검출 시 sibling sync PR 후속 의무.
    detect_command: bash scripts/check-inter-plugin-drift.sh
    workflow: .github/workflows/contract-lint.yml  # job: inter-plugin-drift
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-011
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: comment-prefix-registry
    description: |
      comment-prefix-registry-v1 (kind:registry) 11 phase prefix taxonomy 검증 —
      `[요구사항]` / `[설계]` / `[설계-리뷰]` / `[구현]` / `[구현-리뷰]` 등 lane
      plugin self-write comment prefix 정합.
    detect_command: bash scripts/check-comment-prefix.sh
    workflow: .github/workflows/contract-lint.yml  # job: comment-prefix-registry
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: comment-prefix-registry-v1 (kind:registry)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: label-registry-sync
    description: |
      label-registry-v2 (kind:registry) phase/gate/fix label taxonomy sync 검증 —
      GitHub repo label state vs registry SSOT drift.
    detect_command: bash scripts/check-label-registry.sh
    workflow: .github/workflows/contract-lint.yml  # job: label-registry-sync
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: label-registry-v2 (kind:registry)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: marketplace-sync
    description: |
      ADR-016 marketplace mirrored field sync 검증 — codeforge plugin family 7 plugin
      의 marketplace.json mirrored field (name / version / description / author) 정합.
      [CFP-457 retired 2026-05-12] — redundant with marketplace-parity (CFP-50 / ADR-023).
      Single SSOT = marketplace-parity entry above.
    detect_command: bash scripts/check-marketplace-sync.sh  # DELETED in CFP-457
    workflow: .github/workflows/contract-lint.yml  # job: marketplace-sync (removed in CFP-457)
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-016 (mirrored field)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Retired  # CFP-457 — replaced by marketplace-parity (CFP-50 / ADR-023)
    retired_by: CFP-457
    retired_date: 2026-05-12

  - name: dogfood-artifact-paths
    description: |
      ADR-013 / ADR-017 dogfood-out path 검증 — codeforge family plugin repo 의
      brainstorming spec + writing-plans plan 저장 위치가 internal-docs override
      (default `docs/superpowers/*` 아님) 정합.
    detect_command: bash scripts/check-dogfood-artifact-paths.sh
    workflow: .github/workflows/dogfood-artifact-paths.yml
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-013 / ADR-017
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: superpowers-integration
    description: |
      ADR-028 / CFP-113 superpowers plugin 8 skill × 17 lane agent integration
      매트릭스 정합 검증 — skill 호출 가능 surface drift.
    detect_command: bash scripts/check-superpowers-integration.sh
    workflow: .github/workflows/superpowers-integration.yml
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-028 / CFP-113
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: superpowers-schema-drift
    description: |
      ADR-028 / CFP-121 superpowers skill schema drift 검증 — quarterly scheduled
      (3 month rolling) + PR:paths trigger.
    detect_command: bash scripts/check-superpowers-schema-drift.sh
    workflow: .github/workflows/superpowers-schema-drift.yml
    current_tier: warning            # scheduled quarterly + PR:paths (§결정 13 표 row 13)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-028 / CFP-121
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: parallel-epic-conflict
    description: |
      ADR-050 parallel epic coordination — 복수 open Epic 의 scope_manifest 교집합
      검출 + merge-order label 자동 부여. continue-on-error prior art (warning mode
      reference).
    detect_command: workflow inline — PR file 교집합
    workflow: .github/workflows/parallel-epic-conflict-check.yml
    current_tier: warning            # continue-on-error prior art (§결정 13 표 row 14)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-050
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: branch-protection-drift
    description: |
      ADR-024 Amendment 2 branch protection drift 검출 — gh api response vs
      templates/branch-protection-manifest.yaml diff. weekly cron (Mon 09:00 UTC) +
      manual trigger.
    detect_command: workflow inline — gh api + manifest diff
    workflow: .github/workflows/branch-protection-drift-check.yml
    current_tier: warning            # weekly Mon 09:00 UTC cron (§결정 13 표 row 15)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-024 Amendment 2
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: required-workflow-drift
    description: |
      ADR-048 §결정 3 required workflow drift — enterprise admin 가용 가정 하
      required_status_checks contexts vs manifest contexts diff. weekly cron
      (Mon 10:00 UTC).
    detect_command: workflow inline + check-enterprise-admin.sh
    workflow: .github/workflows/required-workflow-drift-check.yml
    current_tier: warning            # weekly Mon 10:00 UTC cron (§결정 13 표 row 16)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-048 §결정 3
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: rulesets-drift
    description: |
      ADR-048 §결정 1 GitHub repository rulesets drift — daily cron (09:00 UTC)
      gh api rulesets response vs templates/rulesets-manifest.yaml diff.
    detect_command: workflow inline + check-enterprise-admin.sh
    workflow: .github/workflows/rulesets-drift-check.yml
    current_tier: warning            # daily 09:00 UTC cron (§결정 13 표 row 17)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: ADR-048 §결정 1
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: write-permission-redistribution
    description: |
      CFP-26 / ADR-009 write 권한 invariant — lane plugin 의 self-write boundary
      (docs/** + GitHub Issue/PR/comment) 정합. Phase 0a write 권한 재분배 후
      regression 방지.
    detect_command: bash scripts/check-write-permission-redistribution.sh
    workflow: .github/workflows/lint.yml  # job: write-permission-redistribution
    current_tier: warning
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-390
    owner_adr: CFP-26 / ADR-009 (write 권한 invariant)
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-426 Phase 2 backfill (4 entries, ADR-040 Amendment 3 §결정 7.D verbatim, 2026-05-12) ───
  # Story 1 skeleton (warning tier) — actual wire 는 후속 Story:
  #   - CFP-427 (Story 2): worktree-first-session-start-wire + worktree-first-spawn-evidence-cwd actual wire
  #   - CFP-428 (Story 3): worktree-first-pre-checkout + worktree-first-pre-commit-main-block actual git hook
  #   - CFP-429 (Story 4): 4 entry warning → blocking-on-pr 승격 평가 (ADR-060 §결정 6 gate)
  # carrier ADR-040 mechanical_enforcement_actions[] 4 entry self-application 첫 사례 (Amendment 3 §결정 7.D)

  - name: worktree-first-session-start-wire
    description: |
      ADR-040 §결정 5 (Stale GC) + §결정 7 (Amendment 3) — wrapper repo `.claude/settings.json` 의
      `hooks.SessionStart[]` 안에 `check-worktree-stale.sh` 호출 entry 존재 검증. SessionStart hook
      wire 누락 시 stale worktree 자동 GC 미동작 (declaration only → mechanical enforcement 분리).
      warning tier — continue-on-error: true. CFP-427 Story 2 가 actual wire 첫 사례 —
      `scripts/check-session-start-hook-presence.sh` `exec` swap wrapper 패턴 (RefactorArch 결정).
      CFP-429 (Story 4) Amendment 4 declaration: 4/4 actual wire 완료 + ADR-060 §결정 6 promotion
      gate (b) bypass 외 failure > 0 FAIL → current_tier: warning 유지 + actual 승격 follow-up CFP
      carrier (from-cfp-425-followup label). Self-test integration test: tests/integration/stories/CFP-425/self-test-worktree-block.sh E4-1 (Phase 2 PR CFP-429).
    detect_command: bash scripts/check-worktree-first-session-start-wire.sh
    workflow: templates/github-workflows/worktree-first-session-start-wire.yml
    current_tier: blocking-on-pr  # CFP-531 / ADR-040 Amendment 5 — warning → blocking-on-pr 승격 (2026-05-13)
    bypass_label: hotfix-bypass:worktree-session-start-wire
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse (BYPASS_LABEL_PREFIX=hotfix-bypass:)
    promotion_criteria:
      pr_cumulative_min: 20         # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0          # ADR-060 §결정 6 (b)
      sibling_dependencies:         # ADR-060 §결정 6 (c) — Story 4 진입 시 평가
        - CFP-427
        - CFP-428
        - CFP-429
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-426
    owner_adr: ADR-040
    carrier_adr: ADR-040
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: worktree-first-pre-checkout
    description: |
      ADR-040 §결정 7 (Amendment 3) — main working tree 에서 `git checkout cfp-NNN` 등 lane branch
      checkout 차단 (worktree-first 정책 정합). pre-checkout git hook sample 존재 검증 +
      install 권고 출력 (warning tier — install 강제 X). CFP-428 (Story 3) 가 actual wire 첫
      사례 — `templates/.git-hooks/pre-checkout.sample` + `scripts/install-git-hooks.sh` opt-in
      installer. lint pattern = verification-only (sample 존재 + executable bit 검증,
      Story 2 exec swap 패턴 부적용 — canonical 이 git hook sample 자체).
      CFP-429 (Story 4) Amendment 4 declaration: 4/4 actual wire 완료 + ADR-060 §결정 6 promotion
      gate (b) bypass 외 failure > 0 FAIL → current_tier: warning 유지 + actual 승격 follow-up CFP
      carrier (from-cfp-425-followup label). Self-test integration test: tests/integration/stories/CFP-425/self-test-worktree-block.sh E4-3 (Phase 2 PR CFP-429).
    detect_command: bash scripts/check-worktree-first-pre-checkout.sh
    workflow: templates/github-workflows/worktree-first-pre-checkout.yml
    current_tier: blocking-on-pr  # CFP-531 / ADR-040 Amendment 5 — warning → blocking-on-pr 승격 (2026-05-13)
    bypass_label: hotfix-bypass:worktree-pre-checkout
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies:
        - CFP-427
        - CFP-428
        - CFP-429
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-426
    owner_adr: ADR-040
    carrier_adr: ADR-040
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: worktree-first-pre-commit-main-block
    description: |
      ADR-040 §결정 7 (Amendment 3) — main working tree 에서 src/docs commit 차단 (worktree-first
      정책 정합). pre-commit-main-block git hook sample 존재 검증 + install 권고 출력. CFP-428
      (Story 3) 가 actual wire 첫 사례 — `templates/.git-hooks/pre-commit-main-block.sample` +
      `scripts/install-git-hooks.sh` opt-in installer. path matching = `src/**` 또는 `docs/**`
      only (Plan SSOT verbatim — chore commit / typo fix 등 차단 0). lint pattern =
      verification-only.
      CFP-429 (Story 4) Amendment 4 declaration: 4/4 actual wire 완료 + ADR-060 §결정 6 promotion
      gate (b) bypass 외 failure > 0 FAIL → current_tier: warning 유지 + actual 승격 follow-up CFP
      carrier (from-cfp-425-followup label). Self-test integration test: tests/integration/stories/CFP-425/self-test-worktree-block.sh E4-4 (Phase 2 PR CFP-429).
    detect_command: bash scripts/check-worktree-first-pre-commit-main-block.sh
    workflow: templates/github-workflows/worktree-first-pre-commit-main-block.yml
    current_tier: blocking-on-pr  # CFP-531 / ADR-040 Amendment 5 — warning → blocking-on-pr 승격 (2026-05-13)
    bypass_label: hotfix-bypass:worktree-pre-commit-main-block
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies:
        - CFP-427
        - CFP-428
        - CFP-429
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-426
    owner_adr: ADR-040
    carrier_adr: ADR-040
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: worktree-first-spawn-evidence-cwd
    description: |
      ADR-040 §결정 5 + §결정 7 (Amendment 3) + ADR-031 §결정 4 — §14 Lane Evidence row 의
      `Working dir:` field 가 worktree path (`${HOME}/.claude/worktrees/<repo>/<branch-flat>` 정합)
      또는 `N/A — <30자 이상 사유>` (ADR-031 §결정 4 bypass mechanism) 인지 검증. wrapper repo
      `docs/stories/**.md` scan. CFP-427 Story 2 가 actual wire 첫 사례 — enforce-from filter
      도입 (본 Story merged at 이후 신규 Story 만, false-positive 회피) + PLACEHOLDER sentinel
      safe-fallback (Phase 2 PR merge 전 enforce 전체 skip).
      CFP-429 (Story 4) Amendment 4 declaration: 4/4 actual wire 완료 + ADR-060 §결정 6 promotion
      gate (b) bypass 외 failure > 0 FAIL → current_tier: warning 유지 + actual 승격 follow-up CFP
      carrier (from-cfp-425-followup label). Self-test integration test: tests/integration/stories/CFP-425/self-test-worktree-block.sh E4-2 (Phase 2 PR CFP-429).
      CFP-843 (ADR-040 Amendment 6) scope 확장 SSOT 선언: 기존 §14 Lane Evidence `Working dir:`
      field (lane spawn CWD) 검증 외 agent transcript Write/Edit tool target path + agent-internal
      git write target 의 worktree-membership 검증 추가 (harness CWD reset gap 차단 — CFP-825 §3
      RC-1 (a)/(b) 동근원 + #836 spurious artifact). lint timing = 사후 (post-hoc artifact scan,
      real-time intercept 도입 0). self-block 회피 5-layer 동형 (ADR-040 §결정 7.J.4 — enforce-from
      filter CFP-843 merged at 이후 + worktree 없는 구형 = warning sub-tier + BYPASS_WORKTREE_FIRST=1
      short-circuit + common-dir skip). actual detect_command scope 확장 알고리즘 (write target path
      → worktree root membership resolve) = Phase 2 carrier (본 SSOT = Phase 1 declarative 선언,
      Amendment 5 declaration → Phase 2 wire 패턴 동형). tier 무변경 (blocking-on-pr 유지, scope
      확장만 — ADR-060 §결정 6 promotion gate 재평가 불요).
    detect_command: bash scripts/check-worktree-first-spawn-evidence-cwd.sh && bash scripts/check-write-target-membership.sh
    # CFP-843 Phase 2 actual wire: detect_command 이중 체인
    #   1. check-worktree-first-spawn-evidence-cwd.sh — §14 Lane Evidence Working dir: CWD field lint (기존 blocking-on-pr)
    #   2. check-write-target-membership.sh — Write/Edit tool target path + git write target worktree-membership lint (CFP-843 신규, warning sub-tier)
    workflow: templates/github-workflows/worktree-first-spawn-evidence-cwd.yml
    current_tier: blocking-on-pr  # CFP-531 / ADR-040 Amendment 5 — warning → blocking-on-pr 승격 (2026-05-13). CFP-843 / ADR-040 Amendment 6 — scope CWD → write-target-path 확장, Phase 2 actual wire 완료 (2026-05-17)
    bypass_label: hotfix-bypass:worktree-spawn-evidence-cwd
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies:
        - CFP-427
        - CFP-428
        - CFP-429
        - CFP-843              # ADR-040 Amendment 6 — agent-internal write-target-path scope 확장 carrier
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-426
    owner_adr: ADR-040          # ADR-040 Amendment 6 (CFP-843) scope 확장 carrier — owner 무변경
    carrier_adr: ADR-040
    recurrence:
      count: 1                  # ADR-040 Amendment 6 (CFP-843) — `worktree-first-enforcement-gap` 패턴 recurrence (CFP-825 §3 RC-1 (a)/(b) 동근원 3 signal + #836 spurious artifact test-isolation gap, ADR-045 §D-9 threshold 2 reach → adr_draft_emitted)
      promotion_trigger: none   # ADR-060 §결정 19 (Amendment 6) — scope 확장만, tier 무변경 (blocking-on-pr 유지). Phase 2 actual wire 후 재평가 영역
    status: Active

  - name: evidence-registry-schema-validation
    description: |
      ADR-060 Amendment 2 §결정 14 verbatim — evidence-checks-registry.yaml schema
      mechanical verification (6 validation rule: schema_version field + entry
      required field + current_tier enum membership + bypass_label↔bypass_audit_lint
      pair 정합 + entry name uniqueness + owner_adr/carrier_adr ADR file cross-ref).
      메타 lint self-application 첫 사례 — self-entry 도 다른 entry 와 동일 검증
      (Q4 결정 verbatim — special-case 도입 X). 본 entry 와 §결정 14 의 메타 anomaly
      lint 는 분리 (Q6 결정 verbatim — 별도 carrier).
      Exit code 3-tier (ADR-060 Amendment 2 §결정 15): 0=PASS / 1=validation FAIL /
      2=META-ERROR (pyyaml 미설치 / file 부재 / unexpected exception).
    detect_command: bash scripts/check-evidence-registry.sh
    workflow: templates/github-workflows/evidence-registry-check.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    # bypass_label: omit — ADR-060 Amendment 2 §결정 16 정합 (warning tier 의 bypass 의미 부적용)
    # bypass_audit_lint: omit — bypass_label 미정의
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []         # 독립 entry — schema validation, 외부 의존 없음
      evidence_artifacts:              # 승격 carrier PR 의 manual 첨부 의무
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    # modal_anti_pattern_dictionary: omit — schema validation lint, modal 어휘 검사 미포함
    introduced_by: CFP-455
    owner_adr: ADR-060                 # framework SSOT — schema 정합 검증의 정책 owner
    carrier_adr: ADR-060               # Amendment 2 carrier (self-application 의 self-carrier 패턴)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-449 Phase 2 (decision principle forbid-list vocabulary lint, 2026-05-12) ───
  # ADR-064 §결정 2 forbid-list 8 어휘 dictionary 의 mechanical lint — 2nd warning-tier entry
  # (1st = adr-sunset-criteria). framework multi-entry 운영 검증 cross-validation 신호.

  - name: decision-principle-vocab
    description: |
      ADR-064 §결정 2 forbid-list 8 어휘 mechanical lint — 8 어휘 dictionary verbatim mirror
      detection in 5 scope 영역 (ADR / change-plans / CLAUDE.md / playbook / templates).
      Exempt 영역 = markdown blockquote + fenced code block + dictionary 본문 영역
      (ADR-064 §결정 2 verbatim) + registry yaml description + script self + bats fixture
      self. 사용자 발화 verbatim invariant (Story §1) 영역 exempt 보장 (Story file 은
      internal-docs repo 영역, wrapper repo lint scope 외 자연 처리). evidence-enforceable
      framework 2nd warning-tier entry (1st = adr-sunset-criteria).
    detect_command: bash scripts/check-decision-principle-vocabulary.sh
    workflow: templates/github-workflows/decision-principle-vocabulary.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:decision-principle-vocab
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — registry append + lint patterns 검증 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary:
      version: "1.0"                 # ADR-064 §결정 2 verbatim — 8 어휘 only
      dictionary:
        - "임시"
        - "단계적"
        - "일단"
        - "우선"
        - "잠정"
        - "가벼운"
        - "minimal viable"
        - "quick win"
    introduced_by: CFP-449
    owner_adr: ADR-064               # forbid-list dictionary SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-475 Phase 2 (duplicate-session-start-hook-check, 2026-05-12) ───
  # ADR-038 Amendment 3 §결정 12 one-channel rule mechanical lint
  # plugin-root hooks/hooks.json SSOT 이전 후 settings.json 중복 등록 detect

  - name: duplicate-session-start-hook-check
    description: |
      ADR-038 Amendment 3 §결정 12 (one-channel rule) — `.claude/settings.json` `hooks.SessionStart[]`
      와 plugin-root `hooks/hooks.json` 양 channel 안 prereq-check entry 동시 정의 detect.
      하나의 channel 만 SSOT (plugin-root first-class, settings.json fallback deprecated).
      warning tier — continue-on-error: true. CFP-475 Phase 2 PR 가 actual wire 첫 사례 —
      `scripts/check-no-duplicate-session-start-hook.sh` (bash + jq) + `hotfix-bypass:duplicate-session-start-hook` label.
    detect_command: bash scripts/check-no-duplicate-session-start-hook.sh
    workflow: templates/github-workflows/duplicate-session-start-hook-check.yml
    current_tier: warning          # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:duplicate-session-start-hook
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20         # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0          # ADR-060 §결정 6 (b)
      sibling_dependencies: []      # 본 entry 단독 — Story 4 진입 시 평가 sibling 부재
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-475
    owner_adr: ADR-038
    carrier_adr: ADR-038
    status: Active

  # ─── CFP-481 Phase 2 (PR open 시 phase label 자동 부착, 2026-05-12) ───
  # ADR-024 Amendment 4 §결정 6.A.1 branch → phase mapping 표 SSOT 의 mechanical enforcement —
  # 3rd warning-tier entry (1st = adr-sunset-criteria / 2nd = decision-principle-vocab).
  # CFP-455 + CFP-449 retro 식별 sentinel 2 (재현 2회) → mechanical enforcement 도입 timing 도달.

  - name: auto-phase-label
    description: |
      ADR-024 Amendment 4 §결정 6.A.1 branch → phase mapping 표 mechanical lint —
      PR open 시 4-tier inference fallback chain (branch parse / Related Issue inherit /
      terminal default / unclassified+warning) 으로 phase:* label 자동 부착.
      story-init.yml 가 만든 PR 은 가드 skip (책임 분리). race 회피 = concurrency.group
      `phase-label-auto-N` 직렬화 + phase-label-invariant.yml 와는 trigger event type 분리
      (`pull_request:opened` vs `pull_request:labeled`). idempotency = PR body marker
      `<!-- attached_by_auto: true -->` presence dedup. evidence-enforceable framework
      3rd warning-tier entry (1st = adr-sunset-criteria / 2nd = decision-principle-vocab).
    detect_command: github-actions-runtime  # workflow self-fire (PR open event), no static script
    workflow: templates/github-workflows/auto-phase-label.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:auto-phase-label
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — workflow self-app + ADR-024 Amendment 4 정합
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    # modal_anti_pattern_dictionary: omit — workflow lint, modal 어휘 검사 미포함
    introduced_by: CFP-481
    owner_adr: ADR-024 Amendment 4 §결정 6.A.1   # branch → phase mapping 표 SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  - name: claude-md-line-cap
    description: |
      ADR-012 Amendment 1 §결정 6 mechanical lint — CLAUDE.md line count cap ≤320.
      ADR-060 Amendment 5 (4번째 warning-tier entry): scripts/check-claude-md-line-cap.sh
      exit 0 PASS / exit 1 cap 초과 / exit 2 meta-error (ADR-060 Amendment 2 §결정 15 3-tier).
      ADR-058 §결정 5 ratchet 정합 — cap 강화 방향, sunset_justification 면제.
      CFP-455 + CFP-449 retro sentinel 2 재현 2회 → mechanical enforcement 도입 timing 도달 (4th entry).
    detect_command: bash scripts/check-claude-md-line-cap.sh
    workflow: templates/github-workflows/claude-md-line-cap.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:claude-md-line-cap
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_story_merged: required # ADR-060 §결정 6 (c)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    sibling_dependencies: []         # 독립 entry
    introduced_by: CFP-506
    introduced_date: 2026-05-13
    owner_adr: ADR-012               # Amendment 1 cap ≤320 declaration SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry, Amendment 5)
    carrier_amendment: 5
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-521 Phase 2 (sibling-pr label anti-misuse lint, 2026-05-13) ───
  # ADR-010 Amendment 4 §결정 5 anti-misuse 안전망 mechanical enforcement —
  # 5th warning-tier entry (1st = adr-sunset-criteria / 2nd = decision-principle-vocab /
  # 3rd = auto-phase-label / 4th = claude-md-line-cap).
  # EPIC-RESULTS-CFP-462 §6 carrier #2 — sibling-pr label fast-pass mechanism (CFP-499) 후속.

  - name: sibling-pr-label-author-check
    description: |
      ADR-010 Amendment 4 §결정 5 anti-misuse 안전망 mechanical lint — sibling-pr label
      부착 PR 의 paired wrapper PR link 검증 (PR body grep mclayer/plugin-codeforge#NNN
      패턴 — short form + URL form 양쪽). 부재 시 audit comment 부착 + workflow failure
      (warning tier, advisory only — PR merge 미차단). Guard 3종: (a) sibling-pr label
      미부착 PR skip / (b) hotfix-bypass:sibling-pr-author-check label 부착 skip /
      (c) wrapper repo self-PR skip (self-referential 영역). audit comment dedup =
      `[sibling-pr-anti-misuse]` marker presence. CFP-499 sibling-pr fast-pass mechanism
      (ADR-010 Amendment 4) 의 anti-misuse 안전망 forcing function.
      EPIC-RESULTS-CFP-462 §6 carrier #2.
    detect_command: github-actions-runtime  # workflow self-fire (PR label event), no static script
    workflow: templates/github-workflows/sibling-pr-label-author-check.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:sibling-pr-author-check
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — workflow self-app + ADR-010 Amendment 4 정합
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    # modal_anti_pattern_dictionary: omit — workflow lint, modal 어휘 검사 미포함
    introduced_by: CFP-521
    introduced_date: 2026-05-13
    owner_adr: ADR-010 Amendment 4 §결정 5   # sibling-pr fast-pass anti-misuse 안전망 SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-491 Phase 2 (impl manifest AC mapping cross-ref lint, 2026-05-13) ───
  # ADR-060 Amendment 6 — 6번째 warning-tier entry (CFP-451 F-001 Option C systematization)
  # (1st = adr-sunset-criteria / 2nd = decision-principle-vocab / 3rd = auto-phase-label /
  # 4th = claude-md-line-cap / 5th = sibling-pr-label-author-check)
  # 1차 단순화: §8.5 → §5.1 only (2-way). 3-way (Change Plan §1.3/§3.5/§8.1) = follow-up CFP.

  - name: ac-mapping-cross-ref
    description: |
      ADR-060 Amendment 6 / CFP-451 F-001 Option C systematization — Story §8.5 Impl Manifest
      의 AC id 인용 ↔ §5.1 AC 정의 영역 의 cross-reference 검증. 1차 단순화 = §8.5 → §5.1
      only (2-way drift detection). 3-way (Change Plan §1.3/§3.5/§8.1 의 AC 정의) cross-check
      = follow-up CFP scope. Exit code 3-tier (ADR-060 Amendment 2 §결정 15): 0 PASS / 1
      violation (--strict mode 또는 향후 enforce) / 2 meta-error (file 부재). 기본 mode =
      LLM trust (exit 0 + stderr advisory) — CI workflow continue-on-error:true 와 defense in depth.
    detect_command: bash scripts/check-impl-manifest-ac-mapping.sh
    workflow: templates/github-workflows/ac-mapping-cross-ref-check.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:ac-mapping
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — Story §8.5 / §5.1 lint patterns 검증 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    # modal_anti_pattern_dictionary: omit — schema cross-reference lint, modal 어휘 검사 미포함
    introduced_by: CFP-491
    introduced_date: 2026-05-13
    owner_adr: CFP-451 F-001 Option C   # F-001 retro follow-up systematization 동인
    carrier_adr: ADR-060                # framework carrier (warning mode entry)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-527 Wave 2A (boundary completeness wording SSOT lint, 2026-05-13) ───
  # ADR-068 §결정 5 wording SSOT mechanical enforcement — 8번째 warning-tier entry
  # (1st = adr-sunset-criteria / 2nd = decision-principle-vocab / 3rd = auto-phase-label /
  # 4th = claude-md-line-cap / 5th = sibling-pr-label-author-check / 6th = ac-mapping-cross-ref /
  # 7th = evidence-registry-naming)
  # wording-ssot-grep-lint: Story §3/§7 enum identifier → impl source ripgrep 매칭 (warning advisory).

  - name: wording-ssot-grep-lint
    description: |
      ADR-068 §결정 5 wording SSOT mechanical lint — Story §3/§7 의 enum-like identifier
      (UPPER_SNAKE_CASE 4+ chars) 추출 후 impl source (src/ scripts/) ripgrep 매칭.
      불일치 시 warning 출력 (advisory only — PR merge 미차단). Story file 미제공 시
      skip exit 0. Warning tier (continue-on-error: true workflow 정합).
      Hotfix bypass: hotfix-bypass:boundary-wording (ADR-024 Amendment 3 per-entry namespace).
      4 invariants (I-1 API contract semantic / I-2 cross-module propagation /
      I-3 guard placement intent / I-4 wording SSOT) 중 I-4 wording SSOT 의 mechanical enforcement.
      CFP-529 Wave 3 Phase 2 = handoff wording linter expansion (scripts/check_handoff_wording.py +
      3-direction enum forward/backward/lateral + 5 mechanical pre-screen patterns + 3 AI escalate
      stub patterns). 본 entry 의 mechanical scope = ADR-068 wording SSOT 단방향 (§3 → src/scripts);
      CFP-529 Phase 2 가 bidirectional + cross-section coverage 확장 (severity-propagation-v1 SSOT).
    detect_command: bash scripts/check-wording-ssot.sh
    workflow: templates/github-workflows/wording-ssot-check.yml
    enforcement_script_phase_2: scripts/check_handoff_wording.py            # CFP-529 Phase 2 carrier (handoff wording linter, bidirectional + 3-direction enum)
    enforcement_workflow_phase_2: templates/github-workflows/handoff-wording-check.yml  # CFP-529 Phase 2 carrier
    carrier_story_phase_2: CFP-529   # mechanical impl carrier (severity-propagation-v1 SSOT + handoff wording linter)
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:boundary-wording
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: [CFP-529]  # CFP-529 Phase 2 handoff wording linter (bidirectional + 3-direction enum) 가 mechanical coverage 확장
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-527
    introduced_date: 2026-05-13
    owner_adr: ADR-068               # boundary completeness invariants SSOT — wording SSOT §결정 5
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    related_contracts:               # CFP-529 — severity-propagation-v1 (RC#8 bidirectional binding SSOT)
      - severity-propagation-v1
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-508 Phase 2 (entry name ↔ workflow file naming convention lint, 2026-05-13) ───
  # ADR-060 Amendment 7 — 7번째 warning-tier entry (CFP-490 FU-2 carrier)
  # (1st = adr-sunset-criteria / 2nd = decision-principle-vocab / 3rd = auto-phase-label /
  # 4th = claude-md-line-cap / 5th = sibling-pr-label-author-check / 6th = ac-mapping-cross-ref)
  # Conservative no-rename policy: multi-job workflow pattern + naming divergence 정식 인정.

  - name: evidence-registry-naming
    description: |
      ADR-060 Amendment 7 / §결정 20 — evidence-checks-registry entry name ↔ workflow file
      naming convention 검증. workflow file 존재 의무 + DRIFT (no match) allowlist hardcode.
      multi-job workflow pattern 인정 (contract-lint.yml + lint.yml = 5+ entry 공유 jobs).
      Conservative no-rename policy — workflow rename 회피 (CI history + branch protection
      `required_status_checks.contexts` 영향 회피). Exit code 3-tier (ADR-060 Amendment 2
      §결정 15): 0 PASS / 1 violation (workflow file 부재 OR allowlist 밖 DRIFT) / 2
      meta-error (yaml parse 실패).
    detect_command: bash scripts/check-evidence-registry-naming.sh
    workflow: templates/github-workflows/evidence-registry-naming-check.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:evidence-naming
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — naming convention lint 검증 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-508
    introduced_date: 2026-05-13
    owner_adr: ADR-060               # framework SSOT — entry name convention policy owner
    carrier_adr: ADR-060             # Amendment 7 carrier
    recurrence:
      count: 0
      promotion_trigger: none        # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active
  - name: workflow-permissions-block-presence
    description: |
      ADR-060 Amendment 8 §결정 21 mechanical verification —
      .github/workflows/*.yml + templates/github-workflows/*.yml top-level
      `permissions:` block 부재 detect. Glob scope = *.yml only.
    detect_command: bash scripts/check-workflow-permissions-presence.sh
    workflow: templates/github-workflows/workflow-permissions-check.yml
    current_tier: warning
    bypass_label: hotfix-bypass:workflow-permissions
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-530
    introduced_date: 2026-05-13
    owner_adr: ADR-060
    carrier_adr: ADR-060
    recurrence:
      count: 0
      promotion_trigger: none
    status: Active

  - name: workflow-yaml-parse
    description: |
      CFP-583 / ADR-060 Amendment 9 §결정 22 — workflow yml YAML parse validation gate.
      .github/workflows/*.yml + templates/github-workflows/*.yml 전체 대상 PyYAML safe_load +
      actionlint dual validation. BODY heredoc anti-pattern (multi-line + backtick fence +
      variable interpolation) 재발 방지 recurrence prevention forcing function.
      broken workflow = absent 와 동치 → framework promotion gate measurement 무효.
    detect_command: bash scripts/check-workflow-yaml-parse.sh
    workflow: templates/github-workflows/workflow-yaml-parse.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:workflow-yaml-parse
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []         # 독립 entry
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-583
    introduced_date: 2026-05-13
    owner_adr: ADR-060
    carrier_adr: ADR-060
    recurrence:
      count: 6                         # CFP-449 carrier 이후 broken 상태 6 workflow 동시 영향 (§2.1 evidence)
      threshold: 3                     # ADR-060 §결정 6 promotion gate
      promotion_trigger: advisory      # recurrence count ≥ threshold → advisory comment (별도 carrier 승격 평가)
      last_occurrence: 2026-05-13      # CFP-583 fix 시점 — broken-baseline 회복 baseline 재시작
    status: Active

  - name: debate-convergence-quality-marker-presence
    description: |
      CFP-582 Phase 2 / ADR-059 Amendment 2 §결정 8 — convergence_quality_invariant
      3 marker pattern mechanical enforcement. Story §9 debate transcript 내
      [COUNTERARGUMENT] / [ALTERNATIVE_PROPOSED] / [DEBATE_PURPOSE_STATEMENT]
      section header 존재 여부 검사. debate-protocol-v1 v1.2 schema
      convergence_quality_invariant block 과 cross-validate.
      ADR-060 warning-tier entry (첫 debate 영역 entry).
    detect_command: python scripts/check_debate_convergence_quality.py
    workflow: templates/github-workflows/debate-convergence-quality.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:debate-convergence-quality
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-582
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-582
    introduced_date: 2026-05-13
    owner_adr: ADR-059
    carrier_adr: ADR-059
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: Active

  - name: parallel-dispatch-prompt-check
    description: |
      CFP-609 Phase 1 / ADR-064 Amendment 1 §결정 4 — parallel-dispatch-protocol-v1
      §8 mechanical enforcement. Orchestrator → PL spawn prompt 의 4 의무 항목 박제
      검사 (registry §4):
        1. plan DAG batches list verbatim (registry §4.1)
        2. pl_autonomous_parallel_authority required 박제 (disabled 차단, registry §4.2)
        3. sequential_mandate_reasons 6 enum close-set 위반 detect (registry §3)
        4. file_conflict_resolution_patterns 박제 (registry §4.4)
        5. worker_count <= worker_count_max default 7 (registry §6.2, OpRiskArch §7.4.4)
      parallel-dispatch-protocol-v1 kind:registry mechanical enforcement carrier.
      ADR-060 warning-tier entry — execution-time enforcement (proposing-time anchor 와 직교).
    detect_command: bash scripts/check-parallel-dispatch-prompt.sh
    workflow: templates/github-workflows/parallel-dispatch-prompt-check.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:parallel-dispatch-prompt
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-609
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-609
    introduced_date: 2026-05-13
    owner_adr: ADR-064  # Amendment 1 carrier
    carrier_adr: ADR-060
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: Active

  - name: wording-dictionary
    description: |-
      CFP-610 / ADR-064 Amendment 2 + Amendment 5 (CFP-750) — wording-dictionary lint (per-word scope decoupling).
      카테고리 (a) forbid 어휘 발견 시 exit 1 warning: `박제` / `못 박기` / `pin` / `freezing` / `별` standalone (5 entry).
      카테고리 (b) 어휘 평문 정의 누락 시 exit 0 advisory (normative / sibling sync / kind:contract / ratchet / mirrored field).
      SSOT: docs/wording-dictionary.md. per-word scope (ADR-064 Amendment 5 §결정 2):
      `박제`/`못 박기`/`pin`/`freezing` = docs/** + CLAUDE.md + CHANGELOG.md + templates/** (expanded scope);
      `별` standalone = docs/adr/** + docs/change-plans/** + CLAUDE.md + docs/orchestrator-playbook.md + templates/** (5-scope 유지, false-positive carrier 분리).
    detect_command: bash scripts/check-wording-dictionary.sh
    workflow: .github/workflows/wording-dictionary.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:wording-dictionary
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-610
    introduced_date: 2026-05-13
    owner_adr: ADR-064
    carrier_adr: ADR-064
    sunset_gate:
      metric: "Orchestrator user-facing 응답 안 forbid 어휘 발생률 < 1건/100 turn + 사용자 frustration 발화 0건/3 Story"
      who: "PMOAgent (cross-Story retro 패턴 분석)"
      how: "PMOAgent retro file §wording-discipline 표 (lint warning count + frustration timestamp delta)"
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: Active

  - name: retro-mandatory-deployed
    description: |-
      CFP-619 / ADR-045 mandate — retro-mandatory.yml workflow .github/workflows/
      배포 sentinel. byte-identical mirror invariant (ADR-005) + workflow runtime
      활성화 detection. drift 시 required-workflow-drift-check.yml 가 SSOT 와의
      차이를 별도로 lint — 본 entry 는 deployment fact 자체의 evidence-enforceable
      tracking (warning tier, ADR-060 framework 첫 deployment-class entry).
      CFP-612 Phase 2 PR #618 merge 시점 manual fallback 첫 observed sample.
    detect_command: bash -c 'test -f .github/workflows/retro-mandatory.yml && diff -q templates/github-workflows/retro-mandatory.yml .github/workflows/retro-mandatory.yml'
    workflow: null  # static deployment detection — runtime workflow 별도 (retro-mandatory.yml 자체)
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:retro-mandatory-deployed
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a)
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-619
    introduced_date: 2026-05-14
    owner_adr: ADR-045
    carrier_adr: ADR-060
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: Active

  - name: retro-alert-pickup-rate
    description: |-
      CFP-628 Story 2 / ADR-045 §D-5 — Orchestrator SessionStart retro alert scan
      mandate 의 KPI sentinel. open `phase:완료` issue 안 `[PMO] retro alert` prefix
      comment (35min filter 후) 발화 대비 retro file 생성 비율 측정.
      pickup_rate ≥ 90% (3 month window) = sunset_gate 충족.
      monthly cron `retro-alert-pickup-kpi.yml` 자동 측정.
      scripts/check-retro-alerts.sh SessionStart hook 으로 Orchestrator prompt-injection.
    script: scripts/check-retro-alerts.sh
    detect_command: bash scripts/check-retro-alerts.sh
    workflow: .github/workflows/retro-alert-pickup-kpi.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:retro-alert-pickup
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a)
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - kpi_rate_json_history
        - pr_cumulative_count_proof
    introduced_by: CFP-628
    introduced_date: 2026-05-14
    owner_adr: ADR-045
    carrier_adr: ADR-060
    sunset_gate:
      metric: "retro-alert-pickup-rate ≥ 90% (3 month rolling window)"
      who: "PMOAgent (monthly cron retro-alert-pickup-kpi.yml 자동 측정)"
      how: "docs/kpi/retro-alert-pickup-rate.json gate_status 3 consecutive months = ok → sunset 판정"
    recurrence:
      count: 0
      promotion_trigger: none
      last_occurrence: null
    status: Active

  - name: marketplace-description-verbatim
    description: |-
      CFP-631 / ADR-063 Amendment 2 §결정 11 — wrapper plugin.json description ↔
      marketplace.json plugins[codeforge].description byte-identical verify (PR-time enforce).
      mirrored field 4종 중 description 단독 PR-time enforce 추가 — name/author = check-marketplace-parity
      warning sufficient, version = version-bump-atomic-check blocking-on-pr 기존 cover.
      6 sample 누적 drift evidence (CFP-387/393/423/597/612/619, CFP-619 retro §5.2 SSOT).
      blocking-on-pr tier 직접 시작 근거 = ADR-060 §결정 5 default warning explicit exception
      + §결정 19 (Amendment 6, CFP-509) auto_blocking manual gate path (recurrence.count=6 +
      threshold=6 + promotion_trigger=auto_blocking) + 사용자 directive Story §1 verbatim —
      본 carrier Story 자체가 §결정 6 별도 promotion carrier 의무 합병.
      Amendment 1 (design-time self-check, CFP-597) 와 layered 2-layer proactive forcing function.
    detect_command: bash scripts/check-marketplace-description-verbatim.sh
    workflow: templates/github-workflows/marketplace-description-verbatim.yml
    current_tier: blocking-on-pr   # ADR-060 §결정 5 explicit exception + §결정 19 auto_blocking manual gate (Phase 2 PR 부터 active enforce)
    bypass_label: hotfix-bypass:marketplace-description-verbatim
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: []         # 독립 entry — 본 carrier Story 자체가 carrier
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-631
    introduced_date: 2026-05-14
    owner_adr: ADR-063
    carrier_adr: ADR-060
    recurrence:
      count: 6                       # CFP-387/393/423/597/612/619 6 sample 누적 [empirical-source: CFP-619 retro §5.2]
      threshold: 6                   # ADR-060 §결정 19 (Amendment 6) — recurrence threshold (count >= threshold = signal 발화)
      promotion_trigger: auto_blocking  # ADR-060 §결정 19 enum — actual transition 본 carrier Story 자체가 별도 carrier 의무 합병 (사용자 explicit directive Story §1)
      last_occurrence: 2026-05-14   # CFP-619 byte-identical fix commit c199dee [empirical-source: marketplace git log]
    status: Active

  - name: stop-time-continuous-confirm-detect
    description: |-
      CFP-638 / ADR-064 Amendment 3 §결정 9 sister carrier — Continuous "진행해" 패턴
      mechanical detection 영역 (Epic CFP-635 Story D). Orchestrator 가 직전 N (≥3)
      user turn 안 "진행해" / "그대로" / "계속" / "ok" / "yes" / "go" / "맞아"
      pattern 누적 시 후속 turn 의 dialog format (numbered list / decision option /
      "권장 = ..." 형식) 발화 자동 차단 — declare + 결과 보고 only.

      현재 mechanical layer = behavioral directive advisory only (turn-final hook
      부재, session-level user turn history 접근 platform 한계). retro audit signal
      = PMOAgent retro file §over-questioning 표 — frustration timestamp delta +
      "진행해" repetition count tracking.

      sibling Story CFP-637 (ADR-064 Amendment 3 §결정 9 Question quality 3-check)
      = behavioral directive carrier (1차 layer). 본 entry = retro audit signal
      tracking carrier (2차 layer). 미래 mechanical hook (예: PreToolUse hook 안
      AskUserQuestion / numbered list output detection) 도입 = 별 CFP follow-up
      (platform hook capability 확장 의존).
    detect_command: null  # behavioral directive only — mechanical detection script 부재 (Story D 1차 scope)
    workflow: null        # turn-final / per-turn hook platform 한계 — retro audit signal SSOT
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode (advisory only)
    bypass_label: hotfix-bypass:stop-time-continuous-confirm
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a)
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies: [CFP-637]  # ADR-064 Amendment 3 §결정 9 normative base
      evidence_artifacts:
        - pmo_retro_over_questioning_table   # PMOAgent retro file §over-questioning 표 (Story 단위 frustration count + 진행해 repetition)
        - cross_story_recurrence_count       # 3 Story 누적 frustration 발화 count (sunset gate metric)
    introduced_by: CFP-638
    introduced_date: 2026-05-14
    owner_adr: ADR-064  # Amendment 3 §결정 9 normative base
    carrier_adr: ADR-060
    sunset_gate:
      metric: "사용자 frustration 발화 0건 / 3 Story 누적 + 연속 '진행해' pattern (5+ turn) 발화 후 dialog format reflex 0건"
      who: "PMOAgent (cross-Story retro 패턴 분석)"
      how: "PMOAgent retro file §over-questioning 표 (Story 단위 frustration count + 연속 '진행해' repetition trace)"
    recurrence:
      count: 1  # CFP-622 brainstorm Phase 1 session (2026-05-14 KST) — Epic CFP-635 trigger evidence
      threshold: 3
      promotion_trigger: advisory  # warning → blocking-on-pr 승격은 ADR-060 §결정 6 PR 20건 누적 + failure 0건 gate
      last_occurrence: 2026-05-14
    status: Active

  - name: production-cutover-deputy-spawn-evidence
    description: |-
      ADR-72 §결정 3 mechanical verification — Change Plan §13 안
      `production_cutover_touching: true` 선언 시 ProductionEvidenceDeputy
      spawn evidence (verdict packet `production_evidence_deputy_spawned: true`
      + Story §14 Lane Evidence row append) 의무. CFP-954 Story-3 carrier 가
      detect_command (scripts/check-production-cutover-evidence.sh) + workflow
      (templates/github-workflows/production-cutover-evidence.yml + .github/
      byte-identical mirror) 신설 + status 승격 (deferred-followup → warning).
      4 prerequisite measurement source mechanical anchor 4-tuple (MS-1
      live_touching / MS-2 production_cutover_touching dual-source AND /
      MS-3 marketplace_publish_touching / MS-4 consumer_impact_blast_radius)
      Change Plan §3.5 + ADR-72 amendment_log Amendment 2 본문 SSOT.
      follow-up CFP-Z = review-verdict-v4 v4.5 → v4.6 MINOR bump
      (owner_deputy_kind enum production_evidence 추가) + verdict packet
      field-time check 신설 후 warning → blocking-on-pr 승격 carrier.
    detect_command: bash scripts/check-production-cutover-evidence.sh
    workflow: templates/github-workflows/production-cutover-evidence.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:prod-cutover-deputy-evidence
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-Z  # review-verdict-v4 v4.5 → v4.6 MINOR — owner_deputy_kind enum production_evidence (placeholder, ADR-72 §결정 3 carrier — Story-3 = warning tier 활성, blocking-on-pr 승격 영역 외)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-632
    introduced_date: 2026-05-14
    activated_by: CFP-954  # Story-3 status 승격 + script/workflow path populate
    activated_date: 2026-05-18
    owner_adr: ADR-72
    carrier_adr: ADR-060
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: warning  # CFP-954 Story-3: deferred-followup → warning 승격 (script + workflow 신설 동반)

  - name: epic-cutover-gate-evidence-quad-check
    description: |-
      ADR-72 §결정 5 mechanical verification — EPIC CLOSED gate (PMOAgent
      retro epic_close_gate) 의 production 실측 evidence quad (bucket prefix
      listing + WAL sample + Prometheus rate metric + L1 backlog drainage rate)
      4중 명시 verify. CFP-954 Story-3 가 status 승격 (deferred-followup →
      warning) + detect_command/workflow path populate. production-cutover-
      evidence.yml workflow 의 PR-open trigger 안 EPIC CLOSED gate sub-section
      verify 영역 통합 (Story-3 declare scope, Sibling Story-4 plugin-codeforge
      -pmo#18 = retro template 본문 작성 prerequisite for warning → blocking
      -on-pr 승격).
    detect_command: bash scripts/check-production-cutover-evidence.sh
    workflow: templates/github-workflows/production-cutover-evidence.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:epic-cutover-quad-check
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-Z'  # PMOAgent retro epic_close_gate workflow integration (Sibling Story-4 plugin-codeforge-pmo#18 prerequisite for blocking-on-pr 승격)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-632
    introduced_date: 2026-05-14
    activated_by: CFP-954  # Story-3 status 승격 + script/workflow path populate
    activated_date: 2026-05-18
    owner_adr: ADR-72
    carrier_adr: ADR-060
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: warning  # CFP-954 Story-3: deferred-followup → warning 승격 (script + workflow 신설 동반, Sibling Story-4 plugin-codeforge-pmo#18 = blocking-on-pr 승격 prerequisite)

# marketplace-drift-detection entry deferred to Phase 2 carrier PR (CFP-627 FIX iter 3/3 — ADR-067 §결정 3 cross-lane RESET 차원 전환).
# Phase 1 doc-only fast-path (ADR-054) scope = ADR-063 Amendment 3 §결정 13 declarative SSOT mandate only.
# artifact (script + workflow + entry) = Phase 2 carrier PR scope (별 Story / 별 PR).
# ADR-068 I-1 boundary completeness 정합 — Phase 1 declarative vs Phase 2 mechanical 분리 의도 회복.
# evidence-registry-naming-check VIOLATION 회피 + Phase 1 doc-only scope 보존 동시 충족.
# Phase 2 carrier 발의 시 본 entry append + workflow file 도입 의무.

  - name: section-1-verbatim-postmerge
    description: |
      ADR-027 Amendment 2 §결정 6.C mechanical verification — manual fallback
      path 의 §1 verbatim invariant post-merge lint. Story §1 ↔ Issue body §1
      byte-identical 검증 (manual write 영역의 governance ratchet 약화 vector
      차단 forcing function). drift 발견 시 hotfix-bypass:section-1-verbatim-postmerge
      label audit comment 자동 발의 (warning tier — hard block 아님).
      SecurityArch T1 위협 (§1 임의 변조) mitigation 의 mechanical enforcement
      layer. Phase 2 carrier (CFP-658) 가 templates/github-workflows/section-1-verbatim-postmerge.yml
      + .github/workflows/section-1-verbatim-postmerge.yml byte-identical mirror
      배포 후 Active 전환.
    detect_command: workflow inline — Python §1 extract + diff -q byte-identical compare  # CFP-658 Phase 2 Active
    workflow: .github/workflows/section-1-verbatim-postmerge.yml  # CFP-658 Phase 2 신설 (byte-identical mirror ADR-005)
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:section-1-verbatim-postmerge
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-658  # Phase 2 carrier — script + workflow 신설 (deferred-followup → warning 전환 prerequisite)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}  # 본 lint = byte-identical 검증, modal phrase 검사 미포함
    introduced_by: CFP-658
    introduced_date: 2026-05-14
    owner_adr: ADR-027  # Amendment 2 §결정 6.C
    carrier_adr: ADR-060  # evidence-enforceable promotion framework
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: Active  # CFP-658 Phase 2 carrier 신설 완료 → deferred-followup → Active 전환 (2026-05-14)

  - name: workflow-version-drift
    description: |
      CFP-660 / ADR-032 Amendment 2 §결정 6 — consumer `.github/workflows/<name>.yml`
      가 wrapper `templates/github-workflows/<name>.yml` 와 SHA-256 / 핵심 line (concurrency
      / on / permissions) 불일치 시 drift 감지. stale workflow = race condition / counter
      collision / silent skip vector 차단 forcing function.

      Tier 1 SHA-256 byte-identical compare → Tier 2 core marker normalized whitespace
      compare (superficial whitespace-only diff suppress). 본 check 는 runtime detection
      (SessionStart hook 시점). cron-based reactive detection workflow = 별 CFP carrier
      후보 (CFP-627 marketplace drift 패턴 정합).

      Strict mode (ADR-032 §결정 2 strict-eligible 5번째 drift, CFP-660 4 → 5 확장) 시
      STRICT_ELIGIBLE_WORKFLOWS 영역 (7 file — phase-gate-mergeable / phase-label-invariant
      / story-init / story-section-1-immutable / subissue-from-impl-manifest /
      fix-ledger-sync / story-section-schema) drift = exit 1.
    detect_command: python overlay/hooks/check_bootstrap.py --quiet  # check 10 runtime — main() 안 drift_warnings + strict mode
    workflow: null  # 별 cron-based workflow yml 미신설 — 본 Story scope OUT (single-Story 영역 보존)
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:workflow-version-drift
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a)
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-660  # 본 entry carrier — Wave 2 of Epic CFP-431
      evidence_artifacts:
        - check_bootstrap_runtime_drift_count
        - consumer_repo_sweep_pr_url       # cp sweep 적용 PR ref
        - strict_mode_exit_1_history       # strict 활성 환경 strict-eligible drift block count
    modal_anti_pattern_dictionary: {}  # 본 check = SHA byte compare + core marker compare, modal phrase 검사 미포함
    introduced_by: CFP-660
    introduced_date: 2026-05-14
    owner_adr: ADR-032  # Amendment 2 §결정 6 carrier
    carrier_adr: ADR-060  # evidence-enforceable promotion framework
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: advisory
      last_occurrence: null
    status: active  # check_bootstrap.py 본 PR 안 runtime 활성 (deferred-followup 아님 — runtime ready)

  - name: bootstrap-labels-precondition
    description: |
      ADR-060 Amendment 10 §결정 24 mechanical verification — consumer repo PR open 시 codeforge
      필수 label set (phase:* / gate:* / type:* / hotfix-bypass:* / severity:* / audit:* /
      component:*) 부재 자동 감지 + bootstrap-labels.sh idempotent 호출 (RETRO-MCT-104 carrier,
      mctrader-data MCT-104 Phase 2 PR #14 2026-05-09 replay sentinel). PR-time precondition
      check pattern 의 첫 baseline (CFP-583 retro 후 framework legitimacy 회복 후 신규 entry
      도입). workflow body = `bash scripts/bootstrap-labels.sh` 단일 외부 호출 (ADR-061 §결정 1
      외부 script convention reuse — multi-line shell embed 회피, CFP-583 BODY heredoc
      anti-pattern 차단). Token: ${{ secrets.CODEFORGE_CROSS_REPO_PAT }} primary +
      ${{ secrets.GITHUB_TOKEN }} fallback (ADR-066 90 day rotation lifetime).
    detect_command: bash scripts/bootstrap-labels.sh   # CFP-662 Phase 2 — workflow body 본문 호출 대상
    workflow: templates/github-workflows/bootstrap-labels.yml   # CFP-662 Phase 2 신설 (Active 전환)
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:bootstrap-labels
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # 1st-19th entry 동일 패턴
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — bootstrap-labels.sh reuse + workflow self-app 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}  # 본 lint = precondition check, modal phrase 검사 미포함
    introduced_by: CFP-662
    introduced_date: 2026-05-14
    owner_adr: ADR-060               # framework SSOT — PR-time precondition check pattern 의 정책 owner
    carrier_adr: ADR-060             # self-carrier (Amendment 10)
    recurrence:
      count: 1                       # MCT-104 Phase 2 mctrader-data PR #14 (2026-05-09) actual occurrence [empirical-source: spec PR #393 §"동인" + Issue #318]
      threshold: 3                   # ADR-060 §결정 19 (Amendment 6) — advisory promotion signal
      last_occurrence: 2026-05-09    # [empirical-source: mctrader-data PR #14 timestamp, RETRO-MCT-104]
      promotion_trigger: advisory    # count=1 < threshold=3 → 발화 없음 (단일 발생 시점)
    status: deferred-followup        # CFP-662 Phase 2 carrier 신설 후 Active 전환 (workflow self-app 시점)

  - name: marketplace-drift-detection
    description: |-
      CFP-673 / ADR-063 Amendment 3 §결정 13 — marketplace ↔ wrapper plugin.json drift
      reactive scheduled detection (4th defense layer). 7 plugin × 4 mirrored field per
      24h cron + workflow_dispatch manual fallback. drift detected 시 Issue auto-create
      (dedup per-(plugin, field) signature). Proactive 3-layer (§결정 9 + §결정 11 +
      §결정 5) 가 변경 시점 enforce, 본 §결정 13 reactive layer 가 변경 시점 외
      (rebase / 별 PR / direct commit / version collision resolve) drift 차단.
      4-tuple reproduce evidence (CFP-609 / CFP-612 / CFP-610 Story 2 / CFP-673 session live verify).
    detect_command: bash scripts/check-marketplace-drift.sh
    workflow: templates/github-workflows/marketplace-drift-detection.yml
    current_tier: warning   # ADR-060 §결정 5 default — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:marketplace-drift-detection
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - drift_issue_zero_count_proof   # 3 month rolling window
        - cron_run_history_url
    introduced_by: CFP-673
    introduced_date: 2026-05-14
    owner_adr: ADR-063
    carrier_adr: ADR-060
    recurrence:
      count: 5              # CFP-609 / CFP-612 / CFP-610 Story 2 / CFP-673 prep verify 2026-05-14 / CFP-673 sub-PR (b) PR #682 live 2026-05-15 — CFP-686 Amendment 4 ratchet UP 4 → 5
      threshold: 5          # CFP-686 Amendment 4 ratchet UP: 4 → 5
      promotion_trigger: warning_first  # ADR-060 §결정 5 default warning (Amendment 4 preserved)
      last_occurrence: 2026-05-15  # CFP-673 sub-PR (b) PR #682 CI fail live (5th reproduce — marketplace-parity workflow plugin.json 5.63.0 ↔ marketplace 5.64.0 drift)
    sunset_gate:
      metric: "drift Issue 자동 발의 0건 / 3 month rolling window"
      measured_by: github_actions_run_history + drift_issue_count
      sunset_decision_owner: ArchitectPL or 사용자 directive
    status: Active

  # ─── CFP-932 Wave 4 sub-Epic #1 Story-2 (channel 3-tuple drift reactive scheduled detection, 2026-05-18) ───
  # reconcile-protocol-v1 §4.10 channel_drift_detection.story_1_scope → Story-2 carrier 실현.
  # ADR-063 Amendment 3 §결정 13 marketplace-drift-detection precedent byte-pattern 답습 (3-tuple = marketplace 2-leg 확장).

  - name: channel-drift-detection
    description: |-
      CFP-932 / ADR-063 Amendment 3 §결정 13 + reconcile-protocol-v1 §4.10 — consumer
      codeforge.channel.tier ↔ install plugin.json .version ↔ registry marketplace.json
      channels[*].versions[] membership 3-tuple drift reactive scheduled detection.
      24h cron + workflow_dispatch manual fallback. drift detected 시 Issue auto-create
      (dedup per signature sha256("<plugin>|channel|<consumer-tier>|<registry-tier>")|head -c 16).
      marketplace-drift-detection.yml byte-pattern 답습 — E-4 3-branch (401 fail-closed /
      429 fail-open / 5xx 3-retry) + warning-first (drift 감지해도 exit 0, Issue 통보 channel).
      (c) registry leg 미populate (Story-4 전) = warning-first graceful (blocking 0,
      transitional valid — reconcile §4.10 failure_mode_default: warning-first).
    detect_command: bash scripts/check-channel-drift.sh
    workflow: templates/github-workflows/channel-drift-detection.yml
    current_tier: warning   # ADR-060 §결정 5 default — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:channel-drift-detection
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - channel_drift_issue_zero_count_proof   # 3 month rolling window
        - cron_run_history_url
    introduced_by: CFP-932
    introduced_date: 2026-05-18
    owner_adr: ADR-063
    carrier_adr: ADR-060
    sunset_gate:
      metric: "channel drift Issue 자동 발의 0건 / 3 month rolling window (단 Story-4 channels[] populate 후 측정 — populate 전 = (c) leg unknown graceful, sunset 측정 미발동)"
      measured_by: github_actions_run_history + channel_drift_issue_count
      sunset_decision_owner: ArchitectPL or 사용자 directive
    status: Active

  # ─── CFP-685 Phase 1 sub-PR (b) (templates ↔ self-app byte-identical parity lint, 2026-05-15) ───
  # ADR-065 §결정 1 row 3 mechanical enforcement — wrapper templates/github-workflows/*.yml ↔
  # .github/workflows/*.yml SHA-256 byte-identical parity. CFP-609 retro Finding D carrier.
  # owner_adr = ADR-065 (TP#2 정정 결과 — TP#1 ADR-005 재설정, ADR-065 §결정 1 row 3 SSOT 직접 ownership).

  - name: auto-phase-label-sibling-parity
    description: |
      CFP-685 / ADR-065 §결정 1 row 3 — wrapper templates/github-workflows/*.yml ↔
      .github/workflows/*.yml SHA-256 byte-identical parity 검증. drift 발견 시
      GitHub Issue 자동 생성 (warning tier, ADR-060 §결정 5). cron weekly Monday 10:00 UTC
      + workflow_dispatch manual trigger. CFP-609 retro Finding D carrier (sibling
      workflow parity enforcement forcing function). prior-art: check-marketplace-parity.sh
      + check-superpowers-schema-drift.sh 패턴 차용. exit code 3-tier (ADR-060 Amendment 2
      §결정 15): 0 PASS / 1 drift detected (count mismatch) / 2 SETUP error
      (directory 부재 / sha256sum+shasum both unavailable).
    detect_command: bash scripts/check-sibling-workflow-parity.sh
    workflow: templates/github-workflows/sibling-workflow-parity.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:auto-phase-label-sibling-parity
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — templates ↔ self-app parity lint 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-685
    introduced_date: 2026-05-15
    owner_adr: ADR-065               # §결정 1 row 3 — templates ↔ self-app byte-identical SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-442 Phase 2 (evidence-registry inventory anomaly lint, 2026-05-14) ───
  # ADR-060 Amendment 11 — 5번째 warning-tier entry (framework self-application 5-piece chain 완성)
  # (1st = adr-sunset-criteria / 2nd = decision-principle-vocab / 3rd = auto-phase-label /
  # 4th = claude-md-line-cap / **5th = evidence-registry-anomaly**)
  # sub-check 1: registry ↔ ADR-060 §결정 13 표 Group A 18 entry 1:1 parity
  # sub-check 2: 4-criteria AND 후보 식별 + registry 미등록 감지
  # ALLOWLIST 4-path self-exempt + start-up time assertion (EC-9 정합)

  - name: evidence-registry-anomaly
    description: |
      ADR-060 Amendment 11 §결정 25 — 메타 anomaly (inventory) lint.
      sub-check 1: registry yaml entries ↔ ADR-060 §결정 13 표 Group A 18 entry 1:1 inventory parity.
      sub-check 2: scripts/check-*.sh + .github/workflows/*.yml + templates/github-workflows/*.yml
                   4-criteria static analysis 후보 식별 + registry 미등록 감지.
      ALLOWLIST 4-path self-exempt + start-up time assertion (EC-9 정합).
      Exit code 3-tier (Amendment 2 §결정 15 정합).
    detect_command: bash scripts/check-evidence-registry-anomaly.sh
    workflow: templates/github-workflows/evidence-registry-anomaly-check.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    # bypass_label: omit             # Amendment 2 §결정 16 — warning tier optional
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies:                  # F-CL-01 FIX iter 2 — self-carrier CFP-442 제외 convention (prior art `evidence-registry-schema-validation` self CFP-455 미포함 + `adr-sunset-criteria` self CFP-389 미포함)
        - CFP-390
        - CFP-412
        - CFP-455
        - CFP-449
        - CFP-481
        - CFP-506
        - CFP-509
        - CFP-508
        - CFP-530
        - CFP-583
        - CFP-662
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}  # 본 lint = inventory anomaly, modal phrase 검사 미포함
    introduced_by: CFP-442
    introduced_date: 2026-05-14
    owner_adr: ADR-060               # framework SSOT — inventory anomaly axis 의 정책 owner
    carrier_adr: ADR-060             # self-carrier (Amendment 11)
    recurrence:
      count: 0                       # CFP-442 첫 도입 — historical evidence 0건
      threshold: 3                   # ADR-060 §결정 19 (Amendment 6) — advisory promotion signal
      promotion_trigger: none        # count=0 < threshold=3 → 발화 없음 (Option A)
    status: Active

  # ─── CFP-708 Phase 2 (CLAUDE.md Amendment ref drift detection lint, 2026-05-14) ───
  # ADR-074 -- SSOT cross-section coherence lint (CFP-263 doc-locations lint lineage 답습)
  # CLAUDE.md 안 "Amendment N (CFP-NNN)" 참조 + ADR frontmatter amendment_log/amendments 길이 비교
  # 2 drift evidence: CFP-627 stale ref + CFP-477 DesignReview F-DR-001 P1 detection
  - name: claude-md-amendment-ref-drift-check
    description: |
      CFP-708 / ADR-074 -- CLAUDE.md Amendment ref drift detection lint.
      CLAUDE.md 본문 안 "[ADR-NNN](...)" 링크 + 인접 "Amendment N (CFP-NNN)" 패턴 detect +
      해당 ADR frontmatter amendment_log[] 또는 amendments[] 배열 길이와 비교.
      claimed Amendment N != actual length = drift (stale 또는 phantom).
      exit code 3-tier (ADR-060 Amendment 2 §결정 15): 0 PASS / 1 drift / 2 setup error.
      2 occurrence evidence: CFP-627 (CLAUDE.md L296 ADR-063 stale) +
      CFP-477 DesignReview F-DR-001 P1 (Amendment 2 §결정 11 -> 실제 Amendment 3 §결정 13).
      ADR-074 lineage: CFP-263 doc-locations lint cross-section coherence pattern 답습.
    detect_command: bash scripts/check-claude-md-amendment-ref.sh
    workflow: templates/github-workflows/claude-md-amendment-ref-drift.yml
    current_tier: warning            # ADR-060 §결정 5 -- 첫 도입 = warning mode
    bypass_label: hotfix-bypass:claude-md-amendment-ref
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry -- CLAUDE.md + ADR drift cross-section lint 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-708
    introduced_date: 2026-05-14
    owner_adr: ADR-074               # CLAUDE.md Amendment ref drift SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 2                       # CFP-627 + CFP-477 2 occurrence evidence (threshold reach)
      threshold: 2                   # ADR-060 §결정 19 (Amendment 6) -- threshold 2 (drift x2 pattern)
      promotion_trigger: adr_draft_emitted  # count=2 >= threshold=2 -> ADR 초안 발행 (ADR-074 = 본 entry)
    status: Active

  # ─── CFP-688 Phase 2 sub-PR (c) — ADR-026 Amendment 3 §결정 5.G.b (actionlint prevention, 2026-05-15) ───
  # born-broken 재발 차단 forcing function — workflow yml syntax validation (actionlint v1.7.12)
  # 54번째 entry. ADR-026 Amendment 3 §결정 5.G.b deferred-followup → active 전환 carrier.
  - name: workflow-actionlint-precommit
    description: |
      CFP-688 / ADR-026 Amendment 3 §결정 5.G.b — actionlint prevention layer.
      .github/workflows/*.yml + templates/github-workflows/*.yml YAML syntax validation
      via actionlint v1.7.12 (rhysd/actionlint Go binary — PyPI 미배포, download-actionlint.bash 설치).
      CI step (.github/workflows/actionlint-check.yml) + opt-in pre-commit hook
      (templates/.git-hooks/pre-commit.sample) 2-layer.
      born-broken 재발 차단 — CFP-688 P0 incident (post-merge-followup.yml 100% FAILURE 17h 18min) evidence.
      ADR-060 §결정 5 default warning tier 첫 도입. 승격 target = blocking-on-pr (ADR-026 §5.G.b prose).
      Tier reconciliation: 첫 도입 warning / ADR-026 prose "exit non-zero = fail-closed" = 승격 후 동작.
      Exit code 3-tier: 0 PASS / 1 syntax violation / 2 setup error (actionlint binary 부재).
    detect_command: bash .github/workflows/actionlint-check.yml  # CI workflow trigger (PR-time)
    workflow: templates/github-workflows/actionlint-check.yml
    current_tier: warning            # ADR-060 §결정 5 -- 첫 도입 = warning mode
    bypass_label: hotfix-bypass:actionlint
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-688                    # self-carrier
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-688
    introduced_date: 2026-05-15
    owner_adr: ADR-026               # workflow file integrity safeguards SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 1                       # CFP-688 P0 incident (100/100 FAILURE 17h) = 1 occurrence evidence
      threshold: 3                   # ADR-060 §결정 19 (Amendment 6) -- threshold 3 standard
      promotion_trigger: none        # count=1 < threshold=3 -- 발화 없음 (Option A)
    status: Active

  # ─── CFP-688 Phase 2 sub-PR (c) — ADR-026 Amendment 3 §결정 5.G.d (KPI detection, 2026-05-15) ───
  # 9번째 warning-tier entry (ADR-026 §5.G.d 명시). post-merge-followup success rate sentinel.
  # 55번째 entry. ADR-026 Amendment 3 §결정 5.G.d deferred-followup → active 전환 carrier.
  - name: post-merge-followup-workflow-success-rate-kpi
    description: |
      CFP-688 / ADR-026 Amendment 3 §결정 5.G.d — KPI post-detection layer.
      post-merge-followup.yml workflow rolling 14-day success rate sentinel.
      sentinel ≥ 90% — breach 시 Issue 자동 생성 (born-broken 재발 조기 감지).
      측정: scripts/check-post-merge-followup-success-rate.sh (thin bash wrapper, ADR-061).
      cron weekly (매주 월요일 09:00 UTC) + workflow_dispatch ad-hoc.
      ADR-026 §7.3 KPI metric SSOT: gh run list --workflow=post-merge-followup.yml
        --created=>=$(14 days ago) --json conclusion → success ratio.
      Exit code 3-tier: 0 PASS / 1 breach / 2 setup error (ADR-060 §결정 15).
      sunset_justification 면제 — ratchet 강화 방향 (Amendment 3, ADR-058 §결정 5 정합).
    detect_command: bash scripts/check-post-merge-followup-success-rate.sh
    workflow: templates/github-workflows/post-merge-followup-success-rate-kpi.yml
    current_tier: warning            # ADR-060 §결정 5 -- 첫 도입 = warning mode
    bypass_label: hotfix-bypass:post-merge-followup-success-rate
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-688                    # self-carrier
      evidence_artifacts:
        - github_actions_run_history_url
        - kpi_sentinel_passing_window_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-688
    introduced_date: 2026-05-15
    owner_adr: ADR-026               # workflow file integrity safeguards SSOT
    carrier_adr: ADR-060             # framework carrier (warning mode entry)
    recurrence:
      count: 1                       # CFP-688 P0 incident (100/100 FAILURE 17h) = 1 occurrence evidence
      threshold: 3                   # ADR-060 §결정 19 (Amendment 6) -- threshold 3 standard
      promotion_trigger: none        # count=1 < threshold=3 -- 발화 없음 (Option A)
    status: Active

  # ─── CFP-702 — ADR-027 Amendment 3 §결정 7.D (D4 customization marker, 2026-05-15) ───
  # blocking-on-pr tier — D4 marker 위반 = customization wholesale loss 직결 (HIGH risk)
  # warning 시작점 아닌 blocking-on-pr 직접 도입 (ADR-027 §결정 7.F 근거).
  # 56번째 entry.
  - name: wrapper-managed-block
    description: |
      CFP-702 / ADR-027 Amendment 3 §결정 7.D — D4 customization marker block lint.
      consumer file 안 # BEGIN / # END wrapper-managed marker pair 정합성 검증.
      검증 3종: (1) BEGIN/END pairing (orphan = malformed), (2) 순서 invariant (역전 = malformed),
      (3) flat-only nesting 금지 (nested = reject, ADR-027 §결정 7.D.1 Axis 2).
      File type별 marker syntax (ADR-027 §결정 7.A.1):
        .yml/.yaml/.sh: # BEGIN/END wrapper-managed
        .md: <!-- BEGIN/END wrapper-managed -->
        .json: sidecar manifest only (marker-incapable — Wave 2 Story-5 carrier)
      D4 marker 위반 = Wave 2 Story-5 reconcile 시 consumer customization wholesale loss 직결.
      Exit code 3-tier: 0 PASS / 1 malformed marker / 2 setup error (ADR-060 §결정 15).
      sunset_justification 면제 — ADR-027 is_transitional: false (permanent policy, ratchet 강화 방향).
    detect_command: bash scripts/check-wrapper-managed-block.sh
    workflow: templates/github-workflows/wrapper-managed-block.yml
    current_tier: blocking-on-pr   # ADR-027 §결정 7.F + ADR-060 §결정 5 — HIGH risk 직접 blocking-on-pr 도입
    bypass_label: hotfix-bypass:wrapper-managed-block
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b) — bypass 외 failure 0
      sibling_dependencies:
        - CFP-702                    # self-carrier
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-702
    introduced_date: 2026-05-15
    owner_adr: ADR-027               # consumer adoption protocol + D4 marker SSOT
    carrier_adr: ADR-060             # framework carrier (blocking-on-pr tier)
    recurrence:
      count: 0                       # 첫 도입 — prior incident 없음
      threshold: 3                   # ADR-060 §결정 19 (Amendment 6) -- threshold 3 standard
      promotion_trigger: none        # count=0 < threshold=3 -- 발화 없음 (Option A)
    status: Active

  # ─── CFP-744 Wave 2 Story-4 (ADR-037 Amendment 1 — atomic upgrade 후 0 drift invariant, 2026-05-16) ───
  # Codex TP#2 FIX Iter 2 F-P1 — ADR-040 Amendment 3 §결정 7 mechanical action binding 의무.
  # 旧 mismapping (marketplace-parity = wrapper-side publishing-time mirrored-field parity) 정정 → 신규 전용 entry.
  # marketplace-parity 와 의미 분리: marketplace-parity = plugin.json ↔ marketplace.json mirrored field (publishing-time, wrapper-side) /
  # 본 entry = consumer-side installed 7-plugin version 고정 ↔ marketplace SSOT drift after atomic upgrade (runtime, consumer-side).
  - name: atomic-upgrade-zero-drift
    description: |
      ADR-037 Amendment 1 mechanical verification — codeforge family 7 plugin
      (codeforge + codeforge-{requirements,design,review,develop,test,pmo})
      atomic upgrade `--apply` 완료 후 consumer-side installed version 고정 ↔
      marketplace SSOT drift 0 (none) invariant. detect mechanism = 기존
      scripts/check-codeforge-version-drift.sh 를 `--plugin <codeforge-N>` 7회
      invocation (F-002 옵션 A 정합 — codex/superpowers 외부 marketplace 제외,
      7-name loop 구조적 배제). 실 enforcement carrier = Phase 2
      scripts/atomic-upgrade-7-plugins.sh 의 post-atomic 0-drift gate (drift > 0
      = transaction 실패 분류 → 전체 7 plugin atomic rollback). marketplace-parity
      entry (wrapper-side publishing-time mirrored-field cross-repo drift) 와는
      의미 disjoint — 본 entry = consumer-side runtime 0-drift invariant 자체.
    detect_command: bash scripts/check-codeforge-version-drift.sh   # 7-plugin family scope (--plugin <codeforge-N> aggregated, F-002 옵션 A). 기존 script reuse — detect mechanism 실재 (CFP-262 Accepted)
    workflow: templates/github-workflows/atomic-upgrade-zero-drift.yml   # Phase 2 신설 완료 (.github/ self-app byte-identical, ADR-005). Active 전환됨
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:atomic-upgrade-zero-drift   # warning-tier bypass_label optional (ADR-060 Amendment 2 §결정 16) — 미래 promotion 대비 namespace 예약 (ADR-024 Amendment 3 per-entry namespace 정합)
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # 기존 entry 동일 패턴
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — check-codeforge-version-drift.sh (CFP-262 기존) reuse + Phase 2 atomic-upgrade-7-plugins.sh 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}  # 본 lint = post-atomic drift gate, modal phrase 검사 미포함
    introduced_by: CFP-744
    introduced_date: 2026-05-16
    owner_adr: ADR-037               # Amendment 1 SSOT — atomic upgrade 후 0 drift invariant 정책 owner
    carrier_adr: ADR-060             # evidence-enforceable framework carrier
    recurrence:
      count: 0                       # 첫 도입 — prior incident 없음
      threshold: 3                   # ADR-060 §결정 19 (Amendment 6) — threshold 3 standard
      promotion_trigger: none        # count=0 < threshold=3 → 발화 없음 (Option A)
    status: Active                   # Phase 2 (CFP-744) — scripts/atomic-upgrade-7-plugins.sh + templates/.github atomic-upgrade-zero-drift.yml byte-identical self-app land 완료 → deferred-followup → Active 전환 (Phase 1 = registry declare only, bootstrap-labels-precondition 패턴 동형)

  # kst-timestamp-display (CFP-771 / ADR-079 Amendment 1 — KST timestamp display mechanical lint)
  # 58번째 entry. warning tier 신규 도입.
  - name: kst-timestamp-display
    description: |
      ADR-079 Amendment 1 §결정 1 mechanical verification — display layer timestamp 의
      KST +09:00 colon-offset form 강제 lint. 5 scope 영역 (CLAUDE.md /
      orchestrator-playbook / ADR / retros) 의 ISO 8601 dateTime offset 검사.
      EXEMPT: docs/inter-plugin-contracts/ (contract field UTC strict invariant) +
      Story §14 spawned_at/returned_at (machine-layer) + ADR frontmatter date-only line.
      INV-DM-2 정합: detect-only read-only grep — autofix 채널 절대 금지.
    detect_command: bash scripts/check-kst-timestamp.sh
    workflow: templates/github-workflows/kst-timestamp-display.yml
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:kst-timestamp-display   # warning-tier bypass_label (ADR-060 Amendment 2 §결정 16 — 미래 promotion 대비 namespace 예약, ADR-024 Amendment 3 per-entry namespace 정합)
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # 기존 entry 동일 패턴
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — ADR-079 Amendment 1 SSOT. check-kst-timestamp.sh 신설 스크립트 외 의존 없음
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}  # 본 lint = RFC 3339 offset form 검사, modal phrase 검사 미포함
    introduced_by: CFP-771
    introduced_date: 2026-05-16
    owner_adr: ADR-079               # Amendment 1 SSOT — KST display layer mandate
    carrier_adr: ADR-060             # evidence-enforceable framework carrier
    recurrence:
      count: 0                       # 첫 도입 — prior incident 없음
      threshold: 3                   # ADR-060 §결정 19 (Amendment 6) — threshold 3 standard
      promotion_trigger: none        # count=0 < threshold=3 → 발화 없음 (Option A)
    status: Active                   # CFP-771 Phase 1 — scripts/check-kst-timestamp.sh + templates/.github kst-timestamp-display.yml byte-identical self-app + registry row 동시 land

  # ─── CFP-722 Phase 2 (ADR-060 Amendment 13 §결정 27 — per-section ownership mechanical lint, 2026-05-16) ───
  # Incident origin: PR #441 (mclayer/codeforge-internal-docs, DeveloperPL destructive rewrite §1.1/§2/§3/§4/§5/§6/§7/§9/§10/§13/§14 +216/-850)
  # story-section-9-typed 17-field schema verbatim (CFP-410 선례 정합, warning-tier entry).
  # ADR-040 Amendment 3 §결정 7 — 신규 mechanical action 도입 → mechanical_enforcement_actions[] binding 의무 (ADR-060 Amendment 13 frontmatter).
  - name: story-section-ownership
    description: |
      CFP-722 / ADR-060 Amendment 13 §결정 27 — Story per-section ownership mechanical lint.
      per-section diff 를 heading-anchored content slice(NOT line-range — EC-2 parallel-session
      line-shift resilient, story-section-1-immutable.yml:52-53 선례 일반화) 로 분류:
        INV-DI-1 (lane-owned §2/§3/§5/§6/§7/§8/§11): semantic-normalize 후 token-level
          deletion 존재 + non-owner lane 작성 → violation (PR #441 +216/-850 signature).
        INV-DI-2 (monopoly §10/§13/§14/§10.5): ANY base-row mutation without
          Orchestrator/delegate attribution → violation (CFP-32 fix-event-v1 + ADR-031 §14 FROZEN).
      §1 = story-section-1-immutable.yml 영역 cross-ref ONLY (중복 codification 회피).
      proxy attribution: branch `cfp-NNN[/<lane>]` + phase:* label → Orchestrator vs lane-plugin.
      carrier-Story exemption: bootstrap_exempt_protocols `policy:lane-self-write-boundary-mechanical`
        AND carrier_story == own story key (ADR-062 EC-4, first short-circuit).
      Warning tier: continue-on-error: true + sys.exit(0) always (ADR-060 §결정 5).
      Exit code 3-tier: 0 always (warning mode — blocking-on-pr promotion = expedited candidate
        given data-loss severity, evidence gate applies).
      sunset_justification: N/A — ADR-060 is_transitional: false (permanent framework, ratchet UP).
    detect_command: bash scripts/check-story-section-ownership.sh
    workflow: templates/github-workflows/story-section-ownership-check.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode (blocking-on-pr expedited candidate — data-loss severity, §결정 6 evidence gate)
    bypass_label: hotfix-bypass:story-section-ownership  # ADR-024 Amendment 3 per-entry namespace (§결정 6.A verbatim)
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b) — bypass 외 failure 0
      sibling_dependencies:
        - CFP-722                    # self-carrier (Phase 2 = impl)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-722
    introduced_date: 2026-05-16
    owner_adr: ADR-031               # §14 Lane Evidence monopoly SSOT + Amendment 1 delegate subagent
    carrier_adr: ADR-060             # evidence-enforceable framework carrier (Amendment 13)
    recurrence:
      count: 0                       # 첫 도입 — incident = PR #441 (pre-lint era, 별도 historical entry 아님)
      threshold: 3                   # ADR-060 §결정 19 (Amendment 6) — threshold 3 standard
      promotion_trigger: none        # count=0 < threshold=3 → 발화 없음 (Option A)
    status: Active                   # Phase 2 (CFP-722) impl 완료 (script + lib + 2 workflow self-app + fixture 19건 all-green)

  # ─── CFP-745 Phase 2 (wrapper template managed coverage authoring-guard lint, 2026-05-16) ───
  # §8.2 Phase 2 lint declare — authoring drift guard (Codex TP#2 P2 Dim3 ACCEPT → deferred §10)
  # wrapper template overlay 파일 안 wrapper-managed content 가 D4 marker block / sidecar 안에 위치하는지
  # authoring-time lint. runtime reconcile-overlay.sh 보완 (authoring-time guard = disjoint).

  - name: wrapper-template-managed-coverage
    description: |
      CFP-745 §8.2 authoring-guard lint — wrapper plugin template overlay 파일 안
      wrapper-managed content 가 D4 marker block (`# BEGIN/END wrapper-managed`) 또는
      sidecar manifest managed_paths 안에 위치하는지 authoring-time 검증.
      authoring drift (wrapper 저작자가 wrapper-managed 라인을 marker 밖으로 이동) 시
      reconcile-overlay.sh 의 marker 밖 preserve 가 해당 변경 전파를 차단하는 silent
      authoring gap 을 early-detection. runtime reconcile-overlay.sh 와 책임 disjoint
      (consumer runtime 무관 — wrapper template self-audit).
      reconcile-protocol-v1 §4.7 / ADR-027 Amendment 3 §결정 7.D carrier.
      warning tier (ADR-060 §결정 5) — authoring drift = PR merge 차단 아님 (early warning).
      Exit 0 always (warning mode) / --strict exit 1 (opt-in).
    detect_command: bash scripts/check-wrapper-template-managed-coverage.sh
    workflow: templates/github-workflows/wrapper-template-managed-coverage.yml
    current_tier: warning             # ADR-060 §결정 5 — 첫 도입 = warning mode (authoring drift guard)
    bypass_label: hotfix-bypass:wrapper-template-managed-coverage
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20           # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0            # ADR-060 §결정 6 (b)
      sibling_dependencies: []        # 독립 entry — reconcile-overlay.sh + validate_sidecar.py 재사용
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    introduced_by: CFP-745
    introduced_date: 2026-05-16
    owner_adr: ADR-027                # D4 marker block syntax SSOT (Amendment 3 §결정 7.D)
    carrier_adr: ADR-060              # evidence-enforceable framework carrier
    recurrence:
      count: 0
      threshold: 3                    # ADR-060 §결정 19 (Amendment 6)
      promotion_trigger: none         # count=0 < threshold=3 → 발화 없음
    status: Active                    # Phase 2 (CFP-745) — scripts/ + templates/ 실 구현 land 완료 → Active

  # ─── CFP-785 Phase 2 carrier (ADR-077 ratchet + design-reading mandate declared, 2026-05-17) ───
  - name: adr-077-ratchet-declared
    description: |
      ADR-077 §결정 9 ratchet 선언 mechanical verification — ADR-077 계열 ADR 수정 PR 에서
      (a) §결정 9 ratchet 선언 문구, (b) ADR-058 §결정 5 sunset_justification 문구,
      (c) frontmatter `is_transitional: false` 존재 여부 lint.
      grep-testable artifact 존재 (scripts/check-adr-077-ratchet.sh).
    detect_command: bash scripts/check-adr-077-ratchet.sh   # Phase 2 carrier 신설 후 detect_command 채움 (deferred-followup)
    workflow: templates/github-workflows/adr-077-ratchet-declared.yml   # Phase 2 신설 완료 시 Active 전환
    current_tier: warning
    bypass_label: hotfix-bypass:adr-077-ratchet
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-785
    introduced_date: 2026-05-17
    owner_adr: ADR-077
    carrier_adr: ADR-077
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: null
      promotion_trigger: advisory
    status: Active   # Phase 2 carrier lint script+workflow self-app land 시 Active 전환

  - name: adr-077-design-reading-mandate-declared
    description: |
      ADR-077 §결정 3 design-reading 깊이 강화 mandate mechanical verification — ADR-077 계열
      ADR 수정 PR 에서 (a) §결정 3 "skim 금지 + 의도/근거 파악" 선언 문구,
      (b) 적용 3 agent (ChangeImpactAgent/FeasibilityAgent/ContinuityAgent) verbatim 명시
      여부 lint. grep-testable artifact 존재 (scripts/check-adr-077-design-reading-mandate.sh).
    detect_command: bash scripts/check-adr-077-design-reading-mandate.sh   # Phase 2 carrier 신설 후 채움 (deferred-followup)
    workflow: templates/github-workflows/adr-077-design-reading-mandate-declared.yml   # Phase 2 신설 완료 시 Active 전환
    current_tier: warning
    bypass_label: hotfix-bypass:adr-077-design-reading
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-785
    introduced_date: 2026-05-17
    owner_adr: ADR-077
    carrier_adr: ADR-077
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: null
      promotion_trigger: advisory
    status: Active   # Phase 2 carrier lint script+workflow self-app land 시 Active 전환

  # ─── CFP-825 Phase 1 (ADR-024 Amendment 6 §결정 6.A.2 — bypass-as-norm mutation 누적 monitoring 첫 진입, 2026-05-17) ───
  # Incident origin: CFP-770/771 PR #788 admin merge (claude-md-line-cap + wording-dictionary 2 label 동시 부착)
  # + CFP-819 PR #823 (wording-dictionary cosmetic 7건) + CFP-786/801/795 (unit-tests pre-existing) — 5+ evidence cluster.
  - name: bypass-label-counter
    description: |
      hotfix-bypass:<entry> per-entry namespace label family 의 per-(plugin, label)
      signature 누적 사용 카운터 lint — signature ≥3 reach-merged PR 누적 시 carrier
      Issue 자동 발의 (window=all-time / dedup_unit=PR number / dedup by (plugin,label)
      signature). exception → norm mutation (bypass-as-norm mutation) 위험 누적
      monitoring. self-meta loop 회피: hotfix-bypass:bypass-label-counter 부착 PR +
      hotfix-bypass:exempt:<entry> 부착 PR 은 누적 count 제외.
      grep-testable artifact 존재 예정 (scripts/check-bypass-label-counter.sh,
      Phase 2 carrier). CFP-627 marketplace-drift-detection precedent (24h cron +
      workflow_dispatch + Issue auto-create).
    detect_command: bash scripts/check-bypass-label-counter.sh
    workflow: templates/github-workflows/bypass-label-counter.yml
    current_tier: warning
    bypass_label: hotfix-bypass:bypass-label-counter
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-825
    introduced_date: 2026-05-17
    owner_adr: ADR-024            # Amendment 6 §결정 6.A.2 (ratchet 룰 SSOT)
    carrier_adr: ADR-060          # evidence-enforceable framework carrier
    recurrence:
      count: 5                    # CFP-770/771 #788 (2) + CFP-819 #823 (1) + CFP-786/801/795 unit-tests (2) — 5+ evidence cluster
      threshold: 3                # ADR-060 §결정 19 (Amendment 6) — threshold 3 standard
      last_occurrence: "2026-05-16T23:01:11Z"  # CFP-819 PR #823 wording-dictionary cosmetic 7건 (verified: gh pr view 823 --json mergedAt = 2026-05-16T23:01:11Z)
      promotion_trigger: advisory # count=5 ≥ threshold=3 → advisory (본 carrier 자체가 monitoring 도입)
    status: Active

  # ─── CFP-833 Phase 1 — dialog-fidelity-effect entry (Epic CFP-761 Story-3 closing-the-loop) ───
  # ADR-071 Amendment 3 carrier. precedent rate-limit-fallback-rate 동형 (runtime cron metric pattern).
  # Phase 1 = skeleton (non-null detect_command, #827 회피). Phase 2 carrier = workflow yml 2종 + check script + lib .py.
  - name: dialog-fidelity-effect
    description: |
      ADR-071 incident append-rate delta (proxy signal — not causal effectiveness
      measure). Epic CFP-761 Story-3 closing-the-loop — DialogFidelityAgent 도입 전후
      Layer 4 incident realtime detect row append rate A-B baseline delta.
      분자 = Story-1 merge (577f96f, 2026-05-17 KST) 이후 월간 realtime detect row
      count (trigger ∈ layer-3-keyword/layer-4-n1/layer-4-m5). 분모 = measurement
      window 월 수 (1, 1 month rolling). before(baseline) = backfill marker row
      (Story-0 retroactive 3 row CFP-672/701/707) 를 timestamp occurrence span
      (2026-05-14~05-15, ~2 day) 으로 normalize 한 monthly-equivalent rate
      (= 3 / span_days × 30, 단순 3/1month 고정 아님). after = Story-1 merge 이후
      realtime detect only 의 month-bucketed rate. delta = after − before(monthly-eq)
      rate (음수 = verifier 효과 proxy 신호). proxy 한계: before=backfill /
      after=realtime collection mode 상이 → delta 는 instrumentation mode change /
      backfill completeness / reviewer behavior 변화도 반영 가능 (advisory operational
      signal only, 효과 판정 아님). runtime cron metric pattern (vs static lint) —
      advisory dashboard, no PR block. ADR-058 §결정 3 측정성 self-application
      (verifier 도 측정 없이 영구화 금지). sample insufficient sentinel (월간 realtime
      detect row < 3 = N/A — advisory only, pragmatic noise floor: 1-2 row =
      single-incident high-variance noise, 3 = month-over-month 비교 minimum viable
      data point. precedent rate-limit-fallback-rate "월 < 50 = N/A" 동일 성격).
    detect_command: bash scripts/check-dialog-fidelity-effect.sh
    workflow: templates/github-workflows/dialog-fidelity-measurement.yml
    current_tier: warning  # advisory dashboard, no PR block — semantic = "warning mode 의 runtime metric extension" (precedent rate-limit-fallback-rate 동형)
    # bypass_label: omit — runtime metric pattern 에서 의미 부적용 (OQ-3 사용자 확정 2026-05-17 KST, precedent 동형)
    # bypass_audit_lint: omit — bypass_label 미정의
    promotion_criteria:
      # 본 entry 의 promotion 의미 = lint warning→blocking promotion 아닌,
      # sample size sufficient 도달 시 ADR-071 effectiveness sunset gate measurement.
      # pr_cumulative_min=0 = promotion semantic 부적용 표기 (precedent 동형).
      pr_cumulative_min: 0
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - dialog_fidelity_ab_delta_rolling_window_evidence  # sunset 판정 carrier PR 의 산출물 (별도 carrier)
    # modal_anti_pattern_dictionary: omit — lint 가 모달 어휘 검사 미포함
    introduced_by: CFP-833
    introduced_date: 2026-05-17
    owner_adr: ADR-071           # 정책 ADR — dialog convergence permanent governance, effectiveness sunset gate owner
    carrier_adr: ADR-060         # framework carrier (CFP-388 / ADR-060 §결정 12 non-sunset application family)
    recurrence:
      count: 0
      promotion_trigger: none      # ADR-060 §결정 19 (Amendment 6) — schema v1.2 default
    status: Active

  # ─── CFP-845 Phase 1 (ADR-024 Amendment 8 §결정 6.A.3 — per-plugin scope 누적 카운터 첫 진입, 2026-05-17) ───
  # bypass-as-norm-mutation 후속 escalation 3 sub-decision 통합 Phase 1 (Story-1 = 본 #845 ACTIVE).
  # CFP-825 Amendment 6 §scope_boundary 4 out-of-scope 중 1번 영역 (per-plugin scope 누적 카운터) 흡수.
  - name: per-plugin-cumulative-counter
    description: |
      ADR-024 Amendment 8 §결정 6.A.3 — per-(plugin) signature 단위 cross-entry aggregate
      hotfix-bypass:* family member 누적 사용 카운터 lint. 단일 plugin 의 모든 entry 누적
      ≥5 reach-merged PR 시 carrier Issue 자동 발의 (signature = plugin-only, dedup_unit =
      PR number, window = all-time). §결정 6.A.2 (per-entry namespace, CFP-825) 의 상위
      layer 집계 channel — 단일 plugin 이 5 entry 각 1회 = 5회 분산 사용 시 §결정 6.A.2
      미발의 but 본 §결정 6.A.3 발의 (per-plugin scope norm mutation 신호). self-meta
      loop 회피: hotfix-bypass:per-plugin-cumulative-counter 부착 PR + hotfix-bypass:exempt:per-plugin
      template 부착 PR 은 누적 count 제외. §결정 6.A.2 와 disjoint invariant — 동일 PR
      가 양 trigger 시 양 carrier Issue 각 발의 (signature aggregation 금지).
      grep-testable artifact 존재 예정 (scripts/check-per-plugin-cumulative-counter.{py,sh},
      Phase 2 carrier). CFP-825 bypass-label-counter precedent 직접 답습 (24h cron +
      workflow_dispatch + Issue auto-create + per-signature dedup).
    detect_command: bash scripts/check-per-plugin-cumulative-counter.sh
    workflow: templates/github-workflows/per-plugin-cumulative-counter.yml
    current_tier: warning
    bypass_label: hotfix-bypass:per-plugin-cumulative-counter
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
        - per_plugin_threshold_calibration_evidence  # Phase 2 actual wire 후 별 calibration carrier (현 5 = 보수적 시작)
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-845
    introduced_date: 2026-05-17
    owner_adr: ADR-024            # Amendment 8 §결정 6.A.3 (per-plugin scope 누적 카운터 ratchet 룰 SSOT)
    carrier_adr: ADR-060          # evidence-enforceable framework carrier
    recurrence:
      count: 0                    # Phase 1 entry append, Phase 2 actual wire 후 evidence 누적 시점 별 update
      threshold: 5                # ADR-024 Amendment 8 §결정 6.A.3 — per-plugin scope 보수적 시작 (per-entry threshold 3 의 entry 다양성 cover noise floor)
      last_occurrence: null       # Phase 2 actual wire 후 first reach 시점 update
      promotion_trigger: none     # Phase 2 actual wire 완료, 첫 evidence 누적 후 advisory 격상 예정
    status: warning              # Phase 2 wire 완료 (CFP-845 Phase 2, 2026-05-17) — lint script + workflow + bats 모두 Active

  # ─── CFP-845 Phase 1 (ADR-024 Amendment 8 §결정 6.A.4 — `[bypass-justification]` PR comment marker presence lint, 2026-05-17) ───
  # narrative audit 영역 첫 evidence-checks-registry entry.
  # CFP-825 Amendment 6 §scope_boundary 4 out-of-scope 중 3번 영역 (`[bypass-justification]` marker) 흡수.
  - name: bypass-justification-marker
    description: |
      ADR-024 Amendment 8 §결정 6.A.4 — hotfix-bypass:* label 부착 PR 의
      `^\[bypass-justification\]` top-level PR comment grep-presence lint. comment-prefix-registry-v1
      v1.3 14번째 prefix `[bypass-justification]` 신설 정합 (CFP-845 carrier). semantic
      adequacy 불가 (grep-only) — false-positive risk 명시, reviewer responsibility.
      grep PASS but body 부적합 (예: 빈 marker, 단순 "ok") = lint PASS but reviewer reject
      영역 (CFP-845 Research §unknown unknown 2 — NLP 평가 별 carrier deferred).
      lint scope = hotfix-bypass:* label 부착 PR 의 top-level PR comment only (review
      comment 제외). marker 의무 scope = hotfix-bypass:* 부착 PR only (label 부착 없는
      PR 은 marker 의무 X). self-meta loop 회피: hotfix-bypass:bypass-justification-marker
      부착 PR 은 marker presence check skip. workflow event filter 의무 = `on:
      pull_request` (opened/synchronize/reopened) + `on: pull_request_review_comment`
      (top-level body parse) 양 event cover. audit trail 영구화 = PR comment 는
      GitHub-side state (PR close 후도 유지). grep-testable artifact 존재 예정
      (scripts/check-bypass-justification-marker.{py,sh}, Phase 2 carrier).
    detect_command: bash scripts/check-bypass-justification-marker.sh
    workflow: templates/github-workflows/bypass-justification-marker.yml
    current_tier: warning
    bypass_label: hotfix-bypass:bypass-justification-marker
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
        - false_positive_rate_evidence  # grep-only lint 의 false-positive rate 누적 measurement (Phase 2 actual wire 후 별 measurement carrier)
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-845
    introduced_date: 2026-05-17
    owner_adr: ADR-024            # Amendment 8 §결정 6.A.4 (PR comment marker mechanical enforce SSOT)
    carrier_adr: ADR-060          # evidence-enforceable framework carrier
    recurrence:
      count: 0                    # Phase 1 entry append, Phase 2 actual wire 후 evidence 누적 시점 별 update
      threshold: 1                # PR per-presence boolean (binary) — 단일 PR scope, 누적 threshold 미적용
      last_occurrence: null       # Phase 2 actual wire 후 first reach 시점 update
      promotion_trigger: none     # Phase 2 actual wire 완료, 첫 evidence 누적 후 advisory 격상 예정
    status: warning              # Phase 2 wire 완료 (CFP-845 Phase 2, 2026-05-17) — lint script + workflow + bats 모두 Active

  # ─── CFP-845 Phase 1 (ADR-024 Amendment 8 §결정 6.A.5 — cross-repo 3-repo bypass counter extension, 2026-05-17) ───
  # cross-repo 영역 첫 evidence-checks-registry entry.
  # CFP-825 Amendment 6 §scope_boundary 4 out-of-scope 중 4번 영역 (cross-repo extension) 흡수.
  - name: cross-repo-bypass-counter
    description: |
      ADR-024 Amendment 8 §결정 6.A.5 — 3-repo (mclayer/plugin-codeforge wrapper +
      mclayer/codeforge-internal-docs + mclayer/marketplace) cross-repo per-(repo,
      plugin, label) signature 누적 카운터 lint. threshold = per-(repo, plugin, label)
      signature 누적 ≥3 reach-merged PR (§결정 6.A.2 와 동일 — repo namespace 분리 시
      per-repo 독립 trigger). aggregate trigger = 3 repo 동일 (plugin, label) signature
      동시 reach 시 단일 aggregate carrier Issue 발의 (multi-repo signature, 3-repo
      systemic mutation 신호). per-repo 단독 trigger + aggregate trigger 양 channel
      disjoint. dedup_unit = (repo, PR number) 2-tuple (cross-repo PR number 충돌 회피).
      single PAT (CODEFORGE_CROSS_REPO_PAT, ADR-066) reuse — 3 repo `issues:read` +
      `repo:read` 권한, 신규 secret 0건. rotation 90 day / max 180 day lifetime
      정합 (ADR-066). carrier Issue repository = aggregate carrier = mclayer/plugin-codeforge
      (wrapper governance owner SSOT, ADR-013 정합), per-repo carrier = 해당 repo
      (각 repo local governance). self-meta loop 회피: hotfix-bypass:cross-repo-bypass-counter
      부착 PR 은 cross-repo 누적 count 제외. grep-testable artifact 존재 예정
      (scripts/check-cross-repo-bypass-counter.{py,sh}, Phase 2 carrier). CFP-627
      marketplace-drift-detection precedent (24h cron + workflow_dispatch + Issue
      auto-create + per-signature dedup) cross-repo 확장.
    detect_command: bash scripts/check-cross-repo-bypass-counter.sh
    workflow: templates/github-workflows/cross-repo-bypass-counter.yml
    current_tier: warning
    bypass_label: hotfix-bypass:cross-repo-bypass-counter
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
        - cross_repo_aggregate_threshold_calibration_evidence  # aggregate trigger threshold 재calibration evidence (Phase 2 actual wire 후 별 calibration carrier)
        - pat_rotation_audit_log_evidence                       # ADR-066 PAT rotation log 정합 (90 day rotation / 180 day max lifetime)
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-845
    introduced_date: 2026-05-17
    owner_adr: ADR-024            # Amendment 8 §결정 6.A.5 (cross-repo extension SSOT)
    carrier_adr: ADR-060          # evidence-enforceable framework carrier
    recurrence:
      count: 0                    # Phase 1 entry append, Phase 2 actual wire 후 evidence 누적 시점 별 update
      threshold: 3                # ADR-024 Amendment 8 §결정 6.A.5 — per-(repo, plugin, label) signature 단위 §결정 6.A.2 와 동일 (3)
      last_occurrence: null       # Phase 2 actual wire 후 first reach 시점 update
      promotion_trigger: none     # Phase 2 actual wire 완료, 첫 evidence 누적 후 advisory 격상 예정
    status: warning              # Phase 2 wire 완료 (CFP-845 Phase 2, 2026-05-17) — lint script + workflow + bats 모두 Active

  # ─── CFP-820 Phase 2 — version-3way-atomic (ADR-063 Amendment 5 §결정 15 carrier) ───
  # Epic CFP-699 Wave 3 Story-6 B2 — blocking-on-pr tier 직접 시작 (ADR-060 §결정 11 / ADR-063 §결정 11 패턴 답습)
  # Epic §8 scope_manifest planned_label_registry_bumps 3rd entry 정합
  - name: version-3way-atomic
    description: |
      ADR-063 Amendment 5 §결정 15 — 3-way version atomic invariant check.
      publisher (wrapper plugin.json .version) ↔ registry (marketplace.json .plugins[codeforge].version)
      ↔ consumer (.claude/_overlay/project.yaml .codeforge.version_pin.version)
      byte-identical version field compare. consumer 고정 미등록 = warning-first exit 0
      (orthogonality invariant — 고정 미지정 ≠ version mismatch, ADR-027 Amendment 4 fallback) [verified: git show origin/main:scripts/check-3way-version-parity.sh].
      고정 등록 + 3-way mismatch = blocking-on-pr exit 1.
      sanity guard 6-tuple (Story §5.2 AC-13) + ADR-070 verify-before-trust 통합.
    detect_command: bash scripts/check-3way-version-parity.sh
    workflow: templates/github-workflows/version-3way-atomic.yml
    current_tier: blocking-on-pr  # ADR-063 Amendment 5 §결정 16 + ADR-060 §결정 11 blocking-on-pr 직접 시작 (Epic §8 scope_manifest 정합)
    bypass_label: hotfix-bypass:version-3way-atomic
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 0   # blocking-on-pr 직접 시작 — promotion 불필요 (ADR-063 §결정 11 패턴)
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-820
    introduced_date: 2026-05-17
    owner_adr: ADR-063         # 정책 ADR — marketplace atomic invariant Amendment 5 §결정 15
    carrier_adr: ADR-060       # framework carrier (CFP-388 / ADR-060 §결정 12 family)
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: null
      promotion_trigger: advisory
    status: Active

  # ─── CFP-841 Phase 1 — corpus-claim-verify (ADR-082 Amendment 1 §결정 2(a) carrier) ───
  # §결정 6 behavioral→mechanical 전환 scope 2(a). deferred-followup status — Phase 1 declare /
  # Phase 2 actual lint script + workflow wire 후 Active 전환 (ADR-060 §결정 5 모든 신규
  # entry warning 시작 강제 정합). ADR-068 I-5 directly-analogous pattern 재사용 (cross-ref).
  - name: corpus-claim-verify
    description: |
      ADR-082 §결정 2(a) — Story/Change-Plan/ADR 본문 corpus/fixture enumeration write-time verify.
      corpus enumeration token ('예시 N건' / '전무' / '부재' / '다수') + file-path 인용 co-occurrence
      (동일 줄 또는 ±2 줄 window) → [verified: git show <ref>:<path>] annotation 부재 시 flag.
      4-guard FP 완화 (ADR-068 I-5 + ADR-082 §결정 4/6 EC-3 prior art 직접 재사용, 신규 메커니즘 0건):
      (1) file-path co-occurrence guard (2) citation≠assertion 면제 (ADR-082 §결정 4)
      (3) forward-only effective-date (본 carrier merge 후 신규 line) (4) self-referential exemption
      (ADR-082 §결정 6 EC-3 — ADR-082 본문 / CFP-776·CFP-841 Story·Change Plan corpus 인용 self-flag 차단).
    detect_command: bash scripts/check-corpus-claim-verify.sh
    workflow: templates/github-workflows/corpus-claim-verify.yml
    current_tier: warning   # ADR-060 §결정 5 — 모든 신규 entry warning 시작 강제
    bypass_label: hotfix-bypass:corpus-claim-verify
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a) — warning → blocking-on-pr 승격 gate
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:            # ADR-060 §결정 6 (c)
        - CFP-841  # Phase 2 carrier — lint script + workflow + bats actual wire (deferred-followup → warning)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-841
    introduced_date: 2026-05-17
    owner_adr: ADR-082         # 정책 ADR — write-time self-write verification mandate Amendment 1 §결정 2(a)
    carrier_adr: ADR-060       # framework carrier (CFP-388 / ADR-060 §결정 12 family)
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: null
      promotion_trigger: advisory
    status: warning  # CFP-841 Phase 2 actual wire 완료 — lint script + workflow land (2026-05-17)

  # ─── CFP-841 Phase 1 — cross-plugin-ownership-verify (ADR-082 Amendment 1 §결정 2(d) carrier) ───
  # §결정 6 behavioral→mechanical 전환 scope 2(d). lane-self-write-ownership-matrix.yaml
  # cross_plugin_doc_ownership sub-tree query + §13.B 4-way drift-sync invariant.
  # deferred-followup status — Phase 2 actual wire 후 Active 전환.
  - name: cross-plugin-ownership-verify
    description: |
      ADR-082 §결정 2(d) — ChangeImpactAgent Phase 0 mapping templates/* wrapper-local 단정 전
      lane-self-write-ownership-matrix.yaml cross_plugin_doc_ownership sub-tree query 1-step verify.
      Phase 0 mapping artifact 의 templates/* / docs/adr/* / docs/change-plans/* wrapper-local
      단정 line 이 [ownership-verified: <yaml-query-path>] annotation 부재 시 flag.
      §13.B 4-way drift-sync invariant (Researcher U#2 — SKILL.md L36 follow-up 해소):
      yaml cross_plugin_doc_ownership ↔ SKILL.md owner path 표 ↔ story-page-structure.md ↔ lint regex
      4-way sync, yaml-as-canonical single-direction (yaml=SSOT, 나머지 3=derived mirror, mismatch=flag —
      lint 가 yaml read-only, 4번째 target 추가가 drift surface 확대하지 않음).
      CFP-722 §13.A machine_readable_ssot 실재 기반 cross-plugin 영역 확장 (신규 registry 창설 아님).
    detect_command: bash scripts/check-cross-plugin-ownership-verify.sh
    workflow: templates/github-workflows/cross-plugin-ownership-verify.yml
    current_tier: warning   # ADR-060 §결정 5 — 모든 신규 entry warning 시작 강제
    bypass_label: hotfix-bypass:cross-plugin-ownership-verify
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20            # ADR-060 §결정 6 (a)
      failure_threshold: 0             # ADR-060 §결정 6 (b)
      sibling_dependencies:            # ADR-060 §결정 6 (c)
        - CFP-841  # Phase 2 carrier — yaml cross_plugin sub-tree + lint script + §13.B 4-way sync actual wire
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-841
    introduced_date: 2026-05-17
    owner_adr: ADR-082         # 정책 ADR — write-time self-write verification mandate Amendment 1 §결정 2(d)
    carrier_adr: ADR-060       # framework carrier (CFP-388 / ADR-060 §결정 12 family)
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: null
      promotion_trigger: advisory
    status: warning  # CFP-841 Phase 2 actual wire 완료 — lint script + workflow land (2026-05-17)

  # ─── CFP-821 Phase 2 — branch-protection-sync (ADR-024 Amendment 2 §결정 A carrier) ───
  # CFP-841 Phase 1 merge 후 충돌 해소 rebase: branch-protection-sync entry 를 cross-plugin-ownership-verify 뒤에 append.
  # v2.25 claim 충돌 해소 → v2.26 으로 bump (CFP-841 이 v2.25 선점). 36번째 hotfix-bypass:* family member.
  - name: branch-protection-sync
    description: >
      CFP-821 D2 — branch protection manifest drift 감지 warning.
      templates/branch-protection-manifest.yaml (ADR-024 Amendment 2 §결정 A SSOT) 와
      실 GitHub API state (GET branches/main/protection) 비교.
      FORM (b) dry-run/manifest helper (setup-branch-protection.sh) 출력을 기반으로 drift 경고.
      실 등록 = consumer admin operator manual (Administration:write 보유 주체).
      drift = informational (CI fail 아님 — FORM (b) 핵심). repair = operator manual step.
    detect_command: bash templates/scripts/setup-branch-protection.sh --dry-run
    workflow: templates/github-workflows/branch-protection-drift-check.yml  # 기존 detect-only workflow 재사용
    current_tier: warning
    bypass_label: hotfix-bypass:branch-protection-sync
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-821
    introduced_date: 2026-05-17
    owner_adr: ADR-024         # branch protection context SSOT — Amendment 2 §결정 A/B/C
    carrier_adr: ADR-060       # evidence-enforceable framework carrier
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: null
      promotion_trigger: none
    status: Active
  - name: adr-077-integration
    description: |
      ADR-077 stale 게이트·envelope·4-layer disjoint runtime mechanical lint —
      Story file (§9.0 재조사 카운터 disjoint / §10 FIX Ledger 비합산 / ESCALATE
      escalation_class:scope_redefinition_required §10 무기록) + Epic A cross-Story
      layer disjoint 무손상 grep-testable lint. 기존 adr-077-{ratchet,design-reading-
      mandate}-declared (ADR-doc self-check) 와 관심사 disjoint (runtime artifact 검증).
    detect_command: bash scripts/check-adr-077-integration.sh
    workflow: templates/github-workflows/adr-077-integration.yml
    current_tier: warning
    bypass_label: hotfix-bypass:adr-077-integration
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-848
    introduced_date: 2026-05-17
    owner_adr: ADR-077         # stale 게이트·envelope·4-layer disjoint SSOT carrier
    carrier_adr: ADR-060       # evidence-enforceable warning-tier framework
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: null
      promotion_trigger: advisory
    status: Active             # script+workflow self-app land 동반 (CFP-848 Phase 2 carrier)

  # ─── CFP-842 Phase 1 (fix-event-v1 v1.3 depth-aware scope presence lint, 2026-05-17) ───
  # ADR-067 Amendment 1 §결정 4 mechanical 정확도 carrier — broken-link/path 정정 FIX 시
  # §10 FIX Ledger row 의 affected_paths_with_depth 필드 presence advisory.
  # Phase 1 = registry SSOT 신설만, Phase 2 = script + workflow self-app + bats TC.

  - name: fix-event-depth-scope-presence
    description: |
      ADR-067 Amendment 1 / CFP-842 — fix-event-v1 v1.3 depth-aware scope 의 mechanical
      enforcement advisory layer. broken-link / path 정정 FIX (§10 row 의 트리거 또는 재실행 범위
      에 'broken-link' / 'path 정정' / 'link 정정' / 'relative path' / 'doc-location-registry' /
      'link target' / 'href' / 'cross-module path' / 'over-correction' / 'dangling' / '404'
      어휘 patterns grep-detect — **informal example list, canonical SSOT 는 script regex**
      `scripts/check-fix-event-depth-scope-presence.py` line 73-76, CFP-914 F-3 ADR-068 I-4
      annotation) 인데 affected_paths_with_depth 필드 누락 또는 null 시 advisory comment 부착.
      CFP-770 §8 CR-005→CR-006→CR-007 over-correction regression chain lesson directly
      차단 forcing function — depth 정보 누락이 directly carrier 였음. exit 0 advisory only
      (false-positive risk 명시 — 어휘 grep heuristic, semantic precision 불가). Phase 2
      carrier = scripts/check-fix-event-depth-scope-presence.sh + workflow self-app + bats TC.
    detect_command: bash scripts/check-fix-event-depth-scope-presence.sh   # Phase 2 carrier
    workflow: templates/github-workflows/fix-event-depth-scope-presence.yml  # Phase 2 carrier
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:fix-event-depth-scope
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh   # CFP-389 prior art reuse
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) / §결정 10 velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — fix-event-v1 v1.3 schema self-app + ADR-067 Amendment 1 정합
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}  # heuristic grep, modal 어휘 검사 미포함
    introduced_by: CFP-842
    introduced_date: 2026-05-17
    owner_adr: ADR-067 Amendment 1   # cross-lane RESET scope-aware mechanical 정확도 SSOT
    carrier_adr: ADR-060             # evidence-enforceable warning-tier framework
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: null
      promotion_trigger: advisory    # ADR-060 §결정 19 (Amendment 6) — advisory-only first iteration
    status: warning                  # Phase 2 (CFP-842) land — script + workflow + bats 5 TC self-app 동반. warning-tier 첫 iteration (ADR-060 §결정 5). Active 전환 gate: pr_cumulative_min=20 + failure_threshold=0

  # ─── CFP-894 Phase 1 (inter-plugin-contract MANIFEST↔frontmatter parity lint, 2026-05-17) ───
  # ADR-060 §결정 6 evidence-enforceable warning-tier entry — INV-1 wrapper-local parity.
  # Story-4 (CFP-834) §11.4 INV-1 carrier — CFP-42 backfill 류 silent drift 재발 차단.

  - name: inter-plugin-contracts-parity
    description: |
      CFP-894 — inter-plugin-contract MANIFEST.yaml ↔ wrapper sibling .md frontmatter
      contract_version byte-identical parity 자동 검증. Story-4 (CFP-834) 가 표면화한
      silent drift class (wrapper sibling frontmatter "1.1" ↔ body payload "1.0"
      backfill 잔재) 의 wrapper-local mechanical enforcement.
      Phase 1 scope = MANIFEST row contract_version ↔ frontmatter contract_version
      2-touchpoint parity. Phase 2 scope = body `## N.` payload + cross-repo canonical
      frontmatter (CODEFORGE_CROSS_REPO_PAT 의존, ADR-066) 별 carrier CFP.
      Active 만 검증 (Archived = historical record, parity 면제). Missing file =
      separation of concerns (check_inter_plugin_contracts.py CFP-42 영역) — 본 lint 는
      silent skip.
    detect_command: bash scripts/check-inter-plugin-contracts-parity.sh
    workflow: templates/github-workflows/inter-plugin-contracts-parity.yml
    current_tier: warning            # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:inter-plugin-contracts-parity
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20          # ADR-060 §결정 6 (a) velocity-normalized
      failure_threshold: 0           # ADR-060 §결정 6 (b)
      sibling_dependencies: []       # 독립 entry — MANIFEST self-app + wrapper-local scope only
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}  # exact-equality byte-identical compare, modal 어휘 무관
    introduced_by: CFP-894
    introduced_date: 2026-05-17
    owner_adr: ADR-010               # Inter-plugin Contract Sibling Sync — parity invariant SSOT
    carrier_adr: ADR-060             # evidence-enforceable warning-tier framework
    recurrence:
      count: 1                       # CFP-834 silent drift (wrapper sibling 1.1↔body 1.0)
      threshold: 3
      last_occurrence: "2026-05-15T00:00:00Z"  # CFP-834 FIX-Iter1 5-touchpoint reconcile (ISO8601 UTC Z per registry schema)
      promotion_trigger: advisory
    status: Active                   # script+workflow self-app land 동반 (CFP-894 Phase 1 carrier)

  # CFP-923 / ADR-078 P-S4 mechanism (Epic B Story-4 single-Phase land, 2026-05-18) ---
  # architecture-drift mechanical lint — docs/architecture/**/*.md scope
  # 4 H2 closed-enum + 3 detection class active (a/b/d) / 2 deferred (c/e)
  - name: architecture-drift
    description: |
      ADR-078 §결정 1 + §결정 4 mechanical verification — architecture_doc 본문의 4 H2 closed-enum
      (모듈/경계/인터페이스 계약/데이터 흐름) + 3 detection class 정합 lint.
      (a) module enumeration parity / (b) inter-plugin-contracts enumeration parity /
      (d) anti-scope guard violation (line-level patterns + H2 closed-enum 외 H2)
      Deferred: (c) agent count / (e) frontmatter freshness — 후속 carrier
      4-guard FP 완화 (CFP-841 corpus-claim-verify prior art 재사용)
    detect_command: bash scripts/check-architecture-drift.sh
    workflow: templates/github-workflows/architecture-drift.yml
    current_tier: warning
    bypass_label: hotfix-bypass:architecture-drift
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-923
    introduced_date: 2026-05-18
    owner_adr: ADR-078
    carrier_adr: ADR-060
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: none
    status: Active

  - name: dependency-closure-self-test
    description: >
      CFP-898 §4.11 dependency bundle integrity closure resolver self-test.
      mirror-dependency-closure.py --all --dry-run 으로 wrapper-side workflow yml 74개
      전체를 순회 후 누락 deps preview (fail-open, dry-run). warning-tier 도입.
      AM-1 (regex_primary) / AM-2 (transitive_depth_limit=1) /
      AM-3 (shell_script_only_v1: scripts/check-*.sh + templates/scripts/*.py) /
      AM-4 (self_app_exemption) 4 constraint 검증.
    detect_command: python3 templates/scripts/mirror-dependency-closure.py --all --dry-run
    workflow: ~                      # optional — Phase 2 scope 내 자율 결정: 별도 workflow yml 없음 (후속 carrier 가능)
    current_tier: warning
    bypass_label: hotfix-bypass:dependency-closure-self-test
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
        - unit_test_pass_15_15_proof
        - integration_test_bats_pass_proof
        - perf_baseline_max_2_39ms_per_file_proof
    mechanical_enforcement_actions:
      - dependency-closure-self-test   # ADR-040 Amendment 3 §결정 7.D cross-ref
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-898
    introduced_date: 2026-05-18
    owner_adr: ADR-076               # declarative reconciliation upgrade SSOT
    carrier_adr: ADR-060             # evidence-enforceable warning-tier framework
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: ~
      promotion_trigger: advisory
    status: Active

  # CFP-966 / ADR-073 Amendment 2 declarative anchor (Wave 4 sub-Epic family — same-day 2/2 parallel race incidents sentinel)
  # 2026-05-18 KST sentinel: CFP-953 (first incident, label-based search miss) + CFP-946 (second incident, 11분 gap Epic close miss)
  # deferred-followup status — actual script (scripts/check-parallel-work-sentinel.{sh,py}) + workflow (.github/workflows/parallel-work-sentinel-check.yml) + hook json sample (templates/.claude/hooks/SessionStart-parallel-work-poll.json.sample) = sibling Story-2 CFP-967 Phase 2 carrier 후 Active 전환
  # ADR-082 §결정 6 + ADR-060 Amendment 10 §결정 24 deferred-followup precedent (L1417/L1452/L1574 4-instance established pattern) 답습
  - name: parallel-work-sentinel-pickup
    description: |
      ADR-073 Amendment 2 §결정 1-A/1-B/1-C mechanical verification — Orchestrator
      가 SessionStart 시점 cold start + lane spawn / PR open / merge transition 매
      transition 직전 의무 polling 발화 (title-based search + Epic state poll + HEAD
      compare sibling commits). memory rule 6 (title-based search 의무, CFP-953
      incident carrier) + memory rule 7 (Epic 진행 중 polling 의무, CFP-946 incident
      carrier) mechanical binding declarative anchor. CFP-953 + CFP-946 same-day
      2-occurrence sentinel evidence (2026-05-18 KST). deferred-followup status
      — actual script + workflow + hook json sample = sibling Story-2 CFP-967
      mechanical wire Phase 2 carrier 후 Active 전환.
    detect_command: bash scripts/check-parallel-work-sentinel.sh  # CFP-967 Phase 2 wire — scripts/check-parallel-work-sentinel.sh (thin wrapper, ADR-061) + scripts/lib/check_parallel_work_sentinel.py (Python SSOT) 실 file 존재
    workflow: templates/github-workflows/parallel-work-sentinel-check.yml  # CFP-967 Phase 2 wire — byte-identical self-app .github/workflows/parallel-work-sentinel-check.yml (ADR-005 parity)
    current_tier: warning           # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:parallel-work-sentinel-pickup
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20         # ADR-060 §결정 6 (a) velocity-normalized
      failure_threshold: 0          # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-967                   # sibling Story-2 mechanical wire — script + workflow + hook sample + bats
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
        - cfp_953_first_incident_memory_entry          # project_cfp_953_duplicate_incident
        - cfp_946_second_incident_memory_entry         # project_cfp_946_parallel_race
    modal_anti_pattern_dictionary: {}  # transition state verify, modal 어휘 무관
    introduced_by: CFP-966
    introduced_date: 2026-05-18
    owner_adr: ADR-073-Amendment-2  # Orchestrator verify-before-assert Amendment 2 (transition trigger enum + sustained polling) — SSOT 통일 (Story §5 AC-2 + domain-knowledge frontmatter 정합, Codex #2 F#1 ac-semantic-divergence FIX iter 2)
    carrier_adr: ADR-060            # evidence-enforceable warning-tier framework
    recurrence:
      count: 2                      # CFP-953 first incident (label-based search miss) + CFP-946 second incident (11분 gap Epic close miss), both 2026-05-18 KST
      threshold: 3                  # 3-occurrence 도달 시 warning → blocking-on-pr 자동 승격 trigger (ADR-060 §결정 19 Amendment 6 / schema v1.2 auto-firing)
      last_occurrence: "2026-05-18T06:53:30Z"  # CFP-946 PR #962 merge timestamp (second incident, 11분 gap from CFP-953 chain) per ISO8601 UTC Z suffix (registry schema v1.2 정합 — KST `15:53:30+09:00` = UTC Z notation)
      promotion_trigger: auto_blocking  # recurrence.count ≥ threshold 시 별 carrier 강제 (ADR-060 §결정 19 enum)
    status: warning  # CFP-967 Phase 2 wire — script + workflow 신설 동반 승격 (deferred-followup → warning precedent: production-cutover-deputy-spawn-evidence / epic-cutover-gate-evidence-quad-check / CFP-845 답습). Active = 별 follow-up CFP (PR cumulative ≥ 20 + failure = 0 + sibling merged, ADR-060 §결정 6 승격 gate)

  # ─── CFP-963 Phase 1 (codex worker network_scope mechanical layer — declaration source / enforcement source dual-binding, 2026-05-19 KST) ───
  # 12번째 warning-tier entry — ADR-060 Amendment 14 §결정 28 + ADR-081 Amendment 4 §결정 D1.D body 확장 carrier
  # declaration source ADR-081 / enforcement source ADR-060 dual-binding 첫 사례 (parallel-work-sentinel-pickup = ADR-073 single-binding precedent verbatim 차용 + dual-binding 확장)
  # deferred-followup status — actual script (scripts/check-codex-network-scope.{sh,py}) + workflow (templates/github-workflows/codex-network-scope-presence.yml + .github/workflows/ self-app byte-identical) + bats fixture pair (tests/fixtures/codex_spawn_prompt_{with,without}_network_scope.txt, CX-963-3 P2 boundary mandate per Codex TP#4) = CFP-963 Phase 2 carrier 후 Active 전환
  # ADR-082 §결정 6 + ADR-060 Amendment 10 §결정 24 deferred-followup precedent (L1417/L1452/L1574 4-instance established pattern + parallel-work-sentinel-pickup L2729 5-instance established pattern) 답습 — 신규 unique drift value 회피 (CFP-966 §10 deferred marker lesson)
  - name: codex-network-scope-presence
    description: |
      ADR-081 Amendment 4 §결정 D1.D 본문 확장 (`sandbox_network_required: <bool>` →
      `network_scope: <4-tier enum>` strict ratchet-up: `offline` / `repo-fetch-only` /
      `web-fetch` / `offline_substitution_declared`) 의 mechanical enforcement layer
      mechanical verification. Codex worker spawn-prompt 본문 안 `network_scope` field
      presence-grep heuristic (4-tier enum value membership check OR boolean legacy
      `sandbox_network_required: <bool>` advisory grace) + Story §10
      `[codex-substitution-scope-declared: <scope-enum>]` / `[codex-sandbox-fallback:
      <fail-mode>]` marker enum 정합 (6-enum fail-mode = `api_missing` / `version_skew`
      / `enterprise_blocked` / `gh_api_network_blocked` / `manual_substitution_declared`
      / `inline_orchestrator_verify_only` playbook L1349 정합) + §14 Lane Evidence row
      `network_scope_actual` 13번째 optional field 4-tier enum membership check
      (evidence-check-registry-v1 v1.3 신규 schema field).

      ADR-068 I-3 unconditional guard placement 정합: boolean grace 영역 (legacy
      `sandbox_network_required: <bool>`) = unconditional advisory — lint flag gating
      없이 무조건 `[legacy-boolean-detected]` PR comment + exit 0.

      semantic adequacy 검증 불가 — false-positive risk 명시 (어휘 grep heuristic,
      reviewer responsibility — D7.a digest-parse 동형). 4-tier enum value 의미
      분류 (예: `offline` declare 와 실제 spawn-prompt 본문 안 actual `gh api`
      / cross-repo `git fetch` 호출 사이 의미 정합) = lint 영역 외.

      self-meta loop 회피: 본 lint workflow / script 자체의 PR (Phase 1+2) 에
      `hotfix-bypass:codex-sandbox-substitution` 부착 시 본 lint step skip +
      carrier_story self-exempt (CFP-963 carrier Phase 1+2 PR bootstrap-exempt,
      ADR-062 §결정 8 precedent 정합 / CFP-722 §결정 27 carrier-Story exemption 동형).

      dual-binding pattern 첫 사례 — ADR-081 Amendment 4 (declaration source) +
      ADR-060 Amendment 14 (enforcement source) 양 ADR frontmatter
      `mechanical_enforcement_actions[]` 안 동일 `action: codex-network-scope-presence`
      entry 보유 (parallel-work-sentinel-pickup = ADR-073 single-binding precedent
      답습 + dual-binding 확장 axis).

      CFP-946 8-occurrence sentinel closing-the-loop carrier (Story-B carry-forward —
      option 1 codex CLI flag wire + option 2 graceful degradation + option 3
      substitution-side mechanism = 양 면 chain 완결). CFP-722 story-section-ownership /
      CFP-841 corpus-claim-verify / CFP-966 parallel-work-sentinel-pickup precedent
      동형 (declaration-only normative + presence-grep warning lint 보완 관계).

      **Scope: proactive 6 touchpoint spawn prompt 한정** (CFP-1003 / ADR-052
      Amendment 9 + ADR-070 Amendment 5 + ADR-081 Amendment 5 — proactive/reactive
      disjoint codify). reactive `codex:rescue` 채널 (사용자 ad-hoc invocation,
      ADR-022 Deprecated default + ADR-070 D1 L110 `사용자 책임 영역 (적용 외)`)
      = lint scope 외 — best-effort 가이드 anchor (ADR-081 Amendment 5 A2 SSOT,
      사용자 자율 선택, codeforge 강제 0 invariant 보존). reactive 영역 mechanical
      lint scope 확장 = 별 CFP carrier (Wave 2, ADR-064 §결정 1 unitary 정합).
    detect_command: bash scripts/check-codex-network-scope.sh  # CFP-963 Phase 2 ACTIVE — thin bash wrapper (ADR-061 §결정 1) → scripts/lib/check_codex_network_scope.py Python SSOT. Exit 0=PASS / 1=WARNING / 2=META-ERROR (ADR-060 §결정 15 3-tier)
    workflow: .github/workflows/codex-network-scope-presence.yml  # CFP-963 Phase 2 ACTIVE — byte-identical self-app pair (templates/github-workflows/ + .github/workflows/). continue-on-error: true (warning tier). SecurityArch TH-2: permissions: contents: read only
    current_tier: warning           # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:codex-sandbox-substitution
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20         # ADR-060 §결정 6 (a) velocity-normalized — STANDARD threshold (ADR-068 I-5 dimensional empirical grounding 정합)
      failure_threshold: 0          # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-963                   # self-carrier Phase 2 PR (script + lib + workflow + bats + fixture pair + bootstrap parity) merge 의무
    status: deferred-followup  # declarative anchor only — CFP-967 mechanical wire Phase 2 carrier (detect script + workflow + hook json sample) 후 Active 전환 (L1417/L1452/L1574 established 4-instance precedent 답습 — 신규 unique drift value 회피, Codex #2 F#2 schema-enum-systemic conform FIX iter 2). schema enum 확장 = 별 follow-up CFP (ADR-064 §결정 1 scope unitary, §10 deferred marker)

  # CFP-991 / ADR-72 Amendment 3 + ADR-076 §결정 9.6 + reconcile-protocol-v1 v1.11 §4.14 canary_compatibility_check_binding carrier — Wave 4 sub-Epic #1 Story-4 canary promotion criteria enforcement layer
  - name: canary-compatibility-check
    description: |
      ADR-72 Amendment 3 + ADR-076 §결정 9.6 + reconcile-protocol-v1 v1.11 §4.14
      canary_compatibility_check_binding mechanical verification — consumer
      canary→beta promotion gate evaluation 시점 promotion criteria 4-tuple
      (functional + security + monitoring + testing measurement source) + family_7
      atomic canary 고정 (publisher↔registry↔consumer 3-way match length_invariant=7)
      + canary_consumer_evidence_origin enum closed-set (wrapper_self / consumer_self
      / mixed open_extension:false) + inter_plugin_contract_backward_compat_verify
      (ADR-008 MINOR-only rule invariant guard) + promotion_gate_failure_mode (enum
      warning_first / blocking_on_pr default warning_first) + downgrade_asymmetry_marker
      (placeholder_reserve Story-5 carrier) 7 field schema verify. 5 threat × mitigation
      matrix carrier (SecurityArch T-1.1/T-2.1/T-3.1/T-4.1/T-5.1). ADR-070 §결정 D6
      (CFP-988 Amendment 4) mandatory-real-execution-evidence STANDING cross-ref
      T-4.1 4-tuple measurement spoofing mitigation. wrapper-self-app Tier-1
      exemption (production_cutover_touching=true AND repo=wrapper AND code_change=0
      triple-AND fast-PASS, ADR-72 §결정 6 invariant 정합). 4 industry exemplar
      verbatim cite (Chrome 3-channel Stable/Beta/Canary primary + npm dist-tag +
      Rust 3-channel + K8s 3-stage 보조 reference, ADR-076 §결정 9.6 SSOT).
    detect_command: bash scripts/check-canary-compatibility.sh
    workflow: templates/github-workflows/canary-promotion-criteria.yml
    current_tier: warning           # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:canary-promotion-criteria
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20         # ADR-060 §결정 6 (a) velocity-normalized
      failure_threshold: 0          # ADR-060 §결정 6 (b)
      sibling_dependencies: []      # Phase 2 carrier = 별 follow-up CFP (blocking-on-pr 승격)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
        - codex_tp4_p2_boundary_fixture_pair_evidence    # CX-963-3 P2 boundary mandate per Codex TP#4
    modal_anti_pattern_dictionary: {}  # presence-grep heuristic, modal 어휘 무관
    introduced_by: CFP-963
    introduced_date: 2026-05-19
    owner_adr: ADR-081-Amendment-4  # declaration source ADR — D1.D body 확장 (boolean → 4-tier `network_scope` enum strict ratchet-up SSOT)
    carrier_adr: ADR-060            # enforcement source ADR — Amendment 14 §결정 28 evidence-enforceable warning-tier framework carrier
    recurrence:
      count: 0                      # CFP-963 ratchet 확장 carrier — sentinel-driven 도입 아닌 framework entry count 11 → 12 ratchet-UP (CFP-722 §결정 27 / channel-drift-detection / story-section-9-typed default 정합)
      threshold: 3                  # ADR-060 §결정 19 Amendment 6 / schema v1.2 standard default
      promotion_trigger: none       # count=0 < threshold=3 → 발화 없음 (Option A — parallel-work-sentinel-pickup count=2 sentinel 영역과 분리)
    status: warning  # CFP-963 Phase 2 ACTIVE — mechanical wire complete: detect_command + workflow + bats fixture pair (CX-963-3 P2 boundary mandate per Codex TP#4) ALL populated (2026-05-19 KST). deferred-followup → warning transition (ADR-060 §결정 6 — Phase 2 carrier merge 의무 충족). blocking-on-pr 승격 = pr_cumulative_min 20 + failure_threshold 0 + sibling_dependency merge (별 follow-up CFP)
    mechanical_enforcement_actions:
      - canary-compatibility-check   # ADR-040 Amendment 3 §결정 7.D cross-ref
    modal_anti_pattern_dictionary: {}
    introduced_by: CFP-991
    introduced_date: 2026-05-19
    owner_adr: ADR-72-Amendment-3   # ProductionEvidenceDeputy mandate Amendment 3 (CFP-991 carrier — promotion criteria 4-tuple wiring)
    carrier_adr: ADR-060            # evidence-enforceable warning-tier framework
    recurrence:
      count: 0
      threshold: 3
      last_occurrence: ~
      promotion_trigger: advisory
    status: Active

  # ─── CFP-689 Phase 1 (worktree-first self-ownership verify 3-tuple — declarative anchor only, 2026-05-20 KST) ───
  # ADR-073 Amendment 3 §결정 1-A (transition trigger enum 4번째 entry `worktree_lane_spawn` 추가) + §결정 1-D (path-based 3-tuple verify primitive) + §결정 1-E (subagent verdict re-verify mandate) carrier
  # declaration source ADR-073-Amendment-3 / enforcement source ADR-060 dual-binding (codex-network-scope-presence precedent 답습)
  # deferred-followup status — actual script (scripts/check-worktree-self-ownership.{sh,py}) + workflow (templates/github-workflows/worktree-self-ownership-verify.yml + .github/workflows/ self-app byte-identical) + hook json sample (templates/.claude/hooks/PreToolUse-worktree-self-ownership.json.sample) + bats (tests/scripts/check-worktree-self-ownership/test_worktree_self_ownership.bats) + label-registry-v2 신규 entry (hotfix-bypass:worktree-self-ownership-verify) = Wave 2 별 sub-CFP carrier 후 Active 전환
  # parallel-work-sentinel-pickup (CFP-966 → CFP-967 chain) declarative-only-Wave-1 precedent 답습 — Wave 1 declaration / Wave 2 mechanical wire 분리 (ADR-064 §결정 1 CFP scope unitary 정합)
  - name: worktree-self-ownership-verify
    description: |
      ADR-073 Amendment 3 §결정 1-A (transition trigger enum 4번째 entry
      `worktree_lane_spawn` 추가, closed-set ratchet — `lane_spawn` / `pr_open` /
      `merge_transition` / **`worktree_lane_spawn`**) + §결정 1-D (path-based
      self-ownership verify 3-tuple primitive) + §결정 1-E (subagent verdict
      `parallel_session_conflict` 발화 시 Orchestrator re-verify mandate, ADR-082
      verify-before-trust 자기 산출물 영역 확장 cross-ref) mechanical verification —
      Orchestrator (또는 subagent) 가 worktree-first 환경 안 lane spawn 직전 또는
      subagent verdict `parallel_session_conflict` 발화 직후 path-based 3-tuple
      verify primitive 실행: (a) cwd ↔ worktree path 일치 (`git rev-parse
      --show-toplevel` vs `git worktree list --porcelain`, path normalize:
      forward-slash + lowercase drive-letter `c:/users/...`) (b) HEAD lineage ↔
      session reflog membership (`git reflog show <branch> --all` + `git log
      <commit> --oneline` cross-check, long-Phase-gap reflog 90d GC 시 (a)+(c)
      2-source AND fallback) (c) `git worktree list --porcelain | grep <branch>` +
      reflog 2-source AND ownership verify. Edge case: detached HEAD (branch
      reflog 없음 → 자동 fallback (a)+(c)) + anonymous worktree (`branch` field
      부재 → forcing function "anonymous worktree 안 lane spawn 금지", ADR-040
      §결정 1 named worktree mandate 정합) + Windows path normalization
      (lowercase drive + forward-slash canonical form, MSYS2/Git Bash/WSL/cmd.exe
      전 환경 동형). 3-tuple PASS = self-ownership TRUE (continue, subagent
      verdict `parallel_session_conflict` REJECT + Story §10 FIX Ledger
      `subagent_misflag_self_confusion` reason code row append). 3-tuple FAIL =
      self-ownership FALSE (memory rule 6/7 detection layer 절차 인계). Sentinel:
      2026-05-19~20 KST single session 3 occurrences (CFP-1026 STAND-DOWN
      false-positive + CFP-681 cfp-1014 dup worktree RequirementsPL `f39b221`
      self-misflag + CFP-681 ArchitectPL Phase 3 자기 ArchitectAgent commit
      `00b7d8a` parallel_session_conflict mis-flag — subagent contagion 입증).
      deferred-followup status — actual script + workflow + hook + bats + label
      = Wave 2 별 sub-CFP carrier 후 Active 전환 (parallel-work-sentinel-pickup
      CFP-966 → CFP-967 chain precedent 답습).
    detect_command: bash scripts/check-worktree-self-ownership.sh  # Wave 2 별 sub-CFP carrier — scripts/check-worktree-self-ownership.sh (thin wrapper, ADR-061) + scripts/lib/check_worktree_self_ownership.py (Python SSOT — 3-tuple verify primitive 구현) 실 file 신설
    workflow: templates/github-workflows/worktree-self-ownership-verify.yml  # Wave 2 별 sub-CFP carrier — byte-identical self-app .github/workflows/worktree-self-ownership-verify.yml (ADR-005 parity)
    current_tier: warning           # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:worktree-self-ownership-verify  # Wave 2 별 sub-CFP carrier — label-registry-v2 신규 entry append (현재 family member count + 1)
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20         # ADR-060 §결정 6 (a) velocity-normalized
      failure_threshold: 0          # ADR-060 §결정 6 (b)
      sibling_dependencies:
        - CFP-689                   # 본 Story (declarative-anchor-only Wave 1 carrier)
        - TBD-Wave-2-sub-CFP        # Wave 2 mechanical wire — script + workflow + hook + bats + label-registry-v2 신규 entry (별 sub-CFP reservation, sequential next, Wave 1 merge 후)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
        - cfp_1026_first_occurrence_memory_entry          # project_cfp_1026_design_lane_restructure / brainstorm STAND-DOWN false-positive
        - cfp_681_cfp_1014_dup_worktree_occurrence        # RequirementsPL self-misflag `f39b221` dedicated worktree commit
        - cfp_681_architectpl_phase3_subagent_misflag     # ArchitectPL verdict packet 자기 ArchitectAgent commit `00b7d8a` parallel_session_conflict mis-flag (subagent contagion 입증)
        - feedback_worktree_first_not_parallel_session_memory  # normative 승격 carrier memory entry
    modal_anti_pattern_dictionary: {}  # path-based verify primitive, modal 어휘 무관
    introduced_by: CFP-689
    introduced_date: 2026-05-20
    owner_adr: ADR-073-Amendment-3  # declaration source — Orchestrator verify-before-assert Amendment 3 (worktree-first self-ownership verify 3-tuple normative)
    carrier_adr: ADR-060            # enforcement source — evidence-enforceable warning-tier framework
    recurrence:
      count: 3                      # 2026-05-19~20 KST single session 3 occurrences (CFP-1026 + CFP-681 dup worktree + CFP-681 ArchitectPL subagent mis-flag)
      threshold: 3                  # 3-occurrence reached at introduction — auto-blocking 자동 승격 trigger 즉시 활성 (ADR-060 §결정 19 Amendment 6 / schema v1.2 auto-firing pattern_count threshold 도달)
      last_occurrence: "2026-05-20T00:00:00Z"  # CFP-681 ArchitectPL Phase 3 mis-flag KST 09:00 = UTC 00:00 (registry schema v1.2 정합 — KST `09:00:00+09:00` = UTC Z notation, 2026-05-20)
      promotion_trigger: auto_blocking  # recurrence.count = 3 = threshold = 3 도달 → Wave 2 별 sub-CFP carrier (Wave 2 mechanical wire) merge 시 즉시 warning → blocking-on-pr 자동 승격 trigger 활성 (ADR-060 §결정 19 enum)
    status: deferred-followup  # Wave 1 declaration only — actual script + workflow + hook + bats + label-registry-v2 신규 entry = Wave 2 별 sub-CFP carrier 후 warning 전환 (parallel-work-sentinel-pickup CFP-966 deferred-followup → CFP-967 warning 전환 precedent 답습). scope_boundary: 본 entry = worktree-first self-confusion sub-domain (자기 영역, reflog membership 안 lineage) / #983 후보 (a)/(b) = real parallel cross-session collision sub-domain (외부 영역, reflog membership 외 lineage) — disjoint axis 명시 invariant

  - name: active-sessions-presence
    description: |
      ADR-085 §결정 2 active_sessions[] field presence-grep — Story Issue body
      `<!-- active_sessions -->` HTML comment block 또는 Story file frontmatter
      `active_sessions:` array 둘 중 하나 이상 존재 verify (dual carrier
      mandate). 5-tuple schema: git_identity / worktree_path / entry_phase /
      entered_at_kst (ADR-079 KST `+09:00` ISO 8601 zoned strict) /
      last_heartbeat_kst. Wave 1 declaration-only (mechanical lint = Wave 2 별
      sub-CFP carrier). multi-session collaboration protocol carrier 의무 —
      복수 Claude Code session 동시 작업 시 ownership 결정 / 분담 / handoff
      normative SSOT. ADR-082 §결정 6 + ADR-070 §D5 retain pattern 답습.
    introduced_by: CFP-1041
    introduced_date: 2026-05-20
    owner_adr: ADR-085-§결정-2  # declaration source — Multi-session collaboration protocol active_sessions[] field schema
    carrier_adr: ADR-060       # enforcement source — evidence-enforceable warning-tier framework
    recurrence:
      count: 0                 # Wave 1 declaration baseline (sentinel-driven 아닌 ratchet-driven, ADR-085 carrier introduce)
      threshold: 3             # 3-occurrence reached 시 자동 승격 trigger (Wave 2 mechanical wire 후 carrier)
      promotion_trigger: none  # Wave 1 = declaration only, Wave 2 별 sub-CFP merge 후 active
    status: warning  # CFP-1057 Wave 2 mechanical wire merged — scripts + workflow + bats fixture + label-registry-v2 entry promotion 완료 (Wave 1 declarative → Wave 2 warning)
    detect_command: bash scripts/check-active-sessions-presence.sh  # Wave 2 별 sub-CFP carrier — Story file frontmatter `active_sessions:` block OR Issue body HTML comment block presence-grep
    workflow: templates/github-workflows/active-sessions-presence.yml  # Wave 2 별 sub-CFP carrier
    current_tier: warning  # ADR-060 §결정 5 — 첫 도입 = warning mode
    bypass_label: hotfix-bypass:active-sessions-presence  # label-registry-v2 v2.40 신규 entry (active-sessions-presence + lane-entry-ownership-verify 2 entry 동반 append)
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies:
        - CFP-1041               # 본 Story (declarative anchor Wave 1 carrier)
        - TBD-Wave-2-sub-CFP     # Wave 2 mechanical wire (script + workflow + hook + bats + label-registry-v2 신규 entry 별 sub-CFP, sequential next, Wave 1 merge 후)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
        - cfp_983_super_class_ssot_absorption_evidence  # absorbed pattern_count ≥ 8 lineage CFP-953/946/949/932/954/991/967/1014
        - cfp_870_inline_absorb_evidence                # handoff baton transfer §결정 5
        - cfp_681_retroactive_success_variant_evidence  # rebase merge 우선 first success
    modal_anti_pattern_dictionary: {}  # field presence check, modal 어휘 무관

  - name: lane-entry-ownership-verify
    description: |
      ADR-085 §결정 3 lane-entry sentinel ownership verify — `gh pr list
      --search "head:<branch>" --json number,headRepository,author,createdAt
      --state open` ownership verify mandate. ADR-073 Amendment 2 polling enum
      3종 (`lane_spawn` / `pr_open` / `merge_transition`) 의 4번째 source
      `active_sessions_check` cross-ref-only append (ADR-073 본문 0건 변경
      invariant). Wave 1 declaration-only (mechanical lint = Wave 2 별 sub-CFP
      carrier). multi-session collaboration protocol carrier 의무.
    introduced_by: CFP-1041
    introduced_date: 2026-05-20
    owner_adr: ADR-085-§결정-3  # declaration source — lane-entry sentinel ownership verify normative
    carrier_adr: ADR-060        # enforcement source
    recurrence:
      count: 0
      threshold: 3
      promotion_trigger: none
    status: warning  # CFP-1057 Wave 2 mechanical wire merged — scripts + workflow + bats fixture promotion 완료
    detect_command: bash scripts/check-lane-entry-ownership.sh
    workflow: templates/github-workflows/lane-entry-ownership-verify.yml
    current_tier: warning
    bypass_label: hotfix-bypass:lane-entry-ownership-verify  # label-registry-v2 v2.40 신규 entry
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies:
        - CFP-1041
        - TBD-Wave-2-sub-CFP
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
        - cfp_983_super_class_ssot_absorption_evidence
        - cfp_870_inline_absorb_evidence
        - adr_073_amendment_2_polling_enum_4th_source_evidence  # cross-ref `active_sessions_check`
    modal_anti_pattern_dictionary: {}

  - name: deputy-spawn-count-empirical-grounding
    description: |
      ADR-068 I-5 dimensional empirical grounding `count` dimension backref entry.
      Deputy roster 변경 carrier Story (예: CFP-1086 Story-1 Amendment 8 = 7+3+1
      roster 재편) 시 spawn count quantitative parameter 의 empirical-source
      annotation 의무 — `[empirical-source: <ref> | TBD]` (Mitigation 2 explicit
      TBD 기재, local probe / wiretap source 부재 시).

      본 entry = ADR-068 I-5 본문 0건 변경 backref-only entry (Wave 1
      declarative — Wave 2 mechanical lint = `dimensional-empirical-grounding`
      entry promotion 시 본 entry 도 active 전환). ADR-086 §결정 2 5-checklist
      (2) cost-token budget 통과 의무 mechanism.

      검증 패턴: Story §1 (또는 spec §2 axis 최종안 표) 안 spawn count 평균 / full
      activation 명시 + `[empirical-source: TBD]` 또는 `[empirical-source:
      <local probe ref>]` annotation presence-grep. ADR-082 §결정 2(a)
      corpus-claim-verify lint 의 backref pattern verbatim 재사용
      (`[verified: git show <ref>:<path>]` annotation 동형 mechanical 패턴 정합).
    introduced_by: CFP-1086
    introduced_date: 2026-05-20
    owner_adr: ADR-068-Amendment-1  # declaration source — I-5 dimensional empirical grounding (CFP-528) `count` dimension
    carrier_adr: ADR-086            # 본 framework P7 §결정 2 5-checklist (2) cost-token budget binding source
    recurrence:
      count: 1                      # CFP-1086 Story-1 첫 적용 사례 (Amendment 8 spawn count 평균 22 → 28 / full 34 → 40 annotation)
      threshold: 3                  # 3-occurrence reach 시 mechanical wire warning-tier promotion (Wave 2 별 sub-CFP carrier)
      last_occurrence: "2026-05-20T00:00:00Z"  # CFP-1086 Story-1 Phase 1 PR commit time (KST 09:00 = UTC 00:00)
      promotion_trigger: none       # Wave 1 = declaration only (ADR-068 I-5 본문 backref-only). Wave 2 별 sub-CFP merge 후 active 전환 (`dimensional-empirical-grounding` entry promotion 시 본 entry 도 active)
    status: deferred-followup       # Wave 1 declaration only — actual mechanical lint = ADR-068 I-5 Amendment 1 `dimensional-empirical-grounding` entry 의 후속 mechanical wire 별 sub-CFP carrier 후 warning 전환
    current_tier: warning           # ADR-060 schema v1.1 required field (deferred-followup status 이지만 tier 분류 = warning 의도)
    bypass_label: hotfix-bypass:deputy-axis-restructure  # label-registry-v2 v2.41 신규 entry (CFP-1086)
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies:
        - CFP-1086                              # 본 Story-1 (첫 적용 declarative anchor)
        - CFP-528                               # ADR-068 Amendment 1 I-5 신설 (`dimensional-empirical-grounding` entry parent)
        - TBD-Wave-2-sub-CFP                    # Wave 2 mechanical wire (script + workflow + hook + bats + label-registry-v2 신규 entry 별 sub-CFP, sequential next)
      evidence_artifacts:
        - github_actions_run_history_url
        - lint_failure_count_zero_proof
        - pr_cumulative_count_proof
        - adr_086_self_application_first_case_evidence  # 본 framework self-application 첫 사례 = CFP-1086 Story-1 Amendment 8 (spec §8 6 Story self-app 표 verbatim)
    modal_anti_pattern_dictionary: {}  # ADR-068 I-5 patterns (anti-pattern 4종 + Mitigation 4종) 재사용, modal 어휘 무관
