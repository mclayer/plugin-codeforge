# docs/selftest-execution-liveness-inventory.yaml
# self-test execution-liveness 인벤토리 — single SSOT (per `tests/scripts/*.sh` 1 레코드)
# Carrier: CFP-2622 (Epic CFP-2602 G6) / ADR-151
# Meta-gate: scripts/check-selftest-execution-liveness.sh (→ scripts/lib/check_selftest_execution_liveness.py)
# Wiring:    .github/workflows/selftest-execution-liveness-test.yml (wrapper-self-only, non-required, day-1 hard-fail)
#
# 목적: wrapper-self 의 self-test corpus 를 "선언(declared L3 fixture)"에서 "실제 CI 실행(channel alive)"으로
#   승격한다. 전 `tests/scripts/*.sh` 를 enroll 해 신규 self-test 가 무레코드로 침묵 un-run 되는 root(silent-un-run)
#   을 fail-closed 로 차단한다.
#
# 스키마 (ADR-151 §결정2 — 8-field/레코드):
#   self_test              : 스크립트 경로(tests/scripts/*.sh, repo-root 상대).            [AC-1a]
#   execution_channel      : workflow:<file>.yml:<job-id> | inline_self_test_flag:<file>.yml:<job-id>
#                            | agent_runtime | manual_registered.                          [AC-2]
#   channel_status         : alive | dead | permanently_skipped.                           [AC-3]
#   blocking_tier          : required | non_required | warning_tier | manual (정직 기록만).
#   discriminating_fixture : present | smoke_only | N/A (형식 presence 까지만 — 검출력=G3). [AC-4 미강제]
#   l2_full_scope          : both_copies | single | N/A.                                    [AC-5]
#   manual_reason          : ≥30자 substantive (agent_runtime/manual_registered 시 필수, else "").  [AC-2]
#   g_boundary_check       : runtime 축(soak=G2 / DAST=G5 / real-render=§8.7) 무침범 확인 note.       [AC-8]
#
# 정직 천장 (ADR-151 §결정7): 메타-게이트는 presence/alive/형식/L2-presence/manual_reason substantive 까지만
#   fail-closed. discriminating 검출력(G3)·열거 완결성(review)·L1 blocking 승격(ADR-060)은 미강제.
#   "wrapper 동적검증 완전 봉인" hard-claim 금지 — 실행-채널 fail-closed + 선언→실행 갭 저감 + 잔여 정직 공개.
#
# 자연 N/A 3축 (ADR-151 §결정6): wrapper-self runtime 축 soak(G2)/real-render(§8.7)/DAST(G5) = plugin-meta-na
#   (런타임 데몬/frontend/공격표면 산출물 부재). 억지 이식 = over-forcing → g_boundary_check 로 형제 축 참조.
# l2_full_scope: 각 self-test 는 tests/scripts/ 단일 아티팩트(template copy 없음)로 1회 실행 → 실행 scope
#   자연 N/A. workflow byte-identical parity 는 invariant-check.yml(Workflow parity step) 소관 = disjoint 축.
#
# census (execution-backed, 트리 실측 2026-07-13): 38 기존 + 신규 1
#   (CFP-2646 test_check-resource-safety-claim-proof.sh, ADR-082 Amendment 38 §결정 16 resource-safety
#   claim↔proof presence lint self-test, workflow:resource-safety-claim-proof-presence.yml dedicated step alive;
#   직전 CFP-2635 test_check-shell-test-masking.sh shell-test masking self-test) = 39 레코드. dead 레코드 0
#   (silent-un-run 봉인 — bucket A backfill / bucket B agent_runtime / bucket C·D manual_registered 로 전수 enroll).
#   ★ 메타-게이트는 count 상수를 강제하지 않음 — tests/scripts/*.sh ↔ 레코드 bijection(AC-1a)으로 전수 enroll 검증.

self_tests:

  # ── CI workflow 채널 (dedicated self-test/gate workflow, alive) ──────────────
  - self_test: "tests/scripts/test-actionlint-workflows.sh"
    execution_channel: "workflow:actionlint-workflows-test.yml:actionlint-workflows-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "정적 거버넌스 lint self-test — soak=G2·real-render=§8.7·DAST=G5 runtime 축 disjoint (wrapper-self plugin-meta-na)."

  - self_test: "tests/scripts/test-css-lint.sh"
    execution_channel: "workflow:css-lint.yml:css-lint-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "CSS stylelint 게이트 self-test — real-render(§8.7)/soak(G2)/DAST(G5) runtime 축 disjoint (정적 lint 계약 검증)."

  - self_test: "tests/scripts/test_adr-reservation-atomic-claim.sh"
    execution_channel: "workflow:adr-reservation-claim-test.yml:adr-reservation-claim-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "ADR-RESERVATION OCC claim self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 primitive 검증)."

  - self_test: "tests/scripts/test_adr-reservation-backfill.sh"
    execution_channel: "workflow:adr-reservation-claim-test.yml:adr-reservation-claim-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "reservation backfill self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 row 검증)."

  - self_test: "tests/scripts/test_adr-reservation-claim-wiring.sh"
    execution_channel: "workflow:adr-reservation-claim-test.yml:adr-reservation-claim-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "smoke_only"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "claim-wiring 배선 presence self-test(RED-PENDING scaffold) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint."

  - self_test: "tests/scripts/test_adr-reservation-stale-reclaim.sh"
    execution_channel: "workflow:adr-reservation-claim-test.yml:adr-reservation-claim-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "smoke_only"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "stale-reclaim SMOKE self-test(값 대조) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 primitive)."

  - self_test: "tests/scripts/test_check-adr-uniqueness-3way.sh"
    execution_channel: "workflow:adr-reservation-claim-test.yml:adr-reservation-claim-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "ADR dual-key 3-way uniqueness self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 lint)."

  - self_test: "tests/scripts/test_aggregate-stop-event.sh"
    execution_channel: "workflow:orchestrator-autonomy-stop-taxonomy-check.yml:orchestrator-autonomy-stop-taxonomy"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "stop-event 집계 self-test(distinct-marker JSON oracle) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint."

  - self_test: "tests/scripts/test_check-tier-honesty.sh"
    execution_channel: "workflow:orchestrator-autonomy-stop-taxonomy-check.yml:orchestrator-autonomy-stop-taxonomy"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "tier-honesty meta-lint self-test(RED/GREEN mutation oracle) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint."

  - self_test: "tests/scripts/test_check-vague-pause-taxonomy-presence.sh"
    execution_channel: "workflow:orchestrator-autonomy-stop-taxonomy-check.yml:orchestrator-autonomy-stop-taxonomy"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "vague-pause taxonomy presence self-test(RED=토큰 삭제) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint."

  - self_test: "tests/scripts/test_check-subagent-wait-liveness-presence.sh"
    execution_channel: "workflow:subagent-wait-liveness-check.yml:subagent-wait-liveness-presence"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "background-wait liveness presence lint self-test — 여기 liveness=orchestration-stall 축(ADR-139), soak=G2·DAST=G5·real-render=§8.7 disjoint."

  - self_test: "tests/scripts/test_bootstrap-consumer-whitelist.sh"
    execution_channel: "workflow:bootstrap-whitelist-test.yml:bootstrap-whitelist-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "bootstrap whitelist self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 copy-whitelist 검증)."

  - self_test: "tests/scripts/test_check-ac-traceability-matrix.sh"
    execution_channel: "workflow:ac-traceability-self-test.yml:ac-traceability-self-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "G1 AC-traceability 게이트 self-test — G1(요건 traceability) ⊥ G6(실행-liveness) disjoint, soak=G2·DAST=G5·real-render=§8.7 축도 disjoint."

  - self_test: "tests/scripts/test_check-codex-companion-timeout-presence.sh"
    execution_channel: "workflow:codex-companion-timeout-check.yml:codex-companion-timeout-presence"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "codex-companion wall-clock 가드 presence lint self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint."

  - self_test: "tests/scripts/test_check-force-push-base-advance.sh"
    execution_channel: "workflow:force-push-base-advance-check.yml:force-push-base-advance-test"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "force-push base-advance lint self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 git 상태 검증)."

  - self_test: "tests/scripts/test_check-lexicon-drift.sh"
    execution_channel: "workflow:lexicon-drift-check.yml:lexicon-drift-test"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "lexicon-drift lint self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 어휘 검증)."

  - self_test: "tests/scripts/test_check-parallel-work-sentinel.sh"
    execution_channel: "workflow:parallel-work-sentinel-check.yml:parallel-work-sentinel-test"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "parallel-work sentinel lint self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 sentinel 검증)."

  - self_test: "tests/scripts/test_check-responsibility-marker-drift.sh"
    execution_channel: "workflow:responsibility-marker-drift-check.yml:responsibility-marker-drift-test"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "responsibility-marker drift lint self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 marker 검증)."

  - self_test: "tests/scripts/test_check-responsibility-topology.sh"
    execution_channel: "workflow:responsibility-topology-check.yml:responsibility-topology-test"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "responsibility-topology lint self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 topology 검증)."

  - self_test: "tests/scripts/test_check-venue-shape-fidelity-presence.sh"
    execution_channel: "workflow:venue-shape-fidelity-presence-check.yml:venue-shape-fidelity-presence-test"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "venue-shape fidelity presence lint self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 §8 presence)."

  - self_test: "tests/scripts/test_reconcile-overlay-workflow-channel.sh"
    execution_channel: "workflow:reconcile-overlay-workflow-channel-test.yml:reconcile-workflow-channel-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "reconcile-overlay workflow channel self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 sync 채널 검증)."

  - self_test: "tests/scripts/test_spawn_description_prefix.sh"
    execution_channel: "workflow:spawn-description-prefix-detect.yml:spawn-description-prefix-detect"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "spawn description prefix self-test(distinct-marker JSON oracle) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint."

  # ── inline_self_test_flag 채널 (subject 가 --self-test 로 inline 실행, alive) ──
  - self_test: "tests/scripts/test_check-adr-cross-ref-consistency.sh"
    execution_channel: "inline_self_test_flag:adr-cross-ref-consistency.yml:adr-cross-ref-consistency"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "ADR cross-ref consistency — subject check-adr-cross-ref-consistency.sh 가 workflow 에서 --self-test inline 실행. soak=G2·DAST=G5·real-render=§8.7 disjoint."

  # ── bucket A 진성 hollow-gate backfill (신규 workflow 배선 → dead→alive 승격) ──
  - self_test: "tests/scripts/test-check-doc-section-8-7.sh"
    execution_channel: "workflow:selftest-execution-liveness-test.yml:selftest-execution-liveness"
    channel_status: "alive"
    blocking_tier: "required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "required 'doc section schema (CFP-28 — strict)' 게이트 §8.7 UI-render case mutation-kill fixture — 정적 doc lint, soak=G2·DAST=G5·real-render(§8.7 검사대상 자체) runtime 축 disjoint."

  - self_test: "tests/scripts/test-check-doc-section-8-8.sh"
    execution_channel: "workflow:selftest-execution-liveness-test.yml:selftest-execution-liveness"
    channel_status: "alive"
    blocking_tier: "required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "required 'doc section schema (CFP-28 — strict)' 게이트 §8.8 G4-dynamic case mutation-kill fixture — 정적 doc lint, soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint."

  - self_test: "tests/scripts/test-check-doc-section-8-9.sh"
    execution_channel: "workflow:selftest-execution-liveness-test.yml:selftest-execution-liveness"
    channel_status: "alive"
    blocking_tier: "required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "required 'doc section schema (CFP-28 — strict)' 게이트 §8.9 G5-DAST case mutation-kill fixture — 정적 doc lint, soak=G2·DAST(§8.9 검사대상 자체)·real-render=§8.7 runtime 축 disjoint."

  - self_test: "tests/scripts/test-check-doc-section-8-10.sh"
    execution_channel: "workflow:selftest-execution-liveness-test.yml:selftest-execution-liveness"
    channel_status: "alive"
    blocking_tier: "required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "required 'doc section schema (CFP-28 — strict)' 게이트 §8.10 G3(a) dark-path activation manifest + G3(b) 요구-슬라이스 매핑 case mutation-kill fixture(discriminating-A self-test) — 정적 doc lint, soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (activation oracle=검사대상 자체)."

  # ── 메타-게이트 재귀 L3 self-test (ADR-151 §결정9 / AC-9, alive) ──────────────
  - self_test: "tests/scripts/test_check-selftest-execution-liveness.sh"
    execution_channel: "workflow:selftest-execution-liveness-test.yml:selftest-execution-liveness"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "메타-게이트 재귀 L3 self-test(AC-9, self-exclude 금지) — 정적 lint, soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (wrapper-self plugin-meta-na, template copy 없음)."

  # ── bucket B agent_runtime (FIX/merge/review 시점 runtime 호출, CI 게이트 아님) ─
  - self_test: "tests/scripts/test-check-fix-replay-disposition.sh"
    execution_channel: "agent_runtime"
    channel_status: "alive"
    blocking_tier: "manual"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: "check-fix-replay-disposition.sh 는 FIX loop 시점 agent/Orchestrator runtime 에서 fix-event-v1 contract(FIX ledger append) 경로로 호출되는 disposition helper — CI 게이트 아닌 runtime 검증. self-test 는 그 disposition 판정의 mutation-kill fixture."
    g_boundary_check: "runtime disposition helper self-test — 지속-soak=G2·DAST=G5·real-render=§8.7 runtime 축 무침범 (agent-runtime 축이지 CI-soak 아님)."

  - self_test: "tests/scripts/test-check-merge-gate-disposition.sh"
    execution_channel: "agent_runtime"
    channel_status: "alive"
    blocking_tier: "manual"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: "check-merge-gate-disposition.sh 는 PR merge 판정 시점 Orchestrator runtime(orchestrator-playbook merge 게이트 경로)에서 호출되는 disposition helper — CI 배선 아닌 runtime 축. self-test 는 merge disposition 판정의 discriminating fixture."
    g_boundary_check: "runtime disposition helper self-test — 지속-soak=G2·DAST=G5·real-render=§8.7 runtime 축 무침범 (agent-runtime 축, CI-soak 아님)."

  - self_test: "tests/scripts/test-check-mutation-disposition.sh"
    execution_channel: "agent_runtime"
    channel_status: "alive"
    blocking_tier: "manual"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: "check-mutation-disposition.sh 는 리뷰 시점 root-cause-decision·fix-ledger 스킬 경유 agent runtime 에서 호출되는 mutation-peer disposition helper — CI 게이트 아님. self-test 는 그 판정의 mutation-kill fixture."
    g_boundary_check: "runtime mutation-peer disposition self-test — 지속-soak=G2·DAST=G5·real-render=§8.7 runtime 축 무침범 (agent-runtime 축, CI-soak 아님)."

  # ── bucket C dead-path 존치 (live-machinery, ADR-141 dormant) ─────────────────
  - self_test: "tests/scripts/test-check-stakes-tier-gating.sh"
    execution_channel: "manual_registered"
    channel_status: "alive"
    blocking_tier: "manual"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: "check-stakes-tier-gating.sh = ADR-141(전 에이전트 opus 단일 tier) 이 명시한 dead-path 존치(live-machinery) — stakes-tier 머신은 보존되나 all-opus 하 dormant. self-test 는 dormant machinery 회귀 fixture 로 manual 등록(CI 재배선 대상 아님)."
    g_boundary_check: "dormant live-machinery self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (stakes-tier machinery dormant, 정적 검증)."

  # ── bucket D orphan / one-shot 잔재 / near-namesake (manual_registered) ───────
  - self_test: "tests/scripts/test-check-lint-css.sh"
    execution_channel: "manual_registered"
    channel_status: "alive"
    blocking_tier: "manual"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: "near-namesake orphan — subject check-lint-css.sh 부재. 실 CSS lint 게이트 = css-lint.yml + test-css-lint.sh(WIRED, 별도 레코드). 본 test 는 잔재로 존치, CI 재배선 대상 아님(AC-10 advisory 권고). 제거는 별도 cleanup Story(회귀 리스크)로 분리."
    g_boundary_check: "near-namesake orphan self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (subject 부재로 실행축 자연 N/A). AC-10 advisory."

  - self_test: "tests/scripts/test-check-story-stakes-overlay.sh"
    execution_channel: "manual_registered"
    channel_status: "alive"
    blocking_tier: "manual"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: "orphan — subject check-story-stakes-overlay.sh 광역 tree 실측 부재(stakes-tier 계열 잔재). CI 배선할 live subject 없음 → manual 등록. 제거는 별도 cleanup Story(회귀 리스크)로 분리."
    g_boundary_check: "orphan self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (subject 부재로 실행축 자연 N/A)."

  - self_test: "tests/scripts/test_adr-path-dead-path-sweep.sh"
    execution_channel: "workflow:path-relocation-consistency.yml:path-relocation-consistency"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    # CFP-2661 D7 (AC-17): manual_reason 사실오류 정정 — 본 self-test 는 ALIVE(firsthand: 13 passed / 0 failed,
    #   5 mutant-kill). 구 manual_reason("subject adr-path-dead-path-sweep.sh 부재 / 재배선할 live subject
    #   없음")은 FALSE 였다. 신규 path-relocation-consistency lint 이 dead-path 검출을 흡수/확장 → 본 회귀
    #   test 를 그 workflow 실행 채널로 CI 승격(manual→non_required, execution_channel workflow 배선).
    g_boundary_check: "dead-path 회귀 self-test (CI 승격) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 dead-path 검출 판별력 결박, wrapper-self plugin-meta-na)."

  - self_test: "tests/scripts/test_adr-sunset-criteria-pathagnostic.sh"
    execution_channel: "manual_registered"
    channel_status: "alive"
    blocking_tier: "manual"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: "workflow-regression self-test — subject check-adr-sunset-criteria.sh 는 EXISTS(adr-sunset-criteria.yml 8-ref live)이나 어느 workflow 도 --self-test/inline self-test 로 배선하지 않음(실측). 본 test unwired → manual 등록(inline-cover 아님)."
    g_boundary_check: "workflow-parity regression self-test(unwired) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (parity=invariant-check 소관, disjoint)."

  - self_test: "tests/scripts/test_schema-lib-archive-adr-activation.sh"
    execution_channel: "manual_registered"
    channel_status: "alive"
    blocking_tier: "manual"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: "CFP-2523 one-shot schema-lib archive-adr activation 잔재 — subject schema-lib-archive-adr-activation.sh 부재(1-shot 후 소멸). 재배선할 live subject 없음 → manual 등록. 제거는 별도 cleanup Story로 분리."
    g_boundary_check: "one-shot 잔재 self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (subject 부재로 실행축 자연 N/A)."

  # ── CFP-2635 (ADR-060 Amendment 22) — shell-test-exit-masking-detect self-test (dedicated workflow step, alive) ──
  - self_test: "tests/scripts/test_check-shell-test-masking.sh"
    execution_channel: "workflow:shell-test-exit-masking-detect.yml:shell-test-exit-masking-detect"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "정적 shell-test exit-masking/mock-seam lint self-test — soak=G2·real-render=§8.7·DAST=G5 runtime 축 disjoint (wrapper-self plugin-meta-na, 정적 lint 판별력 결박)."

  # ── CFP-2646 (ADR-168 §결정 16 (구 ADR-082 Amendment 38, 재제정 CFP-2840)) — resource-safety-claim-proof-presence self-test (dedicated workflow step, alive) ──
  - self_test: "tests/scripts/test_check-resource-safety-claim-proof.sh"
    execution_channel: "workflow:resource-safety-claim-proof-presence.yml:resource-safety-claim-proof-presence"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "정적 resource-safety claim↔proof presence lint self-test — soak=G2·real-render=§8.7·DAST=G5 runtime 축 disjoint (wrapper-self plugin-meta-na, presence≠truth 정적 lint 판별력 결박: TC-OC 검출/TC-HD·PL·CD·EX 미검출/MK mutation-kill/PERF DoS-bound/AC-3 self-application)."

  # ── CFP-2661 (ADR-136 Amendment 4 §결정 15) — path-relocation-consistency self-test (dedicated workflow step, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 39 → 40 (신규 self-test enroll). born-invalid enroll 회피 위해 정확히 39→40.
  - self_test: "tests/scripts/test_check-path-relocation-consistency.sh"
    execution_channel: "workflow:path-relocation-consistency.yml:path-relocation-consistency"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "relocation-ledger 구동 construct-scoped dead-path 재유입 lint self-test — soak=G2·real-render=§8.7·DAST=G5 runtime 축 disjoint (wrapper-self plugin-meta-na, construct-scoped 판별력 결박: positive anchor 검출/negative 0-FP/born-hollow guard candidate0→FAIL/construct-vs-file FN/field-predicate locked↔append-only/mutation-kill/PERF DoS-bound<5s)."

  # ── CFP-2684 (ADR-154) — hard-gate-self-verification 메타-게이트 재귀 self-test (dedicated workflow step, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 40 → 41 (신규 self-test enroll). 이 self-test 파일은 QADev Phase 2
  #   deliverable — enroll 은 선행하되 파일 생성 전까지 selftest-execution-liveness 게이트가 record→missing-file 로
  #   AC-1a FAIL(정상, DevPL 이 QADev 합류 후 최종 검증). two-meta-gate mutual cross-seal(§7.7(2) meta-hollow 차단).
  - self_test: "tests/scripts/test_check-hard-gate-self-verification.sh"
    execution_channel: "workflow:hard-gate-self-verification-test.yml:hard-gate-self-verification"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "hard-gate self-verification presence/shape 메타-게이트 재귀 self-test(AC-7 self-application, TC-CLEAN-PASS + M1-M6 positive-leak + LIVE ceiling-honesty) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (wrapper-self plugin-meta-na, presence/shape 판별력 결박: positive-control/2-exit-shape/empty-target honest-degrade/unknown-input fail-closed/identity-probe/3-way taxonomy/honest-ceiling presence + M1-M6 mutation-kill + DoS 회귀가드; 검출 sufficiency=undecidable review-tier 미강제, presence ≠ truth)."

  # ── CFP-2702 (ADR-038 Amendment 6) — run-hook.cmd CRLF exec-first 폴리글롯 회귀 self-test 2종 (dedicated workflow, ubuntu-latest, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 41 → 43 (신규 self-test 2 enroll). AC-5/AC-6 회귀.
  # execution_channel = ubuntu-latest Linux 시스템 bash (msys/Git Bash 금지 — CR-관대 마스킹으로
  #   AC-5 discriminating(구 heredoc-CRLF → CR-민감 bash 에서 loud CR-오염 실패(exit 127), silent
  #   no-op 아님) false-GREEN 회피, Story §2.2 / §6 Fact 2).
  #   AC-4(cmd.exe 배너 채널)는 windows-bootstrap-smoke.yml 담당 = Linux self-test corpus 밖 (disjoint).
  - self_test: "tests/scripts/test-run-hook-cmd-unix-exec.sh"
    execution_channel: "workflow:run-hook-crlf-test.yml:run-hook-crlf-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "run-hook.cmd exec-first 폴리글롯 CRLF-safe Unix dispatch 회귀 self-test (CR-민감 Linux 시스템 bash). soak=G2·real-render=§8.7·DAST=G5 runtime 축 disjoint (wrapper-self plugin-meta-na, hook wrapper 정적 dispatch 배선 회귀). discriminating: exec 성공 happy + CR-leak-0 + 구 heredoc-CRLF loud CR-오염 실패(exit 127) RED[CR-민감 gated] + #-가드 제거 인자 누출 RED[CR-민감 gated] + 인자 정확 전달 정밀도."

  - self_test: "tests/scripts/test-gitattributes-cmd-crlf.sh"
    execution_channel: "workflow:run-hook-crlf-test.yml:run-hook-crlf-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "git eol=crlf 배포 채널(checkout/archive) CRLF 산출 override 회귀 self-test. soak=G2·real-render=§8.7·DAST=G5 runtime 축 disjoint (wrapper-self plugin-meta-na, git attr 배포 계약 정적 검증). discriminating: override 有 채널 CR>0 vs override 無 대조 채널 CR==0 load-bearing + blob CR=0 채널-vs-blob 정밀도 + .bat override 누락 회귀가드."

  # ── CFP-2688 (Epic #2686 Story B / ADR-156) — dev-process 지표 aggregate self-test (dedicated workflow step, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 43 → 44 (신규 self-test enroll). B lane QADev Phase 2 deliverable —
  #   aggregator embedded --self-test + honest-degrade lint --selftest(discriminating NC1~NC10) + lint check
  #   (real query_with_stats round-trip, mock-seam 금지)을 execution-backed 로 CI 구동. dedicated .github-only
  #   workflow(dev-process-metrics-test.yml, non-required, day-1 hard-fail) — branch-protection 7-tuple 무변경.
  - self_test: "tests/scripts/test_aggregate-dev-process-event.sh"
    execution_channel: "workflow:dev-process-metrics-test.yml:dev-process-metrics-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "dev-process 지표 aggregate self-test(aggregator embedded --self-test AC-4~23 + honest-degrade lint --selftest NC1~NC10 discriminating negative-control + lint check real query_with_stats round-trip, mock-seam 금지) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (wrapper-self plugin-meta-na, record-only batch 집계 판별력 결박: measured-0≠dormant/over-claim 부재/honest-null/no-blob-deref/idempotency +0/order-preserving negative_duration; 검출력=G3 미강제, presence ≠ truth)."

  # ── CFP-2697 (Epic #2696 canary artifact D6, Change Plan §8.4/AC-20) — decision-record disposition oracle + reference-integrity guard self-test (dedicated workflow step, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 44 → 45 (신규 self-test enroll). self-test + born-alive smoke 실행 채널 =
  #   .github/workflows/decision-record-disposition-test.yml (byte-identical template pair, wrapper-self-only, non-required day-1 hard-fail).
  - self_test: "tests/scripts/test_check-decision-record-disposition.sh"
    execution_channel: "workflow:decision-record-disposition-test.yml:decision-record-disposition"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "정적 disposition oracle(3-축 ablation load-bearing) + reference-integrity guard(4-check conjunction) mutation-kill self-test — soak(G2)·DAST(G5)·real-render(§8.7) runtime 축 disjoint. 판별력 = 5-fixture replay + M1/M2/M3 축 flip + M4 perturbation + M5 conjunction."

  # ── CFP-2689 (Epic #2686 Story C / ADR-158) — author-time self-gate runner self-test (dedicated workflow step, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 45 → 46 (신규 self-test enroll). C lane QADev Phase 2 deliverable —
  #   runner embedded --self-test(독립 oracle + 대칭 fail-closed 양방향 + R1 path-aware N/A + 측정≠emit 구분) +
  #   pytest tests/unit/test_authoring_self_gate.py(AC-1~18)을 execution-backed 로 CI 구동. dedicated .github-only
  #   workflow(authoring-self-gate-test.yml, non-required, day-1 hard-fail) — branch-protection 7-tuple 무변경.
  #   pytest fork 는 QADev 파일 미착지 시 record→missing-file 로 RED(정상, DevPL 이 QADev 합류 후 최종 통합 검증).
  - self_test: "tests/scripts/test_authoring-self-gate.sh"
    execution_channel: "workflow:authoring-self-gate-test.yml:authoring-self-gate-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "author-time self-gate runner self-test(runner --self-test 독립 oracle + 대칭 fail-closed 양방향[known-good→PASS ∧ known-bad(RTM row 누락/§4 위치 오배치/AC header 손상/invalid category)→FAIL] + 실 게이트 invoke[ac-traceability CLI subprocess / doc-section·doc-frontmatter 격리 subprocess / rtm-format 실 파서 재사용] + R1 path-aware N/A + 측정≠emit 구분 + mutation-revert negative-control + pytest AC-1~18) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (wrapper-self plugin-meta-na, 저작시점 구조/위치 conformance 판별력 결박: 검출 완비=review-tier semantic 축 미강제, presence ≠ truth honest-ceiling)."

  # ── CFP-2700 (Epic #2700 D3 / ADR-157 + ADR-060 Amendment 23) — infra-resource drift scanner discriminating self-test (dedicated workflow step, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 46 → 47 (신규 self-test enroll). G2 QADev Phase 2 deliverable —
  #   .sh 만 enroll (meta-gate 는 tests/scripts/*.sh 만 glob — 동반 .py 는 workflow 의 .py step 으로 실행, ac-traceability Hop3 AST 스타일, 인벤토리 대상 아님).
  #   실행 채널 = .github/workflows/infra-resource-manifest-drift.yml (byte-identical template pair, non-required day-1 hard-fail).
  - self_test: "tests/scripts/test_check-infra-resource-drift.sh"
    execution_channel: "workflow:infra-resource-manifest-drift.yml:infra-resource-manifest-drift"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "D3 infra-resource drift scanner discriminating self-test (미선언 표면 검출 AC-5 + orphan AC-6 + substring 오분류 제외 AC-7 + 역색인 verdict-invariant AC-8 + none-disguise anti-hollow AC-10/11 + wrapper 실 secret 실 스캔 AC-17(G6 ratchet: baseline 0 pair·grandfathered==0, 하드카운트 없음) + per-class census floor AC-17a + 2계열 canonical 수렴 AC-22 + census born-hollow guard candidates==0∧inert==0→exit3 + baseline grandfather) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (wrapper-self plugin-meta-na, 정적 선언-대조 판별력 결박; cross-repo=G5·startup=G3 별도 슬라이스; 검출력=review-tier honest-ceiling, presence ≠ truth, ADR-157 §결정8)."

  # ── CFP-2700 (Epic #2700 G3 / ADR-157 §결정1+§결정2) — D2 startup fail-closed reference-impl + manifest schema validator discriminating self-test (dedicated workflow step, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 47 → 48 (신규 self-test enroll). G3 Phase 2 deliverable —
  #   .sh 만 enroll (meta-gate 는 tests/scripts/*.sh 만 glob — 동반 .py 인 test_infra-startup-failclosed.py
  #   는 workflow 의 .py step 으로 실행, 인벤토리 대상 아님).
  #   실행 채널 = .github/workflows/infra-resource-manifest-drift.yml 의 G3 전용 step (G2 스캐너 self-test
  #   와 같은 workflow 의 별도 step — byte-identical template pair, non-required day-1 hard-fail).
  - self_test: "tests/scripts/test_infra-startup-failclosed.sh"
    execution_channel: "workflow:infra-resource-manifest-drift.yml:infra-resource-manifest-drift"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "D2 startup fail-closed reference-impl + manifest schema validator self-test (AC-2 4-필수-필드 필드별 negative + AC-3 4계약[프로세스 env/exit-masking 금지/빈 값 reject/fail-closed default, exit 78 EX_CONFIG] + AC-9 allow-set parity[공유 파서 mutation → PARITY-BROKEN 트립] + AC-15 채택 3-way + AC-19 refimpl-enforced/unenforced 센티넬 판별[env -i, BUSINESS_OP_REACHED] + mutation RED-flip) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (consumer 제품 런타임 강제 = 채택-bounded I-5, wrapper-self 는 declarative-only 라 fixture falsify 만 — AC-4 honest-ceiling; cross-repo=G5 별도 슬라이스)."

  # ── CFP-2700 (Epic #2700 G5 / ADR-157 §결정4) — cross-repo drift 대조(--cross-repo) discriminating self-test (dedicated workflow step, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 48 → 49 (신규 self-test enroll). G5 Phase 2 deliverable —
  #   .sh 만 enroll (meta-gate 는 tests/scripts/*.sh 만 glob — 동반 .py 인 test_infra-cross-repo-drift.py
  #   는 workflow 의 .py step 으로 실행, ac-traceability Hop3 AST 스타일, 인벤토리 대상 아님).
  #   실행 채널 = .github/workflows/infra-resource-manifest-drift.yml 의 G5 전용 step (G2/G3 self-test 와
  #   같은 workflow 의 별도 step — byte-identical template pair, non-required day-1 hard-fail).
  - self_test: "tests/scripts/test_infra-cross-repo-drift.sh"
    execution_channel: "workflow:infra-resource-manifest-drift.yml:infra-resource-manifest-drift"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "G5 cross-repo drift 대조(--cross-repo) self-test — fetch·Issue-발행 seam(INFRA_CROSS_REPO_FETCH_ROOT / INFRA_CROSS_REPO_ISSUE_SINK)을 local mock-repo fixture 로 stub(실 네트워크 0, hermetic). 지속-soak(G2 consumer)·DAST·real-render(§8.7) runtime 축 disjoint — 본 self-test 는 정적/mock 판별력 결박: 3-way(content-mismatch fail-closed/전파 PASS/token 부재 degraded-FAIL) + t1(transient fail-open+WARN+Issue 발행, 미발행 mutant=RED) + t2(content-mismatch ≠ transient 오분류 mutant=최중요 RED) + ref-pin(moving HEAD 거부/idempotent) + 403·404 fail-closed + namespace spoof + secret-masking(§7.3 PAT 값 미출력) + mutation RED-flip. ※ 인벤토리 관용 'DAST=G5'(ADR-136 §8.9 G5-DAST doc-lint)와 명칭 우연 일치이나 무관 — 여기 G5 = ADR-157 §결정4 cross-repo 슬라이스. 실 cross-repo 활성 = consumer 채택-bound(I-5), wrapper-self vacuous(namespace 자원 0). honest-ceiling: 동적 키 구성 / 프록시 위장(socat) / 스캔 밖 표면 미검출(§결정8), presence ≠ truth."

  # ── CFP-2761 (ADR-085 Amendment 2 §결정 10 + ADR-073 Amendment 21) — mid-flight artifact-level provenance marker stale lint discriminating self-test (PR-time workflow, alive) ──
  # bijection: tests/scripts/*.sh 49 → 50. 실행 채널 = mid-flight-marker-check.yml (byte-identical .github + templates pair, warning-mode continue-on-error non-required). run-line: `bash tests/scripts/test_check-mid-flight-marker.sh`.
  - self_test: "tests/scripts/test_check-mid-flight-marker.sh"
    execution_channel: "workflow:mid-flight-marker-check.yml:mid-flight-marker-check"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "mid-flight artifact-level provenance marker 5유형 discriminating self-test (tracked content 마커 1-3 검출 + tracked anchor 마커 5[ADR-RESERVATION 예약 row status+age proxy] 검출 + 정직 marker[status=final·fresh] 미검출 오탐 0 + MK-T3/T5 per-유형 mutation-kill) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 마커 presence 스캔, 런타임 상태 무). honest-ceiling(ADR-151/154/157): presence ≠ truth (마커 presence ≠ 실제 확정여부), 유형5 예약 row age proxy narrow(owner/kst 미주장) + 예약-전 window 미커버, 유형4 미머지 worktree 는 hook-only 별 record. 검출력=G3 미강제, ADR-157 §결정8."

  # ── CFP-2761 (ADR-085 Amendment 2 §결정 8 Wave 2 promotion) — active-sessions presence lint discriminating self-test (PR-time workflow, alive) ──
  # bijection: tests/scripts/*.sh 50 → 51. 실행 채널 = active-sessions-presence-check.yml (byte-identical .github + templates pair, warning-mode continue-on-error non-required). run-line: `bash tests/scripts/test_check-active-sessions-presence.sh`.
  - self_test: "tests/scripts/test_check-active-sessions-presence.sh"
    execution_channel: "workflow:active-sessions-presence-check.yml:active-sessions-presence-check"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "active_sessions 참조-대상 presence discriminating self-test (Story Issue body/frontmatter active_sessions[] presence 검출 + 정직 presence[레지스트리 실재] 미검출 오탐 0 + mutation-kill) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 presence-grep, 런타임 상태 무). honest-ceiling: presence ≠ truth (field 존재 ≠ 실제 ownership 정합). 검출력=G3 미강제, ADR-151 §결정7."

  # ── CFP-2761 (ADR-073 Amendment 21 / Amendment 3 Wave 2 activation) — worktree-self-ownership path-based 3-tuple discriminating self-test (결정적 fixture self-test = CI 채널; LIVE check 은 workflow:null hook-only 존치) ──
  # bijection: tests/scripts/*.sh 51 → 52. ★ CFP-2761 F5(b): 결정적 fixture-injection self-test 는 live worktree·세션 상태 무의존이라 fresh-checkout 에서 정상 실행 → workflow:hook-selftest-execution.yml:hook-selftest-execution CI 승격. LIVE check 자체는 fresh-checkout PR runner 가 로컬 worktree·branch 미관측(hollow 회피, CFP-2692 / ADR-073 Amendment 20)이라 evidence-checks-registry 에서 workflow:null hook-only 존치(무변경) — self-test 실행-liveness ⊥ LIVE check anti-hollow disjoint. run-line: `bash tests/scripts/test_check-worktree-self-ownership.sh`.
  - self_test: "tests/scripts/test_check-worktree-self-ownership.sh"
    execution_channel: "workflow:hook-selftest-execution.yml:hook-selftest-execution"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "worktree-self-ownership path-based 3-tuple discriminating self-test (ownership-mismatch RED → verified GREEN + subagent parallel_session_conflict 오판 re-verify + MK mutation-kill, 9/9 결정적 fixture-injection GREEN) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. ★ CFP-2761 F5(b): 결정적 fixture-injection self-test(--toplevel/--worktree-list-file/--reflog-file 주입, live worktree·세션 상태 무의존)는 CI(hook-selftest-execution.yml, warning-tier continue-on-error) 실행 — LIVE check 자체는 workflow:null hook-only 존치(anti-hollow: fresh-checkout 이 로컬 worktree·branch 미관측). honest-ceiling: presence ≠ truth, ADR-151 §결정7."

  # ── CFP-2761 (ADR-085 Amendment 2 §결정 8 Wave 2 promotion) — lane-entry ownership discriminating self-test (결정적 fixture self-test = CI 채널; LIVE check 은 workflow:null hook-only 존치) ──
  # bijection: tests/scripts/*.sh 52 → 53. ★ CFP-2761 F5(b): 결정적 fixture-injection self-test(--sessions-file 주입)는 live session-time 상태 무의존이라 fresh-checkout 에서 정상 실행 → workflow:hook-selftest-execution.yml:hook-selftest-execution CI 승격. LIVE check 자체는 session-time lane-entry polling(PR content 아님)이라 evidence-checks-registry 에서 workflow:null hook-only 존치(무변경) — self-test 실행-liveness ⊥ LIVE check anti-hollow disjoint. run-line: `bash tests/scripts/test_check-lane-entry-ownership.sh`.
  - self_test: "tests/scripts/test_check-lane-entry-ownership.sh"
    execution_channel: "workflow:hook-selftest-execution.yml:hook-selftest-execution"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "lane-entry ownership discriminating self-test (owner-absent RED / conflict RED / GREEN / MK mutation-kill, 4/4 결정적 fixture-injection GREEN) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. ★ CFP-2761 F5(b): 결정적 fixture-injection self-test(--lane/--git-identity/--sessions-file 주입, live session-time lane-entry 상태 무의존)는 CI(hook-selftest-execution.yml, warning-tier continue-on-error) 실행 — LIVE check 자체는 workflow:null hook-only 존치(anti-hollow: fresh-checkout 이 session-time lane-entry 상태 미관측). honest-ceiling: presence ≠ truth, ADR-151 §결정7."

  # ── CFP-2762 (ADR-133 Amendment 3) — ADR 파일명 3-digit zero-pad 통일 self-test enroll ──
  # bijection: tests/scripts/*.sh 53 → 54 (신규 self-test enroll). 실행 채널 = adr-digit-width-check.yml
  #   (wrapper-self only — templates copy 없음 → l2_full_scope N/A). hard-fail(continue-on-error 없음,
  #   non_required — branch protection 7-tuple 무변경). run-line: `bash tests/scripts/test_adr-digit-width-unification.sh`.
  - self_test: "tests/scripts/test_adr-digit-width-unification.sh"
    execution_channel: "workflow:adr-digit-width-check.yml:adr-digit-width-check"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "ADR digit-width 3-digit zero-pad 통일 self-test (post-state invariant I-A~I-E + discriminating fixture pre-RED→post-GREEN + no-op stub 이빨 + EP/BVA 경계 negative-lookahead 불변) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 정규화 정합). hard-fail 채널(continue-on-error 없음, non_required — branch protection 7-tuple 무변경). honest-ceiling: presence ≠ truth, ADR-151 §결정7."

  # ── CFP-2784 (FU-1588-R Phase 2 wire) — rebase-staleness sentinel discriminating self-test enroll ──
  # bijection: tests/scripts/*.sh 54 → 55 (신규 self-test enroll). 실행 채널 = rebase-staleness-detection.yml:rebase-staleness-detection-test
  #   (wrapper-self only — templates copy 로 consumer 전파). warning-tier(continue-on-error 불요 hard-fail, AC-2 job_id bijection 정합).
  #   run-line: `bash tests/scripts/test_check-rebase-staleness-sentinel.sh`.
  - self_test: "tests/scripts/test_check-rebase-staleness-sentinel.sh"
    execution_channel: "workflow:rebase-staleness-detection.yml:rebase-staleness-detection-test"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "rebase-staleness sentinel lint self-test — commit-count-behind 감지 + 4-tier mitigation 권고(tier 1/2 advisory self-service) discriminating fixture(--rebase-check-mode head-compare + mock upstream commit 격차 inject + rebase-need RED→rebase-ok GREEN + MK mutation-kill) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 commit-count 검증). warning-tier hard-fail 채널(continue-on-error 없음 = exit 1 FAIL branch, non_required — branch protection 7-tuple 무변경). honest-ceiling: presence ≠ truth, ADR-151 §결정7."

  # ── CFP-2786 (Epic #2783 Child B — 의미축 semantics-change sentinel) — discriminating self-test enroll ──
  # bijection: tests/scripts/*.sh N-1 → N (신규 self-test enroll — append 시점 재산출). 실행 채널 = semantic-staleness-detection.yml:semantic-staleness-detection-test (wrapper-self only, warning-tier hard-fail no continue-on-error).
  - self_test: "tests/scripts/test_check-semantic-staleness-sentinel.sh"
    execution_channel: "workflow:semantic-staleness-detection.yml:semantic-staleness-detection-test"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "semantics-change sentinel lint self-test — carrier-path touch ∩ anchor cross-match 후보 산출 discriminating fixture(M1-M12 mutation-kill: carrier-classify/state-filter/anchor false-neg·pos/self-carve-out/degrade 4-class/disjoint-vocab/tier-flip/byte-parity) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 의미-staleness 검증, honest-ceiling ADR-151 §결정7)."

  # ── CFP-2786 보안FIX iter1 — M13 ReDoS-bound regression self-test enroll ──
  # bijection: tests/scripts/*.sh 56 → 57 (신규 self-test enroll — append 시점 재산출). 실행 채널 = semantic-staleness-detection.yml:semantic-staleness-detection-test (기존 hard-fail job 에 신규 step 배선, no continue-on-error).
  - self_test: "tests/scripts/test_check-semantic-staleness-sentinel-redos.sh"
    execution_channel: "workflow:semantic-staleness-detection.yml:semantic-staleness-detection-test"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "semantics-change sentinel PATH_TOKEN_RE ReDoS-bound perf regression — 소스 PATH_TOKEN_RE 동적 추출 후 64KB all-dots findall timing < 2.0s assert(수정본 0.002s / O(n²) 취약본 회귀 시 threshold 초과 RED). correctness M1-M12(별 self-test file)와 disjoint 타이밍 축(CI-load flakiness 격리, baked-in 취약 revert 실행 0). soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. honest-ceiling ADR-151 §결정7 — presence≠truth."

  # ── CFP-2799 (Epic #2696 B(#2698) gray-zone 완결) — death-marker/amendment oracle 확장 discriminating self-test enroll ──
  # bijection: tests/scripts/*.sh 57 → 58 (신규 self-test enroll — append 시점 재산출). 실행 채널 =
  #   decision-record-disposition-test.yml:decision-record-disposition (기존 hard-fail job 에 신규 step 배선, no continue-on-error).
  #   byte-identical template pair(.github ↔ templates), wrapper-self-only(job-level if:), non-required day-1 hard-fail.
  - self_test: "tests/scripts/test_decision-record-disposition-deathmarker.sh"
    execution_channel: "workflow:decision-record-disposition-test.yml:decision-record-disposition"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "death-marker/amendment oracle 확장 discriminating self-test — 실 소스 monkeypatch 양방향 mutation-kill(M1 under-detect·M2 over-detect candidate / M3 amendment_log-off dated region 붕괴 / M4 bare-xref dated-force AC-11 / M5 census-dispatch-off / M6 #2698 exclusion-off AC-6) + baseline GREEN(death candidate·census pl_review_bucket·no-blind-apply·dated 보존·delete-gap seal SecurityArch P1) + positive-control(bare-xref NOT dated / sunset_justification NOT death / INV-R2 byte-unchanged) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (wrapper-self plugin-meta-na, oracle 확장 판별력 결박; 검출력=review-tier honest-ceiling, presence ≠ truth, ADR-151 §결정7 — death staleness 레지스트리 부재로 mechanical 완전분류 미주장)."

  # -- CFP-2803 (ADR-141 Amendment 4) — fable roster-integrity §8 게이트 discriminating self-test enroll --
  # bijection: tests/scripts/*.sh +1 (신규 enroll). 실행 채널 = fable-roster-integrity.yml:fable-roster-integrity-test
  #   (전용 hard-fail job, no continue-on-error, if: mclayer/plugin-codeforge). byte-identical template pair(.github ↔ templates).
  - self_test: "tests/scripts/test-check-fable-roster-integrity.sh"
    execution_channel: "workflow:fable-roster-integrity.yml:fable-roster-integrity-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "fable tier roster-integrity §8 discriminating self-test — GREEN(canonical 10 fable / SecurityTestPL opus / census 41 dist 7-10-10-14) 대 RED 4-mutation(count=9 / exclusion / census=38 preset-drop / bijection non-target) exit-1 판별 + real-repo find-enumeration 41 liveness. soak=G2 / DAST=G5 / real-render=§8.7 runtime 축 disjoint (정적 tier-분배 lint, wrapper-self plugin-meta-na). honest-ceiling(ADR-151 §결정7): presence(tier 값 배치) != truth(실 carve-out 기능성), 검출력=G3 미강제."

  # -- CFP-2813 (ADR-078 Amd3 + ADR-112 Amd1) — living-architecture-update 게이트 discriminating self-test enroll --
  # bijection: tests/scripts/*.sh +1 (신규 enroll). 실행 채널 = living-architecture-update.yml:living-architecture-update-test
  #   (전용 hard-fail job, no continue-on-error, if: mclayer/plugin-codeforge). byte-identical template pair(.github ↔ templates).
  - self_test: "tests/scripts/test_check-living-architecture-update.sh"
    execution_channel: "workflow:living-architecture-update.yml:living-architecture-update-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    identity_bearing: false
    g_boundary_check: "living-architecture-update 게이트 discriminating self-test (ADR-154 §결정 7) — RED(missing-update/mapping-miss/invalid-declare/unknown-surface 4-category fixture) exit 1 vs clean GREEN exit 0 대조 + M1-M8 mutation KILL(marker-regex·presence-judgement·frontmatter-gaming·glob-noop·mapping-fail·exit2-silent·stoplist·classify-degrade) 전수 검증 + ReDoS 시간-상한 회귀(CWE-1333 polynomial quantifier) + invariant 5종(struct=0→0·mapping-never-PASS·frontmatter-never-ok·unparseable→2·bypass-no-silent) + F-4 repo-root bijection(전수 분류) + scanned-N trace emit. soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 CI lint gate, wrapper-self plugin-meta-na). identity_bearing:false (dispatch/routing 식별자 아님 — no-impact marker 수집이 attack surface 아님). honest-ceiling(ADR-151 §결정7): presence(marker schema·regex bounded·rationale floor) != truth(실 no-impact 근거 타당성·semantic drift=L3 review-tier), discriminating 검출력=G3 미강제."

  # -- CFP-2828 (ADR-081 Amendment 14 §결정 D15) — codex-review out.json AC-6 소비 재검증 helper discriminating self-test enroll --
  # bijection: tests/scripts/*.sh +1 (신규 enroll). 실행 채널 = codex-companion-timeout-check.yml:codex-companion-timeout-presence
  #   (기존 warning-tier job 에 신규 step 배선, sibling test_check-codex-companion-timeout-presence.sh 인근). byte-identical template pair(.github ↔ templates).
  - self_test: "tests/scripts/test_check_codex_review_output_schema.sh"
    execution_channel: "workflow:codex-companion-timeout-check.yml:codex-companion-timeout-presence"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "codex-review out.json AC-6 소비 재검증 helper(scripts/lib/check_codex_review_output_schema.py) discriminating self-test — negative 6종(free-form/파일부재/truncated/verdict enum-out/counts↔findings cross-field mismatch/category∉enum) → exit 1 inconclusive(+stdout sentinel 병행 assert) + positive 2종 → exit 0 + 취약 revert cross-field KILL(vacuous-green 방지). soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (wrapper-self plugin-meta-na, 정적 schema 소비 재검증 판별력 결박). honest-ceiling(ADR-151 §결정7): presence(재검증 5단계 형식 통과) != truth(실 리뷰 verdict 정확성=L3 review-tier), 검출력=G3 미강제."

  # ── CFP-2812 (ADR-167 adr-amendment-compaction-ratchet / ADR-060) — ADR amendment 누적 임계 + parity 게이트 discriminating self-test enroll (2건) ──
  # bijection: tests/scripts/*.sh 60 → 62 (신규 self-test 2 enroll). 실행 채널 = adr-amendment-threshold-test.yml:adr-amendment-threshold-test
  #   (byte-identical .github + templates pair, wrapper-self only, non-required day-1 hard-fail — branch protection 8-tuple 무변경).
  #   단일 job 이 두 self-test(threshold/parity)를 각각 run: 배선. run-line: `bash tests/scripts/test_adr-amendment-threshold.sh` / `bash tests/scripts/test_adr-amendment-parity.sh`.
  - self_test: "tests/scripts/test_adr-amendment-threshold.sh"
    execution_channel: "workflow:adr-amendment-threshold-test.yml:adr-amendment-threshold-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "ADR amendment 누적 임계 게이트 discriminating self-test (순수 함수 6종 python-level fixture: dual-key 합산·effective=max·BVA 4분기 N=10·marker presence/consistency + 게이트 subprocess fork mutation-kill 4종[임계검사-제거/경계-off-by-one/baseline-미등재-세탁/first-key-wins-회귀] 정본 RED↔변이본 GREEN + census distinct-marker 병행 + fail-closed(D9) malformed RED + AC-2 산식↔baseline 동일성 + AC-8 count0 GREEN·dangling B-2 RED) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 governance lint self-test, wrapper-self plugin-meta-na). honest-ceiling(ADR-151 §결정7): presence(임계 산식 판정·marker 타입) ≠ truth(재해석 의미 판정=L3 review-tier), 검출력=G3 미강제."

  - self_test: "tests/scripts/test_adr-amendment-parity.sh"
    execution_channel: "workflow:adr-amendment-threshold-test.yml:adr-amendment-threshold-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "heading↔fm parity + 재해석 marker discriminating self-test (check_parity/check_marker_presence python-level fixture: heading-only RED[F-1]·fm≥heading GREEN·marker 미기재/비-boolean RED·boolean GREEN + pure-function mutation-kill 3종[parity-검사-제거/marker-타입검사-회귀/marker-presence-제거] 정본 RED↔변이본 GREEN + orchestration fail-closed malformed RED·clean GREEN 비-vacuous census) — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 governance lint self-test, wrapper-self plugin-meta-na). forward-only 정직 천장(D4): orchestration heading-only 검출은 merge-base delta 소관(git 미사용) — 검출력 discriminating 은 순수 함수 mutation-kill 로 결박. honest-ceiling(ADR-151 §결정7): presence ≠ truth, 검출력=G3 미강제."

  # ── CFP-2822 Phase 2 세션 잔재 수명 규약 GC self-tests (ADR-169, §8-3.4 enroll) ─────
  # 실행 채널 = worktree-completion-gate.yml:worktree-completion-gate (warning-tier,
  #   continue-on-error, non-required — branch protection 8-tuple 무변경). 신규 9 + relocate 2.
  #   ★ 판정-3(§8-3.4): 기존 미등재 2건(backstop-wire/completion-clean)을 scripts/ → tests/scripts/
  #     relocate + enroll (동시보정). scripts/ 원본 물리 제거 = Developer(scripts/ write boundary) 소관.
  - self_test: "tests/scripts/test_check-worktree-gc-e10-race.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "E10 double-delete race self-test (mkdir 원자 lock 3중방어 — Case A 무경쟁 prune 1·remove-syscall 1 / Case B 살아있는 lock 선점→SKIP·pruned 0·remove 0 discriminating[lock load-bearing] / Case C 2 동시발화→remove-syscall==1[double-delete 0]·합계 pruned 1·INV-5 DONE 마커). concurrency oracle = 실 remove syscall 계측(하드코딩 아님). soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (single-pass interleaving, temporal soak 아님). honest-ceiling(ADR-151 §결정7): presence(lock 직렬화 판별) ≠ truth(전 interleaving 봉인=review), 검출력=G3 미강제."
  - self_test: "tests/scripts/test_check-orphan-inv1-classify.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "INV-1 5종 보존(dirty/unpushed-N/locked/pin/INCONCLUSIVE) 각 양방향 변별[보존조건→KEEP+사유 ∧ 조건해제→REMOVE] + AC-12 3축 분류(등록·존재≠보존사유·3-case·INCONCLUSIVE→보존) discriminating self-test. 실 git repo fixture(Windows subprocess shebang 한계 회피) + 자동 unlock/force 금지 정적 확인. soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 판정 함수 검증). honest-ceiling(ADR-151 §결정7): presence(judge 신호 판정) ≠ truth, 검출력=G3 미강제."
  - self_test: "tests/scripts/test_check-scratch-ttl-ac5.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "AC-5 scratch TTL purge 양방향 변별(loose age>TTL→PURGED 실삭제 ∧ .git 보유→REFER-ORPHAN 존치 ∧ young/log 존치 ∧ DRY_RUN would-purge). _scratch_root in-process monkeypatch 로 실 홈 무오염 격리(중대). soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. honest-ceiling(ADR-151 §결정7): presence(TTL 판정·6-guard) ≠ truth, 검출력=G3 미강제."
  - self_test: "tests/scripts/test_check-harness-temp-ac6-inv9.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "AC-6/INV-9 harness Temp 관측기 삭제 syscall 0 self-test(관측 전/후 파일 bijection 일치 → 삭제 0 강 입증 + git-aware PRESERVE(temp-git-worktree) + §8.10 dark-path TEMP_GC_DELETE_ENABLED off→STAGE2 미발화). --temp-root 격리(실 Temp 무접촉). soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. honest-ceiling(ADR-151 §결정7): presence(observe-only 삭제 0) ≠ truth, 검출력=G3 미강제."
  - self_test: "tests/scripts/test_check-location-guard-ac10.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "AC-10 위치 가드 self-test(표준 통과 ∧ 표준밖 위반 + §8.10 warn→block 스위치 discriminating[동일 nonstandard cmd exit 0↔2 반전=tier load-bearing] + bypass audit). 한글·공백 cmd 는 argv/heredoc mangling 회피 위해 파일 채널 + cygpath -m 로 MSYS 경로변환 비대칭 회피. soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. honest-ceiling(ADR-151 §결정7): presence(경로 판정·tier 분기) ≠ truth(완전차단 불가 정직=T-GUARD), 검출력=G3 미강제."
  - self_test: "tests/scripts/test_check-residue-discovery-ac8-11-15.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "AC-8 재알림 dedup/backoff BVA(임계±1·2회 dedup·7d→14d 지수 backoff) + AC-11 미등록 orphan input-driven count(1↔0 변별) + AC-15 용량 임계 BVA(임계±1·회수 강행 0) discriminating self-test. now 주입 결정론 + aging JSONL 상태 monkeypatch 로 실 홈 무오염. soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (backoff = 논리적 시간 주입, wall-clock soak 아님). honest-ceiling(ADR-151 §결정7): presence(BVA 발화 판정) ≠ truth, 검출력=G3 미강제."
  - self_test: "tests/scripts/test_check-stash-census-ac14.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "AC-14 stash census self-test(count=N 실 git 계측 ∧ 자동 삭제 0[census 전/후 stash 개수 불변] ∧ multi-repo dedup ∧ aging age>임계 재알림 ∧ non-git INCONCLUSIVE). old stash = GIT_COMMITTER_DATE 절대 epoch(TZ-독립). soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. honest-ceiling(ADR-151 §결정7): presence(집계·삭제 0) ≠ truth, 검출력=G3 미강제."
  - self_test: "tests/scripts/test_check-orphan-sanitize.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "§7.5 sanitize/redaction discriminating self-test(benign forward-slash/backslash 경로→relativized 정보 보존[REDACTED 금지] ∧ ghp_+36·AKIA+16→obliterate[raw 미출현 leak 0] ∧ control-char strip 양방향). secret leak = raw 토큰 substring 부재로 강 입증. soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (정적 마스킹 계약). honest-ceiling(ADR-151 §결정7): presence(정보보존↔obliterate 변별) ≠ truth(전 secret 형식 봉인 불가=capture-time redact 1차 방어), 검출력=G3 미강제."
  - self_test: "tests/scripts/test_check-residue-born-broken.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "born-broken 5종 회피 discriminating self-test((1)중첩 3-level 재귀 도달[nested-only size 집계] (2)input-driven count[1↔2 bijection, 하드코딩 아님] (3)한글·공백 경로 처리[실 git 채널] (4)fixture newline=\\n 저작[raw CR 0] + round-trip 복원 (5)TZ 독립[age(UTC)==age(KST) 절대 epoch]). soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. honest-ceiling(ADR-151 §결정7): presence(회피 메커니즘 작동) ≠ truth, 검출력=G3 미강제."
  - self_test: "tests/scripts/test-worktree-backstop-wire.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "CFP-2822 §8-3.4 판정-3 relocate(scripts/ → tests/scripts/) — backstop SessionEnd async 단일 wire + 트리거 단일화 invariant + 로직 무변경 회귀 + close lifecycle disjoint (TC-1/TC-2/TC-5/TC-7, hooks.json 정적 mutation-kill + registry warning+workflow:null). 기존 미등재 갭 보정 enroll. soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. honest-ceiling(ADR-151 §결정7): presence(배선 존재) ≠ truth(크래시 발화 여부=harness 런타임 사실 정적 검증 불가), 검출력=G3 미강제."
  - self_test: "tests/scripts/test-check-worktree-completion-clean.sh"
    execution_channel: "workflow:worktree-completion-gate.yml:worktree-completion-gate"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "CFP-2822 §8-3.4 판정-3 relocate(scripts/ → tests/scripts/) — 완료-게이트 F2 대상 판정 계약 변별(sub-worktree 즉시검출 / Story root mergedAt non-null 시만 / open-PR 보존 / gh미인증·dirty fail-safe, TC-3/TC-4/TC-6 git·gh stub 양방향). 기존 미등재 갭 보정 enroll. soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint. honest-ceiling(ADR-151 §결정7): presence(검출 로직) ≠ truth(self-check 호출 강제=비기계 검증), 검출력=G3 미강제."
  # -- CFP-2851 (Phase 2 구현 — CodexReviewAgent dispatch 템플릿 완결성) — dispatch 템플릿 완결성 게이트 discriminating self-test enroll --
  # bijection: tests/scripts/*.sh +1 (신규 enroll). 실행 채널 = codex-companion-timeout-check.yml:codex-companion-timeout-presence
  #   (기존 warning-tier job 에 신규 step 배선, sibling test_check_codex_review_output_schema.sh 인근). byte-identical template pair(.github ↔ templates).
  - self_test: "tests/scripts/test_cfp-2851-dispatch-template-completeness.sh"
    execution_channel: "workflow:codex-companion-timeout-check.yml:codex-companion-timeout-presence"
    channel_status: "alive"
    blocking_tier: "warning_tier"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "CodexReviewAgent.md dispatch 템플릿 완결성 discriminating self-test — AC-1~11 별 fence-interior/변종절 line-shift-robust anchoring(awk) + helper 호출·2-변수 exit 게이트·--uncommitted/--commit 등가 bullet 등 정본 GREEN 대 sed/awk 변이(취약) fixture RED 이중검증(AC-9 제외 전수 discriminating, AC-9=advisory) + EDIT_SHA 동적 앵커(HEAD~1 하드코딩 회피, 후속 커밋 rebase-robust) + pre-edit(git show EDIT_SHA~1) 대조로 additive AC genuine-실패 사후입증. soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (wrapper-self plugin-meta-na, 정적 md 템플릿 완결성 판별력 결박). honest-ceiling(ADR-151 §결정7): presence(grep anchoring 통과) != truth(실 Codex dispatch 런타임 거동=L3 review-tier), 검출력=G3 미강제."
  # -- CFP-2884 (Phase 2 구현 — Codex 프롬프트 3구획 언어경계 + 조립계층 인코딩 위생) — promptfile UTF-8 round-trip fixture suite wrapper enroll --
  # bijection: tests/scripts/*.sh +1 (신규 enroll). 실행 채널 = codex-promptfile-roundtrip-test.yml:codex-promptfile-roundtrip-test
  #   (신규 전용 workflow, day-1 hard-fail·non_required). wrapper 는 동명 .py pytest suite 로 위임 —
  #   직접 pytest run-line 은 AC-2 basename 미등장 RED 이므로 wrapper 경유가 배선 요건 (Change Plan §5 row 7 r9).
  - self_test: "tests/scripts/test_cfp2884_promptfile_encoding_roundtrip.sh"
    execution_channel: "workflow:codex-promptfile-roundtrip-test.yml:codex-promptfile-roundtrip-test"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "promptfile UTF-8 round-trip fixture suite (AC-4 fixture 5종 + AC-6 공존 oracle + whitelist mutation) 의 pytest 위임 wrapper — 단발 실행, soak=G2·DAST=G5·real-render=§8.7 runtime 축 무침범."

  # ── CFP-2963 (ADR-175) — hollow-gate-corpus 메타-게이트 재귀 self-test (dedicated workflow step, alive) ──
  # 인벤토리 bijection: tests/scripts/*.sh 76 → 77 (신규 self-test enroll, firsthand 실측 pre-append = 76 ↔ 76 balanced).
  #   이 self-test 파일은 동 PR sibling 워커(엔진 lane)가 동시 착지 중 — enroll 은 선행하되 파일 생성 전까지
  #   selftest-execution-liveness 게이트가 record→missing-file 로 AC-1a FAIL 하는 것이 정상(중간 상태)이며,
  #   DevPL 이 워커 합류 후 최종 검증한다. two-meta-gate mutual cross-seal(ADR-151 인벤토리 ↔ ADR-175 corpus).
  - self_test: "tests/scripts/test_check-hollow-gate-corpus.sh"
    execution_channel: "workflow:hollow-gate-corpus-test.yml:hollow-gate-corpus-selftest"
    channel_status: "alive"
    blocking_tier: "non_required"
    discriminating_fixture: "present"
    l2_full_scope: "N/A"
    manual_reason: ""
    g_boundary_check: "corpus 표본 동적 실행 분류 메타-게이트(landed 셸/py 게이트의 LIVE vs HOLLOW 판정)의 self-test — soak=G2·DAST=G5·real-render=§8.7 runtime 축 disjoint (wrapper-self plugin-meta-na; 본 채널은 corpus 표본 1회 실행 분류이지 지속-liveness·부하·실렌더 축이 아님). 판별력 결박 5축: ① 2-arm 전건 AND(arm-L LIVE ∧ arm-H HOLLOW 동시 성립 시에만 PASS — arm-L 대조군을 빼면 '전부 HOLLOW 로 찍는 무조건-true 판정기'가 만점을 받으므로 대조군 leg 자체가 판별력의 일부) ② blinded probe flip(모든 커밋 좌표에서 arm-L 과 일치하고 기대 verdict 만 반대인 run-time materialize 입력 — 커밋 좌표만의 함수인 판정기는 정의상 한쪽에서 틀린다) ③ 축-disjoint 대조 leg(arm 라벨 역산 채널 차단: classification[] 판정기 미투입 + 표본 식별자 arm 어휘 denylist + 평면 배치 + exec-tree blinding) ④ census 축별 하한(N_gates/N_armL/N_armH/N_probe/N_detected/N_flip 감소 = RED, N_indeterminate 만 상한 — 축별 대조라 총 N 불변인 채 arm 간 상계로 판별력이 빠지는 경로가 닫힌다) ⑤ mutation-kill(판정 경로 변이 주입 시 RED 도달 실증). honest-ceiling(ADR-151 §결정 7 / ADR-175 상속): corpus 표본에 대한 관측 기반 verdict 까지만 보장 — corpus 밖 게이트 일반으로 외삽하지 않음(표본 밖 hollow 게이트 = 미검출 잔존, declared FN). 판별자 = 마커 문면이지 프로세스 rc 아님. presence ≠ truth."
