# docs/hollow-gate-corpus-baseline.yaml — GENERATED by scripts/check-hollow-gate-corpus.sh
# mode=init-baseline (수기 편집 = content_digest 불일치 = exit 3 substrate-failure).
# 재생성 경로 2종 ONLY: --init-baseline --reason TEXT (파일 부재 시에만) /
#   --allow-baseline-shrink --reason TEXT (기존 파일 재기록, 사유 각인).
# `--write-baseline` 형 자기 하향 재생성 경로 부재 (ADR-175 명시 금지).
# content_digest 산식 = sha256("schema_version=1.0\n" + "<axis>=<value>\n"*7 + "reason=<reason>\n").
schema_version: "1.0"
axes:
  N_gates: 1
  N_armL: 1
  N_armH: 1
  N_probe: 1
  N_detected: 2
  N_flip: 1
  N_indeterminate: 0
reason: "day-1 bootstrap (CFP-2963 corpus 착지 · s01 arm-L / s02 arm-H / p01 probe)"
content_digest: "sha256:3cf348bab24eef85d1652c6db342a8df42dca5111b61cb7962269f3811dfe925"
