# docs/infra-resource-baseline.yaml — GENERATED by scripts/lib/check_infra_resource_drift.py --write-baseline (CFP-2700 / ADR-157)
# DO NOT EDIT BY HAND. Regenerate: bash scripts/check-infra-resource-drift.sh --repo-root . --write-baseline
# grandfather = 승격 시점 pre-existing 미선언 infra 표면(file, env-key) 동결 → new-only subtract (ADR-060 §결정6 monotonic shrink). 신규 미선언 유입만 flag. candidate/inert census 무영향.
schema_version: '1.0'
generated_by: CFP-2700
basis: ADR-157 §결정7(a) FM1-debt 승격 시점 pre-existing undeclared infra surface(file, env-key) 동결
grandfathered_undeclared_surfaces:
- file: .claude/_overlay/project.yaml
  env_key: ATLASSIAN_USER_EMAIL
  reason: pre-existing 미선언 (CFP-2700 baseline snapshot grandfather — 추후 manifest 등재로 shrink)
- file: scripts/canary_auto_promote.py
  env_key: GH_TOKEN
  reason: pre-existing 미선언 (CFP-2700 baseline snapshot grandfather — 추후 manifest 등재로 shrink)
- file: scripts/lib/audit_trail_pii_redact.py
  env_key: AUDIT_PII_KEY
  reason: pre-existing 미선언 (CFP-2700 baseline snapshot grandfather — 추후 manifest 등재로 shrink)
- file: scripts/lib/check_deferred_item_recovery.py
  env_key: GH_TOKEN
  reason: pre-existing 미선언 (CFP-2700 baseline snapshot grandfather — 추후 manifest 등재로 shrink)
