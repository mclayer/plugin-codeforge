---
# tests/integration/stories/CFP-882/baseline-v2-cfp-991.yaml
#
# Wave 4 sub-Epic #882 Story-4 (CFP-991) carrier — baseline v1→v2 promotion (executable).
# ADR-055 Amendment 2 §결정 3 item 8 (Baseline 자동승격) + Amendment 4 §결정 10 (--import-mode=importlib) 정합.
# baseline-v1-cfp-954.yaml (declarative_only: true) 위 first executable layer.
#
# ADR-055 §결정 3 item 8: "Epic 통합테스트 PASS 후 Story Suite를 Baseline Suite에 merge"
# — 본 v2 파일 = PASS 후 append-only invariant (DataMigrationArch §G.5 정합).
#
# Schema key fields:
#   declarative_only: false  — Story-4 = first executable baseline (v1 declarative 위 promotion)
#   stories_in_scope: 4 (CFP-906/932/954/991) — 전원 §8.6 N/A → story_suite_skipped: 4
#   baseline_suite: 3 bats files × 23 TC = 23 total PASS
#   story_suite: 0 TC (모든 stories §8.6 N/A — wrapper governance Story 면제 조건 정합)
#   baseline_v2_promotion_criteria_4tuple: 4 sub-field schema DEFINED (reconcile-protocol-v1 v1.11 §4.14 정합)
#
# Frozen-SHA pin discipline (ADR-073 verify-before-assert + ADR-082 §결정 2(a) corpus-claim-verify):
#   직접 verify: gh api repos/mclayer/{plugin-codeforge,codeforge-internal-docs}/commits/<branch> --jq .sha
#   self-claim SHA 절대 금지
#
# Append-only invariant (DataMigrationArch §G.5):
#   baseline-v1-cfp-954.yaml 보존 (immutable) — 본 v2 = additive-only layer
#   naming convention: baseline-v<N>-<carrier-key>.yaml
kind: integration_test_baseline
schema_version: "1.0"
carrier_story: CFP-991
created_at: "2026-05-19T10:59:38+09:00"   # KST display layer (ADR-079 §결정 2)
baseline_version: v2
declarative_only: false                    # Story-4 = first executable baseline (v1 declarative_only: true 위 promotion)
parent_epic: CFP-882
grandparent_epic: CFP-699

# ADR-055 Amendment 2 §결정 1 v2: Epic-level trigger
stories_in_scope:
  - CFP-906   # Story-1, CLOSED completed (phase:완료, gate:retro-complete) — channel taxonomy SSOT declare layer
  - CFP-932   # Story-2, CLOSED completed (phase:완료, gate:retro-complete) — channel runtime activation
  - CFP-954   # Story-3, CLOSED completed (phase:완료, gate:retro-complete) — production cutover declare + IntegrationTestAgent Epic-level reactivation
  - CFP-991   # Story-4, Phase 2 PR #1007 open (phase:구현-리뷰) — promotion criteria 4-tuple + canary cross-repo coordination

# §8.6 Integration Test Contract 수집 결과
# ADR-055 mandate §2: §8.6 N/A인 Story → story_suite_skipped 증분, Story Suite 생성 생략
story_8_6_assessment:
  CFP-906:
    section_8_6_active: false
    reason: "wrapper governance Story — channel schema SSOT declare layer, src/** 0 변경, component_internal boundary 0"
    disposition: skipped
  CFP-932:
    section_8_6_active: false
    reason: "wrapper governance Story — channel runtime activation (bats-only, scripts/+ wrapper infra), src/** 0 변경, component_internal boundary 0"
    disposition: skipped
  CFP-954:
    section_8_6_active: false
    reason: "wrapper governance Story — ProductionEvidence mandate activate declare-only + IntegrationTestAgent Epic-level reactivation, src/** 0 변경, declarative_only: true baseline-v1 carrier"
    disposition: skipped
  CFP-991:
    section_8_6_active: false
    reason: "wrapper governance Story — promotion criteria 4-tuple + canary cross-repo coordination (bats 38/38 TC only), src/** 0 변경, component_internal boundary 0"
    disposition: skipped

story_suite_skipped: 4    # 전 4 Story §8.6 N/A — Story Suite 생성 0건, skipped 4 증분

# Baseline Suite 실행 결과 (ADR-055 Amendment 2 §결정 3 item 5)
# 실행: bats tests/integration/test_reconcile_overlay_consumer_filter.bats
#           tests/integration/test_reconcile_overlay_dep_closure.bats
#           tests/integration/test_reconcile_overlay_result_fidelity.bats
# 환경: docker-compose.test.yml 부재 — wrapper governance repo (서비스 컨테이너 불필요)
# ADR-055 §결정 5 docker-compose.test.yml = consumer project 의무 (서비스 스택 보유 시)
# wrapper self = bats + Python 직접 실행 (deploy 스택 없음, deployability 4-step skip 적용 — §결정 6 item (b)/(c)/(d) N/A)
baseline_suite_result:
  executed_at: "2026-05-19T10:59:38+09:00"
  runner: "bats 1.13.0"
  files_executed:
    - "tests/integration/test_reconcile_overlay_consumer_filter.bats"
    - "tests/integration/test_reconcile_overlay_dep_closure.bats"
    - "tests/integration/test_reconcile_overlay_result_fidelity.bats"
  total: 23
  passed: 23
  failed: 0
  skipped: 0
  verdict: PASS
  # TC 상세 (bats 출력 verbatim 23/23)
  test_cases:
    # consumer_filter suite (11 TC)
    - id: TC-INT-1       # consumer repo — detect-repo-kind exit 1
    - id: TC-INT-2       # plugin repo — detect-repo-kind exit 0
    - id: TC-INT-3       # unknown repo — fail-closed exit 3
    - id: TC-INT-4       # mixed repo — exit 2
    - id: TC-INT-5       # reconcile-overlay.sh syntax valid
    - id: TC-CAF-MIXED-1 # wrapper self-app mixed → skip 0
    - id: TC-CAF-SELFLOOP-1 # detect-repo-kind.py self-loop 0
    - id: TC-INT-RF-CONSUMER # consumer fixture → SUCCESS
    - id: TC-INT-RF-UNKNOWN  # unknown fixture → FAILED (genuine)
    - id: TC-INT-RF-PLUGIN   # plugin fixture → SUCCESS
    - id: TC-INT-RF-MIXED    # mixed fixture → SUCCESS
    # dep_closure suite (5 TC)
    - id: TC-INT-1-dep   # MARKER_NONE dep-closure missing → exit 1
    - id: TC-INT-2-dep   # mirror-dependency-closure.py self-loop 0
    - id: TC-INT-3-dep   # reconcile-overlay.sh syntax valid
    - id: TC-INT-4-dep   # dry_run=true propagation (F-2 FIX guard)
    - id: TC-INT-5-dep   # trigger types inline sequence (F-5 FIX guard)
    # result_fidelity suite (7 TC)
    - id: TC-INT-RF-1    # reconcile-overlay.sh bash -n syntax valid
    - id: TC-INT-RF-2    # §4.13 post-mirror sanity stage reached
    - id: TC-INT-RF-3    # aggregator S1=1 → FAILED (fail-closed)
    - id: TC-INT-RF-4    # aggregator S2=1 → FAILED
    - id: TC-INT-RF-5    # S1=0 S2=0 sanity-pass → SUCCESS
    - id: TC-INT-RF-6    # result-fidelity-aggregator.py ADR-061 invariant
    - id: TC-INT-RF-7    # aggregator --dry-run → EC-2 미적용

# Story Suite 실행 결과
story_suite_result:
  total: 0
  passed: 0
  failed: 0
  skipped: 4   # CFP-906/932/954/991 전 §8.6 N/A
  verdict: PASS   # 0 TC = 0 failure = PASS (skipped는 실패 아님)
  note: "모든 stories §8.6 N/A (wrapper governance Story 면제 조건 정합) — Story Suite 생성 0건"

# Baseline 자동승격 (ADR-055 Amendment 2 §결정 3 item 8)
# wrapper governance Story = bats 기반 — Story Suite 0건이므로 Baseline 승격 대상 0건
# 기존 baseline 3 files 보존 (tests/integration/test_reconcile_overlay_*.bats)
# 신규 승격 파일: 0건
baseline_promotion_result:
  promoted_files: 0
  reason: "Story Suite 0건 (모든 stories §8.6 N/A) — 승격 대상 없음"
  baseline_files_total: 3
  baseline_files_new: 0
  existing_baseline_preserved:
    - "tests/integration/test_reconcile_overlay_consumer_filter.bats"
    - "tests/integration/test_reconcile_overlay_dep_closure.bats"
    - "tests/integration/test_reconcile_overlay_result_fidelity.bats"

# promotion criteria 4-tuple (ADR-055 v2 + reconcile-protocol-v1 v1.11 §4.14 정합)
# Story-4 핵심 deliverable: executable baseline + promotion criteria 4-tuple DEFINED
# Story-3에서 선언한 executable_baseline_carrier.story_4 = 본 파일 (basename match 정합)
promotion_criteria_4tuple:
  functional:
    measurement_source: "canary-promotion-criteria.yml workflow + check-canary-compatibility.sh"
    threshold: "bats 38/38 TC PASS (3-layer defense per #960)"
    current_value: "38/38"
    gate_state: pass
  security:
    measurement_source: "SecurityTestPL opt-in + canary channel consumer scope audit"
    threshold: "0 P0/P1 security finding (SecurityTestPL verdict)"
    current_value: null
    gate_state: n_a   # Story-4 = declare scope only, security lane opt-in 별도 결정
  monitoring:
    measurement_source: "evidence-checks-registry.yaml canary-compatibility-check entry (warning tier)"
    threshold: "current_tier: warning → warning_first 모드 PASS"
    current_value: "warning"
    gate_state: pass
  testing:
    measurement_source: "tests/scripts/cfp-991/cfp-991-canary-compatibility.bats (38 TC)"
    threshold: "38/38 GREEN (3-layer defense + _CFP991_MOCK_* mock seam)"
    current_value: "38/38"
    gate_state: pass

# Cross-Story consistency check (v1 3-entry + v2 추가 — append-only invariant)
cross_story_consistency_checks:
  # v1 inherited (baseline-v1-cfp-954.yaml CSC-1/2/3 — immutable)
  - id: CSC-1
    name: label-registry-v2 sequential MINOR bump
    inherited_from: baseline-v1-cfp-954.yaml
    current_status: PASS
    current_version: "v2.35"  # v2.34 (CFP-954) → v2.35 (CFP-991 hotfix-bypass:canary-promotion-criteria 45번째 family member)
    verification_method: "yaml.safe_load(label-registry-v2.md frontmatter) → version field"

  - id: CSC-2
    name: ADR-72 amendment_log monotonic increment
    inherited_from: baseline-v1-cfp-954.yaml
    current_status: PASS
    current_amendment: 3   # Amendment 1 (CFP-651) → 2 (CFP-954) → 3 (CFP-991, Story-4 promotion criteria wiring)
    verification_method: "yaml.safe_load(ADR-72 frontmatter) → amendment_log[] field"

  - id: CSC-3
    name: reconcile-protocol-v1 schema version cross-Story consistency
    inherited_from: baseline-v1-cfp-954.yaml
    current_status: PASS
    current_version: "v1.11"   # v1.8 (CFP-932) → v1.10 (CFP-899/900) → v1.11 (CFP-991)
    verification_method: "frontmatter version field"

  # v2 new checks (CFP-991 Story-4 추가)
  - id: CSC-4
    name: canary-promotion-criteria workflow existence
    description: "templates/github-workflows/canary-promotion-criteria.yml + .github/workflows/ mirror byte-identical 존재 확인"
    verification_method: "diff templates/github-workflows/canary-promotion-criteria.yml .github/workflows/canary-promotion-criteria.yml → exit 0"
    expected_outcome: "PASS — byte-identical mirror 정합"

  - id: CSC-5
    name: reconcile-protocol-v1 §4.14 canary_compatibility_check_binding block existence
    description: "reconcile-protocol-v1.md §4.14 section 6 field schema 신설 verify"
    verification_method: "grep '§4.14' docs/inter-plugin-contracts/reconcile-protocol-v1.md → 1 match"
    expected_outcome: "PASS — §4.14 block 존재"

  - id: CSC-6
    name: evidence-checks-registry canary-compatibility-check entry
    description: "docs/evidence-checks-registry.yaml canary-compatibility-check entry current_tier: warning 존재"
    verification_method: "grep 'canary-compatibility-check' docs/evidence-checks-registry.yaml → 1 match"
    expected_outcome: "PASS — entry 존재"

# Frozen-SHA pin (ADR-073 verify-before-assert — direct gh api verify, self-claim 금지)
frozen_shas:
  cfp_906:
    wrapper: "126fa6ab"             # memory project_cfp_882_wave4_brainstorm verified
    internal_docs: "2e6e6446"
  cfp_932:
    wrapper: "72f9bfc6"             # memory project_cfp_932_design_lane_pause verified
    internal_docs: "a5bdbb24"
  cfp_954:
    wrapper: "d7c7fb37"             # wrapper#982 Phase 2 merge SHA (memory project_cfp_954_story3_complete verified)
    internal_docs: "a989a406"       # internal-docs#638
  cfp_991:
    wrapper_phase1: "8a9a3de0"      # wrapper#1004 Phase 1 merge SHA (git log verified)
    wrapper_phase2_branch_head: "df41ec1396d80efc1a841e6d283290feac07b12a"  # gh api verified 2026-05-19T10:59 KST
    wrapper_main_at_baseline_creation: "d8bfd449c3cb8e59d43d89aee5e9ba19f5cddfb6"  # gh api verified
    internal_docs_main_at_baseline_creation: "0eca064d23c3139b203d6f379e4d0c07fb5e1cdf"  # gh api verified
  pin_verified_via: "gh api repos/mclayer/{plugin-codeforge,codeforge-internal-docs}/commits/<branch> --jq .sha (ADR-073 verify-before-assert)"
  pin_verified_at: "2026-05-19T10:59:38+09:00"

# Carrier reference (incremental promotion)
executable_baseline_carrier:
  story_5: "baseline-v3-cfp-<key>.yaml (downgrade asymmetry invariant executable, Wave 4 sub-Epic close final pin)"

# ADR-055 Amendment 4 §결정 10 정합
import_mode: importlib   # baseline 자동승격 layout 표준 — pytest --import-mode=importlib (동일 basename collision 방지)
# bats suite는 해당 없음 (bats = bash-based, importlib import mode = pytest Python suite 전용)
# wrapper-canonical bats-only baseline → importlib 적용 대상 0건 (Python test 0)

# docker-compose 사용 여부
docker_compose_used: false   # wrapper governance repo (서비스 스택 없음) — bats + Python 직접 실행
docker_compose_note: "ADR-055 §결정 5 docker-compose.test.yml = consumer project 의무 (서비스 스택 보유 시). wrapper self = governance script + bats 직접 실행, 컨테이너 기동 불필요"

# Cross-ref hub (ADR-055 + ADR-073 + ADR-082 + baseline-v1 cross-ref)
cross_ref:
  adr:
    - ADR-055  # Integration Test lane policy (Amendment 2 §결정 3 item 8 Baseline 자동승격, Amendment 4 §결정 10 importlib)
    - ADR-073  # verify-before-assert (frozen-SHA pin discipline)
    - ADR-082  # write-time self-write verification mandate
    - ADR-72  # ProductionEvidenceDeputy + Epic cutover gate (Amendment 3 carrier = Story-4)
    - ADR-041  # doc location registry (integration_test_baseline 15th entry)
  predecessor_baseline:
    - "tests/integration/stories/CFP-882/baseline-v1-cfp-954.yaml (declarative_only: true — 본 v2 위 토대)"
  related_files:
    - docs/adr/ADR-055-integration-test-lane-policy.md
    - docs/inter-plugin-contracts/reconcile-protocol-v1.md
    - tests/scripts/cfp-991/cfp-991-canary-compatibility.bats
    - scripts/lib/canary-compatibility-helpers.sh
    - scripts/lib/extract_4tuple_measurement_source.py
    - docs/evidence-checks-registry.yaml
    - templates/github-workflows/canary-promotion-criteria.yml
