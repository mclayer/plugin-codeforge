---
# TC-10: parallel_anchors_checked is a string (non-array type — AC-14)
# Expected: exit 1 (WARNING) — "field type mismatch, expected array got string"
pl_recommendation: FIX
findings:
  - id: F-008
    severity: P1
    category: enum_closure
    description: "Missing enum branch coverage"
    file_line: "src/handler.py:77"
    parallel_anchors_checked: "absent"
