#!/usr/bin/env bash
# hgsv-enroll (hard gate self-test fixture subject)
# positive-control: sanity mutant→RED (결함앞 RED 상시 증명)
run_gate clean; rc=$?
run_gate mutant; mrc=$?
if [ "$rc" -eq 0 ]; then echo clean-ok; fi
if [ "$mrc" -ne 0 ]; then echo mutant-red; fi
