# Fixture: PASS form 1 — git -C cwd directive

Story content with Lane PL spawn + form 1 cwd directive.

## §14 Lane Evidence

### ArchitectPLAgent spawn (test fixture)

spawn command:
```bash
git -C C:/Users/mccho/.claude/worktrees/plugin-codeforge/cfp-test ls
ArchitectPLAgent spawn with verify-via-direct-Read
```

verify-via output (PASS).
