# Fixture 01 — Positive PASS (minimal valid registry)
# Story §8.6.1 #1 — Positive PASS scenario, exit 0
# 1 entry with all required fields + valid current_tier + valid bypass pair + valid ADR cross-ref
schema_version: "1.1"
introduced_by: CFP-455-test
last_updated: 2026-05-12
entries:
  - name: fixture-positive-entry
    description: |
      Test fixture — minimal valid entry for positive PASS validation.
    detect_command: bash scripts/test-detect.sh
    workflow: templates/github-workflows/test-detect.yml
    current_tier: warning
    bypass_label: hotfix-bypass:fixture-positive
    bypass_audit_lint: bash scripts/check-bypass-audit-comment.sh
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
    introduced_by: CFP-455-test
    owner_adr: ADR-060
    carrier_adr: ADR-060
    status: Active
