# Fixture 04 — Negative: current_tier enum violation (user alias 'hard_block')
# Story §8.6.1 #4 — exit 1, violation (c)
schema_version: "1.1"
introduced_by: CFP-455-test
last_updated: 2026-05-12
entries:
  - name: fixture-enum-violation
    description: |
      Test entry — current_tier 'hard_block' (user alias rejected per Q1 결정).
    detect_command: bash scripts/test-detect.sh
    workflow: templates/github-workflows/test-detect.yml
    current_tier: hard_block  # SSOT 외 alias — enum violation
    promotion_criteria:
      pr_cumulative_min: 20
      failure_threshold: 0
      sibling_dependencies: []
      evidence_artifacts:
        - github_actions_run_history_url
    introduced_by: CFP-455-test
    owner_adr: ADR-060
    carrier_adr: ADR-060
    status: Active
