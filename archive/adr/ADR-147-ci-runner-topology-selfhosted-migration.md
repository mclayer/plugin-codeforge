---
adr_number: 147
title: CI runner topology — mclayer org self-hosted 이관 표준 (vars 기반 조건부 runs-on)
status: Accepted
is_transitional: false
category: Infrastructure
date: 2026-07-11
carrier_story: CFP-2607
related_adrs:
  - ADR-048-ci-native-test-execution
  - ADR-027
  - ADR-005
  - ADR-063
  - ADR-020
  - ADR-069
  - ADR-121
  - ADR-125
  - ADR-078
  - ADR-112
  - ADR-066  # Amendment 1 (CFP-2821) — §결정 8 적격 조건 3 cross-ref (ADR-066 Amd5 exception 은 self-hosted 경로 미적용)
  - ADR-013  # Amendment 1 (CFP-2821) — §결정 8 조건 3 reconciliation cross-ref (ADR-013 Amd4 단일 PAT consolidation 을 self-hosted surface 한정 supersede-in-part; F-DR-2821-1)
related_files:
  - templates/github-workflows/
  - .github/workflows/invariant-check.yml
  - .github/workflows/phase-gate-mergeable.yml
  - scripts/bootstrap-consumer.sh
  - scripts/reconcile-overlay.sh
  - scripts/ci-runner-provisioning-check.sh  # Amendment 5 (CFP-2913) — §결정 4(ii) 값 검증 승격(C1) + §결정 4(iii) org-scope negative-presence 게이트(C2') 배선 대상
  - docs/architecture/codeforge-family.md
related_stories:
  - CFP-2607
  - CFP-2821  # Amendment 1 — §결정 8 bounded enumeration 에 3rd sanctioned mitigation path(runtime secret-manager JIT sourcing) 추가
  - CFP-2858  # Amendment 3 — §결정 8 DooD 완화에 service-container 도달 결정론 wiring(extra_hosts host-gateway 정적 배선) additive codify
  - CFP-2890  # Amendment 4 — 러너 infra VC 편입(신규 private repo mclayer/ci-runner-infra) + .rustup/.cargo per-replica anonymous volume EXDEV-hardening; Amendment 3 declared durability-gap follow-up 이행, §결정5 rust-group6 한정 supersede-in-part
  - CFP-2913  # Amendment 5 — §결정 2 용량(capacity) 축 carve-out(예산-bounded 정적 배분, default 불변) + normative C1/C2'/C3(값 검증 승격·org-scope negative-presence·라우팅 값 공간 폐쇄) + 제외 절 stale 전제 정정 + §결정 1 fromJSON 미확정 종결
amendments:
  - amendment: 1
    date: 2026-07-25
    cfp: CFP-2821
    summary: "§결정 8 org-wide PAT 절 bounded enumeration('hosted 유지 또는 OIDC')에 3rd sanctioned mitigation path 추가 — runtime secret-manager JIT sourcing (1Password service-account, load-secrets-action full-SHA pin, env-only + auto-mask + CLI-arg 0, bootstrap = read-only single-purpose-vault SA token). 기저 = CFP-2743 Change Plan §10.1 amendment TEXT 초안의 부활 (구현리뷰 FIX iter1 B-hosted 가 supersede 했던 판정을 CFP-2821 사용자 확정 R2[internal-docs fix-ledger-sync self-hosted 이관 — billing-block 상시-FAIL 의존 구조 제거]가 재개봉 — 전제 재성립). self-hosted 적격 조건 7종 전부 AND: ① private 3중(private ∧ allow_forking=false ∧ allows_public_repositories=false) ② fork-PR self-hosted 미실행 불변 ③ write credential least-privilege(org-wide classic 의 self-hosted 상주 금지 — fine-grained issues:write@대상repo 또는 scoped App ≤1h; ADR-066 Amd5 exception 은 self-hosted 경로 미적용) ④ runtime JIT sourcing 의무(env-only + auto-mask + CLI-arg 0) ⑤ residual 완화 의무(post-job env-purge + permissions 최소 + T4/T5 명시 수용 declare) ⑥ supply-chain(secret-bearing action full-SHA pin + egress allowlist region-correct) ⑦ scope 검증 = SecurityTest lane 실측(presence-only = false-oracle). 정직 한정: standing-credential-0(OIDC 등가) 아님 — standing SA token 1 잔존; persistent 러너 cross-job residue(T4/T5) = OIDC 포함 전 credential 모델 공통 limb(본 path differentiator 아님). 방향 = 저장 축 강화(org-wide-write 정적 상주 → single-vault-read SA token, blast-radius 축소) + 러너 topology 축 residual 재수용 동시 — 순 강화 단정 금지, evidence-gated(ADR-064 §결정 7 Amendment 8 symmetric ratchet; evidence = fix-ledger-sync 14일 상시-FAIL census[#2820, Story §2.1 재실측 895 run = 834 fail] + self-hosted 대조군 4 workflow 성공 지속 + group6 8대 online 실측 2026-07-24)."
    is_transitional: false
    sunset_justification: "N/A — permanent infrastructure topology invariant(본 ADR is_transitional: false) 내부의 sanctioned path 확장. 적격 조건 7종 AND 중 1+ 미충족 시 본 path 무효 — §결정 8 기존 2-path(hosted 유지 / OIDC) default 복귀. 약화 방향(조건 7종 일부 삭제 / org-wide classic write credential 의 self-hosted 상주 허용 / SecurityTest 실측 검증 생략) 발의 차단."
  - amendment: 2
    date: 2026-07-25
    cfp: CFP-2821
    reinterpretation: true
    summary: "§결정 8 Amendment 1 조건 3(org-wide classic PAT 의 self-hosted 상주 금지)·조건 4(runtime JIT sourcing 의무)에 대한 operator risk-accepted static-org-wide-PAT-on-self-hosted user-override exception 을 evidence-gated + Story-local 로 codify. 배경 = CFP-2821 credential 축이 Orchestrator 의 T4/T5 광역-노출 trade-off 명시 제시 후 사용자 override(정적 CODEFORGE_CROSS_REPO_PAT 직접 env-bind, FIX Ledger Iter 4)로 확정 — §결정 8 2-path 도 Amd1 JIT path 도 아닌 정적-org-wide-PAT-on-self-hosted 를 '재편집 0/available-but-unused' 로 방치하면 running system ↔ ADR/arch 문안(정적 상주 금지)이 affirmatively-FALSE 모순 → 본 Amendment 가 정직 codify 로 해소(방향 되돌리기 아님, DR FIX R2 F-R2-01 P0). 4-gate 전부 AND 시에만 조건 3/4 carve-out: ① operator 명시 risk-accept 기록(Story/Change Plan §7·§10) ② fail-closed floor 보존(credential-absent → cross-repo write 前 red = 정적 pat-absent gate, 게이트 무력화 불허) ③ Story-local scope(일반 승인 아님 — 매건 operator 결정 + 4-gate 재충족) ④ green 도달 실증 게이트(self-hosted 러너 실 green ≥1 실측, presence-only false-oracle 불허). §결정 8 default(JIT/hosted 우선) 불변 — Amd1 조건 3/4 원문·7종 AND·sunset 무삭제, JIT path 용 유효 잔존. sunset carve-out: '약화-발의 차단'은 operator 가 명시 수용 근거 기록한 본 exception 에 한해 carve-out(evidence-gated operator-accepted deviation, blanket 약화는 여전히 차단). 자기 선례 = ADR-145 §결정 3(fail-closed 비호환 override-codify) + 본 §결정 8 Amendment 1 supersede-in-part. 정직 한정: 보안 강화 아님 — T4/T5 광역 PAT 노출 재수용(약화 방향, blast-radius Amd1 down-scope 대비 큼), '정적이 더 안전/동등' 서술 금지."
    is_transitional: false
    sunset_justification: "N/A — §결정 8 Amendment 1 의 sunset('permanent topology invariant 내부 sanctioned path 확장')을 상속하되, 본 Amendment 는 확장이 아니라 operator-accepted Story-local carve-out 이다. 4-gate 중 1+ 미충족 시 exception 무효 → Amendment 1 조건 3/4(JIT 의무) default 복귀. 정적 상주의 default 승격·fail-closed floor 삭제·green 실증 생략 방향 발의 차단."
  - amendment: 3
    date: 2026-07-28
    cfp: CFP-2858
    reinterpretation: false
    summary: "§결정 8 DooD 완화에 self-hosted DooD 러너의 sibling service-container 도달 결정론 wiring 을 additive codify — 러너 launch 시 정적 extra_hosts host.docker.internal:host-gateway(compose 층) 배선 의무, 암묵 Docker Desktop runtime DNS 주입 의존 금지(결정론 보장). 배경 = CFP-2607 이 self-hosted DooD 이관에서 host.docker.internal 을 'DooD 표준 해결책'으로 지정하고 러너 compose 주석에 '해결' 을 선언했으나 extra_hosts DNS 배선 미이행(ExtraHosts 빈배열 firsthand) → 해결이 Docker Desktop 암묵 runtime DNS 타이밍에 전적 의존 → non-deterministic 간헐 실패(same-runner flake: 동일 러너 07-26 FAIL / 07-24·25 SUCCESS, 실패 시그니처 = psql 'could not translate host name' = name-resolution). 본 Amendment 가 그 미완결 배선을 완결(CFP-2607 OOS 'runner provisioning 완료' 의 service-container networking 서브속성 부분 무효 gap-fill). fix locus = 러너-infra(compose 공통 층 1회 → 영향 4 repo/7 job workflow delta 0); per-workflow(a) 는 container 미선언 job 이 러너 컨테이너 /etc/hosts 를 수정할 workflow-level knob 부재로 구조적 불가. host-gateway = 추상 special value(Docker 20.10+; 러너 Docker 29.4.3 실측), IP 하드코딩 금지(192.168.65.254 는 Docker Desktop 버전 의존). 직교 축 declare: Amendment 1(runtime secret-manager JIT sourcing)·Amendment 2(operator static-PAT exception)의 credential-sourcing 축과 disjoint — Amendment 1 적격 조건 7종·Amendment 2 4-gate 어느 것도 supersede/reinterpret 하지 않으며 그 sunset/carve-out 을 상속하지 않는다. 집행 정직 한정(ADR-119 over-claim 금지): 러너 compose 는 비-version-controlled 라 §결정 4 provisioning invariant 의 fail-loud lint 집행 모델 상속 불가 — operator-convention 집행(§결정 6 W0 감사 convention 동형)이지 PR-enforceable 기계 게이트 아니며 durability gap(머신 reprovision/compose 재작성 시 fix 유실) 잔존(러너 infra VC 편입은 3문 게이트 follow-up). 검증 oracle = 배선 후 영향 job 다회 green 실측(outcome ground-truth) 및 fleet 8 replica 전수 ExtraHosts 비어있지-않음 결정론 sweep — presence-lint(compose 문자열 존재) 단독 = false-oracle(불허, ADR-119 §결정 4·⑤). 네트워킹 완결이지 보안 강화 아님 — 러너는 이미 docker.sock=host-root-equiv 보유, host-gateway 정적 항목은 한계/무 노출, 신규 trust boundary 0."
    is_transitional: false
    sunset_justification: "N/A — permanent infrastructure topology invariant(본 ADR is_transitional: false) 내부의 sanctioned wiring 확장(신규 결정축 아닌 gap-fill). 약화 방향 발의 차단 대상 = ① 정적 extra_hosts/host-gateway 매핑 제거 ② 암묵 Docker Desktop runtime DNS 의존 복귀 ③ 다회 green 실증(presence-lint 대체) 생략. 단 집행면 = operator-action(러너 compose 비-VC) — PR-enforceable 기계 게이트 아님을 정직 declare(약화 아닌 정직 ceiling). Amendment 2 의 operator-accepted carve-out 은 credential 축 전용이라 본 networking 축에 상속 금지."
  - amendment: 4
    date: 2026-08-06
    cfp: CFP-2890
    reinterpretation: true
    summary: "§결정 8 DooD 완화 durability 서브속성 완결 + §결정 5 rust-toolchain tension 정직 supersede-in-part. (1) 러너 infra 3파일(Dockerfile·docker-compose.yml·entrypoint.sh)을 신규 private repo mclayer/ci-runner-infra 로 VC 편입 — Amendment 3 이 declare 한 durability-gap follow-up(러너 infra version-control 편입은 3문 게이트 follow-up) 이행체. visibility=private 필수(§제약1/§결정2 — docker.sock host-root-equiv·org URL·group·labels·host-gateway 평문 topology 노출), extra_hosts host-gateway verbatim 보존(IP 하드코딩 금지), secret 무커밋(.gitignore + RUNNER_TOKEN runtime env). (2) .rustup(+.cargo)를 per-replica anonymous volume(compose target-only 선언 — Dockerfile VOLUME 미채택, compose 단일 SSOT)로 이전해 overlay 디렉터리 rename EXDEV(os error 18) 근본 소거 — anonymous volume 은 단일 실 fs 라 rustup replace 백업 rename(RUSTUP_HOME/toolchains 대상 → RUSTUP_HOME/tmp)이 volume 내부 완결(경계 소멸, firsthand: volume 내부 dir rename 성공 / overlay EXDEV 는 job-context 특정·manual 재현 불가라 근치는 트리거 무관 robust). named 금지(replicas:8 단일 volume 공유 → 동시 rustup 쓰기 corruption; anonymous=per-replica 격리 firsthand). mount=/home/runner/.rustup 루트 필수(subpath 시 tmp 가 volume 밖=경계 재발). 빈 volume 이미지 콘텐츠 copy-up 으로 pre-bake 유지 → engine/backtest/market main-leg(rustc --version 전용 소비) workflow delta 0. option-(a) --default-toolchain none 은 pre-bake 의존 3 repo cross-repo blast 로 기각. (3) §결정 5(웨이브별 toolchain=setup-* step 주입, rust 명시 열거)를 self-hosted Linux group6 rust toolchain 한정 supersede-in-part — 그 외 toolchain(node/python 매트릭스 버전 등)은 §결정 5 원문 무변경. rust 러너 이미지 pre-bake+per-replica anonymous volume durability 모델이 group6 에 한해 setup-* 주입 default 를 대체(cross-repo blast 회피 근거). (4) reinterpretation=true 근거 = Amendment 3 서술(러너 compose 는 비-version-controlled 라 §결정 4 fail-loud lint 집행 모델 상속 불가·PR-enforceable 기계 게이트 아니며 durability gap 잔존)의 factual premise(비-VC)가 CFP-2890 이행으로 affirmatively-FALSE 화 → 해당 clause supersede-in-part(재해석) — Amendment 2 가 정립한 running system ↔ ADR 문안 affirmatively-FALSE 모순 codify 판정기준 동형(Amendment 1 의 순수 additive bounded-enumeration 확장[reinterpretation 부재]과 대조). VC 편입 후 source 계층 review/PR-diff·lint 가 구조적으로 가능해지고(이전엔 repo 부재로 불가) reprovision/compose 재작성 시 유실 특정 durability gap 이 닫힌다. Amendment 3 텍스트 무편집 보존(append-only supersede-not-mutate). (5) 집행 천장 정직(INV-1 보존, over-claim 금지): 본 Amendment 가 뒤집는 것은 source 계층 PR-enforceable 가능성뿐 — running 8-replica fleet 반영은 여전히 operator docker compose up -d --build 액션 의존(source↔deploy 분리 ceiling 불변, PR-merge 자동 fleet 갱신 아님). volume-shadow staleness 주의(운영): plain up -d --build 는 이전 컨테이너 anonymous volume 데이터 보존·재부착(-V/--renew-anon-volumes 로만 갱신) → toolchain-bump 반영에 down -v(fresh copy-up) 필수. 검증 oracle = 2-축 AND(재생성 후 fleet 8 replica per-replica anonymous volume+copy-up populate 전수 sweep ∧ setup-rust 우회 raw rustup toolchain install stable replace-path reprovision-첫-run 다회 green+EXDEV signature 0) — presence-lint(compose volumes 문자열)·fresh-install-only·manual docker exec = false-oracle(불허, ADR-119 §결정4·⑤; 3중 마스킹[setup-rust 멱등-skip·workflow-측 EXDEV fallback·copy-up self-heal] 우회 필수). consumer pre-bake 회귀 = engine/backtest/market main-leg GREEN(rustc-not-found=copy-up 실패=option-(a) 회귀 시그니처→요구 lane 재진입). credential 축(Amendment 1/2)과 직교 — Amendment 1 조건 7종·Amendment 2 4-gate 무접촉(RUNNER_TOKEN=runtime env, cross-repo write PAT 아님). 보안 강화 아님(infra durability — 신규 trust boundary 0, anonymous volume=기존 docker.sock host-root-equiv 공격표면 부분집합). carrier = CFP-2890(Story wrapper/stories/CFP-2890.md §7)."
    is_transitional: false
    sunset_justification: "N/A — permanent infrastructure topology invariant(본 ADR is_transitional: false) 내부의 durability gap-fill(Amendment 3 declared follow-up 이행, 신규 결정축 아님). 약화 방향 발의 차단 = ① VC 편입 원복(비-VC durability gap 재도입) ② anonymous volume 하드닝 제거(EXDEV 재무장) ③ named/shared volume 전환(replicas:8 동시쓰기 corruption) ④ AC-3 discriminating oracle(raw replace-path reprovision-첫-run)을 presence-lint 로 대체. §결정 5 supersede-in-part 는 group6 Linux rust toolchain 한정 — 전 toolchain/전 웨이브 image pre-bake 일반 허용으로 확장 금지. 집행면 = operator-action(running fleet sync) — VC-backed reviewability 획득이나 PR-enforceable auto-deploy 아님 정직 ceiling(약화 아닌 정직 declare, VC=자동배포 서술 금지). Amendment 2 의 operator-accepted carve-out 은 credential 축 전용이라 본 durability/networking 축에 상속 금지."
  - amendment: 5
    date: 2026-08-09
    cfp: CFP-2913
    reinterpretation: true
    summary: "§결정 2 의 visibility 단일 축 분기(private/internal → self-hosted)에 용량(capacity) 축 carve-out 을 additive codify — private/internal repo 의 특정 job 집합을 커밋 시점에 정적으로 GitHub-hosted 로 배분하되 총량을 hosted 무료 포함분 예산 상한 안으로 봉인(예산-bounded 정적 배분). 기본 배정 불변(넘침은 예외 경로이지 새 default 아님). (a) 포섭 판정 = 이원 — 보안 속성 축 = 미포섭 / 술어-범위 축 = 포섭 우세(적극 배선 의무에 용량 예외를 여는 것은 술어 정의역 축소이며 §해소기준 약화 방향 문언이 보안 속성으로 한정돼 있지 않음), 종합 = 미포섭 유지·확신도 중간(~60%)·조건부(조건 = (b) 자기 선례 경로 ∧ (b) 말미 Amendment 5 자기 carve-out ∧ (c) C1/C2'/C3 이행이 함께 성립; 깨지면 술어-범위 축을 따라 포섭·supersede ADR 필요로 전환) — 보안 속성 축 근거 3(소거법 미사용, 약화 예시 열거가 개방형이라 소거법 불성립): P-1 §해소기준 약화 예시 2종이 §컨텍스트 '제약 3종 (설계 hard constraint)' ①②와 1:1 대응하고 §결정 2 의 private→self-hosted 배정은 그 목록에 부재(동인 = 비용·속도·통제권, §컨텍스트 절 서두 문장) / P-2 §결정 2 가 스스로 명시한 방어 속성(public 봉쇄 근거 = allows_public_repositories=false 의 fail-safe 거부, §결정 4 와 defense-in-depth, §결정 2 'public 봉쇄 근거' bullet)을 본 개정이 무접촉 유지하고 C2' 로 강화 / P-3 §결정 8 과 §결과 부정-(c)가 self-hosted 자체 위험(persistent runner 의 secret·tool-cache cross-job 오염, DooD docker.sock host root 등가 escape)을 자인 ⇒ self-hosted 가 더 안전은 본 ADR 의 전제가 아님. 반대 해석 병기(삭제 금지): 약화 기준을 보안 속성이 아니라 정책 술어의 범위로 잡으면 적극 배선 의무에 용량 예외를 여는 것은 범위 축소이므로 포섭 여지가 있다. (b) 개정 형태 = Amendment(미포섭·포섭 양 경로 공통 귀결) — 자기 선례 1순위 = Amendment 4 (3)(§결정 5 의 정의역을 group6 rust 로 축소하면서 supersede ADR 미발행 = 술어-범위 축 순수 절차 선례. 승격 근거 = Amendment 4 가 자기 문면으로 credential 축 직교·Amendment 2 carve-out 미상속을 declare 하므로 credential 축 scope-bound 조건과 무관), 보강 선례 = Amendment 2(자기 §해소기준을 명시 인용하며 신규 ADR 0건·supersedes 무변경·entry 로 carve-out — 단 그 carve-out 실체는 credential 축 한정이라 Amendment 5 가 상속하지 않으며 취하는 것은 개정 형태뿐), 신설 = Amendment 5 자기 carve-out(술어-범위 축 해석이 채택되는 경우에 한한 독립 조건부 carve-out — 상속이 아니라 자기 축에서의 정립이라 말미 직교 축 declare 와 무모순; 4조건 AND[default 불변·evidence-gated·scope-bounded·명시수용] ∧ C1/C2'/C3 전부 이행 조건부, 하나라도 미이행이면 무효화되어 술어-범위 축 포섭으로 전환, blanket 약화는 범위 밖). 성립 원칙 = blanket 약화는 차단 / evidence-gated·scope-bounded·default-불변·명시수용 carve-out 은 amendment 가능; 본 건 4조건 충족(default 불변 / 사용자 확정 무료분 안에서만 = evidence-gated / 예산 상한 = scope-bounded / blanket 아님 명시). §결정 11 은 원본 최초 저작 공동 원저(amendment 이력 미등재)라 절차 선례 인용 금지 — 설계 템플릿 축으로만 유효. carve-out 경계 = 넘침이 예외가 아니라 default 가 되는 순간(§결정 2 기본 배정 자체를 용량 축으로 대체) supersede ADR 필수. (c) normative 3 조항(하나라도 advisory 강등 시 본 Amendment 자체가 약화 → 포섭 전환): C1 = §결정 4(ii) lint 를 presence 검증에서 값 검증으로 승격(JSON 배열 파싱 ∧ 비어있지 않음 ∧ 등록 러너 라벨 합집합 부분집합 ∧ 그 AND-조합 라벨 보유 online 러너 1+ 실재 ∧ 용량 carve-out 로스터 정합(로스터 SSOT = private repo mclayer/ci-runner-infra 의 capacity-overflow-roster.yaml, hand-authored·append-only — 실 변수값에서 파생 생성하면 SET 된 모든 값이 정의상 등재가 되어 tautological hollow 로 붕괴); 필수 mutant 2종 RED = 로스터 미등재 repo 에 hosted 값 SET / 오타 라벨 dockerr; non-empty 만 보면 여전히 hollow) / C2' = §결정 4(iii) org-level 금지의 신규 키 상속 명문화 ∧ negative-presence 게이트를 2축 AND 로 배선(① 상속 축[반복·PR-enforceable] = 워크플로가 vars 컨텍스트를 직접 관측해 ^CI_RUNS_ON_ 패턴 키 0 을 assert — vars 는 organization/repository/environment 변수를 담고 미설정 키는 빈 문자열이라 credential 불요[source: docs.github.com Contexts], public repo 표준 GitHub-hosted 러너는 무료라 §결정 4(ii) born-dead-gate 예산 co-block 과 물리적 disjoint[source: docs.github.com About billing for GitHub Actions], 단 검사 워크플로 runs-on 은 리터럴 표준 러너 고정 필수[vars-라우팅 시 탐지기가 mutant 와 함께 죽는 자기무력화] / ② 존재 축[operator/hub] = gh api orgs/ORG/actions/variables/NEW_VAR 404=PASS, 200=FAIL-LOUD — org-admin 권한 요구라 operator 층 잔류·반복성은 operator cadence 의존. 두 축은 대체 아닌 AND: 상속 축은 visibility 가 검사 repo 를 포함하는 주입만 보므로 어느 검사 repo 에도 상속되지 않는 visibility: selected 변종은 존재 축만 커버; (ㄹ)~(ㅅ) additive 조항[설계리뷰 iter2 P1-a] = vars 가 값의 출처 층을 구별하지 않으므로 상속 축 assert 의 건전성은 host 전제에 의존한다 ⇒ private host 는 routing-variable-free 전용 private repo 로 고정하고 §결정 4(i)·4(ii) 정의역 밖(out-of-scope)으로 지정[예외 없이 in-scope repo 를 host 로 쓰면 §결정 4(i)를 지킨 repo 에서 assert 가 born-RED], 허용 local key 집합 = 공집합 고정[비-공집합화 = 상속 축 정의역 축소 = C2' 약화, amendment 필요], 라우팅 키 CI_RUNS_ON_ 접두 의무화 + 존재 축을 org 변수 목록 전수 조회로 승격, oracle 3-conjunct AND[청정 exit 0 ∧ 오염 exit 1 ∧ 허용집합 비-공집합 fixture exit 0 — 단측 RED 채택 불가], ceiling 중첩[host 살아있는 구간 한정 참 · repo-level/environment-level/접두 밖 키명은 전 층 미탐 ⇒ C2' 는 adversary containment 아닌 drift 검출]) ∧ org variable visibility private 완화 옵션 불채택(enum 의 internal 포함 여부 공식 미기재 ∧ mctrader 가 internal; precedence = the variable at the lowest level takes precedence + public 2 repo 의 repo-level 변수 실측 0개 ⇒ org-level 키는 즉시 상속되어 delta-0 파손) / C3 = 라우팅 값 공간을 등록 러너 라벨 집합으로 폐쇄 ∧ fork·workflow_run 유래 실행 제외 ∧ 통제-load-bearing 러너 pin 구조적 제외(mctrader deploy-k8s.yml 의 ARC 배포 3 job = job id deploy-dev/deploy-stg/deploy-prd[라벨 mctrader-dev/mctrader-stg/mctrader-prd — 종전 문면이 라벨을 job id 로 적은 오기 정정, 설계리뷰 iter1 F-2] + mctrader sibling-pin-parity-lint.yml 의 job id lint[라벨 self-hosted, Linux] — runs-on 이 라우팅이 아니라 보안 통제, kubeconfig write step 제거 + ARC in-cluster SA 의존. 대상 특정 축 = job id · assert 값 = 라벨 리터럴[라벨로 job 을 탐색하면 0 매치 = 조용한 무력화 born-dead], 차단 = HEAD 정적 assert[보호 job id 존재 ∧ 그 runs-on 리터럴 일치의 AND] 또는 상위집합 — diff 게이트가 놓치는 base 전진·머지 유입·게이트 신설 이전 불일치·job 삭제/rename 까지 포착하며 존재 conjunct 없으면 job 삭제로 공허 참). (d) 동반 개정 = ① §결정 4(ii) required-key 집합 확장 의무(현 구현은 LINUX_VAR/WINDOWS_VAR 2개 하드코딩 scripts/ci-runner-provisioning-check.sh:45-46, 호출자 = bootstrap-consumer.sh + self-test 뿐이고 워크플로 호출 0건 ⇒ 17 repo 전파 문제 없음) ② §결정 4(iii) 신규 키 상속(C2' 가 정본) ③ §결과 제외 절 stale 전제 정정 — 3 ARC job 이 이미 self-hosted 라는 괄호 전제가 affirmatively-FALSE(매칭 online 러너 0대 → 24h queue-starvation 3건 반복) ⇒ 제외 사유를 C3 통제-load-bearing pin 으로 재귀속하고 매칭 러너 부재는 §결정 7 DR 사안으로 분리 ④ §결정 1 자기선언 미확정 종결 — fromJSON-array-in-runs-on 의 dry-run 차단 조항을 공식문서로 해소(runs-on 허용 컨텍스트 = github, needs, strategy, matrix, vars, inputs ∧ 라벨 배열 AND 매칭 ∧ 표현식 공식 지원; 단 fromJSON malformed 의 fail-loud 동작은 여전히 공식 미확정이라 C1 값 검증이 provisioning 층에서 보완) ⑤ ephemeral 권고 사정거리 정정(GitHub 공식 권고 원문은 autoscaling 한정 — 고정 replica fleet 은 사정거리 밖이므로 사용자 지시와 공식 권고 사이 충돌 없음; 권고 약화가 아니라 과대 귀속 정정). (e) T-15 미완화 declare(최대 위협) = hosted 소비가 무료 포함분 초과 시 prevent_further_usage true 로 org 전체 private CI 정지 — 실패 모드가 degradation 에서 outage 로 승격하며 공격자 없이 자기 유발 가능, billing 반영 D-2 지연으로 실시간 fail-closed 가드 불성립 ⇒ 예산 가드 배선을 안전성 단정으로 사용 금지, 유일한 구조적 완화 = 넘침 총량의 정적 상한 봉인. 불변식 I-1~I-4 를 결정 조항으로 편입(I-1 라우팅 결정은 dispatch 시점 1회, 큐 job 취소·재발화 금지 / I-2 실시간 예산 잔량·fleet busy 의존 금지 / I-3 degrade = fail-static-to-self-hosted, fail-open 은 비가역 org 정지로 수렴 / I-4 동일 (SHA, job)은 어느 시도에서도 동일 실행 위치). 정직 한정(over-claim 금지): 사용자 요구의 결과는 충족하나 메커니즘(런타임 busy 관측 기반 동적 넘침)은 미충족 — runs-on 은 AND 매칭이라 OR fallback 표현 불가하고 변수 flip 은 이미 큐에 든 job 을 구제하지 못함(양성대조 포함 라이브 실험 확정) ⇒ 동적 넘침 구현 표기 금지; 보안 강화 선언 아님(self-hosted 잔류 위험과 hosted 예산 절벽의 교환이며 net 판정은 C1~C3 normative 편입을 전제로 한 조건부); 집행 천장(narrowing — 설계리뷰 iter1 F-2) = C2' 상속 축은 PR-enforceable 기계 게이트로 승격(public wrapper 기존 required 컨텍스트 편승, credential 0, 예산·fleet 독립)하나 존재 축(visibility: selected 변종)은 operator/hub 층 잔류·반복성은 operator cadence 의존이고 C1 값-정합도 provisioning-time 이 primary; 상속 축 게이트에도 스케줄 지연·플랫폼 장애·워크플로 수동 disable 침묵 천장이 잔존 ⇒ C2' 가 org-level 주입을 kill 한다는 상속 축 한정 참이며 무조건 명제로 인용 금지(약화가 아니라 정직 ceiling 의 정밀화). 직교 축 declare: 용량/라우팅 배분 축이며 credential-sourcing(Amendment 1/2)·service-container networking(Amendment 3)·runner-infra durability(Amendment 4)와 disjoint, Amendment 2 의 operator-accepted carve-out 미상속. carrier = CFP-2913."
    is_transitional: false
    sunset_justification: "N/A — permanent infrastructure topology invariant(본 ADR is_transitional: false) 내부의 evidence-gated·scope-bounded·default-불변 carve-out(신규 결정축 아님 — §결정 2 기본 배정 무변경). C1·C2'·C3 normative 3 조항 중 1+ 가 advisory 로 강등되거나 미이행이면 본 carve-out 무효 → §결정 2 원문 default(private/internal 전량 self-hosted) 복귀. 약화 방향 발의 차단 = ① 예산 상한 봉인 해제(총량 무제한 hosted 배분) ② C1 값 검증의 presence-lint 회귀(mutant 2종 미살) ③ C2' org-scope negative-presence 게이트 제거 또는 org-level 라우팅 키 허용 ④ C3 값 공간 폐쇄·트리거 유래 제외·통제-load-bearing pin 제외의 완화 ⑤ 실시간 예산 잔량·busy 기반 동적 라우팅 전환(I-1·I-2 정면 위반). 넘침이 예외가 아니라 default 가 되는 순간(§결정 2 기본 배정 자체를 용량 축으로 대체)은 본 amendment 경로 밖 — supersede ADR 필수. 집행면 = C2' 상속 축(vars 컨텍스트 관측)만 PR-enforceable 승격이고 그 외(존재 축 · C1 값-정합)는 operator/hub 스크립트 층 정직 declare(약화 아닌 정직 ceiling). Amendment 2 의 operator-accepted carve-out 은 credential 축 전용이라 본 용량 축에 상속 금지 — 대신 Amendment 5 가 자기 축에서 독립 carve-out 을 정립하며 그 carve-out 도 위 C1·C2'·C3 이행에 조건부다."
---

# ADR-147: CI runner topology — mclayer org self-hosted 이관 표준 (vars 기반 조건부 runs-on)

## 상태

`Accepted` (2026-07-11 KST) — CFP-2607 Epic Phase 1 설계 lane 확정. ArchitectPLAgent design lane supervised chief-author write per ADR-070 / CFP-578 chief author precedent (status `reserved` 미경유 직접 `active` — chief author scope, ADR-RESERVATION row 147). is_transitional: false (permanent infrastructure topology invariant — ratchet, 약화 방향 차단).

## 컨텍스트

mclayer org 의 CI 실행을 GitHub-hosted runner(`ubuntu-latest`)에서 org 소유 self-hosted runner 로 이관해 비용·속도·통제권을 확보한다. Phase 1(본 Epic)은 설계·문서만(코드 변경 0)이며 실 배선은 Phase 2 child Story 가 담당한다. 현재 codeforge family 의 workflow SSOT (`templates/github-workflows/`, 76 file / 96 `runs-on`)는 전부 `ubuntu-latest` 로 고정되어 있고, self-app 사본(`.github/workflows/`)은 `invariant-check.yml` 이 template 과 byte-identical 을 blocking 강제한다.

**runner infra (gh api 실측, PL firsthand)**:
- **group6 `self-hosted-linux-private`**: Linux 4대 online, 라벨 `[self-hosted, X64, Linux, docker]`, `allows_public_repositories=false`, `visibility=all`.
- **group5 `self-hosted-private`**: Windows 1대(`MCCHO-DESKTOP`), 라벨 `[self-hosted, Windows, X64]`(docker 미장착), `allows_public_repositories=false`, `visibility=all`.

**제약 3종 (설계 hard constraint)**:
1. **public repo 물리 배제** — GitHub 은 self-hosted runner 를 private/internal repository 전용으로 권장한다. fork PR 이 self-hosted 에서 임의 코드를 실행하는 것은 최대 공격 표면이기 때문이다 (source: docs.github.com "About self-hosted runners" / "Security hardening for self-hosted runners"). org 은 이를 `allows_public_repositories=false` 로 물리 강제한다 (source: docs.github.com "Managing access to self-hosted runners using groups").
2. **byte-parity blocking** — `.github/workflows/invariant-check.yml`(6-tuple 필수 컨텍스트 `invariant-check`)의 "Workflow parity" step 이 `templates/github-workflows/*.yml ↔ .github/workflows/<base>.yml` 을 `diff -q` 로 blocking 검사한다(L48-77). `CONSUMER_ONLY_WORKFLOWS` 18개만 제외되며 `phase-gate-mergeable.yml` 은 **제외 대상이 아니다** → self-app 사본은 template 과 반드시 byte-identical. (Story KU-9 의 "byte-parity blocking 게이트 부재" 는 self-app workflow 에 대해 오류다 — `wrapper-template-managed-coverage.yml`(warning-tier)은 `.claude/_overlay/**` marker coverage 를 검사하지 워크플로 byte-parity 를 검사하지 않는다.)
3. **public wrapper 는 같은 파일을 배포** — `plugin-codeforge`(public)는 template SSOT 를 보유하고 자기 자신에게 self-app 사본을 배포한다. 따라서 template 의 `runs-on` in-file default 는 public 이 물리적으로 만족 가능한 값(=hosted)이어야 한다.

## 결정

### §결정 1 — vars 기반 조건부 runs-on 계약 (단일 JSON-array key)

모든 in-scope workflow 의 `runs-on: ubuntu-latest` 를 아래 단일 표현식으로 대체한다:

```yaml
runs-on: ${{ fromJSON(vars.CI_RUNS_ON_LINUX_JSON || '["ubuntu-latest"]') }}
```

- **단일 key `CI_RUNS_ON_LINUX_JSON`** (JSON array 문자열) 하나로 scalar(단일 라벨)과 다중 라벨을 interface 1개로 통일한다. 2-key / 2-template-fork 방식은 SSOT 파괴·byte-parity 붕괴로 **기각**한다(RefactorAgent 권고 채택).
- **적용 범위 = uniform single-leg job (P1-2)** — Surface A 거버넌스 템플릿(전부 ubuntu 단일 leg) + Surface B 의 non-matrix job. **matrix os-leg job(Surface B 4 rust-ci + container job)은 §결정 11** — 단일 key 로 모든 leg 가 같은 값으로 resolve 되면 Windows leg 가 group5 라우팅을 상실(붕괴)하므로 leg-keyed 변수로 분기한다.
- **변수명 SSOT (P2-c)** — 요구사항 lane 표기 `CI_RUNNER`(scalar) = 본 설계 lock `CI_RUNS_ON_LINUX_JSON`(array). scalar→array 확장(다중 라벨 지원). matrix Surface B 는 leg-keyed `CI_RUNS_ON_LINUX_JSON` / `CI_RUNS_ON_WINDOWS_JSON`(§결정 11). Story §5.2/§5.3/§6.2 의 구 `CI_RUNNER` 표기는 이 매핑으로 해석한다.
- **in-file default = `["ubuntu-latest"]` (불변)** — §결정 4 참조. 이 default 는 §제약 3(public wrapper 가 같은 파일 배포)에 의해 self-hosted 로 flip 할 수 없다.
- **Ports & Adapters**: workflow 파일 = port(실행 의도), repository variable = adapter(물리 runner 배선). runner 물리 선택은 파일이 아닌 variable 값으로만 표현된다.

> **확인불가 defer (research-before-claims)**: `fromJSON(...)` 이 `runs-on` 에서 **array 로 확장**되는 동작은 GitHub 공식 문서에 명시적 예시가 부재하다(source: docs.github.com "Workflow syntax — `jobs.<job_id>.runs-on`" 은 label array 와 표현식을 각각 허용하나 fromJSON-array-in-runs-on 조합의 명시 예시 없음). 따라서 이 계약은 §결정 6 카나리 이전에 **KU-2 dry-run(Change Plan §8)으로 실증**해야 하며, 실증 전 Phase 2 배선을 차단한다. scalar coalescing(`vars.X || 'default'`)은 흔한 패턴이나 array 확장은 추정 lock-in 금지 — dry-run 실측 대상.

### §결정 2 — public=GitHub-hosted / private·internal=self-hosted 분기

- **분기 규칙**: repository visibility 로 분기한다. public repository(`plugin-codeforge`, `marketplace`)는 `CI_RUNS_ON_LINUX_JSON` **미설정** → §결정 1 coalesce 로 `["ubuntu-latest"]`(hosted) 유지. private/internal repository(대상 18개)는 `CI_RUNS_ON_LINUX_JSON` 을 self-hosted 라벨 배열로 설정한다.
- **public 봉쇄 근거**: runner group 2개 모두 `allows_public_repositories=false` 이므로, 설사 public repo 가 self-hosted 라벨을 요청해도 GitHub 이 fail-safe 로 거부한다(올바른 실패 모드 — source: docs.github.com "Managing access to self-hosted runners using groups"). 이는 §결정 4 provisioning invariant 와 defense-in-depth 를 이룬다.
- **container job 배선**: `container:`/DooD 를 쓰는 job 은 group6(Linux, docker 장착)로 강제한다. group5(Windows, docker 미장착)는 container job 물리 불가.

**Amendment 5 (CFP-2913, 2026-08-09) — 용량(capacity) 축 capacity-overflow carve-out (예산-bounded 정적 배분, §결정 2 default 불변)**: 위 분기 규칙은 **repository visibility 단일 축**으로 배정을 규정하며 **용량을 입력으로 갖지 않는다** — private/internal 이면 유입량·fleet 슬롯 잔량과 무관하게 전 job 이 self-hosted 로 배정된다. 그 결과 고정 슬롯(현 group6 Linux **online 4대** 실측)을 초과하는 유입은 큐에 적체되고 §결정 7 의 24h 자동취소로 귀결된다. 본 Amendment 는 그 gap 에 **용량 축 carve-out** 을 additive codify 한다 — **private/internal repo 의 특정 job 집합을 커밋 시점에 정적으로 GitHub-hosted 로 배분하되, 그 총량을 hosted 무료 포함분(예산) 상한 안으로 봉인한다(예산-bounded 정적 배분).** 기본 배정(private/internal → self-hosted)은 **불변**이며 본 carve-out 은 그 **예외 경로**이지 새 default 가 아니다.

  **인용 규약(본 Amendment 한정 — F-12 정정)**: 본 파일은 매 amendment 마다 frontmatter `amendment_log` entry 가 늘어나 **본문 전체가 삽입 지점과 무관하게 하류로 이동**한다 — 삽입 지점 **위**의 원문도 frontmatter 성장분만큼 밀리므로 *"위쪽은 라인 인용이 안전하다"* 는 전제는 성립하지 않는다(자기 규약이 자기를 못 잡은 사례 — 설계리뷰 iter1 F-12 firsthand: 구 인용 4건 전부 +9 stale, 삽입 지점 아래를 가리키던 인용은 +55 stale). ⇒ **본 Amendment 는 ADR-147 자기 라인 인용을 일절 쓰지 않으며**, 위·아래 불문 §/Amendment/bullet 단위 **구조 앵커**만 인용한다. ★**적용 범위 narrowing (설계리뷰 iter2 P2-b)**★ — 본 금지의 대상은 **이 파일 자신의 라인 번호**다. 본 문서 **밖** 외부 파일의 `file:line` 인용(스크립트 등)은 그 파일 자체의 라인이라 **본 문서 frontmatter 성장에 영향받지 않으므로** 위 기전의 사정거리 밖이다. 종전 문면은 *"라인 인용을 일절"* 로 무조건 선언해 자기 본문의 외부 인용 5건과 충돌했다(전건 현재 정확 — PL·Analyst firsthand 대조). ★단 외부 인용에는 **별도 의무**가 존속한다★ — 본 Story Phase 2 가 바로 그 스크립트(`ci-runner-provisioning-check.sh` 등)를 편집하므로 **편집 시점마다 인용 정확성 재검증 의무**를 지며, 재검증이 불가하면 함수·블록 단위 구조 앵커로 교체한다.

  **(a) 포섭 판정 기록 — 이원(보안 속성 축 = 미포섭 / 술어-범위 축 = 포섭 우세), 종합 = 미포섭 유지(확신도 중간 ~60%) · 조건부, 반대 해석 병기**: 질문 = *"private CI 의 일부를 GitHub-hosted 로 되돌리는 것이 §해소 기준의 '약화 방향'에 포섭되는가?"* **판정 = 이원.** 축을 분리해 기록한다 — **보안 속성 축 = 미포섭**(아래 P-1~P-3 이 원문 대조로 성립) / **술어-범위 축 = 포섭 우세**(§결정 2 의 적극 배선 의무에 용량 예외를 여는 것은 그 술어의 적용 정의역 축소이며, §해소 기준의 "약화 방향" 문언이 보안 속성으로 한정돼 있지 않다). **종합 = 미포섭 유지·확신도 중간(~60%)·조건부** — 조건 = 아래 (b) 자기 선례 경로(Amendment 4 (3) 1순위) ∧ (b) 말미 **Amendment 5 자기 carve-out** ∧ (c) C1/C2'/C3 이행이 **함께** 성립할 것. 이 조건이 깨지면 판정은 술어-범위 축을 따라 **포섭 · supersede ADR 필요**로 전환된다. 근거 3(보안 속성 축, **소거법 미사용** — 약화 예시 열거가 개방형이라 소거법 불성립):

  1. **P-1 문언 대조** — §해소 기준의 약화 예시 2종(*"public repo self-hosted 허용, byte-parity 완화"*)은 **§컨텍스트 "제약 3종 (설계 hard constraint)"** 의 ①(public repo 물리 배제)·②(byte-parity blocking)와 **1:1 대응**한다. **§결정 2 의 private→self-hosted 배정은 그 제약 목록에 없으며**, **§컨텍스트 절 서두 문장**이 그 배정 동인을 *"…비용·속도·통제권을 확보한다"*로 밝힌다 — 보안 hard-constraint 가 아니다.
  2. **P-2 §결정 2 가 방어 속성을 스스로 명시 — 본 개정은 무접촉·강화** — 본 절 자신이 *"**public 봉쇄 근거**: runner group 2개 모두 `allows_public_repositories=false` 이므로, 설사 public repo 가 self-hosted 라벨을 요청해도 GitHub 이 fail-safe 로 거부한다(올바른 실패 모드). 이는 §결정 4 provisioning invariant 와 defense-in-depth 를 이룬다"*(**§결정 2 "public 봉쇄 근거" bullet**)로 방어 대상을 **public 방향 봉쇄**로 특정한다. 본 Amendment 는 그 봉쇄를 **무접촉 유지**(runner group 전건 `allows_public_repositories=false` 실측)하고 아래 **C2'**(org-level 키 금지의 신규 키 상속 + negative-presence 게이트)로 **오히려 강화**한다.
  3. **P-3 self-hosted 우월성은 본 ADR 의 전제가 아님** — §결정 8 이 self-hosted **자체 위험**을 자인한다(DooD 완화 절 *"DooD job 은 host root 등가(`docker.sock` → escape)"* / persistent runner 완화 절 *"cross-job 오염(secret / tool-cache) 차단"*), §결과 부정-(c) 도 *"self-hosted persistent runner 는 secret/tool-cache 오염 신규 공격 표면"* 으로 계상한다. 따라서 *"self-hosted 가 더 안전하므로 hosted 복귀 = 약화"* 는 본 ADR 문면에서 도출되지 않는다.

  **반대 해석 병기(삭제 금지)**: "약화" 의 기준을 *보안 속성* 이 아니라 ***정책 술어의 범위***로 잡으면 — §결정 2 의 적극 배선 의무(private/internal 은 self-hosted 라벨 배열로 **설정한다**)에 용량 예외를 여는 것은 **술어 범위의 축소**이므로 **포섭 여지가 있다**. 이 해석에서는 본 건이 §해소 기준의 supersede 요건을 발동시킨다. 판정 확신도 = **중간(~60%)** 이며, 본 Amendment 는 반대 해석이 옳은 경우에도 성립하도록 (b) 자기 선례 경로와 (c) net-strengthening normative 조항을 **함께** 갖춘다.

  **(b) 개정 형태 = Amendment (양 판정 경로 공통 귀결) — 자기 선례 (1순위 = Amendment 4 (3))**:

  - ★**Amendment 4 (3) 이 1순위 자기 선례 (순수 절차 선례)**★ — §결정 5 를 *"self-hosted Linux group6 rust toolchain 한정 **supersede-in-part**"* 하면서도 supersede ADR 을 발행하지 않았다. **1순위 승격 근거** = Amendment 4 는 **자기 문면으로** *"credential 축(Amendment 1/2)과 직교 — Amendment 1 조건 7종·Amendment 2 4-gate 무접촉"* ∧ *"Amendment 2 의 operator-accepted carve-out 은 credential 축 전용이라 본 durability/networking 축에 상속 금지"* 를 declare 한다. 따라서 Amendment 4 (3) 은 **credential 축 scope-bound 조건과 무관한 순수 절차 선례**다 — §결정 5 의 **정의역을 group6 rust 로 축소**하면서 신규 ADR 없이 amendment 만으로 처리한 사실 자체가 *"정책 술어 범위의 축소 = amendment 로 처리 가능"* 이라는 **술어-범위 축 전례**이며, 위 (a) 의 반대 해석(술어-범위 축 포섭)이 옳은 경우에 **정면으로 적용**된다.
  - **Amendment 2 는 보강 선례** — 본 ADR 은 **자기 §해소 기준을 명시 인용하며** 명백한 약화 주제(org-wide classic PAT 의 self-hosted 상주 재허용)를 **신규 ADR 0건 · `supersedes` 무변경 · amendment entry** 로 처리했다. 원문(§결정 8 Amendment 2 sunset carve-out 문단): *"본 ADR 해소 기준의 '약화 방향(org-wide classic write credential 의 self-hosted 상주 허용) 발의 차단' 은, **operator 가 명시 수용 근거를 기록한 본 exception 에 한해 carve-out** 한다 … **blanket 약화**(4-gate 없이 정적 상주 일반 허용 / fail-closed floor 삭제 / green 실증 생략)**는 여전히 차단**."* **단 그 carve-out 자체는 credential 축 한정이라 Amendment 5 가 상속하지 않는다** — 본 선례에서 취하는 것은 *carve-out 의 실체 범위*가 아니라 **개정 형태(약화 주제를 amendment 경로로 처리한다는 형식)** 뿐이다(아래 자기 carve-out 불릿 및 본 Amendment 말미 직교 축 declare 참조).
  - ⇒ **성립 원칙 = blanket 약화는 차단 / evidence-gated · scope-bounded · default-불변 · 명시수용 carve-out 은 supersede ADR 없이 amendment 가능.** 본 건 4조건 충족: ① **default 불변** — private/internal → self-hosted 가 여전히 기본 배정, 넘침은 예외 경로 ② **evidence-gated** — operator(사용자) 확정 지시가 *"무료분 안에서만"* 으로 총량을 명시 수용(Amendment 2 의 operator risk-accepted 와 형태 일치) ③ **scope-bounded** — hosted 무료 포함분 예산 상한 안의 정적 배분 총량 ④ **blanket 아님** — 아래 carve-out 경계로 명시 한정.
  - **절차 선례 인용 제한(정정)** — §결정 1·§결정 11 은 **원본 최초 저작 공동 원저**이며 amendment 이력(Amendment 1~4)에 §결정 11 이 **미등재**다. 따라서 §결정 11 을 *사후 amendment 판례*(절차 선례)로 인용하는 것은 문언상 부정확하며 **금지**한다. §결정 11 의 leg-keyed `vars[format(...)]` 구조는 **설계 템플릿 축**으로는 여전히 유효·재사용 가능하다(두 용법 혼동 금지).
  - ★**carve-out 경계 (supersede 발동 조건)**★ — **넘침이 예외가 아니라 default 가 되는 순간, 즉 §결정 2 의 기본 배정 자체를 visibility 축이 아닌 용량 축으로 대체할 때 본 amendment 경로는 무효이며 supersede ADR 이 필수다.** 예산 상한 봉인의 해제, 총량 무제한 hosted 배분, private/internal 기본값의 hosted 전환은 전부 이 경계 **밖**이다.
  - ★**Amendment 5 자기 carve-out (약화-발의 차단의 예외 — 술어-범위 축 한정)**★ — 본 ADR 해소 기준의 "약화 방향 … 발의 차단"을, **술어-범위 축 해석**(위 (a) 반대 해석 병기 — §결정 2 의 적극 배선 의무 정의역을 용량 조건으로 축소하는 것을 약화로 보는 해석)이 채택되는 경우에 한해 **본 Amendment 자신의** 조건부 carve-out 으로 codify 한다. 이는 Amendment 2 의 operator-accepted carve-out(§결정 8 "sunset carve-out(약화-발의 차단의 예외)" 문단 — credential 축 전용)을 **상속하는 것이 아니라**, Amendment 5 가 자기 축(용량/라우팅)에서 새로 정립하는 **독립 carve-out** 이다(본 Amendment 말미 직교 축 declare 의 *"Amendment 2 의 operator-accepted carve-out 을 상속하지 않는다"* 와 **무모순** — 상속 대신 자기 정립임을 본 문장이 명시하기 때문). 성립 요건 = 아래 4조건 **전부 AND** ① **default 불변**(private/internal→self-hosted 기본 배정 무변경) ② **evidence-gated**(사용자 확정 지시 *"무료분 안에서만"* 의 bounded 총량 수용) ③ **scope-bounded**(hosted 무료 포함분 예산 상한 봉인) ④ **명시수용**(operator 가 본 amendment 문면으로 인지·수용). 나아가 본 carve-out 은 **(c) C1/C2'/C3 전부 이행에 조건부**다 — 하나라도 advisory 강등·미이행이면 본 carve-out 은 무효화되고 판정은 술어-범위 축 포섭으로 전환된다. **blanket 약화**(4조건 없이 용량 예외 일반 허용 / 예산 상한 삭제 / C1~C3 미이행 방치)는 본 carve-out 범위 **밖**이며 여전히 차단된다.

  **(c) normative 조항 C1 / C2' / C3 (본 Amendment 의 성립 조건 — advisory 강등 불가)**: 아래 3 조항은 **normative** 이며, **하나라도 advisory 로 강등되거나 미이행이면 본 Amendment 자체가 약화가 되어 §해소 기준의 약화 방향에 포섭**된다(속성 단위 판정에서 되돌림이 약화시키는 속성 0 · 강화 2 라는 결론은 C1~C3 편입을 **전제로 한 조건부**이며, 잔여 약화 위험 3종을 셋이 봉인한다).

  1. **C1 — §결정 4(ii) unset 감지 lint 를 presence 검증 → 값 검증으로 승격**: 현 구현은 `gh api repos/{r}/actions/variables/{name}` 의 HTTP 성공 여부만 보고 값을 검사하지 않는다(`scripts/ci-runner-provisioning-check.sh:97-104` `_probe_var` — rc=0 → `present`). 승격 후 **valid** = ① JSON 배열 파싱 성공 ∧ ② 비어있지 않음 ∧ ★**(도메인 분류에 따라 ③④ 축 또는 ⑤ 축을 배타 적용)**★ — **self-hosted 라우팅 값**에는 ③④ 를, **hosted 값**에는 ⑤ 를 각각 단독 적용하고 미분류 값은 무조건 FAIL-LOUD 한다. ★**head 기호식 정정 (설계리뷰 iter2 추가-5)**★ — 종전 head 는 `① ∧ ② ∧ ③ ∧ ④ ∧ ⑤` **리터럴 5-conjunct AND** 였고, 그대로 읽으면 self-hosted 값(③④ 충족·⑤ 로스터 미등재로 실패)도 hosted 값(⑤ 충족 가능·③ 실패)도 **어느 쪽도 전부를 동시에 만족할 수 없어 tautologically FAIL** 이 된다. 바로 아래 산문이 배타 적용을 이미 규정하고 있었으므로(최초 초안부터 존재 — `1ba822f5f` 대조 firsthand) 이는 **head 기호식만의 결함**이며, Change Plan §4.2 가 ADR 을 편의적으로 다시 쓴 것이 **아니다**. 정정 없이 두면 **ADR 만 읽는 구현자가 5-conjunct 분기로 간다**. 각 축 정의: ③ **등록 러너 라벨 합집합의 부분집합**(SSOT = `orgs/mclayer/actions/runners` 의 `.runners[].labels[].name`, **status=online + 대상 runner group 필터**) ∧ ④ **그 AND-조합 라벨을 전부 가진 online 러너가 1대 이상 실재**(라벨 배열은 **AND 매칭** — source: docs.github.com "Choose the runner for a job") ∧ ⑤ **용량 carve-out 로스터 정합**(로스터 SSOT = private repo `mclayer/ci-runner-infra` 의 `capacity-overflow-roster.yaml` — hand-authored · append-only · PR merge 외 쓰기 채널 0. **실 변수값에서 파생 생성 금지** — 파생하면 *"SET 된 모든 값이 정의상 등재"* 가 되어 검사가 **tautological hollow** 로 붕괴한다) — hosted 값(`["ubuntu-latest"]` 등)은 **로스터에 등재된 repo/job 에 한해** valid 이고, 미등재 repo 의 hosted 값은 **FAIL-LOUD**(③④는 self-hosted 라우팅 값에 적용되는 축이므로, 두 축의 AND 로 판정해야 hosted 값이 검증 사각지대가 되지 않는다). **필수 mutant 2종 RED**: (m1) 로스터 **미등재** repo 에 `["ubuntu-latest"]` SET — 현 lint 는 `present` 판정으로 **PASS = 무력**(firsthand) (m2) 오타 라벨 `["self-hosted","X64","Linux","dockerr"]`. **"non-empty" 만 보는 검사는 여전히 hollow** — 두 mutant 를 RED 로 죽이지 못하는 구현은 **C1 미충족**이다. 기존 presence probe 는 §결정 4(i) 문면 충족 신호로 **존치**하고 값-정합은 **별도 축 추가**(대체 아님).
  2. **C2' — §결정 4(iii) org-level 금지의 신규 키 상속 명문화 + org-scope negative-presence 게이트 배선**: (ㄱ) §결정 4(iii) 의 **org-level variable 금지는 본 Amendment 가 도입하는 용량 축 신규 키에 그대로 상속**된다 — 신규 라우팅 키는 **repo-scoped 로만** provisioning 한다. (ㄴ) 이 금지는 현재 **기계 강제가 0**(반복 검증 스크립트·게이트 부재, 1회성 수동 관측뿐)이므로 negative-presence 게이트를 **두 축의 AND 로** 배선한다 — **① 상속 축(반복·PR-enforceable)**: 워크플로가 `vars` 컨텍스트를 직접 관측해 `^CI_RUNS_ON_` 패턴 키가 **0** 임을 assert 한다. `vars` 는 *"organization, repository, and environment levels"* 의 변수를 담고 미설정 키는 빈 문자열을 반환하므로(source: docs.github.com "Contexts") **credential 이 불요**하며, 이 검사를 **public repository 의 표준 GitHub-hosted 러너**에 두면 *"GitHub Actions usage is free for self-hosted runners and for public repositories that use standard GitHub-hosted runners"*(source: docs.github.com "About billing for GitHub Actions")이므로 §결정 4(ii) born-dead-gate(예산 소진 co-block) 취지와 **물리적으로 disjoint** 하다. 다만 검사 워크플로의 `runs-on` 은 **리터럴 표준 러너 고정**이어야 한다 — vars-라우팅으로 두면 바로 그 org 변수 주입이 검사기 자신을 미배정 상태로 몰아 **탐지기가 mutant 와 함께 죽는다**(자기무력화). **② 존재 축(operator/hub)**: `gh api orgs/$ORG/actions/variables/$NEW_VAR` **404 = PASS / 200 = FAIL-LOUD**(기존 presence-required 로직의 역 미러). 이 축은 org-admin 권한을 요구하므로 operator/hub 실행 층에 남으며 **반복성은 operator cadence 에 의존**한다. ★두 축은 대체가 아니라 AND★ — 상속 축은 `visibility` 가 검사 repo 를 포함하는 주입만 보고, **어느 검사 repo 에도 상속되지 않는 `visibility: selected` 변종은 존재 축만 커버**한다. (ㄷ) org variable `visibility: private` 로 완화하는 옵션은 **불채택** — enum 은 `all|private|selected` 이나 **`private` 이 internal 을 포함하는지 공식 문서에 미기재**(source: docs.github.com REST "Actions variables")이고 `mctrader` 가 **internal** 이라 그 모호성이 실 repo 에 걸린다. 근거 = variable precedence 가 *"the variable at the lowest level takes precedence"*(source: docs.github.com "Variables")이며 **public 2 repo 는 repo-level 변수 실측 0개**라 org-level 키를 두면 **즉시 상속**되어 §결정 2/3 의 delta-0(public = hosted 유지)을 파손한다. **(ㄹ) 상속 축 probe host 요건 — 아래 4항은 (ㄴ)① 의 성립 요건이며 요구를 늘리는 additive 조항이다(설계리뷰 iter2 P1-a 처분, 기존 (ㄱ)(ㄴ)(ㄷ) 문면 무접촉).** `vars` 는 organization·repository·environment 3 층을 함께 노출하면서 **각 값의 출처 층을 구별하지 않으므로**(source: docs.github.com "Contexts" — `vars` 속성 표에 레벨 필드 부재), (ㄴ)① assert 의 건전성은 전적으로 *"host repo 가 라우팅 변수를 보유하지 않는다"* 는 전제에 의존한다. **① public host(primary)** = `visibility: all` 및 검사 repo 를 포함하는 `selected` 주입을 관측한다. §결정 2 에 의해 public repo 는 라우팅 변수 미설정이므로 아래 ③ 전제가 자동 충족된다. **② private host((ㄴ)의 `visibility: private` 형태 전담)** = 그 형태는 public repo 에 원리적으로 상속되지 않아 ① 로는 관측 불가능하다. `visibility: private` 는 repo 열거가 아니라 repository 속성 기반 술어이므로(*"limit access to only **private repositories**"*, source: docs.github.com "Use variables") **org 내 임의의 private repository 가 host 자격을 갖는다** ⇒ 라우팅 부담이 0 인 전용 private repo 로 **고정 지정**한다. **③ ★host 의 routing-variable-free 전제(신설 normative)★** = host 는 (i) 모든 job 의 `runs-on` 을 **리터럴**로 적고 (ii) `^CI_RUNS_ON_` 매칭 repo-local 변수를 **SET 하지 않는다**. 이 전제에 따라 host 는 **§결정 4(i)·4(ii) 의 정의역 밖(out-of-scope)** 으로 지정되며, 지정은 로스터 SSOT 의 `probe_hosts[]` 에 hand-authored 로 기록한다(실 변수 상태에서 파생 생성 금지 — C1 ⑤ 의 tautological-hollow 회피 규율 동일 적용). ★**이 예외는 §결정 4(i) 의 약화가 아니라 목적-정합 carve-out 이다**★ — §결정 4(i) 의 목적은 라우팅 변수 unset 으로 인한 배정 파손의 fail-loud 검출인데 `runs-on` 을 전부 리터럴로 적는 repo 에는 그 실패 모드가 **정의상 존재하지 않으며**, 예외를 두지 않고 in-scope private repo 를 host 로 쓰면 §결정 4(i)를 **지킨** repo 에서 (ㄴ)① assert 가 born-RED 가 되어 상속 축이 배선 즉시 무효화된다. **④ ★허용 local key 집합 = 공집합 고정(신설 normative)★** = host 의 허용 repo-local 라우팅 키 집합은 **공집합**이다. `vars` 가 출처 층을 구별하지 못하므로 이 집합에 키를 1건이라도 추가하면 **그 키명에 대한 org 상속 주입이 동시에 불가시화**된다(repo-level 이 precedence 우선이라 값도 개수도 불변) ⇒ 이 집합의 확장은 **상속 축 정의역의 축소 = C2' 약화**로 계상하며 별도 amendment 없이 불허한다. 검사기는 공집합임을 자기 assert 하고 비-공집합 mutant 가 self-test 에서 **RED** 임을 실증한다. **(ㅁ) 라우팅 키 명명 규약의 normative 화** — (ㄴ)① assert 의 정의역은 `^CI_RUNS_ON_` **패턴**이므로 그 밖의 이름을 갖는 라우팅 변수는 **원천 미탐**이고 존재 축(②)도 키 **열거식**이라 미등록 이름을 보지 못한다. ⇒ 용량 축을 포함한 모든 라우팅 변수 이름은 `CI_RUNS_ON_` 접두를 **의무**로 하며(§결정 4(ii) required-key 집합 확장 의무와 짝), 접두 밖 라우팅 키 도입은 본 Amendment 범위 밖이다. 존재 축은 per-key probe 대신 **org 변수 목록 전수 조회 후 `^CI_RUNS_ON_` 매칭 0 assert** 로 승격해 이 축을 존재 축에서도 닫는다(추가 credential 0 — 동일 org-admin 실행면). **(ㅂ) (ㄹ)(ㅁ) 의 검증 oracle(기존 C2' oracle 에 additive)** — private host 에서 **3 conjunct 를 AND** 로 실증한다: ⓐ 청정 fixture(라우팅 키 0) → **exit 0** ∧ ⓑ 오염 fixture(`^CI_RUNS_ON_` 키 1건) → **exit 1** ∧ ⓒ ★**허용 집합이 비어있지 않은 fixture**(= §결정 4(i)를 준수해 repo-local 로 정상 SET 한 in-scope repo 형상) → **exit 0**★. ★단측 RED 는 채택 불가★ — ⓐⓒ 없이 ⓑ 만 보면 *"오염이라 RED"* 와 *"검사기가 정상 상태도 RED 로 읽는다"* 를 **구별하지 못한다**. org 변수 0 상태의 항진 GREEN 을 충족 증거로 사용하는 것을 금지한다. **(ㅅ) 정직 ceiling((e) 집행 천장에 additive — 완화로 표기 금지)** — ① `visibility: private` 주입을 관측할 수 있는 표면은 정의상 private repo 뿐이고 private repo 의 실행면은 **self-hosted(fleet 종속) ∨ hosted(예산 종속)** 둘뿐이므로 ★*"무료 ∧ fleet 독립 ∧ 예산 독립" 인 그 형태의 관측 표면은 원리적으로 존재하지 않는다*★. self-hosted 리터럴을 default 로 하고 fleet down 침묵을 수용한다 — hosted 리터럴 전환은 침묵 시점을 **예산 절벽(최대 위협의 실현 시점)** 으로 옮겨 §결정 4(ii) born-dead-gate 취지에 저촉하므로 **불채택**이다. ② private host probe 는 **non-required** 이므로 host repo 삭제·워크플로 수동 disable·기본 브랜치 개명 시 **무증상 침묵**한다. ⇒ *"C2' 가 `visibility: private` 주입을 kill 한다"* 는 **상속 축 한정 참 ∧ host 가 살아있는 구간 한정 참**이며, (e) 의 한정에 이 두 번째 한정을 **중첩**해 인용해야 한다. ③ 상속 축이 보는 것은 **org-scope 주입 · organization/repository 2 층 · `^CI_RUNS_ON_` 접두 키명** 셋으로 한정되며 — **repo-level 직접 주입**은 §결정 4(i)가 요구하는 정상 SET 과 `vars` 상에서 **원리적으로 구별 불가**, **environment-level 주입**은 검사 job 이 `environment:` 를 선언하지 않는 한 `vars` 에 미출현이면서 precedence 최상위(*"the environment-level variable takes precedence"*, source: docs.github.com "Use variables") — 그 밖은 **전 층 미탐**이다. ⇒ ★**C2' 는 adversary containment 가 아니라 drift·오설정 검출 + defense-in-depth 다**★. repo-level 오염 축의 실 방어선은 **C1 값-정합**이며 C2' 를 라우팅 오염 전반의 방어로 표기하는 것을 금지한다.
  3. **C3 — 라우팅 값 공간 폐쇄 + 트리거 유래 제외 + 통제-load-bearing pin 구조적 제외**: (ㄱ) 라우팅 변수의 **값 공간을 등록 러너 라벨 집합으로 폐쇄**한다(그 밖의 라벨은 provisioning 단계 거부 — C1 ③④와 동일 SSOT). (ㄴ) **fork PR 유래 및 `workflow_run` 유래 실행은 용량 축 재배정 대상에서 제외**한다 — §결정 8 의 fork-PR self-hosted 미실행 불변과 별개로, **특권 트리거의 실행면을 용량 조건으로 흔들지 않는다**(실측 2 site = `mctrader/deploy-k8s.yml`, `mctrader-web/ci-watch.yml`). (ㄷ) **통제-load-bearing 러너 pin 은 구조적으로 제외**한다 — 대상은 `mctrader/deploy-k8s.yml` 의 ARC 배포 3 job 과 `mctrader/sibling-pin-parity-lint.yml`(workflow name `sibling-pin-parity`)의 lint job 이며, 이들의 pin 은 **라우팅이 아니라 보안 통제**다(해당 workflow 는 kubeconfig write step 을 제거하고 ARC in-cluster pod service account 에 의존 — `runs-on` 을 바꾸면 통제가 소멸하거나 kubeconfig 를 GitHub secret 으로 export 하는 수선책이 유발된다). ★**job id ↔ 라벨 오기 정정 (설계리뷰 iter1 F-2)**★ — 종전 문면은 `mctrader-dev`/`mctrader-stg`/`mctrader-prd` 를 *job id* 로 적었으나 이는 **`runs-on` 라벨**이고, 실제 job id 는 `deploy-dev`(라벨 `mctrader-dev`) · `deploy-stg`(라벨 `mctrader-stg`) · `deploy-prd`(라벨 `mctrader-prd`) 이며 lint 는 job id `lint`(라벨 `[self-hosted, Linux]`) 다 [verified-PL firsthand]. **정정 이유** = 게이트가 *라벨로 job 을 탐색*하면 **0 매치 = 조용한 무력화(born-dead)** 가 되므로, **대상 특정 축 = job id · assert 값 = 라벨 리터럴**로 두 축을 분리해야 한다. 이들은 "모집단 제외"(계측 편의)가 아니라 **보안 통제 제외**로 분류하고, 차단은 **HEAD 정적 assert**(보호 job id 가 **존재**함 ∧ 그 job 의 `runs-on` 리터럴이 기대값과 **일치**함 — 두 conjunct 의 **AND**) 또는 그 상위집합으로 수행한다. ★**근거**★ — HEAD assert 는 diff 게이트가 놓치는 경로(base 전진·머지 커밋 유입으로 diff 에 안 잡히는 변경 / 게이트 신설 **이전부터 존재하던** 불일치 / job 삭제·rename)까지 포착하는 **상위집합**이며, **존재 conjunct 가 없으면 job 을 삭제하는 것만으로 일치 검사가 공허 참(vacuously true)** 이 되어 통제가 소멸한다.

  **(d) 동반 개정 5건**:

  1. **§결정 4(ii) required-key 집합 확장 의무** — 라우팅 키 신설 시 lint 의 검사 키 집합을 **동반 갱신**해야 한다(누락 시 신 키는 검사 사각지대). 현 구현은 `LINUX_VAR`/`WINDOWS_VAR` **2개를 하드코딩**(`scripts/ci-runner-provisioning-check.sh:45-46`)하고 §결정 11 windows 키는 `--require-windows` 플래그 블록(`:134-146`)으로 분기한다. 확장 비용 = **단일 파일 변경** — 호출자는 `scripts/bootstrap-consumer.sh:381-398`(operator/hub 실행)과 self-test 뿐이고 **워크플로 호출은 0건**(`scripts/test-ci-runner-provisioning-check.sh:217-222` 의 C9-born-dead-gate 가 그 0건을 강제) ⇒ **17 repo 전파 문제는 발생하지 않는다.**
  2. **§결정 4(iii) 신규 키 상속** — 위 **C2'** (ㄱ)(ㄴ)로 배선한다(조항 본체는 C2' 가 정본).
  3. ★**§결과 "제외" 절 stale 전제 정정**★ — §결과 제외 절의 *"`mctrader`/`deploy-k8s.yml` 3 ARC job(`mctrader-dev/stg/prd` — **이미 self-hosted**)"* 에서 괄호 안 전제(**이미 self-hosted**)는 실측과 어긋난다: 해당 라벨에 매칭되는 **online 러너 0대**이며 결과적으로 **24h queue-starvation 3건**이 반복 관측됐다. 즉 **제외 근거 자체가 affirmatively-FALSE** 다. 정정 = 이 3 job 의 제외 사유를 "이미 self-hosted 라서"가 아니라 **C3 (ㄷ) 통제-load-bearing pin 이라서**로 **재귀속**하고, 매칭 러너 부재 상태는 §결정 7 queue-starvation 가드의 **DR 사안**(사용자 통지)으로 분리 처리한다. Amendment 3/4 의 **append-only supersede-not-mutate** 관례에 따라 §결과 절 원문은 **무편집 보존**하고 본 Amendment 가 전방 참조로 정정한다.
  4. **§결정 1 자기선언 미확정 종결** — §결정 1 말미의 *"확인불가 defer … `fromJSON(...)` 이 `runs-on` 에서 array 로 확장되는 동작은 GitHub 공식 문서에 명시적 예시가 부재 … 실증 전 Phase 2 배선을 차단한다"*(**§결정 1 말미 "확인불가 defer (research-before-claims)" blockquote** — 동일 파일 §결정 11 에도 괄호 없는 *"확인불가 defer"* bullet 이 별도 존재하므로 괄호구가 앵커 유일성의 load-bearing 요소다)를 **공식 문서로 종결**한다: `jobs.<job_id>.runs-on` 에서 사용 가능한 컨텍스트 = **`github, needs, strategy, matrix, vars, inputs`**(source: docs.github.com "Contexts")이고, 라벨 배열은 **AND 매칭**(*"matches **all** of the specified `runs-on` values"*)이며 표현식이 **공식 지원**된다(문서 예시 `runs-on: [self-hosted, "${{ inputs.chosen-os }}"]` — source: docs.github.com "Choose the runner for a job"). ⇒ §결정 1 의 **dry-run 차단 조항은 해소**된다. **정직 한정**: 종결되는 것은 *표현식·배열 확장의 문법 성립*뿐이며, `fromJSON()` **malformed 입력의 fail-loud 동작은 여전히 공식 문서 미확정**[확인 불가]이므로 그 공백은 **C1 의 값 검증이 provisioning 층에서** 메운다(런타임 fail-loud 가정 금지).
  5. **(부기) ephemeral 권고 사정거리 정정** — §결정 8 persistent 완화의 *"ephemeral/JIT 를 권고"* 는 **유지**하되, 이를 **GitHub 공식 권고로 귀속할 때의 사정거리**를 정정한다: 해당 공식 권고 원문은 ***autoscaling* 구성 한정**이며 **고정 replica fleet 은 그 사정거리 밖**이다 ⇒ 고정 replica 운용과 GitHub 공식 권고 사이에 **충돌은 없다**. 권고 자체의 약화가 아니라 **과대 귀속의 정정**이다.

  **(e) 결과 · trade-off · 불변식**:

  - ★**T-15 미완화 declare (최대 위협 — honest ceiling)**★ — hosted 소비가 무료 포함분을 초과하면 예산 정책의 `prevent_further_usage: true` 가 **org 전체 private CI 를 정지**시킨다. 실패 모드가 **degradation → outage** 로 **승격**하며 **공격자 없이 자기 유발 가능**하다. 예산 소비는 **사후 관측**(billing 반영 **D-2 지연**)이라 **실시간 fail-closed 가드가 성립하지 않는다** ⇒ *"예산 가드를 배선했다"* 를 **안전성 단정으로 사용 금지**. **유일한 구조적 완화 = 넘침 총량의 정적 상한 봉인**(= 본 carve-out 의 예산-bounded 정적 배분 형태 그 자체)이며, 이 위협은 **완화되지 않은 채 남는다**.
  - **불변식 I-1 ~ I-4 (결정 조항으로 편입 — normative)**:
    - **I-1 라우팅 결정은 dispatch 시점 1회** — 큐에 든 job 의 실행면을 사후 변경하지 않는다(큐 job 취소·재발화로 재배치 금지). 근거 = 라벨은 job 생성 시점에 **baked** 되어 `vars` flip 이 in-flight job 에 **반영되지 않음**이 양성대조 포함 라이브 실험으로 확정됐다.
    - **I-2 실시간 예산 잔량 의존 금지** — 라우팅 판정 입력에 잔여 예산·fleet busy 등 **런타임 상태를 넣지 않는다**(T-15 의 D-2 지연으로 판정이 항상 stale, 판정 job 자체가 혼잡 뒤에 줄 서는 co-death 도 동반).
    - **I-3 degrade = fail-static-to-self-hosted** — 판정·배선이 불가하거나 모호하면 **self-hosted 로 남는다**. fail-open(hosted 로 흘려보냄)은 **비가역 org 정지**로 수렴하고 fail-closed(self-hosted 대기)는 **가역**이다. 동반 함정 = 체크 성공 상태에 `skipped` 가 포함되므로(*"Successful check statuses are `success`, `skipped`, and `neutral`"* — 공식 문서 인용 **승계**, URL 미승계 = 출처 확인 불가로 정직 표기) 라우팅 판정 step 의 실패가 종속 job 의 `skipped` → **required 게이트 통과**로 세탁되는 경로를 설계에서 **구조적으로 배제**해야 한다.
    - **I-4 동일 `(SHA, job)` 은 어느 시도에서든 동일 실행 위치(재실행 포함)** — 실행면이 시도마다 달라지면 사고 시 blast radius 산정과 *"지난 N일 어떤 실행면이 이 credential 을 materialize 했는가"* 감사 질의가 성립하지 않는다(감사 요건으로 승격).
  - **정직 한정 (over-claim 금지)** — (i) 본 carve-out 은 요구의 **결과**(로컬 고정 슬롯을 넘던 부하가 클라우드로 간다)를 충족하나 **메커니즘**(런타임에 busy 를 관측해 동적으로 넘김)은 **충족하지 않는다**: GitHub 은 self-hosted busy → hosted fallback 을 제공하지 않고(`runs-on` = AND 매칭이라 **OR fallback 표현 불가**), 어떤 변수 기반 장치도 **이미 밀린 큐를 배수하지 못한다**(라이브 실험 확정). **"동적 넘침을 구현했다"로 표기 금지** — 메커니즘 자체가 요구의 본질이라면 **사용자 escalate 사안**이다. (ii) 본 Amendment 는 **보안 강화 선언이 아니다** — 실행면 이동은 self-hosted 잔류 위험(persistent job 간 잔류 · `docker.sock` host blast radius)과 hosted **예산 절벽(T-15)** 을 **맞바꾸는** 것이며, net 판정은 **C1~C3 normative 편입을 전제**로 한 조건부다. (iii) **집행 천장(narrowing — 설계리뷰 iter1 F-2 반영)** — **C2' 의 상속 축은 PR-enforceable 기계 게이트로 승격**된다(public wrapper 의 기존 required 컨텍스트에 편승, credential 0, 예산·fleet 독립). **그러나 존재 축(어느 검사 repo 에도 상속되지 않는 `visibility: selected` 변종)은 operator/hub 층에 남으며 그 반복성은 operator cadence 에 의존**하고, **C1 의 값-정합 축도 provisioning-time 이 primary** 로 남는다. 또한 상속 축 게이트에도 천장이 있다 — 스케줄 지연·플랫폼 장애·워크플로 수동 disable 시 침묵하며, 이는 기계 게이트로 닫히지 않는다. ⇒ *"C2' 가 org-level 주입을 kill 한다"* 는 **상속 축 한정 참**이며 무조건 명제로 인용해서는 안 된다(약화가 아니라 정직 ceiling 의 정밀화).
  - **검증 oracle** — presence-only 검사·문서-only 선언은 **false-oracle 로 불허**(ADR-119 §결정 4·⑤): **C1** 은 위 mutant 2종(m1/m2) **RED**, **C2'** 는 **두 축 각각**(상속 축 = 검사 repo 에 상속되는 org 변수를 실제 주입했을 때 워크플로 assert **RED** ∧ 검사 워크플로의 `runs-on` 을 vars-라우팅으로 되돌린 **자기무력화 mutant 도 RED** / 존재 축 = org-scope 키 주입(200) 시 **FAIL-LOUD**), **C3** 는 보호 job 의 `runs-on` 라벨 변조 **및 그 job 의 삭제·rename** 이 **둘 다 RED**(삭제로 검사가 공허 참이 되지 않음 = 존재 conjunct 실증) 임을 각각 실증해야 한다(*"검사가 존재한다"* 는 충족 신호가 아니다).
  - **직교 축 declare** — 본 Amendment 는 **용량/라우팅 배분 축**이며 credential-sourcing(Amendment 1/2) · service-container networking(Amendment 3) · runner-infra durability(Amendment 4)와 **disjoint** 다. Amendment 1 적격 조건 7종 · Amendment 2 4-gate 어느 것도 supersede/reinterpret 하지 않으며, **Amendment 2 의 operator-accepted carve-out 을 상속하지 않는다**(그 carve-out 은 credential 축 전용). carrier = CFP-2913.

### §결정 3 — byte-parity blocking 정합 (templates ↔ .github lockstep)

- template `runs-on` 을 §결정 1 표현식으로 수정할 때, 비-`CONSUMER_ONLY_WORKFLOWS` workflow 의 self-app 사본(`.github/workflows/<base>.yml`)도 **동일한 표현식으로** 수정해 `invariant-check.yml` byte-parity 를 통과시킨다.
- **public wrapper 기능 불변**: `plugin-codeforge`(public)에서 `CI_RUNS_ON_LINUX_JSON` 은 미설정이므로 표현식이 `["ubuntu-latest"]` 로 coalesce → CI 는 hosted 에서 그대로 실행된다. 즉 "파일은 바뀌되(byte-identical vars 표현식 양쪽) 동작은 불변(unset var → hosted coalesce)". Story §4.1 의 ".github delta 0" 가정은 **오류** — 사본은 template 과 같은 vars 편집을 받아 byte-identical 을 유지해야 한다(delta = 동일 vars 표현식, 기능 inert).

### §결정 4 — provisioning invariant + fail-loud unset 감지 lint (private 안전은 default-flip 이 아니다)

RefactorAgent(in-file default=ubuntu-latest, public 안전)와 InfraOperationalArch(default=self-hosted, private billing-deadlock 회피)의 default 충돌을 chief tie-break ladder 로 해소한다:

- **ADR-068 invariant + §제약 3 지배**: public wrapper 가 같은 파일을 배포하므로 in-file coalesce default 는 **반드시 `["ubuntu-latest"]`** (public 은 self-hosted 물리 불가). default-flip 은 기각.
- **private 안전 = 2-layer 배선**으로 확보한다:
  - **(i) provisioning invariant** — 모든 in-scope private/internal repo 는 bootstrap Stage 5b(=`scripts/bootstrap-consumer.sh` L340-371 `wire-branch-protection.sh` 동형 hook 후보)에서 `CI_RUNS_ON_LINUX_JSON` variable 을 **SET** 한다.
  - **(ii) unset 감지 lint(fail-loud) — 실행면은 billing-독립 (P1-1)** — in-scope private repo 에 required 변수(§결정 11 matrix repo = `CI_RUNS_ON_LINUX_JSON` ∧ `CI_RUNS_ON_WINDOWS_JSON` 둘 다, 그 외 = `CI_RUNS_ON_LINUX_JSON`)가 미설정이면 fail-loud 로 감지한다. unset 은 hosted billing hard-block → 2-3초 FAIL → deadlock(AC-11)을 유발하며, 이는 조용한 pending 보다 나쁘다(InfraOp R-5 근거). **이 lint 를 hosted-CI 워크플로로 구현하면 안 된다** — 감지 대상인 billing 소진에 co-blocked 되어 발화 불가(**born-dead-gate** — Story §2.2/§2.7 이 확립한 hosted required check 2-3초 startup FAIL 과 동일 원인 class: css-lint born-invalid / execution-liveness 반복 재발). **실행면 = billing-독립 3택 조합**: ① **provisioning-time 스크립트(primary)** — bootstrap Stage 5b 에서 `gh api repos/{r}/actions/variables/{name}` 메타데이터 스캔(CI job 아님, operator/hub context = hosted 분 0)으로 변수 존재·값 검증, 미설정 시 provisioning 완료 차단 (**CI 실행 前이라 co-block 물리 불가**) ② **self-hosted runner 실행** — W0 착지 후 ongoing drift(변수 삭제) 검사를 self-hosted 에서(billing-free) ③ **org-level hub gh-api 주기 스캔**. **부트스트랩 순서 배선**: Stage 5b provisioning 검증(변수 present, fail-loud) → W0 runner 등록 → self-hosted ongoing drift 검사. "언제 lint 가 살아나는가" = provisioning-time(CI 이전) + self-hosted(W0 이후) — hosted-CI 아님.
  - **(iii) provisioning scope 제약 (P2-d)** — 변수는 **repo-scoped** 로 provisioning 한다. **org-level variable 금지**(또는 visibility-scoped private-only). org-level `CI_RUNS_ON_*_JSON` 은 public `plugin-codeforge`/`marketplace` 까지 override 해 §결정 2/3 의 delta-0(hosted 유지)을 파손한다.
- default-flip 을 provisioning invariant + fail-loud detection 으로 대체하는 이 판정은 Change Plan §7.4/§9 에 명문화한다.

### §결정 5 — preinstall baseline 계약

- **bare-tool baseline** (`git` / `bash` / `python3`+`pip` / `jq` / `gh` / `node`)는 self-hosted 이미지에 preinstall 을 의무화한다.
- **웨이브별 toolchain** (`rust` / `node` / `python` 버전 등)은 workflow 안 `setup-*` step 으로 주입한다(`runs-on` 이 아니라 job step 층 — RefactorAgent 단일 abstraction 유지).
- 카나리에서 `command -v <tool>` 실측으로 baseline 존재를 gate 한다(Change Plan §8). preinstall 목록 확정값은 카나리 실측 대상(`[empirical-source: canary 실측 대상]`, 추정 lock-in 금지).

### §결정 6 — 카나리 + 웨이브 롤아웃 (W0 부트스트랩 포함)

- **W0 (부트스트랩·1회성)**: SecurityArch P0 완화 순서 준수 — (1) 부트스트랩 PR 을 **runner 미등록 상태에서** 먼저 merge → (2) workflow diff 를 사람이 verbatim 리뷰 → (3) **그 후** runner 를 등록(코드 실행 전 검증). admin-merge 감사기록에 workflow diff 해시를 남긴다. **TOCTOU 가드(P2-a)**: runner 등록 직전 `main HEAD SHA == 검토된 workflow diff 의 base SHA` 를 재확인하는 게이트를 둔다 — 리뷰↔등록 사이 main 이 전진했으면 재리뷰(등록 차단). 부트스트랩 한정성은 "물리 게이트"가 아니라 **감사 convention**(1회성 라벨 + admin-merge 감사기록 + registration-time HEAD 재확인 게이트)으로 실태에 맞춰 bound 한다. 이는 billing hard-block 하에서 거버넌스 required check(check-gate/phase-gate-mergeable)가 dead 인 순환 merge-gate(Story §2.7 C-BOOTSTRAP)를 의도적·수동 admin-merge 1회로 돌파하는 조치이며, ADR-048 Amd2 의 *자동* admin-merge(5분 stuck fallback)와 구분된다.
- **카나리**: 1개 저부담 repo 에서 §결정 1 계약을 실측한다. 성공기준 = Change Plan §8 의 5-AND(실 runner metadata=self-hosted / pickup bounded / required context 전부 green / mergeStateStatus=CLEAN / preinstall baseline 실측).
- **웨이브**: 카나리 PASS 후 나머지 private/internal repo 를 웨이브로 순차 이관. Windows leg(단일 runner group5)는 직렬화 병목이므로 각 웨이브 내 최후순위. `--scale` 은 웨이브 최대 병렬 이상으로 설정한다(값 = 카나리 실측).

### §결정 7 — queue-starvation 24h 가드

- self-hosted 의 3 실패모드(runner offline / label-mismatch / group-거부)는 "Expected but never created" 상태(runner 부재로 check 가 아예 report 되지 않음)를 만들어 admin-merge rerun 조차 무효화한다.
- **가드**: job pickup 이 24h 내 발생하지 않으면 fail, 48h 내 미완료면 취소한다(source: docs.github.com "Self-hosted runners reference — usage limits"). 이 상태는 "정상 대기"가 아니라 DR 사안(사용자 통지)으로 분류한다 — §결정 9 참조.

### §결정 8 — fork PR self-hosted 미실행 + DooD/persistent 완화

- **fork PR + `pull_request_target` = self-hosted 미실행 유지**(SecurityArch Q-3, 비가역·보안). self-hosted 는 `workflow_dispatch`(수동) + 동일 repo 브랜치 push(org 멤버)만 트리거한다. fork PR CI 가 필요하면 hosted 를 유지한다 (source: docs.github.com "Security hardening for self-hosted runners" / GitHub Security Lab "Preventing pwn requests").
- **org allow_forking 실측(PL firsthand)**: `mctrader`(internal) `allow_forking=false`, org `default_workflow_permissions=read`, `can_approve_pull_request_reviews=false` → fork-PR 공격 표면은 대체로 CLOSED. 단 18 repo 전수 `allow_forking` 확인은 구현/리뷰 lane prerequisite 로 명시한다.
- **DooD 완화**: DooD job 은 host root 등가(`docker.sock` → escape) 이므로 non-privileged container / trusted 화이트리스트로 제한한다.
- **persistent runner 완화**: cross-job 오염(secret / tool-cache) 차단을 위해 ephemeral/JIT 를 권고하고, job 후 purge + secret 을 CLI-arg 로 전달 금지한다. org-wide PAT(`CODEFORGE_CROSS_REPO_PAT`) 사용 job 은 hosted 유지 또는 OIDC 로 전환한다.

**Amendment 1 (CFP-2821, 2026-07-25) — runtime secret-manager JIT sourcing = 3rd sanctioned mitigation path (bounded)**: 위 "org-wide PAT(`CODEFORGE_CROSS_REPO_PAT`) 사용 job 은 hosted 유지 또는 OIDC 로 전환한다" 의 bounded enumeration 에 제3의 sanctioned mitigation path 를 추가한다 — **runtime secret-manager JIT sourcing**: org-wide PAT 를 정적 GH-secret store 에서 제거하고, runtime 에 secret-manager(1Password service account, `1password/load-secrets-action@<full-SHA>`)로 **JIT env-only injection**(CLI-arg 0, auto-mask)한다. bootstrap credential = **read-only single-purpose-vault narrow-scope service-account token**(`OP_SERVICE_ACCOUNT_TOKEN`). 기저 = CFP-2743 Change Plan §10.1 amendment TEXT 초안의 부활(구현리뷰 FIX iter1 B-hosted supersede 의 전제[fix-ledger 의 persistent 러너 이탈]를 CFP-2821 사용자 확정 R2 가 재개봉).

  본 path 의 self-hosted 적격 = 아래 7종 조건 **전부 AND** 충족 시에 한한다:
  1. **private 한정** — `private` ∧ `allow_forking=false` ∧ runner group `allows_public_repositories=false` 3중.
  2. **fork-PR self-hosted 미실행 불변** — 본 §결정 8 1문단 무약화.
  3. **write credential least-privilege** — org-wide classic PAT 의 self-hosted 상주 금지: fine-grained `issues:write`@대상 repo 한정 또는 scoped GitHub App installation token(≤1h). **ADR-066 Amendment 5 exception(classic No-expiration)은 self-hosted 경로에 미적용.** **ADR-013 Amendment 4 cross-ref(reconciliation — CFP-2821 DR FIX iter1 F-DR-2821-1)**: 본 조건이 강제하는 self-hosted 전용 down-scope fine-grained credential(별도 물리 토큰)은 ADR-013 Amendment 4 §결정 2/3 의 "단일 `CODEFORGE_CROSS_REPO_PAT` consolidation(신규 PAT 도입 시 별도 ADR 발의 의무)" 을 **self-hosted surface 한정 supersede-in-part** 한다 — 방향 = 강화(persistent 공유 러너에 상주하는 credential 의 org-wide classic write → 1-repo `issues:write` scope 축소, blast-radius 축소), evidence-gated(fix-ledger-sync 14일 상시-FAIL census[#2820] + self-hosted 대조군 지속 성공 + group6 online 실측; ADR-064 §결정 7 Amendment 8 symmetric ratchet 정합). **literal 위반 아님**: (i) ADR-013 §결정 3 "신규 PAT 도입 시 별도 ADR 발의 의무" = 본 Amendment 1 이 그 발의를 **이행**(위반이 아니라 충족) (ii) ADR-013 §결정 2 "신규 secret 도입 금지(cross-repo write-PAT 비확산)" = cross-repo write credential 은 workflow `CROSS_REPO_PAT` env(port) 불변 — org-wide classic PAT 는 static GH secret store 에서 제거(JIT materialization), 신규 GH secret 은 read-only `OP_SERVICE_ACCOUNT_TOKEN`(vault-access 별도 credential class, cross-repo write PAT 아님) 뿐이고, down-scope fine-grained PAT = 1Password vault-item 값 ≠ GH secret. 자기 선례 형식 = ADR-145 §결정 3(fail-closed 비호환 override codify) / ADR-125(required-context divergence 추적)의 supersede-in-part 패턴 답습. reciprocal 각주 = ADR-013 Amendment 4(bidirectional cross-ref).
  4. **runtime JIT sourcing 의무** — env-only injection + auto-mask + CLI-arg 0(정적 env-binding 원복 금지).
  5. **residual 완화 의무** — post-job env-purge + workflow `permissions` 최소 + T4/T5(persistent 러너 cross-job residue) **명시 수용 declare**(Story/Change Plan §7).
  6. **supply-chain** — secret-bearing action full-SHA pin + egress allowlist region-correct(secret-manager endpoint 는 SA 계정 region 실측 — repo 실측 불가 항목은 operator 확인으로 정직 처리).
  7. **scope 검증 = SecurityTest lane 실측** — SA token 의 read-only ∧ single-vault scope 는 presence-only 검사로 검증 불가(false-oracle) → provisioning-side 실측 의무.

  **정직 한정(over-claim 금지)**: 본 path 는 standing-credential-**0**(OIDC 등가) 아님 — standing SA token 1 잔존. T4/T5(persistent 러너 job-env cross-job residue)는 OIDC 포함 **전 credential 모델 공통 limb** 이며 ephemeral/JIT-*runner* 전환으로만 완전봉인 — 본 path 의 differentiator 로 서술 금지. carrier = CFP-2821(Change Plan `cfp-2821-fix-ledger-selfhosted-migration` §7/§10).

**Amendment 2 (CFP-2821, 2026-07-25) — operator risk-accepted static-org-wide-PAT-on-self-hosted user-override exception (evidence-gated, Story-local)**: Amendment 1 조건 3(org-wide classic PAT 의 self-hosted 상주 금지)·조건 4(runtime JIT sourcing 의무, 정적 env-binding 원복 금지)에 대해 **operator 명시 risk-accept 를 기록한 특정 Story 한정 user-override exception** 을 codify 한다. 배경 = CFP-2821 이 Amendment 1 JIT path 를 채택하려던 credential 축을, Orchestrator 가 보안 trade-off(T4/T5 org-wide PAT 의 self-hosted persistent 러너 상시 노출)를 명시 제시한 뒤 **사용자가 정적 `CODEFORGE_CROSS_REPO_PAT` 직접 env-bind 로 override 확정**(FIX Ledger Iter 4, Story §10). 이 정적-org-wide-PAT-on-self-hosted 는 §결정 8 기존 2-path(hosted 유지 / OIDC) 도 Amendment 1 JIT path(조건 3/4) 도 아니므로, "재편집 0 / available-but-unused" 로 방치하면 running system(정적 PAT 사용) ↔ ADR/arch 문안(정적 org-wide classic 상주 금지) 이 **affirmatively-FALSE 모순** 을 이룬다 — 본 Amendment 가 그 모순을 정직하게 codify 로 해소한다(방향 되돌리기 아님).

  본 exception 은 아래 4-gate **전부 AND** 충족 시에 한해 조건 3/4 를 carve-out 한다:
  1. **operator 명시 risk-accept 기록** — org-wide PAT 의 self-hosted 상주(T4/T5 광역 노출 재수용)를 사용자가 보안 trade-off 인지 후 명시 수용, Story/Change Plan §7·§10(FIX Ledger)에 근거 기록.
  2. **fail-closed floor 보존** — credential-absent 시 cross-repo write 前 red(정적 모델 = `env: CROSS_REPO_PAT: secrets.CODEFORGE_CROSS_REPO_PAT` unset → `!pat` script-level pat-absent gate) 능력 무손실. 게이트 무력화(가드 삭제로 강제 green)는 exception 범위 밖(불허).
  3. **Story-local scope** — 본 exception 은 해당 Story(CFP-2821 fix-ledger-sync) 한정 채택이며 정적 PAT 의 self-hosted 상주를 **일반 승인하지 않는다**. 향후 재사용은 매건 operator 결정 + 본 4-gate 재충족 필요(default 승격 아님).
  4. **green 도달 실증 게이트** — 정적 exception path 가 "종이 예외" 로 남지 않도록, 해당 workflow 가 self-hosted 러너에서 실제 green(run conclusion=success ≥1) 에 도달함을 실측으로 증명(AC-4 empirical). presence-only/문서-only 우회 = false-oracle(불허).

  **§결정 8 default 불변**: JIT/hosted 우선 default 는 무변경 — §결정 8 2-path + Amendment 1 JIT path 가 여전히 **preferred sanctioned path** 이고, 정적-PAT-on-self-hosted 는 **operator-accepted per-Story deviation** 이지 새 default 가 아니다. Amendment 1 조건 3/4 원문·조건 7종 AND·sunset 조항은 **무삭제**(JIT path 용으로 유효 잔존).

  **sunset carve-out(약화-발의 차단의 예외)**: Amendment 1 sunset 조항 및 본 ADR 해소 기준의 "약화 방향(org-wide classic write credential 의 self-hosted 상주 허용) 발의 차단" 은, **operator 가 명시 수용 근거를 기록한 본 exception 에 한해 carve-out** 한다 — 즉 정적 상주는 무근거 약화가 아니라 evidence-gated(fix-ledger-sync 14일 상시-FAIL census[#2820] + 사용자 명시 override + green 실증) operator-accepted deviation 으로 codify 됨. blanket 약화(4-gate 없이 정적 상주 일반 허용 / fail-closed floor 삭제 / green 실증 생략)는 여전히 차단.

  **자기 선례 형식** = ADR-145 §결정 3(fail-closed 비호환 override-codify — 사용자 명시 결정에 의한 상반 방향의 정식 codify) + 본 §결정 8 Amendment 1 의 supersede-in-part 패턴 답습. **정직 한정(over-claim 금지)**: 본 exception 은 보안 강화 아님 — T4/T5 광역 PAT 노출을 **재수용**(약화 방향)하는 operator trade-off 이며, blast-radius 는 Amendment 1 JIT path(down-scope) 대비 **크다**. "정적 채택이 더 안전/동등" 서술 금지. carrier = CFP-2821(Change Plan `cfp-2821-fix-ledger-selfhosted-migration` §7.3/§10.1, DR FIX R2 F-R2-01).

**Amendment 3 (CFP-2858, 2026-07-28) — self-hosted DooD service-container 도달 결정론 wiring (host.docker.internal:host-gateway 정적 배선)**: 위 DooD 완화(docker.sock host-root-equiv → non-privileged / trusted 화이트리스트)는 러너 컨테이너의 **권한 격리** 축만 규정하고 sibling **service-container 네트워크 도달**(DNS reachability) 축은 미규정이었다. 본 Amendment 는 그 gap 을 **additive codify** 한다 — **self-hosted DooD 러너는 sibling service-container(postgres · MinIO+etcd 등) 도달 결정론화를 위해 러너 launch 시 정적 `extra_hosts: ["host.docker.internal:host-gateway"]`(compose 층) 을 배선해야 한다. 암묵 Docker Desktop runtime DNS 주입 의존은 금지한다(non-deterministic 간헐 실패 유발).**

  **근본원인(firsthand)**: 러너 compose(`C:\actions-runner-linux\docker-compose.yml`, 비-version-controlled)가 `ExtraHosts:[]`(정적 `/etc/hosts` 항목 부재)라 host.docker.internal 해결이 Docker Desktop 암묵 runtime DNS 타이밍에 전적 의존 → job 시작 시점에 따라 간헐 실패(same-runner flake: 동일 러너 586219a3c646 07-26 FAIL / 07-24·25 SUCCESS 실측; 실패 시그니처 = `psql: could not translate host name "host.docker.internal"` = **name-resolution**, timeout 아님). `extra_hosts`(= `--add-host host.docker.internal:host-gateway`, Docker 20.10+)는 launch 시 컨테이너 `/etc/hosts` 에 **정적 항목**을 기록(nsswitch files-before-dns) → runtime DNS 타이밍 의존 제거 = **결정론화**. `host-gateway` = 추상 special value(러너 Docker Server 29.4.3 실측 충족; **IP 하드코딩 금지** — Docker Desktop 버전 의존 192.168.65.254 등).

  **fix locus = 러너-infra(공통 층)**: 러너 compose 1줄 → `docker compose up -d --build` 재생성으로 영향 4 repo/7 job(mctrader · mctrader-backtest · mctrader-data · mctrader-web, 전부 `container:` 미선언) **1회 해소**, workflow 파일 delta 0(CFP-2607 이 PGHOST host.docker.internal/localhost 분기를 이미 workflow 배선). per-workflow(a) 는 `container:` 없는 job 의 러너 컨테이너 `/etc/hosts` 를 수정할 workflow-level knob(`jobs.<id>.services.<id>.options`=service 전용, `jobs.<id>.container.options`=job 전용) **부재**로 구조적 불가.

  **직교 축 declare**: 본 Amendment 는 service-container **network reachability 축**(§결정 8 DooD 완화)이며, Amendment 1(runtime secret-manager JIT sourcing) · Amendment 2(operator static-PAT exception)의 **credential-sourcing 축**(org-wide PAT materialization)과 **disjoint** — Amendment 1 적격 조건 7종 · Amendment 2 4-gate 어느 것도 supersede/reinterpret 하지 않으며, 본 Amendment sunset 은 Amendment 2 의 operator-accepted carve-out 을 상속하지 않는다.

  **CFP-2607 관계**: CFP-2607 §OOS "runner 인프라 provisioning 자체(이미 완료)" 는 러너 존재·등록·기동은 유효하되 그 안의 **service-container 네트워크 도달 결정성** 서브속성이 미완결(compose 주석 '해결' 선언 ↔ `ExtraHosts:[]` 실측 괴리)임을 본 Amendment 가 표면화·완결한다 — **부분 무효**(전면 무효 아님).

  **정직 한정(over-claim 금지)**: (i) 본 Amendment 는 **네트워킹 완결이지 보안 강화 아님** — 러너는 이미 docker.sock=host-root-equiv(위 DooD 완화)를 보유하므로 host-gateway 정적 항목은 기존 대비 **한계/무 노출**(신규 trust boundary 0, 신규 도달성 부여 아님 — 안정 러너는 이미 runtime DNS 로 host.docker.internal 해석 중, 배선은 그것을 정적·결정론화). (ii) 집행 = **operator-convention**(러너 compose 비-VC → §결정 4 provisioning invariant 의 fail-loud lint 집행 모델 **상속 불가**; §결정 6 W0 감사 convention 동형) — **PR-enforceable 기계 게이트 아님**, durability gap(머신 reprovision/compose 재작성 시 fix 유실) 잔존(러너 infra version-control 편입은 3문 게이트 follow-up). (iii) 검증 oracle = 배선 후 영향 job **다회 green 실측**(outcome ground-truth) ∧ fleet 8 replica 전수 `ExtraHosts≠[]` 결정론 sweep — **presence-lint(compose 문자열 존재) 단독 = false-oracle**(불허, ADR-119 §결정 4·⑤). carrier = CFP-2858(Story `wrapper/stories/CFP-2858.md` §7).

**Amendment 4 (CFP-2890, 2026-08-06) — 러너 infra version-control 편입 + `.rustup`(+`.cargo`) per-replica anonymous volume EXDEV-hardening**: Amendment 3 이 정직 declare 한 durability-gap follow-up("러너 infra version-control 편입은 3문 게이트 follow-up")을 이행한다. 위 DooD 완화·Amendment 3 이 규정한 러너-infra 는 **비-version-controlled**(host-local `C:\actions-runner-linux\`)였고, 이것이 (a) 재빌드/machine reprovision 시 extra_hosts + toolchain fix 유실(single-point-of-loss·review 0·재현성 0) (b) Dockerfile 이 rust stable 을 이미지 lower-layer 에 pre-bake → fresh 컨테이너의 `rustup toolchain install stable` **replace 경로**가 overlay cross-layer rename EXDEV(os error 18)를 일으키는 잠복 결함을 남겼다. 본 Amendment 는 이 durability 축을 **additive codify** 한다.

  **(1) VC 편입 = 신규 private repo `mclayer/ci-runner-infra`**: 러너 infra 3파일(Dockerfile·docker-compose.yml·entrypoint.sh)을 신규 **private** repo 로 편입한다(§제약1 public repo 물리 배제 + §결정2 self-hosted=private/internal 전용 — docker.sock=host-root-equiv·org URL·group·labels·host-gateway 평문 topology 노출). extra_hosts `host.docker.internal:host-gateway` verbatim 보존(Amendment 3, IP 하드코딩 금지). secret 무커밋(`.gitignore` = `.runner`·`.credentials*`·`_work/`·`.env`·`*token*`; RUNNER_TOKEN 은 compose runtime env — cross-repo write PAT 아님, credential 축 직교).

  **(2) `.rustup`(+`.cargo`) per-replica anonymous volume EXDEV-hardening**: overlay 디렉터리 rename EXDEV(os error 18)의 근본원인 = `.rustup` 이 overlay(이미지 lower-layer)에 상주해 rustup replace 백업 rename(`$RUSTUP_HOME/toolchains/<ch>` → `$RUSTUP_HOME/tmp`)이 lower→upper 병합 경계를 넘음. 근치 = `.rustup`(+`.cargo`)을 **per-replica anonymous volume**(compose `services.runner.volumes` host-path 없는 target-only 선언 — Dockerfile `VOLUME` 미채택, compose 단일 SSOT)로 이전 → 단일 실 fs 라 rename 이 volume 내부 완결(경계 소멸; firsthand: volume 내부 dir rename 성공 / overlay EXDEV 는 job-context 특정·manual 재현 불가 → 근치는 트리거 무관 robust). **named 금지**(replicas:8 단일 volume 공유 → 동시 rustup 쓰기 corruption; anonymous = per-replica 격리 firsthand 확증). **mount = `/home/runner/.rustup` 루트 필수**(subpath 시 `tmp` 가 volume 밖=경계 재발). 빈 anonymous volume 이 이미지 pre-baked 콘텐츠로 **copy-up populate**(Docker 표준) → pre-bake 유지 → engine/backtest/market main-leg(`rustc --version` 전용 소비) **workflow delta 0**. option-(a) `--default-toolchain none` 은 pre-bake 의존 3 repo cross-repo blast 로 기각.

  **(3) §결정 5 supersede-in-part (rust group6 Linux 한정)**: §결정 5("웨이브별 toolchain 은 workflow 안 `setup-*` step 으로 주입", rust 명시 열거)를 **self-hosted Linux group6 rust toolchain 한정 supersede-in-part** 한다 — rust 러너 이미지 pre-bake + per-replica anonymous volume durability 모델이 group6 에 한해 setup-* 주입 default 를 대체(cross-repo blast 회피 근거). **그 외 toolchain(node/python 매트릭스 버전 등)은 §결정 5 원문 무변경.** rust 를 §결정 5 가 명시 열거했으므로 암묵 gap 이 아닌 정면 배치 → carve-out 아닌 정직 supersede-in-part(자기 선례 = Amendment 1 §결정8 조건3 ↔ ADR-013 Amd4 reciprocal supersede-in-part / Amendment 2 supersede-in-part 패턴 답습).

  **(4) reinterpretation=true 근거**: Amendment 3 서술 "러너 compose 는 **비-version-controlled** 라 §결정 4 provisioning invariant 의 fail-loud lint 집행 모델 상속 불가 — PR-enforceable 기계 게이트 아니며 durability gap 잔존" 의 factual premise("비-VC")가 CFP-2890 이행으로 **affirmatively-FALSE** 가 된다 → 해당 clause 를 supersede-in-part(재해석). Amendment 2 가 정립한 "'재편집 0 / available-but-unused' 로 방치하면 running system ↔ ADR/arch 문안이 **affirmatively-FALSE 모순** → codify 로 해소" 판정기준과 동형(Amendment 1 의 순수 additive bounded-enumeration 확장[reinterpretation 부재]과 대조). VC 편입 후 source 계층 review/PR-diff·lint 가 **구조적으로 가능**해지고(이전엔 repo 부재로 불가) "reprovision/compose 재작성 시 유실" **특정** durability gap 이 닫힌다. **Amendment 3 텍스트는 무편집 보존**(append-only supersede-not-mutate) — 본 Amendment 가 새 entry 로 전방 참조 supersede-in-part 선언(Amendment 2 가 Amendment 1 조건3/4 를 무편집 supersede-in-part 한 형식 답습).

  **(5) 집행 천장 정직 (INV-1 보존, over-claim 금지)**: 본 Amendment 가 뒤집는 것은 **source 계층 PR-enforceable 가능성뿐** — running 8-replica fleet 반영은 여전히 operator `docker compose up -d --build` 액션 의존(source↔deploy 분리 집행 천장 불변, PR-merge 자동 fleet 갱신 아님). "VC=자동배포/자동강제" over-claim 금지. durability gap 소거 = VC + operator sync 규약 결합. **volume-shadow staleness 주의**(운영): plain `docker compose up -d --build` 는 이전 컨테이너 anonymous volume 데이터를 보존·재부착(`-V/--renew-anon-volumes` 로만 갱신) → toolchain-bump 재빌드가 fleet 에 반영되려면 `down -v`(fresh copy-up) 필수 — reprovision runbook + AC-3 검증 전제.

  **검증 oracle** = 2-축 AND(outcome ground-truth): (A) 재생성 후 fleet 8 replica **전수** — `.rustup`(+`.cargo`) mount Type=volume ∧ per-replica volume Name 상이(named-shared=RED) ∧ copy-up populate(`.rustup/toolchains` non-empty) ∧ rustc 도달 (B) setup-rust composite 우회 raw `rustup toolchain install stable`(replace 경로 강제) **reprovision-첫-run**(`down -v` 직후, self-heal·마스킹 전) 다회 conclusion=success + EXDEV signature 0. **false-oracle 배제**: presence-lint(compose `volumes:` 문자열)·fresh-install-only·manual docker exec = 불허(ADR-119 §결정 4·⑤; 3중 마스킹[setup-rust 멱등-skip·workflow-측 EXDEV fallback·copy-up self-heal] 우회 필수). consumer pre-bake 회귀 = engine/backtest/market main-leg GREEN(rustc-not-found=copy-up 실패=option-(a) 회귀 시그니처→요구 lane 재진입).

  **직교 축 declare**: 본 Amendment 는 durability/EXDEV-hardening 축이며 **네트워킹 완결(Amendment 3)·credential-sourcing(Amendment 1/2)과 disjoint** — Amendment 1 적격 조건 7종 · Amendment 2 4-gate 어느 것도 supersede/reinterpret 하지 않으며 Amendment 2 의 operator-accepted carve-out 을 상속하지 않는다. **보안 강화 아님**(infra durability — 신규 trust boundary 0, anonymous volume=기존 docker.sock host-root-equiv 공격표면 부분집합). carrier = CFP-2890(Story `wrapper/stories/CFP-2890.md` §7).

### §결정 9 — ADR-048(ci-native) Amendment 필요성 (stuck 판정 pickup-기반)

- `ADR-048-ci-native-test-execution` Amendment 2 의 5분 stuck-classifier 는 self-hosted 의 정상 queue-time 대기(특히 group5 Windows 단일 runner 의 matrix leg 직렬화)를 stuck 으로 오판해 premature admin-merge(게이트 무력화)를 유발할 수 있다.
- **carve-out**: stuck 판정을 pickup-기반으로 정련한다 — job 이 runner 에 **assigned + started** 인가(정상 실행 → 임계 상향: self-hosted 20~30분, Windows leg 별도) vs 단순 pending·미배정(진짜 stuck → DR: 사용자 통지, admin-merge 아님). 임계 상향값은 `[empirical-source: canary 실측 대상]` (추정 lock-in 금지).
- **본 §결정 9 가 carve-out 의 normative 정의다**: (a) stuck 판정 입력 = job 이 online·label-match runner 에 **assigned+started** 되었는가(`gh api .../runs/{id}/jobs` 의 `runner_name` non-null ∧ `status=in_progress`) (b) assigned+started = 정상 → self-hosted 라벨 job 은 stuck 임계 상향(카나리 실측값, 잠정 20~30분; Windows group5 leg 별도 상향) (c) **미배정(no matching online runner) = 진짜 stuck → DR 경로(사용자 통지, admin-merge 아님)** — self-hosted queue-time 을 hosted 초 단위 가정으로 오판해 premature admin-merge 하는 게이트 무력화를 차단한다.
- `ADR-048-ci-native-test-execution` **Amendment 3** 은 본 §결정 9 를 cross-ref 로 채택한다(Phase 2 prerequisite — Change Plan §10). 실 ADR-048 파일 amendment(amendment slot claim 포함)는 구현 lane follow-up.

### §결정 10 — ADR-RESERVATION 인용 slug 의무 + ADR-005 mirror 오귀속 정정

- ADR-048 번호는 (CFP-2566 renumber 이전) dual-occupancy 였다: `ADR-048-ci-native-test-execution`(Accepted, canonical) + ghec-governance-as-code(Proposed, orphan — CFP-2566 에서 `ADR-162-ghec-governance-as-code` 로 renumber 해소). 본 ADR 이 ADR-048 을 인용할 때는 **slug 를 명시**한다(과거 ambiguous 이력 — 번호만 인용 금지 관례 유지). related_adrs 에서 `ADR-048-ci-native-test-execution` slug 명시로 이 의무를 이행한다.
- ADR-005 mirror 오귀속 정정: workflow byte-identical mirror invariant 의 SSOT 는 ADR-005 가 아니라 ADR-027 Amd3(marker) + ADR-063(marketplace) + 실 enforcement=`invariant-check.yml` 이다. 본 ADR 은 mirror 를 ADR-005 에 귀속하지 않는다(ADR-005 = N/A 표준화만).

### §결정 11 — matrix os-leg / container-job per-runner 차등 (Surface B, P1-2)

**gap**: `runs-on: ${{ matrix.os }}` 로 `matrix.os:[ubuntu-latest,windows-latest]` 를 도는 job(firsthand 확인: `mctrader-engine/.github/workflows/rust-ci.yml`; 4 matrix rust-ci repo = mctrader/-engine/-market/-data-collector)에 §결정 1 단일 key 를 적용하면 두 leg 가 같은 변수 값으로 resolve → Windows leg 가 group5 라우팅을 상실(붕괴). §결정 5 의 `setup-*` step 은 도구 설치이지 runner **선택**이 아니라 gap 미해소.

**mechanism = leg-keyed 변수 + include-based hosted default**:

```yaml
strategy:
  matrix:
    include:
      - leg: linux
        runner_default: '["ubuntu-latest"]'
      - leg: windows
        runner_default: '["windows-latest"]'
runs-on: ${{ fromJSON(vars[format('CI_RUNS_ON_{0}_JSON', matrix.leg)] || matrix.runner_default) }}
```

- **leg 개별 라우팅 보존**: linux leg → `CI_RUNS_ON_LINUX_JSON`(private=`["self-hosted","X64","Linux","docker"]` → **group6**, public/unset=`["ubuntu-latest"]`); windows leg → `CI_RUNS_ON_WINDOWS_JSON`(private=`["self-hosted","Windows","X64"]` → **group5**, public/unset=`["windows-latest"]`).
- **byte-identical**: `matrix.include` 블록 + `runs-on` 표현식이 repo 간 uniform → §결정 3 byte-parity 무붕괴. 분기는 leg-keyed 변수 **값**에서만.
- **container job**: DooD/`container:` job 은 항상 linux leg key(group6, Windows docker 부재 §결정 2)로 pin.
- **provisioning invariant(§결정 4)**: matrix repo 는 `CI_RUNS_ON_LINUX_JSON` ∧ `CI_RUNS_ON_WINDOWS_JSON` 둘 다 SET; unset 감지 lint 는 둘 다 검사.
- **범위 구분**: Surface A(거버넌스 템플릿 = uniform ubuntu 단일 leg)는 §결정 1 단일 key 로 충분. 본 §결정 11 은 Surface B 4 matrix rust-ci + container job 한정.
- **확인불가 defer**: `vars[format(...)]` 동적 변수 lookup + `matrix.include` 의 `runner_default` 참조 조합은 fromJSON-array(§결정 1)와 동일하게 GitHub 공식문서 명시 예시 부재 → **KU-2 dry-run 에 windows-leg 확장 실증 추가**(Change Plan §8). 기존 `matrix.os` step 참조(setup-*/cargo target 등)는 `matrix.leg` rename 시 Phase 2 InfraEng 가 동반 갱신(또는 `os` 필드 병기 보존).

### §결정 12 — 관측성 (P2-e)

- **app-level runtime metric = N/A** (앱 런타임 0 — CI 배선 governance).
- **runner-level 관측성 (신규 boundary)**: self-hosted runner host 는 신규 실행 경계이므로 (a) runner online status(GitHub runner API `status:online`, §결정 6/7 DR) (b) queue-depth / pickup-latency(§결정 7 queue-starvation 24h 가드 + §8 카나리 5-AND(2) pickup bounded) (c) restart 자동복구(§결정 6 restart-aware) 를 관측한다. 상세 = Change Plan §7.4/§8 cross-ref.

## 결과

- **긍정**: (a) 단일 vars key 로 76 file / 96 `runs-on` 을 SSOT 1개 interface 로 통일 → byte-parity 무붕괴. (b) public repo 는 코드 변경 없이 unset-var coalesce 로 hosted 유지(위험 CLOSED). (c) private/internal 은 variable 값만으로 self-hosted 배선 → 파일 재작성 0. (d) branch protection 필수 컨텍스트 문자열은 불변(job id / job name 은 `runs-on` 과 orthogonal — INV-2).
- **부정·trade-off**: (a) fromJSON-array-in-runs-on 은 dry-run 실증 전 미확정(§결정 1) — Phase 1.5 dry-run gate 필요. (b) provisioning invariant 미충족 repo 는 billing-deadlock 위험 → fail-loud lint 신설 비용(Phase 2). (c) self-hosted persistent runner 는 secret/tool-cache 오염 신규 공격 표면(§결정 8) → ephemeral 전환 운영 비용.
- **영향 경계**: `templates/github-workflows/` (SSOT) + `.github/workflows/` (self-app lockstep) + `scripts/bootstrap-consumer.sh`(Stage 5b provisioning hook) + `docs/architecture/codeforge-family.md`(C4 Container + trust boundary 갱신, ADR-078). branch protection 필수 컨텍스트 문자열은 **불변**.
- **제외**: public 2(`plugin-codeforge`/`marketplace`) + `mctrader`/`deploy-k8s.yml` 3 ARC job(`mctrader-dev/stg/prd` — 이미 self-hosted). deploy scope 는 ADR-121 활성이나 deploy job 도 본 runner topology 를 cross-ref 통과.

## 해소 기준

N/A — permanent policy (is_transitional: false). 본 ADR 은 org CI runner topology 의 영구 invariant 이며 sunset 대상이 아니다. 약화 방향(예: public repo self-hosted 허용, byte-parity 완화)은 supersede ADR 없이 금지된다.

## 관련 파일

- `templates/github-workflows/` — runs-on SSOT (Surface A, Phase 2 편집)
- `.github/workflows/invariant-check.yml` — byte-parity blocking enforcement (L48-77)
- `.github/workflows/phase-gate-mergeable.yml` — 6-tuple 컨텍스트 (job id `check-gate`, runs-on orthogonal)
- `scripts/bootstrap-consumer.sh` (L340-371) — Stage 5b provisioning hook 후보
- `scripts/reconcile-overlay.sh` — workflow 채널 wholesale cp (L546-547, MARKER_NONE)
- `docs/architecture/codeforge-family.md` — C4 Container + trust boundary 갱신 (ADR-078 lane gate)
