---
adr_number: 172
title: 로컬 스케줄 작업 기반 세션-독립 잔재 관측 — 도입기 관측-only 와 승격 게이트
date: 2026-08-13
status: Accepted
category: orchestration-discipline
carrier_story: CFP-2949
supersedes: null
amends: null  # new-sibling — ADR-128 §결정 4 정의역 amendment 는 별 carrier(§결정 7). 기존 계약 supersede 0.
related_adrs:
  - ADR-128  # 완료 단계 수렴 — §결정 4 "단일 자동 수렴 검증기 구조적 불가" 의 정의역이 본 ADR 로 좁혀짐 (§결정 7)
  - ADR-169  # 세션 잔재 수명 규약 — 잔여 4클래스 중 crash 미발화 1건이 본 ADR 의 겨냥 대상. §결정 4 가 OS 스케줄러를 opt-in 보조로 격하한 판례 승계 (§결정 7)
  - ADR-110  # prior art — Windows Task Scheduler 로 세션 밖 Claude Code 자동 재개. disjoint-layer write boundary 원칙 동형
  - ADR-106  # 관측→보고→판정→집행 4단계. 본 ADR 은 관측·보고까지만 (판정·집행 0)
  - ADR-026  # post-merge automation — telemetry only / disable-by-flag / idempotency. 이중 자동화 경합 경계 대상
  - ADR-040  # worktree convention — 관측 대상 도메인 정의
  - ADR-119  # 검증 후 단언 — lever 계상 금지 조건·미확인 declare 의 근거
  - ADR-112  # living-arch declare 요건 (closed-binary) — 본 ADR 은 declare 부적격
  - ADR-133  # ADR 번호 atomic claim — 본 ADR 번호(172) 예약 mechanism
  - ADR-145  # AC traceability zero-drop — normative AC ↔ 명명 테스트 Hop2
  - ADR-109  # 429 완화 — 세션 내부 정의역이라 본 트래픽 미통제 (주기 선택이 유일 lever)
related_stories:
  - CFP-2949
related_cfps:
  - CFP-2949  # carrier — Desktop 로컬 스케줄 작업으로 잔재 정리 자동화
related_files:
  - scripts/lib/check_workspace_residue_discovery.py  # 관측 대상 스캐너 (mode 분기 = 관측-only 게이트 1)
  - scripts/lib/check_codeforge_scratch_ttl.py  # GC_DRY_RUN 게이트 2
  - scripts/lib/check_harness_temp_residue.py  # TEMP_GC_DELETE_ENABLED default-off 게이트 3
  - docs/orchestrator-playbook.md  # 비대화형 호출 계약 서브섹션 + ADR-128 정의역 mirror 5 anchor
  - skills/worktree-lifecycle/SKILL.md  # 정본 절차 SSOT (부트스트랩 지목 대상)
is_transitional: false
mechanical_enforcement_actions: []  # Phase 2 이행 — scheduled_task_reconcile.py + §8 명명 테스트(normative 8건, iter5 집계) + playbook/skill 편집. 본 ADR = 결정 SSOT.
---

# ADR-172: 로컬 스케줄 작업 기반 세션-독립 잔재 관측 — 도입기 관측-only 와 승격 게이트

## 상태

Accepted (2026-08-13) — CFP-2949 Phase 1(설계) carrier.

## 컨텍스트

codeforge 의 로컬 잔재 수렴(worktree eager teardown · residue-clean · scratch TTL · orphan 판정)은 **Orchestrator 자율준수에 의존**한다. required CI 로 승격할 수 없는데, 그 이유가 구조적이다 — 잔재 스캔 대상(`~/.claude/worktrees` · `~/.claude/codeforge-scratch` · workspace root · Temp)이 **운영자 홈에 결박**돼 있고 GitHub Actions 러너에는 그 홈이 존재하지 않는다. ADR-128 은 이를 "단일 자동 수렴 검증기 **구조적 불가**"로, ADR-169 는 잔여 4클래스(crash 미발화 · advisory ceiling · fail-safe 보수성 · per-repo 협소)로 각각 박제했다.

여기에 **세션과 독립적으로 운영자 머신에서 세션을 자동 개시하는 실행 surface**(Claude Desktop 로컬 스케줄 작업)가 등장했다. 이 surface 는 로컬 FS 도달 · 최소 간격 1분 · 세션 독립을 **동시에** 만족하는 유일한 축이다(Actions 는 로컬 미도달, 관리 클라우드·self-hosted 는 운영자 홈이 기본 구성 밖).

동시에 이 surface 는 codeforge 에 **신뢰 밖 입력을 신뢰 안 환경에서 처리하는 최초 노드**를 도입한다. 지금까지 모든 기계 주체(Actions 러너)는 신뢰 밖 입력을 신뢰 밖 환경에서 처리했다. 스케줄 작업 세션은 운영자 머신에서 사람 없이 개시되며 권한면 **6종**을 **주입이 아니라 상속·누적**한다 — 5종은 세션 시작 시점에 host 설정·인증 상태로부터 **상속**되고, 1종(태스크별 저장 승인 "Always allowed")은 운영자의 1회 승인이 이후 전 run 에 영구 적용되며 **누적**된다.

실행 surface 선택은 2026-08-12T12:15:00+09:00 사용자 결정이며, 그 지위는 **가치 판단**이다(배제된 축들은 "도달 불가"라는 사실 주장이 아니라 비용·위험 판단으로 범위 밖).

## 결정

### §결정 1 — 채택 surface 와 도입 곡선

로컬 스케줄 작업을 채택하되 **도입기는 관측-only** 로 고정한다. 산출은 `선언값 · 실측값 · 불일치` **사실 3-tuple** 이며, `PASS`/`FAIL`/`OK`/`정상`/`문제없음` 같은 **verdict 어휘가 등장하면 그 순간 ADR-106 의 "판정" 단계에 진입**해 기결정을 위반한다. 하위 스크립트 출력에 verdict 어휘가 있으면(실측 오염원 1건 존재) **그 줄은 인용하지 않고 수치 필드만 재서술**한다.

**1호 대상 = 로컬 잔재 관측 보고 1종.** 3필터(주기 ≥1분 ∧ 실행 예산 ∧ 로컬 FS 도달) AND 통과 ∧ 기존 workflow 116개와 중복 0(그 0 은 구조적 — Actions 러너에 운영자 홈이 없어 해당 workflow 가 작성될 수 없다).

### §결정 2 — 정본 SSOT 무손상, 저장 프롬프트는 부트스트랩

절차 정본은 **기존 skill** 이다. 저장 프롬프트는 절차를 **0줄** 담고 정본을 지목만 한다. 정본 미도달 시 fallback 은 **정직 중단**이며 **경로 하드코딩은 금지**한다(정본 이동 시 조용히 stale 해져 hollow 가 된다). 정본 skill 에 "스케줄 작업 인지 분기"를 넣는 **역참조를 금지**한다(현재 정본 2종에 관련 리터럴 0건이며 이 0 을 불변식으로 유지).

부트스트랩 프롬프트 전문은 **본 ADR 에 박제**한다 — repo 신규 지시 파일을 만들지 않으면서 프롬프트 축의 버전관리·리뷰를 성립시키는 유일한 자리다.

```
codeforge 로컬 잔재 관측 (관측-only · 보고 전용)

1. <repo 절대경로> 로 cd 한다. 작업 디렉터리 설정에 의존하지 않는다.
2. codeforge:worktree-lifecycle skill 의 잔재 관측 절차를 정본으로 읽는다.
   정본을 찾지 못하면 "정본 미도달" 을 사실로 보고하고 즉시 종료한다.
   절차를 기억이나 추측으로 대체하지 않는다.
3. scripts/lib/scheduled_task_reconcile.py 를 실행한다.
   이 CLI 가 채널 조회·중복 제거·마커 부착·보고 채널 발화까지 전부 수행한다.
4. 보고 채널에 직접 쓰지 않는다. CLI 산출의 수치 필드만 인용해 실행 결과를 확인하고,
   실패·미도달이면 그 사실만 보고하고 종료한다.

금지: 파일 삭제·이동·worktree 제거·stash drop / GitHub label·state·merge·close 변경 /
      보고 채널에 직접 코멘트 작성 / 보호 브랜치 push / 자기 스케줄·프롬프트 수정 /
      하위 출력의 verdict 어휘 인용.
이 작업은 관측하고 보고만 한다. 판정도 집행도 하지 않는다.
```

### §결정 3 — 정의 거버넌스: two-master 는 **잔존한다**

프롬프트 축은 §결정 2 로 git-resident 가 되지만, **등록 축(schedule · folder · model · enabled · permission mode)은 git 으로 재현할 수 없다.** 등록 상태를 repo 에 미러하는 선언 파일은 **도입하지 않는다** — 등록 상태를 기계 판독할 표면이 없어 **drift 대조 수단이 없고**, 대조 불가능한 미러는 버전관리의 외형만 만들고 실질을 주지 않는다.

⇒ **`[git-재현-불가: 등록 축]` 으로 라벨하고 수기 절차로 인정한다. "two-master 없음" 이라고 쓰지 않는다.**

### §결정 4 — 관측-only 의 기계 lever 와 **계상 금지 조건**

기계 강제는 두 층에서만 성립한다.

1. **스크립트 층 3중 게이트** — `mode`=observe-only(삭제 코드경로 미진입) ∧ `GC_DRY_RUN=1`(실 삭제 직전 분기) ∧ `TEMP_GC_DELETE_ENABLED` unset. **신규 플래그 0건.**
2. **권한 층** — `permissions.deny` 키 신설(전 모드에 적용되는 유일 통제이며 **현재 키 부재**) + per-task permission mode `Manual` 고정 + `disableBypassPermissionsMode`.

**host `~/.claude/settings.json` 을 방어 lever 로 계상하지 않는다** — 실측이 `allow` 40건 · `deny` 키 부재 · `defaultMode: bypassPermissions` 이므로 **넓히는 방향으로만 작동하며 제한 기여가 0** 이다. 제한 기여 0 인 항목을 lever 로 세는 것 자체가 검사연극이다.

★ **계상 금지 조건**: 위 권한 층은 **각 lever 의 실효성을 스케줄 작업 세션에서 live 실측하기 전까지 lever 로 계상하지 않는다** — 3축 전건이 대상이다: (V1) `deny` 가 태스크 세션에 적용되는가 (V2) 태스크 permission mode 가 host `defaultMode` 를 상속하는가(상속하면 born-at-max-privilege) (V3) `disableBypassPermissionsMode` 가 user-scope 설정에서 실효하는가. 미실측 상태의 "안전하다"는 서술은 그 자체가 검사연극이다(ADR-119). **이는 어느 lever 가 무효라는 판정이 아니라, 판정 이전에 계상하지 말라는 요구다.**

### §결정 5 — reconcile 은 **상태 무의존**이고 **결정론 코드**다

플랫폼이 누락 실행을 회수할 때 **최근 1회만 실행하고 나머지는 폐기**하므로, 대상을 "이 tick 이 담당하는 구간"으로 정의하면 폐기된 tick 의 대상이 **영구 미관측**이 된다.

⇒ 대상은 `g(현재 상태)` 다. **매 실행이 현재 잔재 전량을 재관측**하고 상태를 이월하지 않는다. dedup 키는 `class + 홈-상대 경로` 로 **대상에서 유도**하며 저장하지 않는다(상태 파일을 두면 그 파일 자신이 잔재가 되는 자기참조가 생긴다). dedup 상태의 실질 저장소는 **append-only 보고 채널 자신**이며, 조회 실패는 **fail-closed**(다음 실행이 자기치유).

**이 reconcile·dedup·마커 부착·보고 채널 발화는 자연어 프롬프트가 아니라 결정론 CLI 에 둔다.** 프롬프트에 두면 검사 대상이 LLM 이 되어 해당 수용 기준들의 결정론 오라클이 붕괴하고, 그러면 그 기준들을 강등해야 하는데 강등 근거가 없다.

**★ 발화가 이 열거에 포함되는 것은 필수다.** 렌더에서 끊고 발화만 LLM 세션에 넘기면 (a) 발화 개체 수·중복 0·정지 시 무발화·조회 실패 fail-closed 를 검사하는 오라클 전건이 **확률적 대상**을 겨누게 되고 (b) LLM 이 마커를 **운반**하게 되어 "LLM 이 마커를 부착하는 형상은 비결정론이므로 불가" 라는 판정과 정면 충돌하며 (c) 세션의 채널 write 도구가 §결정 4 의 `deny`(`mcp__github__*` write)에 막혀 **태스크는 매 tick 돌지만 보고는 영원히 미착지하는 무음 사망**이 된다. ⇒ 발화 귀속은 본 결정의 **분리 불가 부분**이며, 그 대가는 결과 10 에 기재한다. 이는 §결정 2 의 "지시 파일 금지"에 저촉되지 않는다 — 금지 대상은 **절차를 서술하는 지시 파일**이지 **절차를 실행하는 코드**가 아니다.

### §결정 6 — 운영 파라미터 (전건 확정)

| 항목 | 값 | 근거 |
|---|---|---|
| 주기 | **Daily** | 실행 예산 소비 여부가 미확인이므로 보수 선택. 잔재는 누적형이라 저빈도에서 가치 손실이 작다 |
| worktree 격리 토글 | **OFF** | ON 은 실행마다 worktree 를 만드는데 **생성 위치·회수 주체가 둘 다 미확인**이다. 잔재 스캔 도메인에 생기면 매 보고가 자기 소음이 되고, workspace root 에 생기면 유일한 삭제 허용 root 에 자기 발밑을 넣는다. **미확인은 안전 근거가 아니다** |
| 작업 디렉터리 | **기존 main 체크아웃** | **INV-OBS: 관측자는 자기 관측 도메인에 새 객체를 추가하지 않는다.** 이미 존재하는 객체라 신규 0 |
| 긴급 정지 | **2-플래그 OR, fail-closed** | 기존 repo-resident 플래그는 **활성화에 커밋이 필요**해 즉시성이 없다. 신규 로컬 플래그를 **scratch 밖**(`~/.claude/worktree-gc-state/`)에 둔다 — scratch 에 두면 TTL purge 가 7일 뒤 **정지 장치를 조용히 삭제**한다 |
| 생존 감시 | **로컬 세션 시작 축 watchdog** — heartbeat 기록 주체·시점은 **결정론 CLI 의 정상 종료**로 고정 | 클라우드 역구조는 성립하나 **겨냥이 틀리다** — 승인 대기 정지와 앱 미상주를 구별할 정보가 없어 머신 오프 구간을 전부 오탐한다. **기록 주체는 결정의 load-bearing 부분이다** — 세션이 heartbeat 를 기록하면 CLI 를 기동조차 못한 실행도 fresh 기록을 남겨 watchdog 이 **구조적 false-negative**(관측자가 죽었는데 살아 있다고 보고)가 된다 |

### §결정 7 — 선행 결정과의 관계

- **ADR-128 §결정 4 정의역 amendment 를 발의한다** — "구조적 불가"의 정의역을 **클라우드 평면**으로 좁히고 세션 독립 로컬 스케줄러는 그 정의역 밖임을 각주한다. 약화되는 통제 0(순수 정의역 명확화). **단 "기계 게이트 신설 과설계 금지" 조항은 무손상 유지** — 정의역을 좁힌 것이 새 게이트를 만들어도 된다는 허가가 아니다.
- **ADR-169 본문은 amendment 하지 않는다** — 본문에 "클라우드 러너" 표현이 없다(그 표현은 playbook 전용). playbook mirror 만 수정한다.
- **★ ADR-169 §결정 4 판례를 승계한다** — 그 결정은 **같은 문제 도메인(로컬 잔재 GC)에서 이미** OS 스케줄러를 "**opt-in 보조이며 주 트리거가 아님**"으로 격하했다. 본 ADR 의 도입기 관측-only 는 이 판례와 정합이나, **주 트리거·required 로 승격하려면 그 판례의 재검토가 선행 의무**다.

### §결정 8 — 실행환경 3축 비교와 결정 기록

채택에 앞서 후보 실행환경을 **3축 × {비용, 보안} = 6셀**로 비교했다. 아래 표가 그 6셀이며 본 결정의 근거 기록이다.

| 축 | 비용 축 | 보안 축 |
|---|---|---|
| **P3a Anthropic 관리 클라우드** | 구독 usage 소진 + 일일 run cap(Team·Enterprise 25/day — 시간당 schedule 시 예산 96% 소비). usage credits 활성 시 metered overage 라 하드 벽은 아니다. 인프라 신설 0 | 전용 secret store **부재**(환경변수가 환경 사용자 전원 열람) + 승인 프롬프트·permission-mode picker **부재** + 커넥터가 write 포함 무승인 ⇒ **기계 강제 lever 0**. 발화가 운영자 개인 계정 명의 |
| **P3b self-hosted 러너** | P3a 와 동일 cap + **인프라 신설 비용**(Linux/macOS 호스트 · public beta · admin 토글 · fleet 운영·유지) | 능력은 최상(`--disallowed-tools` deny-wins + 호스트 `~/.claude/` seed + 세션 범위 단기 자격증명). **단 운영자 홈을 마운트하면 세션 children 이 같은 UID 를 공유해 운영자 홈 전권** = 미평가 위험 |
| **P4 Desktop 로컬 (채택)** | 인프라 신설 0. 구독 usage 소진은 불가피. **일일 run cap 소비 여부 = 미확인**(공식 문서 미기재). 실 비용은 금전이 아니라 **앱 상주 + 머신 각성 요구** | 기계 lever 실재(per-task permission mode Manual 시 미승인 도구 호출에서 run 이 승인까지 정지 + worktree 격리 토글 + Active/Paused + Delete). **단 lever 는 그뿐**이며 host `settings.json` 은 제한 기여 0. 자격증명 주입 0 이지만 **상속 최대**. 1회 승인이 이후 run 에 영구 확대 |

**결정 기록**

- **채택 축** = **P4 — Desktop 로컬 스케줄 작업**
- **결정 주체** = **사용자** (Orchestrator 가 선택지 4개와 각 대가를 제시해 직접 질의)
- **결정 시각** = **2026-08-12T12:15:00+09:00**
- **선택의 지위** = **가치 판단** (기술 판정으로 좁혀지지 않는 trade-off)

**배제 축별 사유의 지위**

| 배제 축 | 사유 | 지위 |
|---|---|---|
| **P3a** | 로컬 FS 미도달 조각은 사실이나, 축 **전체**의 배제는 그 사실 단독이 아니라 가치 대비 비용 판단이다 — 기계 강제 lever 0 + 기존 Actions workflow 116 과 거의 겹침 + 일일 run cap | **판단** |
| **P3b** | 미평가 위험(운영자 홈 마운트 시 세션 에이전트가 운영자 홈 전권) + 인프라 신설 비용. ★ **"운영자 홈 도달 불가" 는 사실 주장으로 미성립**(금지 근거 0건 + 경로 미평가)이므로 배제 근거로 인용하지 않는다 | **판단** |

**재논의 조건**: Linux/macOS 러너 호스트 상시화 · self-hosted GA 전환 · 운영자 홈 마운트 위험 평가 완료 중 하나라도 성립하면 P3b 를 재논의할 수 있다.

### §결정 9 — 승격 게이트 (조건 · 주체 · rollback 경로)

**본 Story 는 게이트 *설계*까지이며 승격 실행은 범위 밖이다.** 아래는 도입기 관측-only 를 넘어 집행·주 트리거·required 로 올릴 때 충족해야 하는 게이트의 설계다.

**승격 조건 8항** (1-7 = 원 판정 / ⑧ = Amendment 1 신설)

1. 도입기 연속 운용에서 **오보 0**
2. **V1**(`permissions.deny` 가 태스크 세션에 적용되는가) · **V2**(태스크 permission mode 가 host `defaultMode` 를 상속하는가) **해소** (§결정 4 계상 금지 조건)
3. 로컬·GitHub 상태 델타 **0 유지**
4. prompt-injection 완화가 **실측으로** 확인
5. **"read-modify-write 부재" 전제 재검사** — 승격은 이 전제를 깬다
6. **ADR-169 §결정 4 재검토 선행** — 그 결정이 같은 도메인에서 "OS 스케줄러 = opt-in 보조, 주 트리거 아님" 으로 이미 격하 판정했으므로, P4 를 주 트리거·required 로 올리는 것은 기존 판례와 정면 배치된다
7. **승격 시점 플랫폼 문서 재실측** — catch-up 7일 창 · 단일 catch-up 후 older 폐기 · skip 3형상 · per-task permission 모델 · 작업 프롬프트 파일 경로 · 자기수정 도구명이 무변경임을 재확인한다. 이것이 승격 판단이 stale 한 벤더 계약 위에 서지 않게 하는 유일한 장치이며 **상시 감시 트리거는 없다**(결과 9)
8. **⑧ dedup 억제 방어(코멘트 author-verify) 배선** — dedup 이 **관측 외 판단에 쓰이기 전 선행 의무** (Amendment 1 신설)

**승격 주체** = **사용자**. 자동 승격은 금지한다 (기결정 "자동결정 절대 금지" 의 직접 상속).

**rollback 경로 — P4 lever 3종 실물**

- **(가)** Manual permission mode 에서 미승인 도구 호출 시 **run 이 승인까지 정지**한다
- **(나)** 태스크 Status **Active / Paused** 토글
- **(다)** **Delete** ("Also delete files on disk" 체크박스 포함)

**승격 시 함께 바뀌는 것 — 산출물 마커 종수**

기계 산출물임이 기계적으로 식별돼야 하는 근거는 행위가 **운영자 자격증명으로 나타나기** 때문이다. 단 **마커는 오독을 줄일 뿐 귀속 자체를 바꾸지 못한다**(결과 7 의 trade-off 와 정합).

| 단계 | 마커 종수 | 구성 |
|---|---|---|
| **도입기(관측-only)** | **2종** | 고정 sentinel 1줄 `[scheduled-task-observe]` + 태스크·run 참조 trailer `[scheduled-task-run]`. 형식은 기존 관용 `[prefix] k=v` 를 상속한다(신규 형식 발명 0) |
| **승격 후** | **3종** | 위 2종 + **브랜치 prefix** |

★ **브랜치 prefix 가 도입기에 적용 제외인 사유 (load-bearing)**: 도입기에는 커밋·push 가 **0건**이라 브랜치 prefix 검사가 **검사 대상 부재로 vacuous pass** 가 된다. 3종을 뭉뚱그리면 **마커 검사 자체가 hollow** 해진다. 따라서 도입기 검사 대상은 **2종으로 못박고**, 브랜치 prefix 는 **브랜치·커밋이 실제로 생성되는 승격 이후에만** 적용 대상이 된다.

**부착 주체 = 결정론 CLI** (§결정 5). LLM 이 마커를 운반하지 않는다.

**승격 이력 = 0건** (도입기 상태).

**미확인 1건 (정직 기재)**: (나)·(다)가 **진행 중인 run** 을 중단시키는지는 **확인 불가**다 ⇒ "즉시 정지된다" 고 쓰지 않는다. 진행 중 run 에 대한 정지는 **정지 플래그 판독 시점**에 의존한다.

### §결정 10 — 상속·누적 권한면 6종 (AC-4 하한 정본 전사)

본 절은 Change Plan §7.3 의 전사이며 새 결정 0.

스케줄 작업 세션이 상속·누적하는 권한면은 다음 6종이다. 이 테이블은 각 면의 **실효 범위**, **제한 수단**을 식별한다.

| # | 면 | 실효 범위 | 제한 수단 |
|---|---|---|---|
| 1 | `gh` CLI 인증 | 운영자 GitHub 전 권한 | 도구층 deny 만 (scope 축소 불가) |
| 2 | MCP `github` 서버 | `mcp__github__*` write 포함 | deny 필요. **실효 도달 범위 미확인(U10)** |
| 3 | `~/.claude/settings.json` allow 40 | **넓히는 방향 전용** | **제한 기여 0 — lever 계상 금지** |
| 4 | repo-local / global git config | 커밋 identity | folder 설정 의존 |
| 5 | **`additionalDirectories`** | **PRIVATE repo + live 트레이딩 20+ repo 를 무프롬프트 개방** | **미완화** |
| 6 | **태스크별 저장 승인 — "Always allowed" (상속형 아닌 누적형)** | **미측정.** 1회 `always allow` 가 그 태스크의 이후 전 run 에 영구 적용되며 승인 도구 집합이 시간에 따라 단조 확대된다 | 태스크 상세 페이지의 review·revoke("Always allowed" 패널). **회수 입도 미확인(U11)** |

**★ 완결성 미보증 declare**: 이 열거는 닫힌 집합이 아니다. 미확인 권한면이 새로 발견되면 본 표에 편입되어야 한다. 신규 면 발견 시 해당 §결정 amendment 와 함께 테스트 AC-4 의 검사 대상을 확대해야 한다.

**근거**: 본 표 전체 = Change Plan §7.3 전사. 5번 면(`additionalDirectories`)은 요구사항 lane iter5 에서 실측 발견돼 AC-4 문면이 닫힌 4종 → 6종으로 정정된 근거이며, 6번 면은 공식 문서 firsthand 실측(2026-08-13)이다 [source: code.claude.com/docs/en/desktop-scheduled-tasks — "Permissions for scheduled tasks" / "Manage scheduled tasks"].

## 결과

**얻는 것**: ADR-169 잔여 4클래스 중 **1건(crash 미발화 = observer-death)** 에 대해, 세션과 독립된 관측자가 사실을 보고한다. 나머지 3건은 본 ADR 이 해소하지 않는다(advisory ceiling 은 승격 이후 영역, fail-safe 보수성은 orthogonal, per-repo 협소는 ADR-169 §결정 2 가 이미 독자 해소).

**치르는 것 (honest ceiling — 결함이 아니라 선언된 상한)**

1. **다른 운영자는 본 결정을 merge 하는 것만으로 아무것도 얻지 못한다.** 작업 정의가 repo 밖 이 머신 로컬 config 다. "자동화를 배포했다"는 서술은 과장이다.
2. **등록 축 two-master 잔존** (§결정 3).
3. **관측 공백에 절대 상한이 없다** — 앱 미상주와 승인 대기 정지 어느 쪽도 문서상 상한이 0 이다. "공백 ≤ X" 를 단정하지 않는다.
4. **무음 사망은 탐지되지 않는다** — 승인 대기 정지는 막힌 것이 도구 호출 자체라 **자기보고가 원리적으로 불가능**하다. watchdog 은 완화이며 **watchdog 의 watchdog 은 없다. 최종 backstop 은 사람이다.**
5. **prompt-injection 은 완전 차단 불가** — 이 형상에서 플랫폼 안티-인젝션 4종이 전건 무력하다(권한 시스템은 `bypassPermissions` 가 무력화하며 문서가 "prompt injection 에 대한 보호를 제공하지 않는다"고 명시, 컨텍스트 격리는 특정 도구 한정, 승인 전 검토는 무인 실행이 정의상 제거, VM 권고는 로컬 FS 도달이라는 채택 근거와 충돌). 완화는 읽는 표면 축소 + 재량 축소이며 **잔여가 남는다**.
6. **상속 자격증명은 회수 불가** — `deny` 는 도구층 차단이지 scope 축소가 아니다.
7. **명의 귀속의 근본 해소는 별도 identity 발급인데 그것은 "자격증명 주입 0" 원칙과 정면 충돌한다** — 진짜 trade-off 이며 본 ADR 은 마커 완화만 선택했다.
8. **비대화형 호출 계약은 문서 = advisory.** 기계 강제는 §결정 4 두 층에만 있다.
9. **플랫폼 계약은 벤더 소유이고 본 결정이 그 위에 서 있다 — 변경 감시 트리거가 0 이다.** 누락 실행 회수의 7일 창, 단일 catch-up 후 older 폐기, skip 사유 3형상, per-task permission 모델, 작업 프롬프트 파일 경로, 자기수정 도구명은 전부 **공식 문서가 서술하는 벤더 계약**이지 우리가 통제하는 불변식이 아니다. 특히 **이름·경로에 결박된 검사**는 벤더가 rename 하면 실패로 드러나지 않고 **조용히 항상 통과**한다(vacuous-pass). §결정 5 의 상태 무의존 설계가 catch-up 정책 변경에는 견고하지만 **그것은 의도된 완화가 아니라 다른 목적의 부수 효과**다. ⇒ 대응은 **승격 시점 재실측 1점**뿐이며 상시 감시는 없다.
10. **보고 채널 write(발화) 축의 도구층 기계 lever 가 0 이다.** §결정 5 가 발화를 결정론 CLI 에 귀속시킨 결과, 채널 write 는 python subprocess 안에서 일어나 **도구층 `deny` 의 정의역을 구조적으로 벗어난다** — 6번이 말한 "도구층 차단" 이 이 축에는 도달하지 않는다. 남는 통제는 (a) CLI 코드 자신의 결정론(명명 테스트가 검사) (b) 상속 자격증명 scope — **축소 불가** 둘뿐이다. **대안(세션이 발화)은 이 write 를 `deny` 정의역 안에 두지만 그 귀결은 통제된 발화가 아니라 4번의 무음 사망**이다. ⇒ 발견된 구멍이 아니라 **선택된 대가**이며 숨기지 않고 기재한다.

**승격은 본 결정의 범위 밖이다.** 본 ADR 이 정하는 것은 승격 게이트의 설계(조건 · 주체 = **사용자** · rollback 경로 3종)까지이며 승격 실행은 별 Story 다. 자동 승격은 금지된다.

## 관련 파일

- `scripts/lib/check_workspace_residue_discovery.py` — 관측 대상 스캐너. `mode` 분기 = 관측-only 게이트 1
- `scripts/lib/check_codeforge_scratch_ttl.py` — `GC_DRY_RUN` 게이트 2
- `scripts/lib/check_harness_temp_residue.py` — `TEMP_GC_DELETE_ENABLED` default-off 게이트 3
- `scripts/lib/scheduled_task_reconcile.py` — **Phase 2 신설.** 결정론 reconcile·dedup·마커·**보고 채널 발화** 배선 (§결정 5). 발화 주체는 본 CLI 단일이며 세션은 채널에 쓰지 않는다
- `hooks/session-start-scheduled-task-watchdog` — **Phase 2 신설.** 생존 감시 hook(기존 `hooks/session-start-gc-catchup` 복제 형상). heartbeat 는 본 hook 이 **판독**만 하고 **기록은 위 CLI 의 정상 종료 시점**이 담당한다 (§결정 6)
- `hooks/hooks.json` — 위 watchdog hook 의 SessionStart 등록 파일 (등록이 없으면 hook 파일은 존재해도 발화하지 않는다)
- `docs/orchestrator-playbook.md` — 비대화형 호출 계약 서브섹션 + ADR-128 정의역 mirror 5 anchor (§결정 7)
- `skills/worktree-lifecycle/SKILL.md` — 정본 절차 SSOT, 부트스트랩 지목 대상 (§결정 2)
- `~/.claude/scheduled-tasks/<task-name>/SKILL.md` — 작업 프롬프트. **repo 밖 user-level**

## 해소 기준

N/A — permanent policy. 단 **승격 시점에 §결정 1·4·5 의 전제가 재검사 대상**이 된다(관측-only 가 깨지면 §결정 5 의 경합 방어 근거인 "read-modify-write 부재"도 함께 깨진다). **아울러 승격 시점의 플랫폼 문서 재실측이 선행 의무**다 — 7일 창 · 단일 catch-up · skip 3형상 · permission 모델 · 작업 프롬프트 파일 경로 · 자기수정 도구명의 무변경을 확인하고, 변경 시 §결정 5 를 재검사 대상으로 올린다(결과 9 의 유일한 대응점). 실행 surface 가 폐지되거나 로컬 잔재 수렴이 다른 경로로 required 승격되면 본 ADR 은 재검토된다.

## Amendment 1 (CFP-2949, 2026-08-14) — §결정 6 heartbeat 발화 조건을 "관측 사이클 완주"로 좁힘

> **본 Amendment 의 지위**: **새 결정이 아니라 문면을 자기 근거에 맞추는 정정**이다. §결정 6 본문 행과 "관련 파일" 서술은 **무편집 보존**하며, heartbeat 기록 조건에 한해 본 절이 그 문면을 대체한다.

### A1-1 계기 — 문면과 자기 근거가 어긋난 채 구현됐다

Phase 2 구현리뷰 FIX iter1 에서, §결정 6 의 **문면**("heartbeat 기록 주체·시점은 **결정론 CLI 의 정상 종료**로 고정")과 **같은 결정의 근거문**이 어긋난 채 구현된 것이 드러났다.

근거문은 *"세션이 heartbeat 를 기록하면 **CLI 를 기동조차 못한 실행도 fresh 기록을 남겨** watchdog 이 **구조적 false-negative**(관측자가 죽었는데 살아 있다고 보고)가 된다"* 를 금지 사유로 든다. 그런데 **정지 플래그 감지 종료 경로가 스캐너를 호출하지 않고**(그 경로의 자기 warn 문구가 "스캐너 미호출 · 채널 미접촉" 을 스스로 인정한다) 종료하면서 heartbeat 를 기록해 **정확히 그 형상**이 됐다 — 관측을 0회 수행한 실행이 생존 신호를 남긴다.

⇒ 틀린 것은 근거가 아니라 **문면이 자기 근거보다 넓었던 것**이다. 문면을 근거에 맞춰 좁힌다.

### A1-2 정정 문면 (heartbeat 기록 조건)

**heartbeat 기록 조건** = "정상 종료" → **`collect_observations()` 가 실제로 호출되어 반환한 종료 경로**.

| 종료 경로 | heartbeat |
|---|---|
| 정지 (F1 ∨ F2 ∨ read-failure) | **기록하지 않는다** |
| `--dry-run` | **기록하지 않는다** |
| 관측 0건 · 채널 미지정 · 채널 조회 실패 · 신규 0건 · 정상 발화 | **기록한다** — 사이클을 완주했다 |

"관측 0건" · "채널 미지정" · "채널 조회 실패" · "신규 0건" 이 기록 대상인 이유는 **관측자가 살아서 한 바퀴를 돌았다는 사실이 그 네 경우에 모두 참**이기 때문이다. watchdog 이 겨냥하는 것은 "발화가 있었는가" 가 아니라 "관측자가 살아 있는가" 다.

### A1-3 대가 (declare — 은폐 금지)

1. **의도적 정지 중 소음** — 정지가 임계(기본 **172800초 = 2일**)를 넘기면 watchdog 이 **세션 시작마다** stderr 1줄을 발화한다. **"매일"이 아니라 "매 세션"** 이다(세션을 여러 번 여는 날에는 그만큼 반복된다).
2. **watchdog 은 "의도적 정지"와 "관측자 사망"을 구별하지 못한다.** 억제 수단은 `BYPASS_SCHEDULED_TASK_WATCHDOG=1` 뿐이고, **그 억제는 진짜 사망도 함께 가린다**. 이 미구별을 **결함이 아니라 선언된 상한**으로 받는다.
3. **구별 수단을 채택하지 않는다** — 구별하려면 정지 사실을 별도 상태로 기록하고 watchdog 이 그 상태로 분기해야 한다. 도입기 관측-only 에서 1번의 소음이 bounded 하고, 새 상태 파일은 §결정 4 **"신규 플래그 0건"** 정신과 §결정 6 **INV-OBS**(관측자는 자기 관측 도메인에 새 객체를 추가하지 않는다) 재검사를 요구한다. ⇒ **§결정 9 승격 게이트 재검토 대상으로 올린다.**

### A1-4 F1 소유권 declare (신규 — 종전 미기재)

**F1 = `.codeforge/post-merge-automation.disabled` 는 본 축 소유가 아니다.** 소유 = **ADR-026 §결정 4 + ADR-045 D-7**, 실 소비자 = `post-merge-followup.yml` · `retro-mandatory.yml`.

⇒ **post-merge 자동화를 끄면 잔재 관측도 함께 정지한다.** 이 공동 운명은 **유지한다**. 단 종전에는 그 정지가 **무신호**였고, A1-2 의 정정이 이를 **임계 초과 후 watchdog 발화로 가시화**한다 — **이 가시화가 F1 공유를 유지 가능하게 하는 조건이다**(가시화가 없으면 타 축의 정지 플래그가 본 축을 조용히 죽인다).

### A1-5 read-failure 서술 정정 (실측 기반)

`read_stop_flags` 의 `except Exception → halted=True("read-failure")` 는 **CLI 진입점에서 미도달**로 실측됐다 — `run()` 은 항상 `str` `repo_root` 를 넘기므로 이 분기는 **bytes/int 타입 오류에서만 발동**한다. embedded NUL · 초장문 · `None` 입력은 예외 없이 `False` 를 돌려준다.

⇒ 방어적 fail-closed 로 **유지**하되, *"환경 오류를 생존으로 덮는다"* 는 서술은 **현 호출 형상에서 근거 없음**으로 정정한다. (read-failure 를 정지로 판정하는 fail-closed 자체는 A1-2 표에 그대로 존치한다.)

## Amendment 2 (CFP-2949, 2026-08-15) — §결정 6 생존 감시의 **판독** 정의역을 채택자 한정으로 좁힘

**정정 문면 (§결정 6 `생존 감시` 행에 대한 판독 축 추가)**: watchdog 의 **발화 사유는 "채택 표식이 있는데 heartbeat 가 stale 또는 invalid" 인 경우로 한정한다 — heartbeat 파일 부재(`absent`)는 발화 사유가 아니다.** 미채택 환경에서 부재는 사망이 아니라 **정상 미채택**이며, 부재를 사망 신호로 읽는 것은 **관측 대상의 부재를 관측의 실패로 오분류**하는 것이다.

★ **Amendment 1 과 disjoint**: A1-2 가 좁힌 것은 heartbeat 의 **기록** 조건("관측 사이클 완주" 시에만 기록)이고, 본 Amendment 가 좁히는 것은 그 값의 **판독** 조건(부재를 어떻게 해석할 것인가)이다. **기록 축과 판독 축은 서로를 함의하지 않으며**, A1-2 의 false-negative 방지 논거(기록 주체가 세션이면 미도달 run 도 fresh 를 남긴다)는 본 결정에 적용되지 않는다 — 본 결정은 기록 주체·시점을 **무변경**으로 둔다.

**계기 (실측)**: `hooks/hooks.json` 의 SessionStart entry 가 `${CLAUDE_PLUGIN_ROOT}` 로 **전 consumer 에 무조건 등록**되고 `hooks/session-start-scheduled-task-watchdog` 이 파일 부재를 `absent → should_report=true` 로 두어, **태스크를 채택하지 않은 운영자 전원이 매 세션 1줄을 받는다**. 회피 수단은 `BYPASS_SCHEDULED_TASK_WATCHDOG=1` **상시 export** 뿐이라, 합리적 귀결은 Amendment 1 A1-3 이 이미 경고한 **탐지자 자발적 폐기**다.

**대가 (declare — 은폐 금지)**:

1. **"채택했으나 1회도 실행되지 않음" class 는 heartbeat 부재만으로 판별 불가**하다. 그 판별의 유일 근거는 **채택 표식**이며, 표식 없이 부재만 있으면 **구조적으로 무음**이다.
2. 따라서 본 결정은 A1-3 이 선언한 *"억제가 진짜 사망도 함께 가린다"* 축의 **추가 억제**다 — false-negative 를 하나 늘리는 방향임을 숨기지 않는다. 교환 대상은 **비채택자 전원에게 가해지던 무조건 소음**이며, 그 소음이 탐지자를 죽이면 A1-4 가 F1 공동 운명 유지의 **성립 조건**으로 건 가시화가 함께 소실된다는 점이 본 교환의 근거다.
3. 본 결정 착지 전까지 *"다른 운영자는 merge 만으로 아무것도 얻지 못한다"* 는 계획서 문면은 **거짓**이다(얻는 것이 소음이다). 착지 후에 비로소 참이 된다.

**본 Amendment 의 지위**: 결정 SSOT 문면만 정정한다. 대응 오라클 계약(hook 판독 케이스 ①)과 테스트 갱신은 **구현 lane 이행 대상**이며, 이행 전까지 계획서 §8.1·§8.2 와 본 문면은 **상충 상태**다 — 그 상충은 계획서 §7.4.1 에 명시 declare 돼 있다.

> ★ **Amendment 3 이 위 세 문단을 정정한다** (`:291` 발화 사유 · `:300` FN 방향 · `:303` 상충 지위). 위 문면은 **삭제하지 않고 이력으로 보존**하며, 충돌 시 **Amendment 3 이 우선**한다.
>
> ★ **Amendment 5 추기 — 위 `:299`-`:300` 의 축 라벨은 옳았다 (정정 아니라 명예 회복)**: A4-3 `:416` 이 본 Amd 2 본문을 *"잔여 3종을 거짓 발화 축으로 라벨한 오염면"* 으로 지목했으나 **오지목**이다 — 전수 `grep` 실측상 `fail-safe` 계열은 본문 범위(`:289`-`:306`) 안 **0 hit**, 잔여 3종 열거 최초 등장은 `:364`(A3-4)다. 오히려 `:300` 의 *"false-negative 를 하나 늘리는 방향"* 은 손실을 **탐지 축으로 정확히** 라벨한 것이다. 정본 = **A5-3**. ★ 이 추기는 위 Amd 3 포인터를 **무효화하지 않는다** — Amd 3 A3-2 가 좁힌 것은 *"그 FN 서술이 **본 판본**에 적용되는가"* 이고, 본 추기가 회복하는 것은 *"**축 라벨**이 옳았는가"* 로 **두 축이 disjoint** 하다.

## Amendment 3 (CFP-2949, 2026-08-15) — Amd 2 판독 정의역을 **채택 표식 분기 도입 이후** 상태로 정정 + 실배선 상충 해소

**계기**: 구현리뷰 iter6 F-CR6-02 — 본 ADR Amendment 2 의 문면이 **실배선과 정면 모순**임이 firsthand 적발됐다. Amd 2 `:291` 은 *"heartbeat 파일 부재(`absent`)는 발화 사유가 아니다"* 라고 쓰는데, 착지한 배선(`hooks/session-start-scheduled-task-watchdog`)은 **`표식 존재 + absent` 에서 발화**한다.

**모순의 원인 = 결정 미확정이 아니라 전사 누락**이다. 해당 결정 자체는 Orchestrator 판정(2026-08-15, Story §8.5 site 24 행)으로 **이미 확정**됐고 구현도 착지했다. 그 확정을 문면에 옮기는 작업(site 27, *"FIX6 착지 문서 정합"*)이 갱신 대상으로 **계획서 4 site 만 열거하고 본 ADR 을 누락**해, **결정 SSOT 만 정정 이전 상태로 고착**됐다. 계획서는 이미 정정돼 있었으므로 상충은 CP↔ADR 사이에 남았다.

### A3-1 — `:291` 정정: 판독 정의역은 "채택자 한정" 이 맞으나 `absent` 는 단일 case 가 아니다

Amd 2 는 **채택 표식 판별자가 도입되기 이전**에 쓰였다. 그 시점 배선에서 `absent` 는 분기 불가능한 단일 상태였고, 선택지는 *무조건 발화* 와 *무조건 침묵* 의 이지선다뿐이었다 — Amd 2 는 그 이지선다에서 후자를 택한 것이다.

**정정 문면 (Amd 2 `:291` 을 대체)**: 판독 정의역이 **채택자 한정**이라는 원칙은 **유지**한다. 단 채택 판정에 **표식 판별자**가 도입되어 `absent` 는 두 case 로 분화한다:

| heartbeat | 채택 표식 | 판정 | 발화 |
|---|---|---|---|
| `absent` | **부재** | 미채택 = 정상 (관측 대상 자체가 없음) | **무발화** |
| `absent` | **존재** | *"채택했는데 1회도 안 돌았다"* = 실 이상 | **발화** |
| `stale` / `invalid` | 검사 불요 | heartbeat 존재 = 최소 1회 실행 = 채택 증거 | **발화** |

⇒ Amd 2 의 *"`absent` 는 발화 사유가 아니다"* 는 **표식 부재 분기로 정의역이 좁혀진다.** 표식 존재 분기에서 그 문장은 **성립하지 않는다**.

원칙은 불변이다 — 억제 대상은 *"진짜 사망"* 이 아니라 **미채택(관측 대상의 부재)** 이며, 부재를 사망으로 읽는 것이 **관측 대상의 부재를 관측의 실패로 오분류**하는 것이라는 Amd 2 의 판정 논리는 그대로 유효하다.

### A3-2 — `:300` FN 방향 서술 정정

Amd 2 `:300` 은 본 결정을 *"false-negative 를 하나 늘리는 방향"* 으로 기술했다. 표식 판별자 도입 후 그 서술은 **틀리다** — 3-regime 대조:

| regime | 거짓 발화(비채택자 소음) | 채택자 탐지 |
|---|---|---|
| pre-FIX6 (`absent → 무조건 발화`) | **O** | **O** |
| FIX6 1차판 (`absent → 무조건 침묵`) | X | **X** — FN +1 발생 |
| **본 판본** (표식 2분기) | X | **O** |

⇒ FN 을 늘린 것은 **FIX6 1차판**이고, 표식 판별자가 그 손실을 **되돌렸다**. 본 판본이 pre-FIX6 대비 제거한 델타는 **거짓 발화뿐**이다. `:300` 의 FN 서술은 **1차판 한정 기술로 정의역이 좁혀지며**, 본 판본에는 적용되지 않는다.

**★ 단 이 명제는 조건부다 — 반례가 실재한다 (은폐 금지, A3-4 참조).**

> ★ **후속 Amendment 2건이 위 절에 도달한다 — 귀속을 분리해 기재한다.**
>
> - **Amendment 4 가 위 3-regime 표를 재작성한다** (`:335`-`:341` — A4-2 헤더 문면 자체가 *"A3-2 대조표 재작성"*). 위 표는 **채택자 탐지를 단일 칸**으로 뒀고 그 칸이 **서로 다른 두 손실 경로를 뭉갰다** — 한 축(자원 bound)이 닫혔을 때 칸 전체가 닫힌 것처럼 읽힌다. 정정 정본 = **A4-2**(탐지 칸을 **자원 bound 축 / 표식 가시성 축 2 축으로 분해**). ⇒ *"제거한 델타는 **거짓 발화뿐**"* 은 **표식 가시성 축을 계상하지 않은 서술**이다. ★ **본 bullet 은 Amendment 4 시점 누락의 사후 보완**이며 **Amendment 5 가 뒤집은 문면이 아니다** — 본 라운드 공로로 계상하지 않는다(`:370` 과 동일 처리).
> - **Amendment 5 가 위 절에 새 지위를 부여한다**: A4-3 이 축 오분류의 기원으로 *"Amd 2 본문"* 을 지목한 것이 **오지목**임이 실측으로 드러나면서, **ADR 안에서 축을 실제로 옮긴 표면 = 본 A3-2**(*"탐지 손실 0"* 서술)로 **새로 지목**됐다. 정본 = **A5-3**. 위 문면은 **삭제하지 않고 이력으로 보존**하며, 충돌 시 **후속 Amendment 가 우선**한다(A4-2 → A5-3 순).

### A3-3 — `:303` 상충 지위 해제 (부분 — 잔여 명시)

Amd 2 `:303` 은 *"이행 전까지 계획서 §8.1·§8.2 와 본 문면은 상충 상태"* 라고 뒀다. **이행은 완료됐다**(구현 착지 + 계획서 site 27 정합). 그럼에도 상충은 **이행 후에도 잔존**했다 — 방향만 뒤집힌 채로. 계획서가 정정되고 **본 ADR 이 정정되지 않아**, 상충 축이 `CP↔ADR` 로 이동했을 뿐이다.

본 Amendment 로 그 잔여를 닫는다. `:303` 의 **"상충 상태" 지위는 해제**된다.

★ **재발 방지는 문면 규율이지 기계 게이트가 아니다** — site 27 이 ADR 을 누락한 것을 잡은 것은 규격이 아니라 **다음 라운드 리뷰**였다. 이 사실을 *"이제 절차가 보장한다"* 로 격상해 쓰지 않는다.

### A3-4 — "탐지 손실 0" 은 **조건부** — 반례 실재 (F-CR6-01)

A3-2 의 3-regime 대조가 근거로 삼는 *"pre-FIX6 대비 탐지 손실 0"* 은 **표식이 관측 가능한 정의역 안에서만** 참이다. 구현리뷰 iter6 F-CR6-01 이 그 정의역 **밖의 반례**를 냈고, 본 ADR 저작 시점에 ArchitectPL 이 **firsthand 재현**했다:

| 침묵 경로 | 기전 | 실측 |
|---|---|---|
| **파일수 축** | 표식 탐색 결과가 앞 200개로 절단(`head -n 200`) — 무관 태스크가 200개를 넘고 우리 정의가 그 뒤에 오면 `grep` 에 **도달조차 못한다** | 총 251 파일 · 우리 정의 251번째 → 절단 후 잔존 **0건** ⇒ 채택자인데 **무음** |
| **깊이 축** | 표식 탐색이 `-maxdepth 3` — 앱이 정의를 더 깊이 두면 안 보인다 | depth 4 배치 시 `maxdepth 3` 관측 **0 파일** (무제한 관측 1 파일) ⇒ 채택자인데 **무음** |

⇒ 이 두 경로에서 **채택자가 pre-FIX6 이라면 받았을 발화를 받지 못한다** — 즉 **탐지 손실이 0 이 아니다.**

**정직 귀결 3항**:

1. 종전 잔여 3종((i) 벤더 rename·이전 (ii) sentinel 소실 (iii) 파일 아닌 형태·다른 HOME)은 **자원 bound 축을 포함하지 않았다.** 선언된 정의역이 실제 검사 정의역보다 **넓었다** — 본 ADR·계획서가 반복 처벌해온 바로 그 class 다.
2. **구현 lane 이 이 축을 좁히는 중이다** — 처분 방향 = *"bound 도달 = 판정 불가 → 발화(fail-safe)"*. bound 를 늘리는 것이 아니라 **bound 소진을 미확정으로 읽어 침묵하지 않게** 하는 것이다.
3. ★ **그 봉합이 착지하기 전까지 A3-2 의 "탐지 손실 0" 은 조건부 명제로만 성립한다.** 착지 확인은 **다음 라운드**이며, 본 Amendment 는 그 확인을 **선취해 기재하지 않는다**.

> ★ **Amendment 4 가 위 3항을 정정한다** (`:366` — *"착지하기 전까지 … 조건부 명제로만 성립"*). 그 문면은 **착지 후에는 조건부가 풀린다고 읽힐 여지**를 남겼고 **A4-1 실측이 그 읽기를 반증**했다 — 해제된 것은 **반례 2 경로**(파일수 축 · 깊이 축)뿐이고 **조건부 지위 자체는 유지**된다((i)(ii)(iii) 이 탐지 축 손실로 잔존). 정정 정본 = **A4-4**. 위 문면은 **삭제하지 않고 이력으로 보존**하며, 충돌 시 **Amendment 4 가 우선**한다. ★ **본 포인터는 FIX8 전수 점검이 발견한 Amendment 4 시점 누락의 사후 보완**이며(Amd 4 는 앞 절 포인터를 **0개** 남겼다), **Amendment 5 가 뒤집은 문면이 아니다** — 귀속을 흐리지 않기 위해 명시한다.

### A3-5 — 본 Amendment 를 정의역으로 삼는 오라클 = **0** (정직 기재)

Amd 2 문면이 실배선과 5 라운드 넘게 모순인 채 생존한 직접 원인은, **그 문면을 정의역으로 삼는 검사가 하나도 없다**는 것이다. 현재도 같다:

- 본 ADR 의 판독 규범(A3-1 표 3행)을 **오라클이 참조하지 않는다.** 대응 검사는 계획서 §8.1 케이스·§8.2 mutant 를 정의역으로 삼으며, **ADR 문면과의 정합은 어떤 테스트도 잼하지 않는다.**
- 따라서 A3-1 이 배선과 다시 어긋나도 **RED 가 되는 검사는 없다.** 검출 경로는 여전히 **사람 리뷰**다.
- ⇒ 본 Amendment 는 *"결정 SSOT 를 배선에 맞췄다"* 까지만 주장하며, *"앞으로 어긋나지 않는다"* 로 **나아가지 않는다.**

**본 Amendment 의 지위**: 결정 SSOT 문면 정정 + 상충 해소. **Amd 2 본문 삭제 0**(이력 보존, 충돌 시 본 Amendment 우선). 코드·계약 델타 0 — A3-4 의 fail-safe 봉합은 **구현 lane 소관**이며 본 문면이 그 착지를 선언하지 않는다.

## Amendment 4 (CFP-2949, 2026-08-15) — A3-2 대조표를 **2 탐지 축**으로 재작성 + 잔여 (i)(ii)(iii) 축 오분류 정정

FIX7 이 A3-4 가 지목한 자원 bound 축을 봉합했고(`319b49d28` — bound 도달을 "표식 없음"이 아니라 **판정 불가**로 결론지어 발화측에 붙임), ArchitectPL 이 그 착지를 **firsthand 재현**했다. 재현 과정에서 A3-4 보다 **한 겹 안쪽 결함**이 드러나 본 Amendment 로 정정한다.

### A4-1 — firsthand 실측 (동결 4 판본 × 8 시나리오)

판본 = pre-FIX6 `d0e812730` / FIX6 1차판 `cbb17ebb0` / FIX6 2차판 `004d1dfdd` / FIX7 `67e4abbd3`. 각 시나리오는 스크래치 `HOME` 격리로 실행했고 동결 트리는 mutate 하지 않았다. heartbeat 는 전 시나리오에서 부재(= `absent` 분기 진입).

| # | 시나리오 | 채택 여부 | pre-FIX6 | FIX6 1차 | FIX6 2차 | **FIX7** |
|---|---|---|---|---|---|---|
| A | 태스크 정의 251 파일 · sentinel 이 `head -200` 절단 밖 | **채택** | 발화 | 침묵 | **침묵** | **발화** |
| B | sentinel 이 depth 4 | **채택** | 발화 | 침묵 | **침묵** | **발화** |
| H | sentinel 이 bound 안 (정상 채택자) | **채택** | 발화 | 침묵 | 발화 | 발화 |
| E | 벤더가 `scheduled-tasks` 를 rename — 잔여 (i) | **채택** | **발화** | 침묵 | 침묵 | **침묵** |
| F | 운영자 프롬프트 재작성으로 sentinel 소실 — 잔여 (ii) | **채택** | **발화** | 침묵 | 침묵 | **침묵** |
| C | 무관 태스크 251 파일 · sentinel 없음 | 미채택 | 발화 | 침묵 | 침묵 | **발화** |
| G | 무관 내용이 depth 4 · sentinel 없음 | 미채택 | 발화 | 침묵 | 침묵 | **발화** |
| D | 무관 태스크 5 파일 (bound 안) · sentinel 없음 | 미채택 | 발화 | 침묵 | 침묵 | 침묵 |

> ★ **Amendment 5 가 위 측정 도구의 정의역을 한정한다** (`:382`-`:395` — A4-6 `:463` 의 재현 절차 서술 포함). 표의 **값은 참이고 재현 가능**하나, 설계가 **4 판본 × 8 픽스처 × 단일 세션**이라 **세션 축(시간 축)이 없다** — 저빈도 주기 정책(P4 류)은 정의상 세션 1 에서 침묵하므로 이 표에서 **P0 과 동일하게 기록**되어 구별이 불가능하다. ⇒ 본 표를 *"정책 공간을 덮는 증거"* 로 읽지 말 것. 정본 = **A5-6**(계측기 차원 누락) · honest ceiling 독립 행 = 계획서 **§9.5 행 22**. 위 표는 **무손상 보존**한다.

### A4-2 — A3-2 대조표 재작성 (탐지 칸을 **2 축으로 분해**)

A3-2 의 3-regime 표는 "채택자 탐지"를 **단일 칸**으로 뒀다. 그 단일 칸이 서로 다른 두 손실 경로를 뭉뚱그렸고, 그래서 한 축이 닫혔을 때 칸 전체가 닫힌 것처럼 읽혔다. 아래로 대체한다:

| regime | 거짓 발화 (비채택자 소음) | 채택자 탐지 — **자원 bound 축** | 채택자 탐지 — **표식 가시성 축** (i)(ii)(iii) |
|---|---|---|---|
| pre-FIX6 (`absent` → 표식 무관 발화) | **O — 전 consumer** | O | **O** |
| FIX6 1차판 (`absent` → 표식 무관 침묵) | X | X (FN 신설) | X |
| FIX6 2차판 (표식 2분기, bound 미고려) | X | **X — 반례 2 경로 (A·B)** | X |
| **FIX7 현행판** (표식 2분기 + bound 소진 = 판정 불가 → 발화) | **조건부 O — 태스크 정의 200 파일 초과 ∨ depth 4 이상 환경 한정 (C·G)** | **O — 폐쇄 (A·B)** | **X — 잔존 (E·F)** |

**정직 귀결 3항**:

1. **자원 bound 축의 탐지 손실은 실제로 해소됐다** — 이 축에 한정해서는 조건부가 아니다(A·B 실측).
2. **표식 가시성 축의 탐지 손실은 그대로 남는다**(E·F 실측). ⇒ *"탐지 손실 0"* 전체 명제는 **여전히 조건부**다. 조건은 좁아지지 않았고 *"표식이 관측 가능한 정의역 안"* 그대로다. [ceiling: 인용된 종전 문면의 mention — 본 항의 주장은 그 반대(조건부 유지)이며, A4-1 표가 정의역]
3. **거짓 발화는 제거된 것이 아니라 축소됐다** — *"전 consumer"* → *"bound 초과 환경 한정"*. ★ *"거짓 발화가 없다"* 로 기술하지 말 것. 그 형식의 단정이 iter6 에 반증된 바로 그 형식이다.

### A4-3 — 잔여 (i)(ii)(iii) 의 **축 오분류 정정** (A3-4 보다 한 겹 안쪽)

Amd 2 본문·계획서 §7.4.1·hook 헤더가 잔여 3종을 전부 **거짓 발화 축**으로 라벨했다 — *"셋 다 fail-safe 방향이며 거짓 발화를 만들지 않는다"*. 그 서술은 **참이지만 축이 틀렸다**.

> ★ **Amendment 5 가 위 문단의 오염면 열거를 정정한다** (`:416` — *"Amd 2 본문"* 오지목). 실측상 그 문면은 **Amd 2 본문에 없고**, 축을 실제로 옮긴 표면은 **A3-2**·잔여 3종 도입면은 **A3-4** 다. 위 문면은 **삭제하지 않고 이력으로 보존**하며, 충돌 시 **Amendment 5(A5-3)가 우선**한다.

E·F 실측이 보이듯 (i)(ii)(iii) 은 **탐지 축의 손실 경로**다. 진짜 채택자가 heartbeat 부재 상태(= *"채택했는데 한 번도 안 돌았다"* 실 이상)일 때, pre-FIX6 은 발화하고 표식 기반 판정은 침묵한다. 거짓 발화를 만들지 않는다는 것은 **다른 축의 사실**이며 탐지 손실이 없다는 뜻이 아니다.

⇒ **축을 잘못 붙였기 때문에** *"탐지 손실 0"* 과 *"잔여 (i)(ii)(iii)"* 이 같은 문서에 **모순으로 보이지 않고 병존**했다. [ceiling: 반증 대상 명제의 mention — 주장이 아니라 인용] A3-4 는 *"잔여 열거가 자원 bound 축을 누락했다"* 로 진단했는데, 실제 결함은 그보다 안쪽이다 — **열거는 있었고 축이 틀렸다.** 재사용 가능한 교훈은 *"잔여를 빠짐없이 세라"* 가 아니라 **"잔여를 어느 축의 손실인지로 라벨하라"** 다.

**본 Amendment 이후 잔여 라벨 (정정본)**:

| # | 잔여 | 축 | 방향 |
|---|---|---|---|
| (i) | 벤더가 `~/.claude/scheduled-tasks` rename·이전 | **탐지 축** | 채택자 무음 |
| (ii) | 운영자 프롬프트 재작성으로 sentinel 소실 | **탐지 축** | 채택자 무음 |
| (iii) | 앱이 정의를 파일 아닌 형태·다른 HOME 에 보관 | **탐지 축** | 채택자 무음 |
| (iv) | **스캔 bound 도달 = 판정 불가** (FIX7 신설) | **거짓 발화 축** | 미채택자 소음 (bound 초과 환경 한정) |

(iv) 는 FIX7 이 (i)~(iii) 과 **반대 방향**으로 신설한 대가다. 구현 lane 이 hook 헤더 정직 천장에 같은 기호로 선언했으며, 본 Amendment 는 그것을 결정 SSOT 로 승계한다.

### A4-4 — A3-4 의 **조건부 지위는 유지**된다 (해제된 것은 반례 2 경로뿐)

A3-4 정직 귀결 3항은 *"봉합이 착지하기 전까지 A3-2 의 「탐지 손실 0」은 조건부 명제로만 성립한다"* 로 적어, 착지 후에는 조건부가 풀린다고 읽힐 여지를 남겼다. **그 읽기는 A4-1 실측으로 반증됐다.** [ceiling: A3-4 원문 verbatim 인용 — 본 항이 반증하는 대상]

- 착지로 해제된 것 = **A3-4 가 든 반례 2 경로**(파일수 축 · 깊이 축).
- 해제되지 않은 것 = **조건부 지위 자체**. (i)(ii)(iii) 이 탐지 축 손실로 남기 때문이다(A4-3).
- ⇒ *"조건부 → 무조건 승격"* 이라는 프레임 자체가 성립하지 않는다. [ceiling: 기각되는 프레임의 mention — 본 항은 그 프레임을 부정한다] **한 칸의 승격이 다른 칸을 강등**하기 때문이다 — bound 소진을 발화로 돌려 탐지 칸을 올린 대가로 거짓 발화 칸이 내려갔다(C·G). 따라서 처분은 승격이 아니라 **표의 재작성**이었고, 그것이 A4-2 다.
- **구현 lane 의 자기 선언과 정합**: `hooks/session-start-scheduled-task-watchdog` 의 자원 안전 절이 *"이 명제는 본 hook 이 보는 정의역 안에서만 성립하며 (i)(ii)(iii) 은 여전히 잔여"* 라고 이미 기재해 뒀다. 본 Amendment 는 그 선언을 결정 SSOT 로 끌어올린 것이지 새로 발견한 것이 아니다.

### A4-5 — §7.4.1 (ㄴ) 판정 정산 (판정 이력 4 단계 보존)

Story §8.5 site 24 의 3-conjunct 중 (ㄴ)(*"완화 방향 없음"*)에 대한 판정 이력을 **어느 단계도 삭제하지 않고** 기록한다:

| 단계 | 시점 | 판정 주체 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | FIX6 1차판 | ArchitectPL | **미충족** — 자기 면제 거부, Orchestrator 회부 | `absent` 를 표식 무관하게 침묵시킨 FIX6 1차판 형상이 (ㄴ) class 를 구조적 무음화 |
| 2 | 2026-08-15 | Orchestrator | **충족** — 설계리뷰 재진입 불요 | 표식 판별자 도입으로 *"pre-FIX6 대비 탐지 손실 0"* — [ceiling: 단계 4 가 반증한 근거의 mention, 현재 참인 주장 아님] |
| 3 | 2026-08-15 (본 Amendment) | ArchitectPL firsthand | 단계 2 의 **근거 반증** | E·F 실측 — 채택자 탐지가 pre-FIX6 대비 실제로 감소 |
| 4 | 2026-08-15 | Orchestrator | **미충족 — 설계리뷰 재진입 대상** | 단계 2 의 전제가 반증됨. 선언(FIX6 대가 ①)은 **정직성**을 만족시킬 뿐 (ㄴ) 를 만족시키지 않는다 |

**재진입 사유의 성격** = 문서 정합이 아니라 **정책 판정**이다. 표식 가시성 축의 손실은 *표식 기반 채택 판정에 내재*하며, 없애려면 표식 부재 시 발화로 되돌려야 하고 그것은 pre-FIX6 소음의 복원이다. ⇒ **소음 vs 미탐지 trade-off** 이며 설계가 판정할 사안이다.

> ★★ **Amendment 5 가 위 문단을 정정한다** (`:455` — *"없애려면 표식 부재 시 발화로 되돌려야 하고 그것은 pre-FIX6 소음의 복원이다"*). 그 문면은 **이분법**이며 **FIX8 설계리뷰 실측으로 반증**됐다 — P0(소음 0 · 영구 무음)과 P1(소음 100%) 사이는 **연속이고 중간점이 실재**한다(`P=30` 무상태 P4 = 미채택자 소음 **3.3%** · 첫 발화 기대 세션 **30**). 정정 정본 = **A5-1**(탐지 metric 고정 = 유계 종국) + **A5-4**(P0 vs P4 실평가 — 중간점을 실제로 계산하고 그 수치를 **알고도** 기각). ★ **판정 자체(단계 4 = 미충족·재진입)는 유지**되며 정정 대상은 **사유 문면**이다. 위 문면은 **삭제하지 않고 이력으로 보존**하며, 충돌 시 **Amendment 5 가 우선**한다.

★ **판정 방향 자기 기재 (은폐 금지)**: 단계 2 의 오류 방향은 **설계리뷰 재진입을 회피하는 쪽**이었다 — 즉 절차 비용을 낮추는 방향이다. 같은 Story 의 implementability reassessment 패킷 2회가 RESET 에 유리한 방향으로 틀렸던 것과 **방향이 일치**한다. 방향의 3회 일치를 우연으로 처리하지 않으며, 본 Amendment 가 그 재발을 막는 기계적 대체물이 아니라 **문면 규율**임을 함께 기재한다.

★ **conjunct 독립 유지**: (ㄴ) 미충족이 (ㄱ)·(ㄷ) 판정을 바꾸지 않는다. 각 conjunct 는 독립이며 연동시키지 않는다.

### A4-6 — 본 Amendment 를 정의역으로 삼는 오라클 = 계속 없음 (A3-5 승계)

A3-5 가 기재한 사실은 본 Amendment 에도 그대로 적용된다 — A4-2 표·A4-3 잔여 라벨을 정의역으로 삼는 테스트는 없으며, 이 문면이 배선과 다시 어긋나도 RED 가 되는 검사가 없다. 검출 경로는 사람 리뷰다.

단 **A4-1 실측 자체는 재현 가능**하다: 4 판본을 `git show <sha>:hooks/session-start-scheduled-task-watchdog` 로 꺼내 8 픽스처에 `HOME` 을 갈아 끼우면 같은 표가 나온다. 이것은 검사가 아니라 **재현 절차**이며, *"검사가 있다"* 로 격상해 읽지 말 것.

**본 Amendment 의 지위**: 결정 SSOT 문면 정정(대조표 재작성 + 잔여 축 재라벨) + 판정 이력 정산. **Amd 2·Amd 3 본문 삭제 없음**(이력 보존, 충돌 시 본 Amendment 우선). 코드·계약 델타 없음.

## Amendment 5 (CFP-2949, 2026-08-15) — (ㄴ) 탐지 **metric 고정** + A4-5 이분법 반증 정산

**계기**: 설계리뷰 재진입(FIX8)이 A4-5 `:453` 의 재진입 사유 문면 — *"없애려면 표식 부재 시 발화로 되돌려야 하고 그것은 pre-FIX6 소음의 복원이다"* — 를 **이분법으로 판정하고 실측으로 반증**했다. 곡선 위 중간점이 실재하며, **본 ADR·계획서는 그 점을 평가한 적이 없다.**

**본 Amendment 의 지위**: Amd 4 본문 **삭제 0**(이력 보존, 충돌 시 본 Amendment 우선). 코드·계약·오라클 델타 0.

### A5-0 — 반증 실측 (설계리뷰 제출, ArchitectAgent 재검증)

| 정책 | 미채택자 소음 | 채택 + 표식 불가시 (잔여 (i)(ii)(iii)) | 채택 + 표식 가시 |
|---|---|---|---|
| **P0 = 현행 FIX7** (표식 부재 → 침묵) | **0%** | **0% — 영구 무음 (FN)** | 100% (세션 1) |
| P1 = pre-FIX6 복원 (표식 무관 발화) | **100% 매 세션** | 100% (세션 1) | 100% (세션 1) |
| **P4 = 주기적 저빈도** (표식 부재 시 확률 `1/P` 로 발화, `P=30`) | **3.3%** (1/30) | **3.3% — 첫 발화 기대 세션 30** | 100% (세션 1) |

⇒ A4-5 가 *"소음 vs 미탐지"* 를 **양 끝점 2택**으로 제시한 것은 거짓이다. **P0 과 P1 사이는 연속이며 P4 는 그 위의 실재하는 점이다.** 본 Amendment 는 그 점을 실제로 평가한다(A5-3).

### A5-1 — 탐지 metric 고정 = **유계 종국(bounded-eventual)** [결정]

(ㄴ)(*"완화 방향 없음"*) 판정은 **탐지 metric 을 고정한 적이 없다.** 판정이 4단계 진동한 근인이 여기 있다 — 같은 실측을 놓고도 metric 을 바꾸면 판정이 뒤집힌다.

**metric 3종 정의 (본 Amendment 가 값 공간을 닫는다)**:

| metric | 정의 | 이 metric 에서 (ㄴ) 를 충족하는 정책 |
|---|---|---|
| **즉시 (per-session)** | 채택자가 이상 상태인 **그 세션에** 발화하는가 | P1 **만** |
| **무계 종국 (unbounded-eventual)** | 채택자가 이상 상태이면 **언젠가** 발화하는가 (지연 상한 없음) | P1 · P4 (확률적 포함) |
| **★ 유계 종국 (bounded-eventual) — 채택** | 채택자가 이상 상태이면 **선언된 유한 상한 `D` 세션 안에 반드시** 발화하는가 | P1 · **결정론적 `≤D` 보장을 가진 정책만** (확률적 tail 은 상한 부재라 미충족) |

**채택 = 유계 종국. 근거 2항**:

1. **harm 모델 정합**: 본 축이 탐지하려는 harm(= 관측자 사망 · *"채택했는데 1회도 안 돌았다"*)은 **자기 치유하지 않고 지속**한다(A1-2 · A1-3 이 이미 기술한 성질). 지속하는 harm 은 유계 지연 탐지기로 잡히므로 **즉시성은 이 harm 이 요구하는 것보다 강한 요구**다. 반대로 100% 소음이 유도하는 **탐지자 자발적 폐기**(A1-3 항목 2 · A1-4)는 **무계 탐지 손실**이라 즉시 metric 을 고집한 대가가 metric 자신을 파괴한다.
2. **무계 종국 기각 — metric 이 완화 장치가 되는 것을 막는다**: 무계 종국은 *"언젠가는 잡힌다"* 로 **임의의 지연을 정당화**하는 데 재사용된다. `P=30` 을 정당화한 논리는 `P=30000` 도 똑같이 정당화하며, 그 경계를 metric 자신이 갖지 못한다. ⇒ **지연 상한 `D` 를 수치로 못박지 않은 종국 metric 은 채택하지 않는다.** 이 결박이 없으면 metric 고정이 완화 승인 절차로 전도된다.

★ **이 선택이 (ㄴ) 판정을 결정하는 방식 (은폐 금지)**: 유계 종국은 **P0 과 stateless-P4 를 둘 다 미충족으로 만든다** — P0 은 `D` 가 존재하지 않고(영구 무음), stateless-P4 는 기대값만 유한하고 **상한이 없다**(A5-3 tail 수치). 즉 본 metric 선택은 **본 Story 의 현행 판본에 불리한 방향**이며, 판정을 쉽게 만들지 않는다. 3회 연속 절차 비용을 낮추는 방향으로 틀린 이력이 있으므로 이 방향성을 명시 기재한다.

### A5-2 — 진동의 구조적 근인 = **판별자가 metric 이 아니라 제3축이었다**

(ㄴ) 의 자기 선언 판별자는 Story §8.5 **site 24** 에 *"판별 기준은 재량이 아니라 **발화 건수 방향**(줄면 완화)"* 로 적혀 있다. 이는 즉시·종국 **어느 쪽도 아닌 제3축**이다 — **집계량의 부호**다.

**부호 판별자가 구별하지 못하는 것 3종**:

1. **축** — 거짓 발화가 줄어도 부호는 음수, 탐지 발화가 줄어도 부호는 음수다. A4-3 이 정정한 축 오분류가 이 판별자에서는 **원리적으로 보이지 않는다**.
2. **정의역** — 표식 가시 환경에서 늘고 불가시 환경에서 주는 정책의 부호는 **집계 모집단에 의존**한다. 모집단을 선언하지 않은 부호는 판정이 아니다.
3. **지연** — 즉시 발화와 30세션 후 발화는 **장기 집계량이 같다**. 부호 판별자는 이 둘을 분간하지 못하며, 그래서 P4 가 (ㄴ) 심사에 **후보로조차 오르지 못했다**.

⇒ **판정이 4단계 진동한 것은 근거가 부실해서가 아니라 판별자가 판정 대상을 표현하지 못해서다.** 부호 판별자는 metric 이 아니라 **metric 의 그림자**이며, 본 Amendment 가 고정한 유계 종국이 그 자리를 대체한다. ★ site 24 의 부호 문면을 **삭제하지 않는다** — 그것이 무엇을 못 보는지를 여기 기록하는 것이 처분이다.

### A5-3 — A4-3 **귀속 오류 정정**: 오염면은 3면이 아니라 **2면**이었고, Amd 2 는 축을 **옳게** 붙였다

A4-3 `:416` 은 잔여 3종을 거짓 발화 축으로 라벨한 오염면을 *"**Amd 2 본문**·계획서 §7.4.1·hook 헤더"* **3면**으로 열거했다. **그 중 첫 면은 거짓이다.**

**실측 (전수 `grep`, 본 파일 기준)**:

| 검사 | 결과 |
|---|---|
| `fail-safe` · `fail safe` · `failsafe` 전수 | **5 hit** — `:44` `:214` `:365`(A3-4) `:376`(A3 지위) `:416`(A4-3 자신) ⇒ **Amd 2 본문 범위 `:289`-`:306` 안 0 hit** |
| 잔여 3종 `(i)(ii)(iii)` 열거 최초 등장 | **`:364`** (A3-4 안) ⇒ Amd 2 본문 안 0 hit |
| Amd 2 `:299` 대가 1항 | *"「채택했으나 1회도 실행되지 않음」 class 는 heartbeat 부재만으로 판별 불가 … 구조적으로 무음"* = **탐지 축** |
| Amd 2 `:300` 대가 2항 | *"false-negative 를 하나 늘리는 방향임을 숨기지 않는다"* = **탐지 축 · 방향까지 명시** |

⇒ **Amd 2 는 손실을 `false-negative` = 탐지 축으로 올바르게 라벨했다.** A4-3 이 축 오분류의 기원으로 지목한 것은 **오지목**이며, 본 항이 그 명예를 회복시킨다.

**축을 실제로 옮긴 표면 (정정 열거)**:

| 표면 | 무엇을 했나 |
|---|---|
| **A3-2 (`:329`-`:341`)** | *"본 판본이 pre-FIX6 대비 제거한 델타는 **거짓 발화뿐**"* — **탐지 손실 0** 서술로 축을 옮겼다 |
| **A3-4 (`:364`)** | 잔여 3종 (i)(ii)(iii) 을 **도입**하면서 *"자원 bound 축을 포함하지 않았다"* 로 진단, `:365` 에서 `fail-safe` 어휘를 사용 |
| **계획서 §7.4.1 (`:664`)** | *"★ 셋 다 fail-safe 방향(표식 미검출 → 침묵 → 거짓 발화 0)"* — 이 면의 귀속은 **정확** |
| **hook 헤더** | 현재는 `[탐지 축]` 으로 정정돼 있고 `:56` 이 *"셋을 거짓 발화 축으로 라벨했고 그 오분류를 A4-3 이 정정했다"* 로 이력 보유 — 과거 표면으로서 귀속 **정확** |

★★ **자기 발현 기재 (은폐 금지 — 본 항의 핵심)**: **축 라벨 오류를 정정하려고 쓴 A4-3 자신이 자기 기원을 오지목했고, 계획서 §9.5 행 19 가 그 문면을 복제**했다. 즉 A4-3 이 겨냥한 class 가 **정정본 자신에게서 재발**했고 2 라운드 동안 3자 누구도 잡지 않았다. **A4-3 의 교훈(*"잔여를 어느 축의 손실인지로 라벨하라"*)은 유효하되, 본 항이 하나를 더한다** — **오염면을 열거할 때 그 열거 자체를 실측으로 검증하라.** 축 라벨을 고치는 문장이 귀속 라벨을 틀리는 것은 같은 결함의 한 겹 위 재발이다.

★ **기계 검사 표면 부재 (정직 기재)**: 본 정정을 정의역으로 삼는 오라클은 **없다**(A3-5 · A4-6 승계). 다만 위 표의 `grep` 4행은 **재현 가능한 절차**이며, 이를 *"검사가 있다"* 로 격상해 읽지 말 것.

### A5-4 — P0 vs P4 **실평가** (A4-5 이분법 기각 — 중간점을 실제로 계산한다)

**불변 제약 (평가 전 고정)**: 도입기 = **관측-only · 삭제 0** · 계획서 `:405` **"신규 플래그 0건"** · §결정 6 **INV-OBS**(`:117` — *"관측자는 자기 관측 도메인에 새 객체를 추가하지 않는다"*). 이 제약을 푸는 것은 §결정 9 승격 게이트 사안이지 본 라운드 사안이 아니다.

#### A5-4-a — 무상태 P4 의 정체 = **확률적 종국**이지 결정론 `≤P` 아님

`P=30` 무상태 키잉에서 *"채택 + 표식 불가시"* 상태가 `N` 세션째까지 **무음**일 확률 = `(1 − 1/P)^N` = `(29/30)^N` (ArchitectAgent 직접 계산):

| 세션 수 `N` | 아직 무음일 확률 |
|---|---|
| 10 | **71.25%** |
| 30 (= 기대 첫 발화 세션) | **36.17%** |
| 60 | 13.08% |
| 90 | 4.73% |
| 200 | 0.114% |

⇒ **기대값은 유한(30 세션)하나 상한은 존재하지 않는다.** A5-1 이 고정한 **유계 종국**은 *"선언된 유한 상한 `D` 안에 **반드시**"* 를 요구하므로 **무상태 P4 는 (ㄴ) 를 충족하지 못한다** [bound: 무상태·확률 키잉 판본 한정 — 상태 보유 판본은 A5-4-c]. ★ **은폐 금지**: *"첫 발화 기대 세션 30"* 은 **기대값**이지 보장이 아니다. 이를 *"30 세션 안에 잡힌다"* 로 읽는 것은 위 표의 **36.17%** 를 지우는 것이다.

#### A5-4-b — 무상태 P4 의 **추가 대가 3종** (설계리뷰 제출 표에 없던 것 — 본 평가가 새로 계상)

1. **결정론 계약 파손**: §9.5 행 13 이 *"남는 통제는 **CLI 코드의 결정론**(§8 오라클)과 축소 불가한 상속 자격증명 scope 뿐"* 으로 적어 결정론은 **잔여 통제 2종 중 1종**이다(계획서 `결정론` **21 hit** 실측). 난수 키잉은 그 통제를 **발화 축에서 소거**한다 — 오라클이 *"이 입력에 이 출력"* 을 잼지 못하고 분포만 잼게 된다.
2. **날짜 키잉 대안의 aliasing (확률 tail 보다 나쁜 class)**: 난수를 피해 `날짜 mod P` 로 결정론을 지키면 **세션 cadence 와 `P` 가 공약수를 가질 때 영구 무음**이 된다(예: 특정 요일에만 세션을 여는 운영자 ↔ `P` 가 7 의 배수). 확률적 tail 은 시간이 줄이지만 **aliasing 은 줄지 않는다** — 즉 결정론을 되찾는 대가가 **무계 FN class 신설**이다.
3. **§9.5 행 1 이 다시 거짓이 된다**: 행 1(*"다른 운영자는 merge 만으로 아무 **산출물**도 얻지 못한다"*)은 FIX6 착지로 비로소 참이 됐다. 무상태 P4 는 미채택자에게 3.3% 확률로 산출물을 주므로 **그 행을 다시 거짓으로 만든다.** 정합을 되찾으려면 행 1 을 완화 서술로 고쳐야 하고 그것은 **선언 하향** = (ㄴ) 가 금지하는 방향이다.

#### A5-4-c — 결정론 `≤P` 보장은 **상태 파일 1개**를 요구한다 ⇒ 승격 게이트 회부 (선례 인용·검증)

유계 종국을 **보장**하려면 *"마지막 발화 이후 경과 세션 수"* 를 어딘가 기록해야 한다 = **새 상태 객체 1개**. 이는 계획서 `:405` **"신규 플래그 0건"** 과 §결정 6 **INV-OBS** 재검사 대상이다.

★ **동형 선례가 본 ADR 안에 이미 있다 (firsthand 확인 — `:275` = A1-3 항목 3)**: *"구별하려면 정지 사실을 별도 상태로 기록하고 watchdog 이 그 상태로 분기해야 한다. 도입기 관측-only 에서 1번의 소음이 bounded 하고, 새 상태 파일은 §결정 4 「신규 플래그 0건」 정신과 §결정 6 INV-OBS 재검사를 요구한다 ⇒ **§결정 9 승격 게이트 재검토 대상으로 올린다**."* ⇒ 본 항은 그 선례와 **구조적으로 동일한 사안**(새 상태 파일로 watchdog 분기를 늘림)이므로 **같은 처분**을 적용한다.

#### A5-4-d — 처분 + **정직 declare 4항**

**도입기 처분 = P0 유지.** 단 **(ㄴ) 는 충족되지 않으며 그렇게 기록한다** — 본 처분은 *"(ㄴ) 를 만족시켰다"* 가 아니라 *"도입기 불변 제약 안에서 (ㄴ) 를 만족시키는 정책이 없고, 제약을 푸는 것은 승격 게이트 사안"* 이다.

1. **축·방향 라벨** — 손실은 **탐지 축 · 방향 = 채택자 무음**. 이미 `c3063d28c` 로 착지(hook 헤더 `(i)(ii)(iii)` 전건 `[탐지 축]` + *"진짜 채택자가 heartbeat 부재 상태일 때 pre-FIX6 은 발화했고 현행은 침묵한다"*, ArchitectAgent 확인).
2. **metric 명시** — 유계 종국(A5-1). 이 metric 에서 P0 은 `D` 가 **존재하지 않는다**(영구 무음).
3. ★★ **기각된 중간 후보와 그 실측 대가를 함께 기재** — 기각한 대안은 *"pre-FIX6 소음 100% 복원"* 이 **아니다**. 실제로 기각한 것은 **`P=30` 무상태 P4 = 미채택자 소음 3.3% · 첫 발화 기대 세션 30** 이며, **그 수치를 알고도 기각**했다. 기각 사유는 A5-4-a(상한 부재) + A5-4-b(결정론 파손 · aliasing · 행 1 거짓화) 3항이지 *"어차피 소음 복원이라서"* 가 아니다. **A4-5 `:453` 의 이분법 문면은 이로써 기각된다.**
4. ★ **P4 잔여도 은폐 금지** — P4 를 채택했더라도 남았을 것: 무음 확률 tail(A5-4-a 표, `N=90` 에서도 **4.73%**) · 날짜 키잉 시 aliasing · 미채택자 3.3% 소음. **P4 는 (ㄴ) 를 충족하는 선택지가 아니라 harm 노출을 줄이는 선택지**였고, 그 구별을 흐리지 않는다.

**승계**: **상태 보유 P4**(결정론 `≤P` 보장)를 **§결정 9 승격 게이트 재검토 대상**으로 올린다 — `:275` 선례와 동일 처분. 도입기에 배선하지 않으며, 본 문면이 그 착지를 **선언하지 않는다**.

### A5-5 — 설계리뷰 기각 후보 4종 검증 (ArchitectAgent 재평가 — 전건 기각 유지, 근거 1건 보강)

| # | 후보 | 기각 사유 (검증 후) | 검증 |
|---|---|---|---|
| ① | **표식 정의역 확장** (경로·깊이·파일수 상한을 넓힘) | 잔여 (i)(ii)(iii) 은 **rename · sentinel 소실 · 다른 HOME/비파일 형태**라 *넓히기* 로 닿는 축이 아니다. 넓히는 것은 §9.5 **행 12** 가 이미 선언한 **이름·경로 결박 → rename 시 vacuous-pass** class 를 **키우는** 행위다 | 기각 유지 |
| ② | **다중 표식 OR** (후보 경로·형태를 열거해 하나라도 맞으면 채택) | ★★ **hollow-oracle 생성기** — 열거한 경로마다 픽스처를 쓸 수 있으므로 **오라클이 `FN=0` 을 보고하는데 실 `FN>0`** 이 된다. 검사 정의역이 열거와 **정확히 같아지므로** 열거 밖 실패가 구조적으로 안 보인다 | 기각 유지 · **판별 교훈 승격**(아래) |
| ③ | **CLI 자기 기록** (CLI 가 실행 시 자기 채택 표식을 남김) | **순환** — 탐지 대상 harm 이 *"채택했는데 **1회도 안 돌았다**"* 이므로 그 상태에서는 CLI 가 표식을 쓸 기회가 **정의상 없다**. 게다가 표식 파일 자체가 새 상태 객체라 **INV-OBS 재검사** 대상이다 | 기각 유지 |
| ④ | **판정 불가 3번째 상태** (표식 부재를 `undetermined` 로 두고 발화) | **P1 의 다른 이름**이다 — 표식 부재는 **미채택자 전원의 상태**이므로 *"판정 불가 → 발화"* 를 그 분기에 적용하면 **전 미채택자 발화 = 100% 소음**이 된다. `(iv)` 가 이 처분을 **bound 소진**에만 적용한 것은 그 정의역이 미채택자 전원이 아니기 때문이다 | 기각 유지 · **근거 보강**(P1 동치임을 명시) |

★★ **재사용 가능한 판별 (②에서 승격 — §9.5 행 19 · 행 22 와 동일 계보)**: **검사 정의역이 처방의 열거와 정확히 일치하면, 그 오라클은 자기 열거 밖 실패를 구조적으로 보고할 수 없다.** 이런 처방은 탐지를 늘리는 것이 아니라 **탐지 실패를 보이지 않게 만든다**(hollow-oracle 생성기). 판별 질문 = *"이 처방을 채택하면 오라클의 정의역이 처방의 열거와 같아지는가?"* — 그렇다면 `FN=0` 보고는 **측정이 아니라 동어반복**이다. ★ 이 판별에도 **기계 검사 표면은 없다**(A3-5 · A4-6 승계).

### A5-6 — **A4-1 계측기의 차원 누락** (본 Amendment 가 기각하는 것 중 가장 안쪽)

A4-1 의 측정 도구는 **동결 4 판본 × 8 픽스처 × 단일 세션**이다. 이 설계에는 **세션 축이 없다** — 각 셀은 *"세션 1 에서 발화하는가"* 만 기록한다.

그런데 (ㄴ) 를 충족시킬 수 있는 유일 후보군인 **저빈도 주기 정책(P4 류)은 정의상 세션 1 에서 침묵**한다. ⇒ **A4-1 표에서 P4 는 P0 과 글자 그대로 동일하게 기록된다.**

| 시나리오 | P0 (세션 1) | P4 `P=30` (세션 1) | A4-1 표에 남는 값 |
|---|---|---|---|
| E (벤더 rename — 채택자) | 침묵 | 침묵(확률 29/30) | **양쪽 "침묵" — 구별 불가** |
| F (sentinel 소실 — 채택자) | 침묵 | 침묵(확률 29/30) | **양쪽 "침묵" — 구별 불가** |

⇒ **계측기가 후보를 현행판과 구별할 수 없었고, 그래서 후보가 심사에 오를 수조차 없었다.** 판본을 4 개에서 40 개로 늘리고 픽스처를 8 개에서 80 개로 늘려도 이 공백은 닫히지 않는다 — **부족한 것은 표본 수가 아니라 측정 차원**이다.

★★ **class 귀속 (A4-3 · A5-3 과 disjoint)**: 본 Story 는 *"선언의 정의역이 검사의 정의역보다 넓다"* 를 §8.0-c · §8.2 · A3-4 · A4-3 에서 반복 처벌했다. 본 항은 그 class 가 **처방·선언면이 아니라 그것을 판정하는 측정 장치**에서 발현한 사례다 — **판정 도구는 자기 정의역 검사를 면제받지 않는다.** A4-3(축 라벨 오류) · A5-3(귀속 라벨 오류) 과 **축이 다르다**: 저 둘은 *"무엇의 손실인지"* 를 틀린 것이고, 본 항은 *"애초에 재지 않은 차원이 있다"* 는 것이다. 셋을 뭉뚱그리지 않는다.

★ **재사용 가능한 판별 질문**: *"이 계측기에서 내가 채택하려는 답과 기각하려는 답이 서로 다른 값을 내는가?"* — 같은 값을 낸다면 그 계측기는 그 비교에 대해 **판별력 0** 이며, 그것으로 얻은 *"차이 없음"* 은 측정이 아니다.

★ **기계 검사 표면 = 없음 (정직 기재)**: 측정 설계의 차원 완전성을 판정하는 오라클은 부재하며, 결박 게이트(ADR-177)도 이 class 를 잡지 못한다(어휘 결박이지 측정 차원 판정이 아니다). 검출 경로는 **사람 리뷰**였고 실제로 그렇게 검출됐다(FIX8 설계리뷰). *"이제 절차가 보장한다"* 로 격상해 쓰지 않는다. honest ceiling 독립 행 = 계획서 **§9.5 행 22**.
