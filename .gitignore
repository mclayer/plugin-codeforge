# dev-orchestrator plugin — generated files (if this repo is also used as a consumer test workspace)
.claude/agents/*
# CFP-1273 W5-S15 — Confluence sync agents (ADR-103 §결정 3 — project-scope, non-plugin, tracked)
!.claude/agents/confluence-sync-read-verify.md
!.claude/agents/confluence-sync-write-commit.md
.claude-work/

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp
*~

# Python (for merge.py / test scripts)
__pycache__/
*.pyc
*.pyo
.mypy_cache/
.ruff_cache/
.pytest_cache/
.venv/
venv/

# Node (if future hooks use node)
node_modules/

# Claude Code — local agent worktrees
.claude/worktrees/

# Claude Code — local project MCP config (per-developer, may contain token env refs)
.mcp.json

# CFP-45: dogfood artifacts moved to mclayer/codeforge-internal-docs
docs/superpowers/
docs/stories/
docs/retros/
docs/change-plans/
