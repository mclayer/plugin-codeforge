#!/usr/bin/env bash
# CFP-2408 fixture target — non-exec .sh (git index 100644). Check 4 strict
# branch 가 "not executable" FAIL 을 발화하는지 검증하는 fail-nonexec-sh fixture 의 대상.
echo "fixture: non-executable exec-script — must FAIL Check 4 (Linux oracle)"
