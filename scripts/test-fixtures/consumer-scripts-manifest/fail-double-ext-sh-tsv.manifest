# CFP-2408 fixture: 이중확장 foo.sh.tsv non-exec = exit 1.
# 말단확장자 .tsv = allowlist 안 이나 경로에 .sh (exec-script 확장자) 존재 →
# 이중확장 차단 분기(*.sh.*)가 exempt=0 강제 → strict → non-exec FAIL.
# 면제 우회(.tsv 말단으로 깨진 .sh slip) 차단 mutation-kill 핵심 fixture.
scripts/test-fixtures/consumer-scripts-manifest/targets/double-ext.sh.tsv
