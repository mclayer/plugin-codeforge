# Fixture: INVALID — lane field missing (CFP-137 Phase 2 test fixture — expect FAIL)
description: |
  Test fixture — lane field 누락 (FAIL expected).

teammates:
  - name: SomeAgent
    role: PL
    system_prompt_path: agents/SomeAgent.md
    model: opus
    spawn_mode: default
    dispatch_mode: default

dispatch_pattern: sequential

env_divergent_fallback:
  env_0_behavior: "Fallback behavior."
