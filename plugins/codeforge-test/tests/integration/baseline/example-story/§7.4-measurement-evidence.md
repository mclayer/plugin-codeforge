# §7.4 운영 리스크 측정 evidence (template — CFP-1088)

본 file 영역 IntegrationTestLane (codeforge-test plugin) Axis 2 실측 결과 self-write SSOT. ADR-014 Amendment 4 §결정 2 evidence-driven 3-axis 정합.

## Metadata

- **story_key**: `<STORY-KEY>` (예: `CFP-NNN`)
- **epic_key**: `<EPIC-KEY>` (Epic-level lane spawn 시 — 단독 Story 영역 `null`)
- **measured_at**: `YYYY-MM-DDTHH:MM:SS+09:00` (KST ISO 8601, ADR-079 정합)
- **measurer**: `IntegrationTestAgent` (Sonnet tier, codeforge-test plugin)
- **carrier_cfp**: CFP-1088 (template self-write path codify)

## Axis 2 실측 결과 (본 lane 책임)

§8.6 IntegrationTest contract pointer 영역 정의된 측정 대상 별 실측 row append:

| measurement_id | pointer (§8.6) | measured_value | unit | measurement_method | timestamp | empirical_source |
|---|---|---|---|---|---|---|
| <M1> | `<pointer-1>` | <value> | <unit> | <wiretap / probe / synthetic / observed> | <KST ISO 8601> | `<file-path / script-ref>` |
| <M2> | `<pointer-2>` | <value> | <unit> | ... | ... | ... |

## Axis 3 policy 결정 evidence pointer (ArchitectLane post-measurement)

Axis 3 policy 결정 영역 본 lane 책임 외 — Phase 1 follow-up PR 영역 ArchitectLane post-measurement 영역 진행. 본 §"Axis 3 policy 결정 evidence pointer" 영역 = policy 결정 carrier 영역 cross-ref pointer 영역:

| policy_id | pointer (§8.6) | measured_value (M-row ref) | proposed_policy_value | rationale_ref | follow_up_carrier |
|---|---|---|---|---|---|
| <P1> | `<pointer-1>` | M1 | <proposed-threshold> | `<ADR ref / domain-knowledge ref>` | `<follow-up CFP-NNN>` |

## Cross-ref

- ADR-014 Amendment 4 §결정 2 — evidence-driven 3-axis (Axis 1 측정 대상 정의 / Axis 2 실측 / Axis 3 policy 결정)
- ADR-068 Amendment 3 §결정 1 I-6 — audit-gate-pointer-existence (Axis 1 pointer 영역 4-form scope: link target / section anchor / file path reference / ADR §결정 N reference)
- codeforge-review canonical `templates/review-pl-base.md` §8.6 — DesignReviewPL audit gate pointer-presence-check (CFP-1089 mechanical workflow Phase 2 merged)
- review-verdict-v4 v4.7 `findings[].type "audit-gate-pointer-missing"` + `audit_gate_pointer_self_check_passed` (CFP-1087)

## Usage

본 file path = `tests/integration/baseline/<STORY-KEY>/§7.4-measurement-evidence.md`. Per-Story aggregation — Epic-level lane spawn 1회 within N Story measurement, 결과 path 정합 (`tests/integration/stories/<EPIC-KEY>/<STORY-KEY>/` self-write 표 already-codified).

본 template 영역 신설 = CFP-1088 carrier scope (Wave 2 mechanical wire) — actual measurement data 영역 future Story (Axis 2 실측 trigger 발화 시) 영역 IntegrationTestAgent self-write.
